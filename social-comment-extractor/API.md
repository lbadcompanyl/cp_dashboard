# API ของเครื่องมือ sentiment — เรียกใช้ยังไง

> คู่มือ endpoint ทั้งหมดของ `worker/worker.js`
> ใช้ได้ทั้งกรณีเรียกข้ามห้อง และกรณีที่ห้องอื่นรับไปพัฒนาต่อ
>
> 🎯 **ไม่ต้องสร้างตัวตี sentiment ใหม่ ไม่ต้องทำ lexicon เอง**
> ของที่มีอยู่ผ่านการวัดกับชุดเฉลย 475 ใบมาแล้ว (ดู `BASELINE.md`)
>
> 🚫 **ห้ามก๊อปโค้ดตี sentiment ไปวางที่อื่นแล้วแก้แยกกัน** — จะกลายเป็น 2 มาตรฐานทันที
> แก้เกณฑ์ที่หนึ่งแล้วอีกที่ไม่ตาม แล้วตัวเลขบนเว็บเดียวกันจะขัดกันเอง

Worker: `https://comment-sentiment.s3445028.workers.dev`
เกณฑ์ตัดสิน: `RUBRIC-CP.md` · ระบบเรียนรู้: `FEEDBACK.md` · จุดอ่อนของ Zocial: `ZOCIAL-GAPS.md`

---

## 1. 🎛 ตี sentiment ตาม profile — `POST /sentiment` ← **ใช้อันนี้สำหรับงานใหม่**

เจ้าของเคาะโครงนี้ 3 ก.ย. 2026: **ใช้ worker ตัวเดียวกัน แต่แยกเป็น profile**
(ไม่ใช่ rubric รวมก้อนเดียว และไม่ใช่แยก worker)

| ใช้ร่วมกัน | แยกตาม profile |
|---|---|
| engine เรียก LLM · แคช · retry · FEEDBACK loop · โครง BASELINE · `brands.json` · `labels.md` | rubric/prompt · few-shot · ชุดวัดผล |

```jsonc
// ขอ
{ "texts": ["คอมเมนต์ 1", "คอมเมนต์ 2"],   // ≤ 50 ใบ · ส่ง "text" เดี่ยวก็ได้
  "profile": "cp_comment",                  // ใส่ใน body หรือ ?profile= ก็ได้
  "context": "โพสข่าวปลาหมอคางดำ",          // ไม่บังคับ — ช่วยให้แม่นขึ้นมาก
  "lens": "cp",                             // ไม่ใส่ = ใช้แกนหลักของ profile
  "model": "claude-opus-5", "effort": "low" }

// ตอบ
{ "ok": true, "ver": 35,
  "profile": "cp_comment", "rubric_version": "cp-v6", "lens": "cp",
  "model": "claude-opus-5",
  "missing": 0,
  "results": [{
    "label": "negative",                    // ป้ายของแกนที่ขอ
    "confidence": null,                     // ⚠️ ระบบยังไม่มีค่านี้ — คืน null ตรงๆ
    "lenses": { "cp": "Negative", "overall": "Negative" },
    "is_sarcasm": 0,
    "missing": 0                            // 1 = โมเดลไม่ตอบใบนี้
  }],
  "tokens": { "input": 1234, "output": 567 } }
```

**profile ที่มีตอนนี้** — ถามได้จาก `GET /` (คืน `profiles` มาให้)

| profile | input | rubric_version | เกณฑ์อยู่ที่ | สถานะ |
|---|---|---|---|---|
| `cp_comment` | คอมเมนต์โซเชียล | `cp-v6` | `RUBRIC-CP.md` | ✅ วัดแล้ว 92.8% |
| `news_post` | โพสข่าวจาก Zocial | — | — | 🚧 รอห้อง Zocial ตั้งต้นแล้วส่ง review |

> 🚫 **ชื่อ profile ที่ไม่รู้จัก = ตอบ 400 ไม่ใช่ตกกลับไปตัวปริยาย**
> ไม่งั้นพิมพ์ผิดแล้วได้ผลจาก rubric คนละตัวโดยไม่มีใครรู้
>
> 🚫 **`confidence` คืน `null` เสมอ — ระบบยังไม่มีตัวเลขนี้จริง**
> จะมีได้ต้องให้โมเดลตอบเพิ่ม = แก้ prompt = กระทบตัวเลข 92.8% ที่วัดไว้
> **ห้ามแต่งค่าขึ้นมาให้ดูมี** (กฎ "ไม่รู้ ≠ ค่าใดค่าหนึ่ง")
>
> ⚠️ **`missing` = โมเดลไม่ตอบใบนั้น ผู้เรียกต้องเช็คทุกครั้ง ห้ามนับเป็น neutral**
>
> 📌 **ทำไมไม่ใช่ `/api/sentiment`** — worker ตัวนี้เป็น Cloudflare **Worker** แยกจาก Pages
> (`comment-sentiment.s3445028.workers.dev`) ไม่ได้อยู่ใต้ `/api/` ของเว็บ
> ย้ายไป Pages Function ได้ แต่ต้องยก secret/KV/ขั้นตอน deploy ตามไปทั้งชุด
> → ถ้าอยากได้ path นั้นจริงๆ ทำ proxy บางๆ ที่ Pages ทีหลังได้

### 🔐 ต้องมีกุญแจ

`/sentiment` เป็น endpoint เดียวที่บังคับกุญแจ — เพราะเรียกจาก **เซิร์ฟเวอร์ถึงเซิร์ฟเวอร์**
กุญแจจึงเก็บเป็น Secret ได้จริง ไม่หลุดเหมือนของที่ฝังในหน้าเว็บ

```
x-worker-key: <WORKER_KEY>        # หรือ ?key=<WORKER_KEY>
```

| | |
|---|---|
| ไม่ตั้ง `WORKER_KEY` ที่ Cloudflare | **endpoint ปิด** (403 `endpoint_disabled`) — ไม่ใช่เปิดให้ทุกคน |
| ไม่ส่ง / ส่งผิด | 403 `bad_key` · **ไม่ยิงออกไปข้างนอกเลย ไม่เผาเงิน** |

> ⚠️ **ตั้ง `WORKER_KEY` ที่ Cloudflare เป็น Secret ก่อนเรียก** ไม่งั้นได้ 403 ตลอด
> 🚫 **ห้าม commit กุญแจลง repo — repo เป็น public**
>
> 🌐 **`ALLOW_ORIGIN`** (ไม่บังคับ) — ตั้งไว้แล้วจะ**บล็อกจริง** ถ้าคำขอมี `Origin` ไม่ตรง
> · กันได้แค่เบราว์เซอร์จากเว็บอื่น · `curl` ไม่ส่ง Origin มา จึงผ่าน — **ตัวที่กันสคริปต์คือ `WORKER_KEY`**
> · คำขอที่ไม่มี Origin ผ่านเสมอ ไม่งั้น server-to-server พังหมด

### 🚫 endpoint อื่นยังไม่มีกุญแจ — ตั้งใจ และต้องรู้ความเสี่ยง

`/analyze` `/resynth` `/paraphrase` `/comments` `/classify` `/credits` **ใครก็ยิงได้**
เพราะหน้าเว็บ `/issue/sentiment*.html` เรียกจากเบราว์เซอร์ — ใส่กุญแจแล้วต้องฝังในโค้ด
ที่ใครก็เปิดดูได้ = **ไม่ใช่ความลับตั้งแต่แรก** แถมหน้าเว็บพังทันที
(ปัญหาเดียวกับปุ่ม ⚑ กับ `/api/flags` ที่จดไว้ใน `CLAUDE.md`)

| ยิงแล้วเสียอะไร | |
|---|---|
| `/analyze` · `/comments` | 🔴 เครดิต ScrapeCreators (จ่ายเงิน) |
| `/classify` · `/resynth` · `/paraphrase` | 🔴 โควตา Claude (จ่ายเงิน) |
| `POST /feedback` | 🔴 เขียน KV — โควตา 1,000/วัน **ใช้ร่วมทั้งโปรเจกต์** |

**ที่ยังทำได้และยังไม่ได้ทำ:** ตั้ง Rate Limiting Rules ที่ Cloudflare (ไม่ต้องแก้โค้ด)

## 1b. `POST /classify` — ของเดิม ยังใช้ได้

หน้า `sentiment-eval.html` ใช้อยู่ · คืน 2 แกนดิบ (`sentiment_cp` · `overall_cred` · `is_sarcasm`)
ตอนนี้ติด `profile` + `rubric_version` กลับมาด้วยเหมือนกัน · **งานใหม่ควรใช้ `/sentiment`**


## 2. 🧠 ระบบเรียนรู้ — `POST /feedback`

**นี่คือส่วนที่ทำให้ "tool เรียนรู้ได้"** · ทำงาน 3 ชั้น (รายละเอียดเต็มใน `FEEDBACK.md`)

```
① ผู้ใช้กดแก้ป้ายบนหน้าเว็บ    → แก้รายงานใบนั้นทันที ไม่ยิงหลังบ้าน
② POST /feedback              → เข้ากองรอตรวจ · ⚠️ ยังไม่มีผลกับใคร
③ เอากองมาวัดกับชุดเฉลย 475 ใบ → ผ่านเกณฑ์แล้วคนเขียนเป็นกฎในโค้ด
```

```jsonc
{ "items": [{
    "text": "ขอบคุณที่ดูแลผืนป่าให้พวกเรา",  // ≤ 400 ตัวอักษร
    "was": "neutral",      // ป้ายก่อนแก้ (ที่แสดงอยู่บนจอ)
    "now": "positive",     // ป้ายที่ผู้ใช้แก้เป็น
    "target": "overall",   // แก้แกนไหน: "cp" | "overall"
    "from": "neutral",     // 🏷 ป้ายเดิมจาก Zocial  ← เพิ่มให้ห้องใหม่โดยเฉพาะ (v34)
    "src": "zocial",       // 🏷 แก้มาจากชั้นไหน: "zocial" | "ai"
    "model": "zocial", "ver": 34, "rubric": "v6"
  }] }                                        // ≤ 60 ใบต่อคำขอ
```

**`from` + `src` คือช่องที่เพิ่มให้ตอบโจทย์ "จำ pattern ที่ Zocial ผิด"** (worker v34)
ไม่มี 2 ช่องนี้ = รู้แค่ว่าผิด แต่ไม่รู้ว่า **ต้นทางผิดแบบไหน** ซึ่งเป็นข้อมูลที่เอาไป
คัดกรองได้ว่าใบไหนต้องส่งให้ AI ตรวจซ้ำ → **ประหยัดเงินโดยตรง**

> 🚫 **ห้ามส่งชื่อผู้คอมเมนต์ · ลิงก์โพส · เวลาแบบละเอียด** — เก็บแค่ข้อความกับป้าย
>    (`PRIVACY_NOTE.md` ข้อ 4.1) · ระบบตัดค่าที่ไม่รู้จักทิ้งให้อยู่แล้ว แต่อย่าส่งมาแต่แรก
> 🚫 **ห้ามส่งอัตโนมัติตอนผู้ใช้กดแก้ป้าย** — ต้องให้ผู้ใช้กดปุ่มส่งเอง
>    (แก้ทีละใบ 10 ใบ = ยิง 10 ครั้ง) · หน้า `/issue/sentiment.html` ทำแบบนี้อยู่ ดูเป็นตัวอย่างได้
> 🚫 **ของในกองไม่มีผลกับ AI จนกว่าจะผ่านการวัด** — เป็นกฎที่เจ้าของสั่งไว้ชัด
>    ห้ามเขียนโค้ดให้กองไปสอน AI เอง ไม่ว่ากรณีใด

**อ่านกอง** — `GET /feedback?key=<FEEDBACK_KEY>` (ต้องมีกุญแจเสมอ · ไม่ตั้ง = ปิด)
มีหน้าอ่านให้แล้วที่ `/issue/sentiment-eval.html` (กล่อง "กองรอตรวจ")

---

## 3. อะไรมีอยู่แล้ว / อะไรต้องทำเอง

| | มีอยู่แล้ว | ต้องทำเอง |
|---|---|---|
| ตี sentiment | ✅ `/classify` | |
| ระบบเรียนรู้ 3 ชั้น | ✅ `/feedback` | |
| เกณฑ์ตัดสิน | ✅ `RUBRIC-CP.md` (แกน CP) | ⚠️ แกนโทนรวมยังไม่มีเอกสาร — อยู่ใน `systemTwoLens()` ต้องยกออกมาเขียน |
| นิยามป้าย 3 ค่า + ไม่นับ reply | ✅ | |
| รายชื่อแบรนด์ CP / ชื่อลวง | ⚠️ ยังกระจายอยู่ 5 ไฟล์ — ควรรวมเป็น `/shared/sentiment/brands.json` | |
| ยอดของ **ตัวโพส** (ยอดดู · ถูกใจ · แชร์) | ✅ `post_stats` ใน `/analyze` และ `/comments` (worker ≥ v37) | |
| อ่านไฟล์ Zocial · จับคู่โพส-คอมเมนต์ · หน้าฟีด | | ✅ ของห้องใหม่ |

> 📊 **`post_stats` — ยอดของตัวโพส ไม่ใช่ของคอมเมนต์**
> `{ views, likes, comments, shares }` · **ไม่รู้ = `null` ห้ามอ่านเป็น 0**
> (YouTube ไม่เปิดเผยยอดแชร์ · โพสข้อความของ Facebook ไม่มียอดดู)
> · 💰 **ไม่เสียเครดิต/โควตาเพิ่มเลย** — อ่านจาก response ก้อนเดียวกับที่ขอหัวข้อ/รูปปกอยู่แล้ว
> · ⚠️ เอา `likes+comments+shares` ของ 2 แพลตฟอร์มมาเทียบกันตรงๆ ไม่ได้ ถ้าชนิดที่มีไม่เท่ากัน

---

## 4. เพดานที่มีอยู่ — ต้องออกแบบให้อยู่ในนี้

| | ค่า | ทำไม |
|---|---|---|
| `/classify` | 50 ใบ/คำขอ | หน้าเว็บวนเอง |
| `/feedback` | 60 ใบ/คำขอ · กองเก็บ 500 ใบ | KV ของแผนฟรีเขียนได้ 1,000 ครั้ง/วัน **ใช้ร่วมทั้งโปรเจกต์** |
| ข้อความ | 400 ตัวอักษร | |

> ⚠️ **`/feedback` เขียน KV** — โควตาใช้ร่วมกับแดชบอร์ดข่าวทั้งหมด
> ยิงถี่เกินจะทำให้ทั้งเว็บพัง · ส่งเป็นก้อนตอนผู้ใช้กดปุ่ม ไม่ใช่ยิงทีละใบ
> 🚫 **`/classify` ไม่เขียน KV เลย** — ยิงได้ตามสบายในแง่โควตา KV แต่เสียเงินค่า AI

---

## 5. 💰 ต้นทุนจริง (วัดแล้ว)

จาก `BASELINE.md`: **เข้า 257 · ออก 93 โทเคน ต่อ 1 ข้อความ** (รอบ 452 ข้อ)
→ Opus 5 ≈ **0.126 บาท/ใบ** · Haiku 4.5 ถูกลง 5 เท่า

> ⚠️ **ยังไม่เคยวัดความแม่นของ Haiku กับชุดเฉลย** — วัดแล้วมีแค่ Opus (92.8%) กับ Sonnet
> จะเปลี่ยนไปใช้ Haiku ต้องวัดก่อน (~$1.2/รอบ) ห้ามเปลี่ยนเพราะราคาอย่างเดียว
>
> 💡 **ตัวลดค่าใช้จ่ายที่ได้ผลที่สุดไม่ใช่การเปลี่ยนโมเดล** — คือไม่ตีของซ้ำ
> ไฟล์ Zocial มี `_id` ไม่ซ้ำ 100% เก็บไว้แล้วข้ามใบเดิมได้เลย (`ZOCIAL-GAPS.md` ข้อ 1.3)

---

## 6. เปลี่ยนโมเดลกลางทาง

ตั้ง env `CLAUDE_MODEL` ที่ Cloudflare แล้ว Retry deployment — ไม่ต้องแก้โค้ด
หรือส่ง `model` มากับคำขอ `/classify` เป็นรายครั้ง (มี allowlist กันพิมพ์ผิด)

> ⚠️ แคชคำสั่งเป็นของแต่ละโมเดล → เปลี่ยนแล้วรอบแรกจ่ายเต็ม
> ⚠️ **ต้องเก็บว่าแถวไหนตีด้วยโมเดลอะไร** (`model` + `ver` + `rubric`) ไม่งั้นข้อมูลข้ามวัน
>    จะมี 2 โมเดลปนกันโดยไม่มีใครรู้ แล้วเทียบตัวเลขข้ามช่วงไม่ได้

_เขียน 3 ก.ย. 2026 · worker v36 (profile-based + shared secret)_
