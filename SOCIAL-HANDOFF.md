# Social Media Dashboard — handoff (ยังไม่เริ่มเขียนโค้ด)

แดชบอร์ดใหม่: ดูผลของ **ช่องโซเชียลของเราเอง** (ไม่ใช่เทรนด์สาธารณะแบบ `/trend/`)

> 📌 **อ่าน `CLAUDE.md` ก่อนเสมอ** — กฎ branch/production · ฟีเจอร์มาตรฐาน · กฎ KV
> อยู่ในนั้นหมดแล้ว ไฟล์นี้เสริมเฉพาะเรื่องแดชบอร์ดใหม่ **อย่าก๊อปกฎมาเขียนซ้ำที่นี่**

---

## ❓ 2 เรื่องที่เจ้าของยังไม่ได้ตอบ — ถามก่อนเริ่ม

### 1. เอาแบบไหน

| | `cp-dashboard-680.pages.dev/social/` | โปรเจกต์ Cloudflare ใหม่แยก |
|---|---|---|
| ตัวกรอง/ปุ่ม/ระบบอัปเดตร่วมกัน | ✅ ได้ทันที | ❌ ต้องตั้งใหม่ทั้งชุด |
| KV / env / secret | ใช้ของเดิม | ต้องผูกใหม่หมด |
| URL | เป็น path ต่อท้าย | แยกจริง |
| **แนะนำ** | ✅ **อันนี้** | เมื่อจำเป็นจริงๆ เท่านั้น |

> ⚠️ `*.pages.dev` ตั้งชื่อ subdomain เองไม่ได้ — ชื่อผูกกับ **ชื่อ branch**
> อยากได้โดเมนสวยต้องใช้ **Custom domain** ของตัวเอง (ดู `CLAUDE.md` หัวข้อ `/admin/`)

### 2. จะดูช่องอะไรบ้าง
YouTube · Facebook Page · Instagram · TikTok · X — และ **ดูอะไร** (ยอดวิว/ผู้ติดตาม/engagement/โพสต์ที่ดังสุด)

---

## 🔌 การต่อข้อมูล — ต่างจากที่เคยทำมาทั้งหมด

ที่ผ่านมาเราดึง **ข้อมูลสาธารณะ** (RSS/หน้าเว็บ) · คราวนี้เป็น **ช่องของเราเอง** จึงขอสิทธิ์ทางการได้

| ช่อง | ทางที่ใช้ได้ | หมายเหตุ |
|---|---|---|
| **YouTube** | YouTube Data API — **มี `YT_API_KEY` ใน Cloudflare แล้ว** | ข้อมูลช่องตัวเองเชิงลึก (watch time/retention) ต้องใช้ **YouTube Analytics API** ซึ่งต้อง OAuth ไม่ใช่ API key |
| **Facebook Page / Instagram** | Meta Graph API (Page Access Token) | เจ้าของเพจขอเองได้ · **ยิงแบบไม่มี token ปิดตายแล้ว** (เคยลองครบทุกทาง — ดู `CLAUDE.md` ตารางที่เคยทดสอบ) |
| **TikTok** | TikTok for Business / Display API | **ทางฟรีปิด** (`40101 no permission`) ต้องสมัครเป็น developer |
| **X** | X API แบบเสียเงิน | ทางฟรีที่เคยลองใช้กับเทรนด์ ใช้กับสถิติช่องตัวเองไม่ได้ |

> 🔑 **ทุกอันต้องมี token — ห้ามส่งมาในแชท ห้าม commit** (repo เป็น public)
> ใส่ที่ Cloudflare → Settings → Variables and Secrets แบบ **Secret**
> และ **ต้องใส่ทั้ง Production และ Preview** ไม่งั้น `dev` จะไม่เห็น (เคยเสียเวลามาแล้ว)
> แก้ env แล้วต้อง **Retry deployment** ทุกครั้ง

> ⚠️ **token ของ Meta หมดอายุ** — Page token แบบยาวก็ยังต้องต่ออายุ
> ต้องมีทางบอกเจ้าของเมื่อ token ตาย ไม่ใช่ให้คอลัมน์ว่างเงียบๆ

---

## ♻️ ของที่มีอยู่แล้ว เอามาใช้ได้เลย (อย่าเขียนใหม่)

- `functions/api/_lib/noise.js` — ตัวกรอง "อะไรไม่ใช่ข่าว" **ถ้าแดชบอร์ดใหม่แตะข่าว ต้อง import จากที่นี่ตั้งแต่วันแรก** (กฎถาวร)
- `flags.js` — ปุ่ม ⚑ 🗂 🔤 + กล่อง keyword (สั่งด้วย `ui:` ตอน `Flags.init()`)
- `sw.js` + ตัวเช็คเวอร์ชัน + ป้าย `#vtag` — ก๊อปสคริปต์จาก `sd.html` มาทั้งก้อน
- โครงหน้า carousel มือถือ — ดู `trend/index.html` + `trend/styles.css`
- `/admin/` — ถ้ามีของให้เจ้าของกดจัดการ ให้เพิ่มใน `SCOPES` ของ `admin/app.js`

## ✅ ถือว่าเสร็จเมื่อ

- ฟีเจอร์มาตรฐาน **ครบทั้ง 6 ข้อ** (favicon · PWA · เช็คเวอร์ชัน · ปุ่ม 🏠 · รีเฟรชเงียบ · ไอคอนหมุน) — ดูตารางใน `CLAUDE.md`
- **มีการ์ดบน `index.html`** (landing) — แดชบอร์ดใหม่ต้องเท่าเทียมของเดิมตั้งแต่วันแรก
- ผ่านเช็คลิสต์ 4 ข้อ (ทุกแดชบอร์ด · แดชบอร์ดใหม่ · มือถือ · แอปที่ติดตั้งแล้ว)
- **นับ KV read/write ต่อ request แล้วไม่เกิน 1-2 ครั้ง** (โควตา 1,000/วัน ใช้ร่วมทั้งโปรเจกต์)

> ⚠️ **API ของโซเชียลมีโควตาของตัวเอง** ต้อง cache ผลลง KV/edge เหมือน `feeds.js`
> และมี **เลขเวอร์ชันข้อมูล** (`DATA_VER`/`CACHE_VER`) คั่นใน key ไม่งั้นแก้โครงข้อมูลแล้วผู้ใช้เห็นของเก่า

---

## 📌 ค้างจาก session ก่อน (ไม่เกี่ยวกับงานนี้ แต่ยังไม่จบ)

1. **กราฟ Micro Plastic ของ `/issue/trends.html` กับ `sd.html` ให้ค่าไม่ตรงกัน** — เจ้าของยังไม่ตอบว่าจะแก้แบบไหน
   - เหตุ 1: ตั้งช่วงเวลาคนละอัน (3 ปี vs 6 เดือน) — Google ให้ 100 กับจุดสูงสุด **ในช่วงที่เลือก** เทียบข้ามช่วงไม่ได้
   - เหตุ 2: `uniqKeywords()` ลบช่องว่างก่อนเทียบ เลยมองว่า `ไมโครพลาสติก` = `ไมโคร พลาสติก` แล้วเก็บตัวที่มาก่อน — สองหน้าเรียงชิพไม่เหมือนกัน คำที่ส่งให้ Google เลยต่างกัน (และภาษาไทย ช่องว่างทำให้เป็นคนละคำค้นจริงๆ)
   - ⚠️ ตัวกันคำซ้ำนี้ใส่ไว้แก้ Google ขึ้น "Oops" (`pm 2.5` + `pm2.5`) — แก้แล้วอาจเจอ Oops อีก และ **ยิงทดสอบกับ Google จาก sandbox ไม่ได้**
2. เจ้าของยังต้องเอา `sheet/news-to-sheet.gs` ไปวางใน Apps Script เองแล้วรัน `sortNewestFirst()` 1 ครั้ง
3. ยังไม่ได้ยืนยันว่า query ใน Google Alerts ของจริงตรงกับ 74 คำใน `trend-feeds.config.js`
