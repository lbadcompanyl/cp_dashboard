# Social Media Dashboard — handoff

แดชบอร์ดใหม่: ดูผลของ **ช่องโซเชียลของเราเอง** (ไม่ใช่เทรนด์สาธารณะแบบ `/trend/`)

> 📌 **อ่าน `CLAUDE.md` ก่อนเสมอ** — กฎ branch/production · ฟีเจอร์มาตรฐาน · กฎ KV
> อยู่ในนั้นหมดแล้ว ไฟล์นี้เสริมเฉพาะเรื่องแดชบอร์ดใหม่ **อย่าก๊อปกฎมาเขียนซ้ำที่นี่**

---

## ✅ ตกลงกันแล้ว (14 ส.ค. 2026) — เลิกถามซ้ำ

| เรื่อง | สรุป |
|---|---|
| **ที่อยู่** | **`cp-dashboard-680.pages.dev/social/`** (ไม่แยกโปรเจกต์) |
| **API อยู่ไหน** | **ใต้ `/social/api/...`** ไม่ใช่ `/api/social/...` — เพื่อให้ Access คุมด้วย **กฎเดียว** ครอบทั้งหน้าเว็บและ API |
| **ความปลอดภัย** | **Cloudflare Access เฉพาะ `/social/*`** · แดชบอร์ดเก่ายังเปิดสาธารณะ ไม่ถูกแตะ |
| **ช่อง** | YouTube · Facebook Page · TikTok |
| **ตัวเลข** | เอาทั้งหมดที่ดึงได้ (ผู้ติดตาม · ยอดวิว · engagement · โพสต์ที่ดังสุด) |
| **TikTok** | **ทำโครงรอไว้** — คอลัมน์ขึ้นว่า "ยังไม่ได้เชื่อมต่อ" จนกว่าจะได้สิทธิ์ |
| **YouTube** | ช่อง `@CPFNews` · เริ่มจาก**ชั้นสาธารณะ**ก่อน แล้วค่อยเติมชั้นหลังบ้าน (OAuth) |

### ทำไมไม่แยกโปรเจกต์ ทั้งที่เป็นข้อมูลภายใน

เพราะ **ตัวที่กันคนนอกคือ Access ไม่ใช่การแยกโปรเจกต์** — โปรเจกต์ใหม่ก็เปิดสาธารณะเหมือนกัน
ถ้าไม่ตั้ง Access · แยก "สิทธิ์เข้าถึง" ก็พอ ไม่ต้องแยก "โปรเจกต์"

ข้อได้เปรียบเดียวที่ B (แยกโปรเจกต์) มีจริงคือ **โอกาสตั้งกฎ path พลาดน้อยกว่า** —
แก้ด้วยการ**ยัด API ไว้ใต้ `/social/` ด้วย** เหลือกฎเดียว ไม่มีอะไรให้ลืมล็อก

> 🔴 **ยังไม่ได้ตั้ง Access จริง (สถานะวันที่ 19 ส.ค. 2026)** — เจ้าของยืนยันว่าจะตั้ง แต่ยังไม่ได้ทำ
> **ระหว่างนี้ `/social/` และ `/social/api/*` ใครมีลิงก์ก็เปิดได้** (มีแค่ `noindex` กัน Google)
> · ถอดการ์ดออกจากหน้าหลักแล้วเพื่อไม่ให้เดินไปเจอโดยบังเอิญ — **การซ่อนลิงก์ไม่ใช่การกันคนนอก**
> · ⚠️ **`privacy.html` กับ `terms.html` เขียนไว้แล้วว่า "Access is restricted by an identity check"**
>   ซึ่งตอนนี้ยังไม่จริง — 2 หน้านั้นเปิดสาธารณะและใช้ยื่นขอสิทธิ์ API
>   **ตั้ง Access ให้เสร็จก่อนส่งลิงก์ 2 หน้านี้ให้ใครอ่าน**
> · วิธีตั้ง: Cloudflare → Zero Trust → Access → Applications → Self-hosted
>   ใส่โดเมนของโปรเจกต์ + path `social/*` แล้วตั้ง policy เป็นอีเมลของทีม
>   ⚠️ ต้องครอบ **ทั้ง production และ preview** ไม่งั้น preview URL ของ branch งานยังเปิดโล่ง

> ⚠️ **ยังไม่ยืนยันว่า Access ใช้กับ `*.pages.dev` ตัว production ได้** — ที่แน่ๆ คือใช้กับ preview ได้
> **ยืนยันจาก session ไม่ได้** (เข้า Cloudflare กับ docs ไม่ได้) ต้องให้เจ้าของเปิดดูเอง
> · **ถ้าใส่ไม่ได้** → ทางแก้คือ Basic Auth ในโค้ด (ล็อกราย path ได้เหมือนกัน ใช้กับ `pages.dev` ได้แน่นอน)
> หรือใช้ Custom domain

> ⚠️ `*.pages.dev` ตั้งชื่อ subdomain เองไม่ได้ — ชื่อผูกกับ **ชื่อ branch**
> อยากได้โดเมนสวยต้องใช้ **Custom domain** ของตัวเอง (ดู `CLAUDE.md` หัวข้อ `/admin/`)
> · เติม Custom domain ทีหลังได้ ไม่ต้องรื้อ `/social/`

---

## 🔧 n8n — ให้พิจารณาเป็นทางเลือกด้วย

**เจ้าของแจ้ง (14 ส.ค. 2026)** ว่ามี n8n ใช้ได้ · รายละเอียดกลางอยู่ใน **`CLAUDE.md` หัวข้อ `🔧 n8n`**
**อย่าเขียนซ้ำที่นี่** ตรงนี้เก็บเฉพาะที่เกี่ยวกับแดชบอร์ดนี้

### 2 แบบที่เลือกได้ — ยังไม่ตัดสิน

| | **n8n ดึงแล้วส่งเข้ามาเก็บ** | **Cloudflare ดึงเอง** |
|---|---|---|
| ใครคุยกับ API โซเชียล | n8n | Pages Functions |
| token เก็บที่ไหน | ใน n8n | Cloudflare Secret |
| **ตั้งเวลาดึงได้ไหม** | ✅ ได้ | ❌ ต้องรอคนเปิดเว็บ |
| **OAuth ของ 3 ช่อง** | n8n จัดการให้ | ต้องเขียนเอง |
| ถ้าตัวมันล่ม | ข้อมูลค้าง (ยังเสิร์ฟของเก่าได้) | ไม่เกี่ยว |
| งานที่ต้องทำเพิ่ม | ตั้ง workflow + ทางรับข้อมูลเข้า + กุญแจ | เขียนโค้ดต่อ API เอง |

> 💡 **น่าจะคุ้มที่สุดกับ 3 อย่างนี้:** ตั้งเวลาดึงข้อมูล · จัดการ refresh token ของ YouTube/FB/TikTok ·
> ปลุก `/api/trend/feeds` แทนตัวตั้งเวลาของ Google Sheet
>
> ⚠️ ถ้าเลือกทาง n8n **ยังต้องมีที่เก็บข้อมูลฝั่งเราอยู่ดี** (KV) และ **หน้าเว็บต้องบอกได้ว่าข้อมูลเก่าแค่ไหน**
> ไม่ใช่โชว์ตัวเลขค้างโดยไม่มีอะไรบอก — กฎเดียวกับที่ห้าม `/ir/` เก็บสำเนาข่าวไว้โชว์

### 2. จะดูช่องอะไรบ้าง — ตอบแล้ว ดูตารางบนสุด

---

## 🔌 การต่อข้อมูล — ต่างจากที่เคยทำมาทั้งหมด

ที่ผ่านมาเราดึง **ข้อมูลสาธารณะ** (RSS/หน้าเว็บ) · คราวนี้เป็น **ช่องของเราเอง** จึงขอสิทธิ์ทางการได้

| ช่อง | ทางที่ใช้ได้ | หมายเหตุ |
|---|---|---|
| **YouTube** | YouTube Data API — **มี `YT_API_KEY` ใน Cloudflare แล้ว** | ข้อมูลช่องตัวเองเชิงลึก (watch time/retention) ต้องใช้ **YouTube Analytics API** ซึ่งต้อง OAuth ไม่ใช่ API key |
| **Facebook Page / Instagram** | Meta Graph API (Page Access Token) | เจ้าของเพจขอเองได้ · **ยิงแบบไม่มี token ปิดตายแล้ว** (เคยลองครบทุกทาง — ดู `CLAUDE.md` ตารางที่เคยทดสอบ) |
| **TikTok** | TikTok for Business / Display API | **ทางฟรีปิด** (`40101 no permission`) ต้องสมัครเป็น developer |
| **X** | X API แบบเสียเงิน | ทางฟรีที่เคยลองใช้กับเทรนด์ ใช้กับสถิติช่องตัวเองไม่ได้ |

> 🔑 **ทุกอันต้องมี token — ห้ามส่งมาในแชท ห้าม commit** (repo เป็น public)
> ใส่ที่ Cloudflare → Settings → Variables and Secrets แบบ **Secret**
> และ **ต้องใส่ทั้ง Production และ Preview** ไม่งั้น `dev` จะไม่เห็น (เคยเสียเวลามาแล้ว)
> แก้ env แล้วต้อง **Retry deployment** ทุกครั้ง

> ⚠️ **token ของ Meta หมดอายุ** — Page token แบบยาวก็ยังต้องต่ออายุ
> ต้องมีทางบอกเจ้าของเมื่อ token ตาย ไม่ใช่ให้คอลัมน์ว่างเงียบๆ

### 📊 YouTube มีข้อมูล 2 ชั้น — แยกให้ออก

| | **ชั้นสาธารณะ** | **ชั้นหลังบ้าน** |
|---|---|---|
| ได้อะไร | ผู้ติดตาม · ยอดวิวรวม · จำนวนคลิป · ต่อคลิป: วิว/ไลก์/คอมเมนต์ · คลิปไหนดังสุด | เวลาที่คนดูรวม · **ดูจบกี่ %** · ผู้ชมเป็นใคร · มาจากทางไหน · รายได้ |
| ใช้อะไร | YouTube **Data** API + `YT_API_KEY` (**มีแล้ว**) | YouTube **Analytics** API + **OAuth** |
| ต้องเตรียม | **แค่ชื่อช่อง** (`@CPFNews`) | เจ้าของช่องกดอนุญาต 1 ครั้ง → เก็บ refresh token |

**ทำชั้นสาธารณะก่อน** แล้วเติมหลังบ้านทีหลัง — ติดขั้นตอน OAuth ก็ยังมีแดชบอร์ดใช้ได้

> 🔴 **กับดักที่คนเจอบ่อยที่สุดของ OAuth: refresh token ตายใน 7 วัน**
> ถ้าแอปใน Google Cloud ยังเป็นสถานะ **"Testing"** · **ต้องกด Publish เป็น Production** ถึงจะอยู่ยาว
>
> ⚠️ **ขอ scope แบบอ่านอย่างเดียวเท่านั้น** (`yt-analytics.readonly`)
> ต่อให้ token หลุด คนได้ไปก็**ลบคลิปไม่ได้ แก้ช่องไม่ได้ โพสต์ไม่ได้** — ห้ามขอ `youtube.force-ssl`
>
> 💡 **ไม่ต้องทำหน้าล็อกอินบนเว็บ** — ให้เจ้าของกดอนุญาตครั้งเดียวนอกระบบ แล้วเอา refresh token
> มาใส่เป็น Secret · ง่ายกว่าและปลอดภัยกว่าการเปิดทางล็อกอินบนเว็บจริง (หรือให้ n8n ถือ token ให้)

### 📌 TikTok — "ทางฟรีปิด" ไม่ได้แปลว่าเป็น dev ไม่ได้

สมัคร TikTok for Developers ได้ **ฟรี ใครก็สมัครได้** · ที่ปิดคือ**ทางลัดที่ไม่ต้องสมัคร** (`40101`)
· ขั้นตอน: สมัคร → สร้าง app → ขอสิทธิ์อ่านข้อมูลตัวเอง → **รออนุมัติ (หลายวันถึงเป็นสัปดาห์)**
· บางกรณีต้องมี **หน้านโยบายความเป็นส่วนตัว** บนเว็บด้วย

---

## ♻️ ของที่มีอยู่แล้ว เอามาใช้ได้เลย (อย่าเขียนใหม่)

- `functions/api/_lib/noise.js` — ตัวกรอง "อะไรไม่ใช่ข่าว" **ถ้าแดชบอร์ดใหม่แตะข่าว ต้อง import จากที่นี่ตั้งแต่วันแรก** (กฎถาวร)
- `flags.js` — ปุ่ม ⚑ 🗂 🔤 + กล่อง keyword (สั่งด้วย `ui:` ตอน `Flags.init()`)
- `sw.js` + ตัวเช็คเวอร์ชัน + ป้าย `#vtag` — ก๊อปสคริปต์จาก `sd.html` มาทั้งก้อน
- โครงหน้า carousel มือถือ — ดู `trend/index.html` + `trend/styles.css`
- `/admin/` — ถ้ามีของให้เจ้าของกดจัดการ ให้เพิ่มใน `SCOPES` ของ `admin/app.js`

## ✅ ถือว่าเสร็จเมื่อ

- ฟีเจอร์มาตรฐาน **ครบทั้ง 6 ข้อ** (favicon · PWA · เช็คเวอร์ชัน · ปุ่ม 🏠 · รีเฟรชเงียบ · ไอคอนหมุน) — ดูตารางใน `CLAUDE.md`
- **มีการ์ดบน `index.html`** (landing) — แดชบอร์ดใหม่ต้องเท่าเทียมของเดิมตั้งแต่วันแรก
- ผ่านเช็คลิสต์ 4 ข้อ (ทุกแดชบอร์ด · แดชบอร์ดใหม่ · มือถือ · แอปที่ติดตั้งแล้ว)
- **นับ KV read/write ต่อ request แล้วไม่เกิน 1-2 ครั้ง** (โควตา 1,000/วัน ใช้ร่วมทั้งโปรเจกต์)

> ⚠️ **API ของโซเชียลมีโควตาของตัวเอง** ต้อง cache ผลลง KV/edge เหมือน `feeds.js`
> และมี **เลขเวอร์ชันข้อมูล** (`DATA_VER`/`CACHE_VER`) คั่นใน key ไม่งั้นแก้โครงข้อมูลแล้วผู้ใช้เห็นของเก่า

---

## 🎛 หน้าตาที่ตกลงกันแล้ว (19 ส.ค. 2026) — อย่ารื้อโดยไม่ถาม

ทั้งหมดนี้มาจากรอบรีวิวของเจ้าของ อ้างแบบจาก GA4 · **เทสต์ `tests/social.mjs` คุมไว้ทุกข้อ**

| ของ | กติกา |
|---|---|
| **กราฟแนวโน้ม** | **อันเดียว** สลับตัวชี้วัดด้วยชิพ (ผู้ติดตาม/ยอดวิว/การเข้าถึง/การมีส่วนร่วม/ER) — เดิมวางกราฟใหญ่ 3 อันเรียงกัน หน้ายาวและอ่านทีละอันไม่ได้เทียบอะไร |
| **เส้นในกราฟ** | เส้นแรก **"รวมทุกช่อง"** (ดำ) แล้วตามด้วยเส้นรายช่อง — เจ้าของแจ้งว่าเส้นรายช่องอย่างเดียว "นิ่งเกิน" · **ER รวมต้องคิดจาก การมีส่วนร่วมรวม ÷ การมองเห็นรวม** ห้ามเอา ER รายช่องมาเฉลี่ย |
| **กล่องสรุปให้อ่าน** | ใต้ตัวเลขรวม สูงสุด 3 ข้อ · **เป็นกฎตายตัว ไม่ใช่ AI** อธิบายได้ว่าทำไมถึงขึ้นข้อนั้น · ข้อที่อ้างช่วงก่อนหน้าห้ามขึ้นเมื่อเลือก "ไม่เทียบ" · ฐานต่ำกว่า `INSIGHT_MIN_BASE` ห้ามคิดเป็น % |
| **ตัวเลขรวม** | ป้ายต้องบอกว่า **รวมกี่ช่อง** — ชิพปิดช่องได้ ตัวเลขจึงเปลี่ยนได้ทั้งที่ป้ายเขียนว่า "รวม" |
| **หัวคอลัมน์ตารางรายช่อง** | กดแล้วเด้งไปแท็บของช่องนั้น (drill-down) · ต้องเป็น `<button>` จริง |
| **แถบควบคุมติดขอบ** | ช่วงเวลา + แท็บ + ชิพเลือกช่อง อยู่ในแถบเดียวกันและติดขอบบน · **บนมือถือห้ามสูงเกิน 1 ใน 3 ของจอ** (ตอนนี้ 231/844) เพิ่มอะไรลงแถบนี้ต้องวัดใหม่ทุกครั้ง |
| **คอนเทนต์เด่น** | 2 อันดับวางคู่กัน (คนมีส่วนร่วมมากสุด / คนดูมากสุด) แต่ละอันดับ **แยกกล่องตามช่อง** หยิบช่องละใบ · ⚠️ ห้ามเอาทุกช่องมาเรียงรวมกัน — ER คิดคนละสูตร ช่องที่นับแชร์ด้วยจะกวาดอันดับไปหมด |
| **แกน %** | **ห้ามเติมเครื่องหมาย `+`** — ER เป็น "ระดับ" ไม่ใช่ "การเปลี่ยนแปลง" (เคยขึ้น `+8%` ทั้งที่แปลว่า 8%) |

### 🔌 ช่องที่ยังไม่ได้เชื่อมต่อ — เป็นสถานะที่ตั้งใจ ไม่ใช่ข้อผิดพลาด

> ⚠️ **"ยังไม่ได้เชื่อม" กับ "เชื่อมแล้วแต่ช่วงนี้ไม่มีข้อมูล" คนละเรื่องกัน**
> ถ้าขึ้นข้อความเดียวกัน เจ้าของจะไล่หาสาเหตุผิดทาง (ขยายช่วงเวลาเท่าไหร่ก็ไม่มีอะไรมา)

- อ่านสถานะจาก **ชั้นข้อมูล** (`DATA.platforms[pk].status = {connected, need[]}`) ไม่ใช่ที่หน้าเว็บ
  → พอต่อของจริงแล้ว หน้าเว็บไม่ต้องแก้อะไรเลย เอา `need[]` จาก response ของ API มาใส่ตรงนี้
- แท็บของช่องนั้น = การ์ดบอกว่า **ต้องใส่ค่าอะไรใน Cloudflare** (ไม่ใช้สีแดง ไม่มีไอคอนเตือน)
- ยอดรวมบนหน้าภาพรวม **ไม่นับช่องนั้น** และมีบรรทัดบอกไว้ใต้ตัวเลขรวมเสมอ
- ชิพของช่องนั้น **กดไม่ได้แต่ห้ามซ่อน** — ซ่อนแล้วไม่มีใครรู้ว่ามีช่องที่หายไปจากยอดรวม
- ดูหน้าตาสถานะนี้ได้ด้วย `?off=facebook` (คั่นหลายช่องด้วยจุลภาค) — ตอนนี้อ่านจากข้อมูลจำลอง

## 🗺 ทำต่อใน phase 2 (เจ้าของสั่งไว้ว่ายังไม่ต้องทำตอนนี้)

- **Export / share** — ดึงตารางออกเป็น CSV · ลิงก์ที่แชร์แล้วเปิดมาได้ช่วงเวลา/ช่องเดิม
  (ตอนนี้ state อยู่ในหน่วยความจำอย่างเดียว ยังไม่ผูกกับ URL)
- **แท็บโฆษณา (paid)** — โครงรองรับแล้ว เพิ่มใน `TABS` ของ `social/metrics.js` ที่เดียว
- ต่อข้อมูลจริงแทนข้อมูลจำลอง — ดูหัวข้อ 🔌 การต่อข้อมูล ข้างบน

---

## 📌 ค้างจาก session ก่อน (ไม่เกี่ยวกับงานนี้ แต่ยังไม่จบ)

1. **กราฟ Micro Plastic ของ `/issue/trends.html` กับ `sd.html` ให้ค่าไม่ตรงกัน** — เจ้าของยังไม่ตอบว่าจะแก้แบบไหน
   - เหตุ 1: ตั้งช่วงเวลาคนละอัน (3 ปี vs 6 เดือน) — Google ให้ 100 กับจุดสูงสุด **ในช่วงที่เลือก** เทียบข้ามช่วงไม่ได้
   - เหตุ 2: `uniqKeywords()` ลบช่องว่างก่อนเทียบ เลยมองว่า `ไมโครพลาสติก` = `ไมโคร พลาสติก` แล้วเก็บตัวที่มาก่อน — สองหน้าเรียงชิพไม่เหมือนกัน คำที่ส่งให้ Google เลยต่างกัน (และภาษาไทย ช่องว่างทำให้เป็นคนละคำค้นจริงๆ)
   - ⚠️ ตัวกันคำซ้ำนี้ใส่ไว้แก้ Google ขึ้น "Oops" (`pm 2.5` + `pm2.5`) — แก้แล้วอาจเจอ Oops อีก และ **ยิงทดสอบกับ Google จาก sandbox ไม่ได้**
2. เจ้าของยังต้องเอา `sheet/news-to-sheet.gs` ไปวางใน Apps Script เองแล้วรัน `sortNewestFirst()` 1 ครั้ง
3. ยังไม่ได้ยืนยันว่า query ใน Google Alerts ของจริงตรงกับ 74 คำใน `trend-feeds.config.js`
