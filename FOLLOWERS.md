# 👥 ตัวดึงยอดผู้ติดตาม — YouTube · TikTok · Instagram · X · Facebook

ถามในแชทได้เลยว่า **"ตอนนี้ follower เท่าไหร่บ้าง"** แล้วได้ตัวเลขจริงกลับมา
พร้อมส่วนต่างจากครั้งก่อน · ใช้บัญชี **ScrapeCreators** กับ **Apify** ที่มีอยู่แล้ว

```
แชท ──(MCP)──► /api/followers/mcp ──► /api/followers ──┬─ ScrapeCreators  (ตัวหลัก)
                                                        └─ Apify           (ตัวสำรอง)
```

> 🔴 **ยังไม่เคยยิงของจริงเลยสักครั้ง** — เครื่องที่เขียนโค้ดนี้ออกเน็ตไปหา ScrapeCreators/Apify
> ไม่ได้ (403) · ที่ทดสอบแล้ว 87 ข้อคือ **"โค้ดทำตัวถูกไหมเมื่อได้คำตอบแบบนั้น"**
> **ไม่ใช่ "endpoint ของเขาชื่อนี้จริงไหม"** — ต้องลองบน preview ก่อน แล้วดูข้อ *ถ้าดึงไม่ได้* ข้างล่าง

---

## ตั้งค่า 3 ขั้น

### 1) ใส่กุญแจใน Cloudflare (Settings → Variables and Secrets → **Secret**)

| ตัวแปร | ต้องมีไหม | เอามาจากไหน |
|---|---|---|
| `FOLLOWERS_TOKEN` | ✅ **บังคับ** | ตั้งเอง สุ่มยาวๆ — ไม่มีตัวนี้ endpoint จะไม่ทำงานเลย |
| `SCRAPECREATORS_API_KEY` | ✅ (ตัวหลัก) | scrapecreators.com |
| `APIFY_TOKEN` | ⬜ (ตัวสำรอง) | Apify Console → Settings → Integrations |
| `APIFY_ACTOR_TIKTOK` ฯลฯ | ⬜ | ใส่เมื่อชื่อ actor ที่ตั้งไว้ไม่ตรง (ดูข้างล่าง) |

> ⚠️ **ใส่ทั้ง Production และ Preview** แล้วกด **Retry deployment** ไม่งั้นค่าใหม่ไม่มีผล
> 🚫 **ห้าม commit กุญแจลง repo เด็ดขาด — repo นี้เป็น public**
>
> 🔑 **ทำไมต้องมี `FOLLOWERS_TOKEN`** — `/api/*` เอาเข้า Cloudflare Access ไม่ได้
> (แดชบอร์ดสาธารณะเรียกใช้ร่วมกันอยู่) ปล่อยเปิดโล่ง = ใครก็ยิงได้ = **เผาเครดิตที่จ่ายเงิน**
> เรื่องเดียวกับที่ `GET /debugmeta` เคยทำมาแล้ว · ยังไม่ตั้ง = ตอบ 503 ไม่ยิงต้นทางสักครั้ง

### 2) ใส่บัญชีที่จะติดตามใน `followers.config.js`

```js
{ id: "tt-cpf", platform: "tiktok", handle: "cpfworldwide", label: "CPF (TikTok)", off: true },
```

ลบ `off: true` ออกเมื่อ **ยืนยัน handle ถูกแล้ว** (ยิงผิดบัญชี = เสียเครดิตฟรี)
ตอนนี้เปิดไว้ตัวเดียวคือ YouTube `@CPFNews` ที่ยืนยันได้จาก `SOCIAL-HANDOFF.md`

### 3) ต่อเข้าแชท — เลือกทางใดทางหนึ่ง

**ทาง ก. Connector (ง่ายสุด ไม่ต้องลงอะไร)**
Claude → Settings → Connectors → Add custom connector → ใส่ URL:
```
https://cp-dashboard-680.pages.dev/api/followers/mcp?key=<FOLLOWERS_TOKEN>
```

**ทาง ข. รันในเครื่อง (ใช้กับ Claude Desktop / Claude Code)**
```bash
claude mcp add cp-followers \
  --env FOLLOWERS_MCP_URL=https://cp-dashboard-680.pages.dev/api/followers/mcp \
  --env FOLLOWERS_TOKEN=<กุญแจ> \
  -- node /path/to/cp_dashboard/tools/followers-mcp.mjs
```

> ⚠️ **ยังไม่ได้ลองต่อกับ Claude ของจริง** — ทดสอบแล้วว่าโค้ดตอบตามสเปค MCP ถูกต้อง
> (initialize · tools/list · tools/call · notification) แต่ **หน้าจอ Connector จะรับหรือไม่
> ต้องให้เจ้าของกดลองเอง** · ทาง ข. ชัวร์กว่าเพราะเป็น stdio มาตรฐาน

---

## เครื่องมือที่แชทเรียกได้

| เครื่องมือ | ทำอะไร |
|---|---|
| `get_followers` | ยอดล่าสุด + ส่วนต่างจากครั้งก่อน · ถามบัญชีนอกลิสต์ได้ (ส่องคู่แข่ง) |
| `list_accounts` | บัญชีที่ตั้งไว้ ตัวไหนเปิด/ปิด |
| `get_follower_history` | ประวัติรายวันย้อนหลัง |

ตัวอย่างที่แชทตอบกลับ (ข้อมูลเข้าสมมติ · รูปแบบผลลัพธ์เป็นของจริงจากโค้ด):

```
ยอดผู้ติดตาม ณ 2026-08-28 (เวลาไทย)
CPF News (YouTube) · 124,300 ผู้ติดตาม · +180 จาก 1 วันก่อน
CPF (TikTok) · 88,900 ผู้ติดตาม · (ยังไม่มีค่าก่อนหน้าให้เทียบ)
❌ CPF (Facebook) — ดึงไม่สำเร็จ (ScrapeCreators: /v1/facebook/profile → HTTP 404)

เครดิต ScrapeCreators คงเหลือ 940
```

## เรียกตรงด้วย URL ก็ได้ (ไม่ต้องผ่านแชท)

```
/api/followers?key=<กุญแจ>                          ทุกบัญชีที่เปิดไว้
/api/followers?key=…&accounts=yt-cpfnews            เฉพาะที่เลือก
/api/followers?key=…&platform=tiktok&handle=xxx     บัญชีนอกลิสต์
/api/followers?key=…&provider=apify                 บังคับใช้เจ้าไหน
/api/followers?key=…&refresh=1                      ไม่เอาของใน cache
```

---

## 💰 ค่าใช้จ่าย · โควตา

| | |
|---|---|
| ScrapeCreators | **1 credit ต่อ 1 บัญชี ต่อ 1 ครั้งที่ดึงจริง** |
| Apify | คิดตามการรัน actor (แพงกว่าและช้ากว่า — เป็นตัวสำรองเท่านั้น) |
| cache | **6 ชม.** → ถามซ้ำในช่วงนั้นไม่เสียเครดิตเลย |
| เพดานต่อคำขอ | **12 บัญชี** (กันสั่งยิงทีละร้อย) |
| KV | อ่าน ≤1 · เขียน ≤1 ต่อคำขอ และเฉพาะตอน cache หมดอายุ = **ไม่เกิน 4 ครั้ง/วัน** |

> ⚠️ `refresh=1` **ข้าม cache = เสียเครดิตทุกครั้ง** อย่าเอาไปตั้งเป็นค่าตั้งต้นของอะไร

## 📈 ประวัติย้อนหลัง — โตเฉพาะตอนมีคนถาม

เก็บ **วันละ 1 จุดต่อบัญชี** ใน blob เดียว (`followers:history`) เก็บ 400 วัน
**วันไหนไม่มีใครถาม วันนั้นไม่มีข้อมูล** — เหมือนคลังข่าวที่โตเฉพาะตอนมีคนเปิดแดชบอร์ด
(Cloudflare Pages ตั้ง cron ไม่ได้) · อยากได้ครบทุกวันต้องให้ **n8n** ตั้งเวลายิงมาวันละครั้ง:

```
GET https://cp-dashboard-680.pages.dev/api/followers?key=<กุญแจ>   ← วันละ 1 ครั้งพอ
```

---

## 🧪 หน้าทดสอบ `/followers/` — ใช้พิสูจน์ว่าเดา endpoint ถูกไหม

```
https://<preview-url>/followers/
```

เปิดหน้านี้ → วางกุญแจ → กดปุ่ม → อ่านผล ✅/❌ · **ไม่มีลิงก์จากหน้าแรกโดยตั้งใจ**
(เป็นเครื่องมือ ไม่ใช่แดชบอร์ด — แบบเดียวกับ `/selftest/` และ `/admin/`)

| ปุ่ม | ทดสอบอะไร |
|---|---|
| ▶ ทดสอบบัญชีที่เปิดอยู่ | ดึงตามที่ตั้งไว้ใน `followers.config.js` |
| ▶ รายแพลตฟอร์ม | ใส่ชื่อบัญชีอะไรก็ได้ ทดสอบทีละเจ้า |
| 🔌 ทดสอบเครื่องมือแชท | ยิง MCP จริง (initialize → tools/list → tools/call) **ก่อนไปตั้ง Connector** |
| 📋 คัดลอกผลทั้งหมด | ได้ข้อความที่ส่งกลับมาให้ Claude แก้ได้เลย |

> 🎯 **สิ่งที่ต้องดูที่สุดคือข้อความตอนดึงไม่สำเร็จ** — มันบอกว่าลอง endpoint ไหนแล้วได้อะไร
> เช่น `ScrapeCreators: /v1/tiktok/profile → HTTP 404` = เดา path ผิด แก้ที่ `SC_ROUTES`
> ส่วน `ตอบมาแต่ไม่มีตัวเลขผู้ติดตาม` = path ถูกแล้วแต่ชื่อฟิลด์ไม่รู้จัก แก้ที่ `FOLLOWER_KEYS`
>
> 🔑 **กุญแจเก็บใน localStorage ของเครื่องนั้นเท่านั้น** ไม่ได้อยู่ในโค้ด และ
> **ปุ่มคัดลอกตัดกุญแจออกให้ก่อนเสมอ** — เทสต์ `followerspage.mjs` [11] คุมไว้
>
> ⚠️ **ค่าตั้งต้นใช้ cache 6 ชม. = กดซ้ำไม่เสียเครดิต** · ติ๊ก "บังคับดึงใหม่" เมื่อไหร่
> **เสียเครดิตทุกครั้งที่กด** — เปิดเฉพาะตอนเพิ่งแก้โค้ด/แก้ env

---

## 🔧 ถ้าดึงไม่ได้ — แก้ตรงไหน

ข้อความ error บอกไว้ครบว่าลอง endpoint ไหนแล้วได้อะไร เช่น
`ScrapeCreators: /v1/tiktok/profile → HTTP 404 | Apify (clockworks~tiktok-profile-scraper): HTTP 404`

| อาการ | แก้ที่ |
|---|---|
| **HTTP 404 จาก ScrapeCreators** = เดา path ผิด | `SC_ROUTES` ใน `functions/api/followers/_lib/providers.js` — เทียบกับ docs ของเขาแล้วแก้ |
| **"ตอบมาแต่ไม่มีตัวเลขผู้ติดตาม"** = ชื่อฟิลด์ไม่รู้จัก | เติมชื่อฟิลด์ที่ **บนสุด** ของ `FOLLOWER_KEYS` ในไฟล์เดียวกัน |
| **Apify 404** = ชื่อ actor ไม่ตรง | ไม่ต้องแก้โค้ด — ตั้ง env `APIFY_ACTOR_TIKTOK` / `_INSTAGRAM` / `_X` / `_FACEBOOK` / `_YOUTUBE` (ใน URL ของ Apify ใช้ `~` แทน `/`) |
| **Facebook ได้ตัวเลขแต่เป็นยอดไลก์** | ปกติ — เพจเก่าบางเพจไม่ให้ยอดผู้ติดตาม · ระบบติดป้าย `metric: "likes"` ให้แล้ว **อย่าเอาไปเทียบกับผู้ติดตามของช่องอื่น** |

> 🔴 **ดึงไม่ได้ = `followers: null` ไม่ใช่ 0** — ตอบ 0 คือโกหกว่าไม่มีคนติดตาม
> เทสต์ `followers.mjs` [10] คุมไว้
>
> 🚫 **ไม่คืน response ดิบของต้นทางออกมาเด็ดขาด** (บทเรียนจาก `/debugmeta`)
> ที่คืนได้มีแค่ "เอาตัวเลขมาจากฟิลด์ชื่ออะไร" ซึ่งพอสำหรับไล่ปัญหาแล้ว

## 🧪 เทสต์

```bash
cd tests && node followers.mjs      # ไม่ต้องเปิดเบราว์เซอร์ ไม่ต้องมีเน็ต
```

## ⚖️ ข้อควรรู้

- **YouTube มีทางที่ถูกกว่านี้** — YouTube Data API ให้ยอดผู้ติดตามฟรี (`YT_API_KEY` มีอยู่แล้ว
  ในโปรเจกต์) ถ้าใช้บ่อยควรต่อทางนั้นแทน จะได้ไม่เสียเครดิต scraper · **ยังไม่ได้ทำ**
- **การ scrape เป็นพื้นที่สีเทาของ ToS แพลตฟอร์ม** — ยอดผู้ติดตามเป็นข้อมูลสาธารณะที่ไม่ใช่
  ข้อมูลส่วนบุคคล ความเสี่ยงต่ำกว่าการดึงคอมเมนต์มาก (ดู `social-comment-extractor/PRIVACY_NOTE.md`)
- **ไม่ได้ทำหน้าเว็บให้** โดยตั้งใจ — `/social/` เป็นแดชบอร์ดสถิติช่องของเราเองอยู่แล้ว
  (session อื่นดูแล) ถ้าจะโชว์ตัวเลขพวกนี้บนจอ ควรไปโผล่ที่นั่น ไม่ใช่สร้างหน้าใหม่มาซ้อน
