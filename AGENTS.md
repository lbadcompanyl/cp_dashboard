# AGENTS.md — อ่านก่อนแตะอะไรในโปรเจกต์นี้

> ไฟล์นี้สำหรับ **AI ทุกตัว** ที่เข้ามาทำงานกับ repo นี้
> (Codex · Cursor · Copilot · Gemini · Claude · หรือตัวอื่น)

## 📍 ความจริงกลางอยู่ที่ `CLAUDE.md` — ไฟล์เดียว

**อ่าน [`CLAUDE.md`](./CLAUDE.md) ให้จบก่อนเขียนโค้ดบรรทัดแรก**

ในนั้นมีทุกอย่าง: โครงสร้างโปรเจกต์ · กฎการ deploy · กับดักที่เคยพังมาแล้ว 2 เดือน ·
วิธีเขียนโค้ดให้เข้ากับของเดิม · วิธีคุยกับเจ้าของ

> 🚫 **ห้ามก๊อปเนื้อหาจาก `CLAUDE.md` มาไว้ในไฟล์นี้หรือไฟล์อื่น**
> บั๊กที่โปรเจกต์นี้เจอซ้ำที่สุดคือ **"กฎเดียวกันอยู่ 2 ที่ แก้ที่หนึ่งลืมอีกที่"**
> (เคยเกิดกับ `CPF_RE` ที่อยู่ 3 ไฟล์ · เลขหมวดที่อยู่ 2 ที่ · แถบแท็บที่อยู่ 3 ไฟล์)
> ไฟล์อื่นทั้งหมดเป็น **แค่ป้ายชี้ทาง** มาที่นี่

## ⛔ 7 ข้อที่ผิดแล้วเสียหายจริง — ท่องไว้ก่อนอ่านอย่างอื่น

**1. `main` = เว็บจริง · push แล้ว deploy ทันที ไม่มีขั้นยืนยัน**
🚫 ห้าม push ขึ้น `main` โดยไม่ได้รับคำสั่งชัดเจนจากเจ้าของ · "บอกทีหลัง" ไม่นับว่าขออนุญาต
· ทำงานบน branch ของตัวเองเสมอ

**2. `node --check` ไม่พอ — ต้อง `import` ไฟล์จริง**
Cloudflare **รวมทุกไฟล์ใน `functions/` เป็นก้อนเดียวก่อน build ไฟล์เดียวพัง = ทั้งเว็บสร้างไม่ได้**
เคยทำเว็บหยุดอัปเดต **3 วัน** จากจุลภาคเกินมา 1 ตัว — และ `node --check` ผ่านฉลุย

```bash
node tests/imports.mjs      # ← ด่านบังคับ ก่อน push ทุกครั้งที่แตะ functions/
```
🚫 **ตกแม้แต่ไฟล์เดียว = ห้าม push** ไม่ว่าจะเป็นไฟล์ของใคร

**3. repo นี้เป็น public — ห้าม commit ความลับ**
API key · token · sheet id · campaign id **ใส่ที่ Cloudflare เท่านั้น** (แบบ Secret)
· และต้องใส่ทั้ง **Production และ Preview** แล้ว **Retry deployment**

**4. โควตา KV เขียนได้ 1,000 ครั้ง/วัน ใช้ร่วมกันทั้งโปรเจกต์**
endpoint ไหนเขียนเกิน **1–2 ครั้งต่อ request** ถือว่าผิด ต้องแก้
🚫 ห้ามเขียนแยก key ต่อรายการ — รวมเป็น blob เดียวแล้วเขียนครั้งเดียว

**5. หนึ่งห้องหนึ่งงาน — ห้ามแตะไฟล์ของ session อื่น**
มีหลาย AI/session ทำงานพร้อมกันบน repo นี้ · ดูตารางว่าใครดูแลอะไรใน `CLAUDE.md`
และไฟล์ `*-HANDOFF.md` ของงานนั้นๆ

**6. ห้ามเชื่อผลที่ตัวเองรายงาน — ต้องรันจริงแล้ววางผลที่ออกมา**
"เทสต์ผ่านแล้ว" ที่ไม่ได้รันจริงคือคำโกหก · **ตัวเลขทุกตัวต้องมาจาก output จริง**
· ยังไม่ได้วัด ให้เขียนว่า **"ยังไม่ได้วัด"** ห้ามเดาให้ดูดี

**7. "ไม่รู้" ห้ามกลืนให้กลายเป็นค่าใดค่าหนึ่ง**
บทเรียนแพงที่สุดของโปรเจกต์: โค้ดเติมค่าปริยายให้เงียบๆ ตอนแกะข้อมูลไม่สำเร็จ
→ 79% ของผลลัพธ์กลายเป็นค่าเดียวกัน แล้วเกือบสรุปผิดทั้งหมด
· ตัดสินไม่ได้ = คืน `null` / แจ้ง error **ห้ามเดา**

## 🧭 อ่านไฟล์ไหน เมื่อทำงานเรื่องอะไร

| ทำงานเรื่อง | อ่าน |
|---|---|
| อะไรก็ตาม (บังคับ) | `CLAUDE.md` |
| วิธีปล่อยงานขึ้นเว็บจริง | `WORKFLOW-PROPOSAL.md` |
| เทสต์ — ไฟล์ไหนคุมอะไร | `tests/README.md` |
| งานรับข้อมูล Zocial Eye → `/issue/` | `ZOCIAL-HANDOFF.md` |
| แดชบอร์ดช่องของเราเอง `/social/` | `SOCIAL-HANDOFF.md` |
| ที่มาของหน้า 3 คอลัมน์ | `TREND-HANDOFF.md` |

## 🧪 รันเทสต์

```bash
cd tests && npm install                          # ครั้งเดียว (เฉพาะเทสต์ที่เปิดเบราว์เซอร์)
python3 -m http.server 8899 --directory .. &     # เทสต์เบราว์เซอร์ต้องมีเซิร์ฟเวอร์นี้

node imports.mjs                 # ด่านบังคับ
node noise.mjs                   # ตรรกะล้วน
TZ=Asia/Bangkok node sheetorder.mjs   # ตัวที่เกี่ยวกับเวลา ต้องตั้ง TZ
```

**แก้ตรรกะที่ไหน ให้รันเทสต์ที่คุมเรื่องนั้นก่อนบอกว่าเสร็จ**
· 🧪 และ **เทสต์ต้องพิสูจน์ว่าจับของพังได้จริง** — ลองทำให้พังแล้วดูว่าตก
  ไม่งั้นเทสต์ที่ผ่านตลอดไม่มีค่า

## 🏗 ข้อเท็จจริงของโปรเจกต์ที่มักเข้าใจผิด

- **เป็น static ล้วน + Cloudflare Pages Functions — ไม่มีขั้นตอน build เลย**
  🚫 ไม่มี React/Vite/Tailwind · `package.json` ที่เห็นใช้กับ **เทสต์เท่านั้น**
- **Cloudflare Pages ตั้ง cron ไม่ได้** (Workers ทำได้ Pages ไม่ได้)
- **ภาษาไทยไม่มีช่องว่างคั่นคำ** — `\b` ใช้กับอักษรไทยไม่ได้ · `includes()` ไปเจอกลางคำอื่นได้ง่ายมาก
- เครื่องที่รัน AI **ยิงเน็ตออกไม่ได้หลายที่** (Google · Cloudflare · เว็บจริงของเราเอง)
  → **ห้ามเคลมว่าทดสอบกับของจริงแล้ว** ถ้ายังไม่ได้ทำ
- **มีแต่ Chromium ทดสอบ Safari/iOS ที่นี่ไม่ได้** → ต้องให้เจ้าของเปิดดูบนเครื่องจริง

## 💬 คุยกับเจ้าของเป็นภาษาไทย

- **บรรทัดแรกตอบคำถามที่ถามก่อน** ("ใช้ได้ไหม" → ได้/ไม่ได้/ยังไม่รู้)
- ใช้ภาษาคนธรรมดา ไม่ใช่ชื่อฟังก์ชัน · ตอบไม่เกิน ~10 บรรทัด
- เรื่องซับซ้อน **ยกตัวอย่างของจริงให้เห็นภาพ** ดีกว่าอธิบายลอยๆ
- จบด้วย "ต้องทำอะไรต่อ" เป็นข้อๆ

> ✍️ **เจอบทเรียนใหม่ ให้เขียนลง `CLAUDE.md` ทันที อย่าปล่อยไว้ในแชท**
> แชทหายเมื่อจบ session ไฟล์ไม่หาย
