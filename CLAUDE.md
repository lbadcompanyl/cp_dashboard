# CLAUDE.md

บันทึกสำหรับ session ถัดไป — อ่านก่อนเริ่มงานทุกครั้ง

## 🚦 Environment / Branch — สำคัญที่สุด อ่านก่อนแตะอะไร

Cloudflare Pages project = **`cp-dashboard-680`**

Git เชื่อมกับ `lbadcompanyl/cp_dashboard` · Automatic deployments: **Enabled**
→ **ทุก push ขึ้น production branch = deploy ขึ้นเว็บจริงทันที ไม่มีขั้นยืนยัน**

### เช็ค production branch ก่อนเสมอ — ห้ามเดา

```
Cloudflare → cp-dashboard → Settings → Build → Branch control → Production branch
```

ค่าที่ตั้งอยู่ตอนนี้คือ branch ไหน **branch นั้นคือ production** ไม่ว่ามันจะชื่ออะไร

> 📌 **ประวัติที่ต้องรู้:** เคยตั้งเป็น `claude/dashboard-google-alerts-news-0mw9bg`
> (branch งาน!) อยู่พักหนึ่ง ทำให้ push งานระหว่างทางขึ้นเว็บจริงทุกครั้งโดยไม่มีใครรู้
> เป็นต้นเหตุที่ debug endpoint หลุดขึ้น production 2 รอบ

### เป้าหมายที่ตกลงกันไว้

| branch | บทบาท | URL | ใครสั่งได้ |
|---|---|---|---|
| `claude/*` | branch งาน | preview URL อัตโนมัติ | Claude push เองได้ |
| `dev` | **staging** | `dev.cp-dashboard-680.pages.dev` | Claude push เองได้ |
| `main` | **production** | `cp-dashboard-680.pages.dev` | ❌ **เจ้าของสั่งเท่านั้น** |

ไหลของงาน: `claude/*` → `dev` (ดูบน staging) → **อนุมัติ** → `main` (ผู้ใช้เห็น)

> ⚠️ **อย่าสับสนกับ `trend-dashboard.pages.dev`** — อันนั้นคือโปรเจกต์ **ต้นทาง**
> ที่โค้ดหน้า 3 คอลัมน์ถูกยกมา (ดู `TREND-HANDOFF.md`) **ไม่ใช่แดชบอร์ดนี้**
> เคยเข้าใจผิดมาแล้ว ทำให้ยิง URL ผิดโปรเจกต์ทั้งหมด

### กฎเหล็ก

1. **ห้าม push ขึ้น production โดยไม่ได้รับคำสั่งชัดเจน** — เคยพลาดมาแล้ว 2 ครั้ง
   การ "บอกทีหลัง" หรือ "หมายเหตุไว้ท้ายข้อความ" ไม่นับว่าขออนุญาต
2. **โค้ดทดลอง / debug endpoint ห้ามขึ้น production** — ลบทิ้งก่อน merge ทุกครั้ง
3. งานพัฒนาอยู่บน branch งาน (`claude/...`) → ทดสอบบน staging → ขึ้น production เมื่อได้รับอนุมัติ
4. ถ้าไม่แน่ใจว่า branch ไหนคือ environment ไหน — **ถาม อย่าเดา**

## 📦 โครงสร้างโปรเจกต์

```
index.html      landing — การ์ดลิงก์ทุกแดชบอร์ด
trend/          PR Trend Dashboard — Google Alerts + Google Trends   ← งานฝั่ง PR
ir/             IR News Monitor — ข่าวนักลงทุนสัมพันธ์
issue/          Issue Dashboard — CP + หัวข้อที่จับตามอง
sd.html         SD Trends — ความยั่งยืน
trends.html     Trends Explorer (เก่า) — ไม่มีลิงก์จาก landing แล้ว
functions/api/  Cloudflare Pages Functions (trend / ir / sd / flags)
```

เป็น static + Pages Functions ไม่มีขั้นตอน build

## ✅ ฟีเจอร์มาตรฐาน — ทุกแดชบอร์ดต้องมีครบ

**รวมถึงแดชบอร์ดใหม่ที่จะสร้างต่อจากนี้ด้วย** ทำไม่ครบถือว่ายังไม่เสร็จ

| # | ฟีเจอร์ | ทำยังไง |
|---|---|---|
| 1 | **favicon** | `<link rel="icon">` emoji ให้ตรงกับไอคอนการ์ดบน landing |
| 2 | **ติดตั้งเป็นแอปได้ (PWA)** | `manifest.webmanifest` + `sw.js` + `apple-touch-icon` |
| 3 | **เช็คเวอร์ชันใหม่อัตโนมัติ** | `<meta name="page-ver">` + สคริปต์เทียบเลข (ก๊อปจาก `sd.html`) |
| 4 | **ปุ่มกลับหน้าหลัก** | `<a href="/">🏠 หน้าหลัก</a>` ใน appbar |
| 5 | **รีเฟรชข้อมูลเงียบ** | `setInterval` ทุก 3 นาที (ถ้าหน้านั้นมีข้อมูลสด) |

> ⚠️ ข้อ 3: ต้อง bump **ทั้ง `<meta name="page-ver">` และ `var VER`** คู่กันทุกครั้งที่แก้ไฟล์
> ไม่งั้นเครื่องที่ติดตั้งเป็นแอปจะค้างหน้าเก่า (เคยเกิดกับการ์ด Issue มาแล้ว)

### 🔢 ป้ายเลขเวอร์ชัน — ใช้ตรวจว่าเครื่องได้ของใหม่หรือยัง

ทุกหน้ามี `<span id="vtag">` แสดงผลเป็น **`v34 · sw4`**

- `v` = เวอร์ชันของหน้า (อ่านจาก `<meta name="page-ver">` ถ้าไม่มีก็อ่าน `?v=` ของ `app.js`)
- `sw` = เวอร์ชัน service worker ที่ทำงานอยู่ในเครื่องจริง (หน้าถาม sw ตรงๆ)

> เอาไว้เลิกเดาว่า "แอปอัปเดตหรือยัง" — ให้ผู้ใช้อ่านเลขให้ฟังแล้วเทียบกับ
> ที่อยู่บน production ได้เลย เคยเสียเวลาไล่กันหลายรอบเพราะไม่มีตัวนี้

## 🧪 เช็คลิสต์ก่อนบอกว่า "เสร็จ" — ทำทุกครั้งที่มีของใหม่

**ยังไม่เสร็จจนกว่าจะตอบครบทั้ง 4 ข้อ** เคยพลาดเพราะทำเสร็จบนเดสก์ท็อปแล้วคิดว่าจบ

| # | ต้องเช็ค | ถามตัวเองว่า |
|---|---|---|
| 1 | **ทุกแดชบอร์ด** | ของใหม่นี้ต้องมีที่ `trend/` `ir/` `issue/` `sd.html` และ landing ด้วยไหม? ถ้าเป็นฟีเจอร์พื้นฐาน = ต้องมีครบทุกที่ · ถ้าเป็นของเฉพาะทาง = **เขียนไว้ว่าทำไมถึงอยู่แค่หน้านั้น** |
| 2 | **แดชบอร์ดที่เพิ่งเพิ่มเข้ามา** | ถ้ารอบนี้สร้างแดชบอร์ดใหม่ มันได้ **ฟีเจอร์มาตรฐานทั้ง 5 ข้อ** ครบไหม? และมี **การ์ดบน landing** แล้วหรือยัง? แดชบอร์ดใหม่ต้องเท่าเทียมของเดิมตั้งแต่วันแรก ไม่ใช่ค่อยตามเก็บทีหลัง |
| 3 | **มือถือ** | จอแคบพังไหม? `/trend/` `/ir/` `/issue/` เป็น carousel ปัดซ้ายขวา ไม่ใช่กริด — ของที่กางออกมาในกริดจะดันจนหน้าพัง (เคยเกิดกับกล่องข่าวในคอลัมน์ X มาแล้ว) |
| 4 | **แอปที่ติดตั้งแล้ว** | เครื่องที่ติดตั้งเป็นแอปจะได้ของใหม่ไหม? bump `page-ver` + `?v=` ครบหรือยัง? |

### 🔁 ทำทุกครั้งหลังเขียนโค้ดเสร็จ ก่อนบอกผู้ใช้ว่าเสร็จ

**อย่ารีบบอกว่าเสร็จ** ให้ไล่ตรวจตัวเองก่อนเสมอ — ที่ผ่านมาบั๊กร้ายแรงที่สุด
(KV เขียน 50 ครั้ง/รอบ จนจะทำโควตาหมดทั้งโปรเจกต์) เจอตอนไล่ตรวจย้อนหลัง
ไม่ใช่ตอนเขียน

1. ตอบเช็คลิสต์ 4 ข้อข้างบนให้ครบ
2. **นับ KV read/write ต่อ 1 request** — เกิน 1-2 ครั้งถือว่าผิด ต้องแก้
3. รันตรวจ syntax + ทดสอบตรรกะด้วยข้อมูลจำลอง อย่าเดาว่าโค้ดถูก
   - **syntax ผ่าน ≠ ทำงานถูก** เคยใส่สคริปต์ไว้ใน `<head>` แล้วให้มันหา element
     ที่อยู่ท้าย `<body>` — ผ่าน syntax แต่ได้ `null` ทุกครั้ง ไม่ทำงานเลย
   - **ตรวจ harness ที่ใช้ทดสอบด้วย** Node รุ่นใหม่มี `navigator` เป็น global
     ที่เขียนทับตรงๆ ไม่ได้ ต้องใช้ `Object.defineProperty` ไม่งั้นผลทดสอบหลอก
4. ถามว่า "ถ้าของนี้พัง จะพังแค่ตรงนี้ หรือลามไปทั้งระบบ?" — ถ้าลาม ต้องมีตัวกัน
5. เขียนสิ่งที่เจอ/แก้ ลงข้อความที่ตอบผู้ใช้ **อย่าซ่อนความเสี่ยงที่รู้อยู่**

### กฎเรื่อง KV — สำคัญมาก

`FLAGS_KV` ใช้ร่วมกันทั้งโปรเจกต์ (flag · archive ข่าว · related · หมวด X)
แผนฟรีเขียนได้ **1,000 ครั้ง/วัน** — ถ้า endpoint ไหนเขียนเกิน 1-2 ครั้งต่อ request
**โควตาจะหมดแล้วพังทั้งระบบ**

> ❌ อย่าเขียนแยก key ต่อรายการ (เคยเขียนไว้ 50 ครั้ง/รอบ)
> ✅ รวมเป็น blob เดียวแล้วเขียนครั้งเดียว — ดูตัวอย่างที่ `xtrends.js` (`xcatmap`)

### กฎเรื่อง `sw.js` — อันตรายกว่าไฟล์อื่น

`sw.js` ดักทุกการเปิดหน้าในโดเมน และมัน**ค้างอยู่ในเครื่องผู้ใช้** ถ้า push ตัวที่มีบั๊ก
ทั้งเว็บพังสำหรับคนที่ติดตั้งแอปไว้ และแก้ยากกว่าหน้าเว็บพังธรรมดา

1. แก้ `sw.js` ทีไร **ต้องทดสอบบน staging ก่อนเสมอ ห้ามข้าม**
2. ต้องบวก `SW_VERSION` ด้วย ไม่งั้นเบราว์เซอร์ไม่ติดตั้งตัวใหม่ (มันเทียบ byte)
3. ห้ามส่ง Request ที่ `mode=navigate` เข้า `fetch()` พร้อม init — สเปคจะลดระดับ
   mode เป็น `same-origin` เงียบๆ ให้ยิงด้วย `req.url` ตรงๆ แทน
4. ลงทะเบียนด้วย `{updateViaCache:"none"}` + เรียก `registration.update()` ทั้งตอน
   เปิดหน้าและตอน `visibilitychange` — ผู้ใช้จะได้ของใหม่เองโดยไม่ต้องทำอะไรเลย
   **ห้ามบอกผู้ใช้ให้ล้าง cache หรือถอนแอปติดตั้งใหม่** ถ้าต้องบอก แปลว่ากลไกยังผิด

**โหมดสว่าง/มืด — เจ้าของบอกว่าไม่ต้องทำ** มีแค่ `index.html` กับ `sd.html`
ส่วน `/trend/` `/ir/` `/issue/` เป็นโหมดมืดล้วน **อย่าไปเพิ่มเอง**

## 📱 แพลตฟอร์มที่ต้องคำนึงถึงทุกครั้ง

**ทุกอย่างที่เป็นฝั่งแอป (PWA / service worker / การอัปเดต / หน้าตาบนมือถือ)
ต้องคิดครบทุกช่องในตารางนี้เสมอ — ไม่ใช่เทสบนเดสก์ท็อปแล้วจบ**

|  | Chrome | Edge | Safari |
|---|:---:|:---:|:---:|
| **เดสก์ท็อป** | ✅ | ✅ | ✅ |
| **Android** | ✅ | ✅ | — |
| **iOS (iPhone/iPad)** | ✅ | ✅ | ✅ ← **ผู้ใช้ส่วนใหญ่อยู่ตรงนี้** |

Edge เป็น Chromium ตัวเดียวกับ Chrome — อะไรที่ผ่าน Chrome ก็ผ่าน Edge
ตัวที่ต้องระวังคือ **Safari/iOS** เพราะพฤติกรรมต่างจากตัวอื่น

### ข้อควรระวังของ Safari / iOS

- service worker ใช้ได้ตั้งแต่ **iOS 11.3** — ฟีเจอร์อัปเดตอัตโนมัติทำงานปกติ
- **iOS อาจล้าง service worker ทิ้ง** ถ้าไม่เปิดแอปเกิน ~7 วัน → พอเปิดใหม่
  มันลงทะเบียนเองอัตโนมัติ ไม่ต้องให้ผู้ใช้ทำอะไร
- **Safari โหมดส่วนตัวไม่มี service worker** → หน้าเว็บทำงานปกติทุกอย่าง
  แค่ไม่มีระบบอัปเดตอัตโนมัติ
  ⚠️ อย่าวางโค้ดที่ต้องทำงานเสมอไว้ใน `if("serviceWorker" in navigator){...}`
  (เคยพลาดกับป้ายเวอร์ชันมาแล้ว — Safari โหมดส่วนตัวไม่เห็นป้ายเลย)
- iOS ต้องมี `apple-touch-icon` + `apple-mobile-web-app-capable` ถึงจะติดตั้งสวย
- ⚠️ **Safari กับไอคอนบนหน้าจอโฮมอาจไม่แชร์ service worker กัน** (iOS แยก storage
  ของ home-screen app ออกจากตัวเบราว์เซอร์) ต่างจาก Android ที่ Chrome กับแอป
  ที่ติดตั้งใช้ registration เดียวกัน → บน iOS การ "เปิดใน Safari ให้อัปเดต"
  อาจไม่ช่วยแอปบนหน้าจอโฮม ต้องเปิดจากไอคอนเอง

### วิธีดันของใหม่เข้าเครื่องที่ค้าง (เรียงจากเบาไปหนัก)

| แพลตฟอร์ม | ทำอะไร |
|---|---|
| **Android** | เปิดเว็บใน Chrome ธรรมดา 1 ครั้ง — แอปที่ติดตั้งใช้ registration เดียวกัน จะได้ตามไปด้วย |
| **iOS** | เปิดจากไอคอนบนหน้าจอโฮม (iOS ปิดแอปจริงบ่อย เปิดใหม่มักเป็นการโหลดสด) |
| **ทั้งคู่ ถ้ายังค้าง** | ถอน/ลบไอคอน แล้วติดตั้งใหม่ 1 ครั้ง |

> ใช้ **ป้ายเลขเวอร์ชัน** ตรวจว่าสำเร็จหรือยัง ไม่ต้องเดา

## 🗂️ งานแต่ละแดชบอร์ดควรอยู่ที่ไหน

- **เทรนด์ทั่วไป** (# ไหนดัง, คนค้นอะไร ไม่ผูกกับ CP) → `trend/` (PR)
- **CP ถูกพูดถึงยังไง** (mentions, ประเด็นที่จับตา) → `issue/`
- ข่าวนักลงทุน → `ir/` · ความยั่งยืน → `sd.html`

## 🔍 สิ่งที่เคยทดสอบแล้ว — อย่าเสียเวลาลองซ้ำ

ยิงจาก Cloudflare Worker (5 ส.ค. 2026):

| แหล่ง | ผล |
|---|---|
| YouTube RSS | ✅ ใช้ได้ ฟรี ไม่ต้องมี key |
| getdaytrends.com/thailand/ | ✅ ได้เทรนด์ X ไทย (71KB, สะอาด) — **ตัวหลักที่เลือก** |
| trends24.in/thailand/ | ✅ ได้เหมือนกัน (305KB, ขยะเยอะกว่า) — ตัวสำรอง |
| TikTok Creative Center | ❌ `40101 no permission` ทั้งยิงเปล่าและใส่ header ครบ — ทางฟรีปิด |
| Facebook (www/mbasic/embed) | ❌ login wall / 400 / ได้แต่เปลือก — ต้องใช้ Meta API เท่านั้น |
| Nitter (net/poast) | ❌ 520 / bot check |
| Pantip (tag/search API) | ❌ 404 / 401 access denied |
| X syndication | ⚠️ ได้ข้อมูล แต่เป็นทวีตเก่าในโปรไฟล์ ไม่ใช่เทรนด์ |

## ⚠️ หนี้ทางเทคนิคที่รู้อยู่

- `/api/sd/img?u=<url>` เป็น **image proxy แบบเปิด** — ใครใส่ URL อะไรก็ได้
  (มีตัวกัน: ต้องเป็น http/https, ต้องได้ `content-type: image/*`, จำกัด 3MB, timeout 8 วิ)
  ถ้าจะปิดช่อง = จำกัดเฉพาะโดเมนข่าวที่อยู่ในลิสต์
- การ์ด `issue/` บน landing ยังติดป้าย 🚧 Under Construction ทั้งที่ใช้งานได้แล้ว
- `trends.html` ยัง deploy อยู่แต่ไม่มีลิงก์จาก landing (มีช่วงเวลา 1เดือน/3เดือน/12เดือน/5ปี
  ที่ `trend/` ปัจจุบันไม่มี — ปัจจุบันมีแค่ `now 1-d` / `now 7-d`)
- `COORDINATION.md` ตกยุค — ยังอ้างไฟล์ที่ root ทั้งที่ย้ายเป็นโฟลเดอร์แล้ว
- branch `claude/google-trends-dashboard-lt5k09` เป็น history คนละสาย (ต้นแบบ ก.ค.) ทิ้งได้
- **ไม่มี staging แยกจริง** — `dev` คือ production ถ้าอยากได้ที่ทดสอบที่ปลอดภัย
  ต้องเปิด preview deployment ของ Pages หรือตั้งโปรเจกต์ staging แยก
- `main` ค้างที่ `6614efa` ไม่ได้ deploy ที่ไหน ตกรุ่นไป 23 commit — จะ sync ให้ตรงหรือลบก็ได้
