# CLAUDE.md

บันทึกสำหรับ session ถัดไป — อ่านก่อนเริ่มงานทุกครั้ง

## 🚦 Environment / Branch — สำคัญที่สุด อ่านก่อนแตะอะไร

Cloudflare Pages project = **`cp-dashboard-680`**

Git เชื่อมกับ `lbadcompanyl/cp_dashboard` · Automatic deployments: **Enabled**
→ **ทุก push ขึ้น production branch = deploy ขึ้นเว็บจริงทันที ไม่มีขั้นยืนยัน**

### เช็ค production branch ก่อนเสมอ — ห้ามเดา

```
Cloudflare → cp-dashboard → Settings → Build → Branch control → Production branch
```

ค่าที่ตั้งอยู่ตอนนี้คือ branch ไหน **branch นั้นคือ production** ไม่ว่ามันจะชื่ออะไร

> 📌 **ประวัติที่ต้องรู้:** เคยตั้งเป็น `claude/dashboard-google-alerts-news-0mw9bg`
> (branch งาน!) อยู่พักหนึ่ง ทำให้ push งานระหว่างทางขึ้นเว็บจริงทุกครั้งโดยไม่มีใครรู้
> เป็นต้นเหตุที่ debug endpoint หลุดขึ้น production 2 รอบ

### เป้าหมายที่ตกลงกันไว้

| branch | บทบาท | URL | ใครสั่งได้ |
|---|---|---|---|
| `claude/*` | branch งาน | preview URL อัตโนมัติ | Claude push เองได้ |
| `dev` | **staging** | `dev.cp-dashboard-680.pages.dev` | Claude push เองได้ |
| `main` | **production** | `cp-dashboard-680.pages.dev` | ❌ **เจ้าของสั่งเท่านั้น** |

ไหลของงาน: `claude/*` (ดูบน preview ของ branch) → **อนุมัติ** → `main` (ผู้ใช้เห็น)
· `dev` = ที่ลองเอาของทุก session มารวมกันดู **ไม่ใช่ทางผ่านของการปล่อย**

### 🚀 วิธีปล่อยขึ้น production — **ปล่อยจาก branch ตรงเข้า `main`** (เริ่มใช้ 25 ส.ค. 2026)

เจ้าของเคาะแล้วหลังทั้ง 3 session รีวิว · รายละเอียดเต็มอยู่ที่ `WORKFLOW-PROPOSAL.md`

**ของเดิม** ปล่อยจาก `dev` ด้วย `cherry-pick` → commit ถูกก๊อปไปสร้าง sha ใหม่
→ `main` กับ branch **แตกสายกัน** → รอบหน้าชนกัน หรือของหายเงียบ (เจ็บมาแล้ว 3 ครั้ง)

**ของใหม่** — ปล่อยเป็น merge ธรรมดา ไม่มี sha ถูกก๊อปเลย

| ขั้น | ทำอะไร |
|---|---|
| 1 | `git merge origin/main` เข้า branch งานก่อนเสมอ (ไม่งั้น preview ไม่ใช่หน้าตาที่ production จะได้) |
| 2 | รันเทสต์ทั้งชุดบนเนื้อหาที่จะปล่อยจริง |
| 3 | ไล่ตรวจ 2 ข้อ (ดูข้างล่าง) |
| 4 | `git push origin <sha>:main` |

**2 ข้อที่ต้องตรวจก่อน push ทุกครั้ง**

```bash
git diff --name-only origin/main <sha> | grep -E '^(social/|functions/social/|social-comment-extractor/|issue/sentiment)'   # ต้องว่าง = ไม่มีของ session อื่นติดไป
git log --oneline origin/main..<sha> | grep -Ei '^\w+ (tmp|debug|test)\('                                                    # ต้องว่าง = ไม่มีโค้ดทดลอง
```

> 🚫 **ห้ามใช้ `git branch -f <branch> dev`** เพื่อ "ตั้งต้นใหม่" — จะได้สำเนางานของทุก session
> มาทั้งก้อน แล้วปล่อย branch นั้นขึ้น `main` = ลากงานคนอื่นขึ้นไปด้วย (ผู้เสนอเองก็เคยใช้ท่านี้)
> ✅ ท่าที่ถูก: `git fetch origin main && git checkout -B <branch> origin/main` แล้วเอาเฉพาะงานตัวเองกลับขึ้นมา
>
> 🚫 **ห้ามใช้ `cherry-pick` ปล่อยงานอีก** ยกเว้นกรณีที่ปล่อยได้แค่บางส่วนของ branch จริงๆ
> ถ้าเลี่ยงได้ให้ทิ้ง commit ที่ยังไม่ปล่อยไว้ **บนสุด** แล้วปล่อยถึงแค่ commit ก่อนหน้า
> (ทำจริงแล้ว 26 ส.ค. 2026 — ปล่อย 10 commit เว้นตัวบนสุด ไม่ต้อง cherry-pick เลย)
>
> ⚠️ **ปล่อยเสร็จแล้ว merge `main` กลับเข้า `dev` ทันที** เพื่อบอก git ว่าของพวกนั้นขึ้นไปแล้ว

> ⚠️ **อย่าสับสนกับ `trend-dashboard.pages.dev`** — อันนั้นคือโปรเจกต์ **ต้นทาง**
> ที่โค้ดหน้า 3 คอลัมน์ถูกยกมา (ดู `TREND-HANDOFF.md`) **ไม่ใช่แดชบอร์ดนี้**
> เคยเข้าใจผิดมาแล้ว ทำให้ยิง URL ผิดโปรเจกต์ทั้งหมด

### ✅ keyword ชุดใหม่ 74 คำ — ปล่อยขึ้น production แล้ว (จันทร์ 10 ส.ค. 2026)

กันไว้ตั้งแต่ 7 ส.ค. ตามที่เจ้าของสั่ง แล้วปล่อยพร้อมของอื่นทั้งหมดวันจันทร์
มีอะไรอยู่ในนั้น: `trend-feeds.config.js` (query ตัวเต็ม) · `functions/api/trend/feeds.js`
(`CACHE_VER` 50 · ตัดคำ `-ไม่เอา` · คำละตินต้องตรงทั้งคำ) · `HARD_KW` ใน `trend/app.js` + `issue/app.js`

> ⚠️ **ยังไม่ได้ยืนยันว่า query ใน Google Alerts ของจริงตรงกับใน config** — เครื่องที่รัน session
> ยิงเข้าเว็บไม่ได้ ต้องให้เจ้าของเปิดเทียบเอง **ถ้าไม่ตรง คอลัมน์จะหดลงเรื่อยๆ เงียบๆ**
> (ดูหัวข้อ 🔑 แก้ keyword ของ Google Alert)

### 📦 วิธีกันของบางอย่างไม่ให้ขึ้น production — ถ้าต้องทำอีก

**ห้ามใช้วิธี "merge แล้วค่อย revert"** — เคยทำ 2 รอบแล้วติดกับดัก: พอ main มี commit ที่ revert
ทับไว้ ครั้งต่อไป git จะมองว่า "main แก้ dev ไม่ได้แก้" แล้ว **เก็บ revert ไว้เงียบๆ**
ของใหม่เลยไม่ขึ้นทั้งที่ merge ผ่าน

✅ วิธีที่ถูก: **reset `dev` ให้เท่า `main` แล้ววางของที่กันไว้เป็น commit บนสุด**
งานระหว่างสัปดาห์แทรกไว้ "ใต้" commit นั้นเสมอ (`reset` แล้ว `cherry-pick` ของที่กันไว้กลับขึ้นบน)
· ให้ commit ที่กันไว้ใช้ **เลขเวอร์ชันสูงๆ** (เช่น 100 / 50) จะได้ไม่ต้องไล่แก้เลขทุกครั้งที่ cherry-pick

### 🚧 ยังไม่พร้อม = ยังไม่เข้า dev

**เจ้าของสั่ง (19 ส.ค. 2026):** งานที่ยัง**ไม่พร้อมปล่อย** ให้อยู่บน **branch ของตัวเอง** เท่านั้น
**ห้าม merge เข้า `dev`** จนกว่าจะพร้อมจริง — **`dev` ไม่ใช่โต๊ะทำงานรวม เป็นห้องพักก่อนขึ้น production**

| สถานะงาน | อยู่ที่ไหน | ทดสอบยังไง |
|---|---|---|
| กำลังทำ ยังไม่พร้อม | branch ของตัวเอง (`claude/…`) | **preview URL ของ branch นั้น** (Cloudflare แจกให้อัตโนมัติทุก branch) |
| พร้อมปล่อยแล้ว | **ยังอยู่ที่ branch เดิม** + merge เข้า `dev` ไว้ลองรวมกับของคนอื่น | preview ของ branch · และ `dev.cp-dashboard-680.pages.dev` |
| เจ้าของอนุมัติ | ปล่อย **จาก branch ตรงเข้า `main`** | เว็บจริง |

> 🔄 **เปลี่ยนแล้ว 25 ส.ค. 2026** — เดิม `dev` เป็นทางผ่านของการปล่อย ตอนนี้ไม่ใช่แล้ว
> **การปล่อยยิงจาก branch เข้า `main` ตรงๆ** (ดูหัวข้อ 🚀 ข้างบน) · `dev` เหลือหน้าที่เดียวคือ
> **ที่ลองเอาของทุก session มารวมกันดูก่อนว่าชนกันไหม** — ของใน `dev` ไม่ได้แปลว่ากำลังจะขึ้น production

**ทำไมต้องมีกฎนี้** — เคยมี 2 session ทำงานคนละเรื่องแล้วโยนเข้า `dev` พร้อมกัน
พอเจ้าของสั่ง release เฉพาะเรื่องเดียว ของมันปนกันไปแล้ว ต้องมานั่งหยิบ commit ทีละอัน
(`cherry-pick`) ซึ่ง **ทำให้ `main` กับ `dev` แตกสายกัน** และเสี่ยงหยิบผิด/หยิบตกทุกครั้ง
· วิธีใหม่แก้ที่ต้นเหตุ: **ไม่มีขั้นตอนที่ต้องหยิบ commit ออกจากกองรวมอีกแล้ว**

> ✅ ทำตามกฎนี้แล้ว งานของแต่ละ session แยกกันชัด → **ปล่อยเรื่องไหนก่อนก็ได้ ไม่ต้องรอกัน**
> · ⚠️ **ถ้าเผลอ cherry-pick ไปแล้ว** ให้ merge `main` กลับเข้า `dev` ทันที เพื่อบอก git ว่า
>   commit พวกนั้นขึ้น production ไปแล้ว ไม่งั้นรอบหน้าจะชนกันตอน merge

- ทดสอบระหว่างทางใช้ **preview URL ของ branch ตัวเอง** ได้ตามปกติ
  (Cloudflare Pages สร้างให้อัตโนมัติทุก branch — ไม่ต้องผ่าน `dev`)
- `dev` มีไว้สำหรับของที่ **พร้อมให้ตรวจก่อนขึ้น production** ไม่ใช่ที่พักงานระหว่างทำ
- เหตุผล: `dev` เป็นที่ที่เจ้าของเปิดดูเพื่อ **อนุมัติขึ้น production** ถ้ามีงานครึ่งๆ กลางๆ
  ปนอยู่ จะแยกไม่ออกว่าอันไหนพร้อมอันไหนยัง แล้วอนุมัติของที่ยังไม่เสร็จขึ้นเว็บจริง

> ✅ **กฎนี้ใช้กับของใหม่ ไม่ได้สั่งให้รื้อของเก่า** (เจ้าของยืนยัน 19 ส.ค. 2026)
> ของที่อยู่บน `dev` มาก่อนมีกฎ **ไม่ต้องลบ** — ลบแล้วเจอกับดัก revert ที่หัวข้อ 📦 เตือนไว้เป๊ะๆ
> (ตอน merge ของจริงกลับเข้ามาจะชนกัน หรือหายเงียบๆ)
> ✅ ทำแค่ **เอาการ์ดออกจากหน้าแรก** ไม่ให้ใครเดินไปเจอโดยบังเอิญ
> แล้วปล่อยไฟล์ไว้ที่เดิมจนกว่าของจริงจะพร้อม (เข้าได้ถ้ารู้ URL)
> · ทำแบบเดียวกับที่ branch งานทำไว้ ตอน merge จะได้ไม่ชนกัน · **bump `page-ver` ด้วย**
> ไม่งั้นเครื่องที่ติดตั้งเป็นแอปจะยังเห็นการ์ดเดิม
> · ทำจริงกับ `/social/` แล้วที่ commit `137749b`

> ⚠️ **เผลอ merge เข้า `dev` ไปแล้วต้องเอาออกยังไง** — ห้ามใช้ `revert`
> (ดูหัวข้อ 📦 ข้างบน: revert แล้วครั้งต่อไป git จะมองว่า dev แก้แล้ว ของใหม่เลยไม่ขึ้นเงียบๆ)
> ✅ วิธีที่ถูก: **reset `dev` กลับไปที่ commit ก่อนหน้าแล้ว force push**
> `git push --force-with-lease=dev:<sha ปัจจุบัน> origin <sha ที่ถูกต้อง>:dev`
> · ทำได้เฉพาะตอนที่ commit ที่จะถอดอยู่บนสุด และไม่มีใครต่อยอดทับ — เช็คก่อนเสมอ

### กฎเหล็ก

1. **ห้าม push ขึ้น production โดยไม่ได้รับคำสั่งชัดเจน** — เคยพลาดมาแล้ว 2 ครั้ง
   การ "บอกทีหลัง" หรือ "หมายเหตุไว้ท้ายข้อความ" ไม่นับว่าขออนุญาต
2. **โค้ดทดลอง / debug endpoint ห้ามขึ้น production** — ลบทิ้งก่อน merge ทุกครั้ง
   · เคยหลุดมาแล้ว 3 ครั้ง ล่าสุด `GET /debugmeta` ใน `social-comment-extractor/worker/worker.js`
     (commit `b08bff6`) — เปิดให้ใครก็ได้ยิงแล้ว **เผาเครดิต ScrapeCreators ที่จ่ายเงิน**
     พร้อมคืน response ดิบของต้นทาง · ถอดเส้นทางออกแล้ว 20 ส.ค. 2026 ตัวฟังก์ชันยังอยู่
   · **ก่อน release ทุกครั้ง ให้ไล่ดู commit ที่ขึ้นต้นด้วย `debug`/`test`/`tmp` ในช่วงที่จะปล่อย**
3. งานพัฒนาอยู่บน branch งาน (`claude/...`) → ทดสอบบน staging → ขึ้น production เมื่อได้รับอนุมัติ
4. ถ้าไม่แน่ใจว่า branch ไหนคือ environment ไหน — **ถาม อย่าเดา**

## 🧭 หลักการทำงานกับ AI — เจ้าของวางไว้เอง (27 ส.ค. 2026) · **เตือนเมื่อกำลังจะผิดข้อไหน**

> เจ้าของสั่งให้จดไว้ที่นี่ และ **ให้เตือนเมื่อจำเป็น** — ไม่ใช่รอให้ถามก่อน

| # | หลักการ | ทำอะไรจริงในโปรเจกต์นี้ |
|---|---|---|
| 1 | **ความจำดีกว่า context ยาว** — อย่ายัดประวัติทั้งก้อนให้ AI อ่านใหม่ทุกรอบ (แพงและงง) · ให้ฐานข้อมูลเป็นความจริงกลาง แชทเป็นแค่ที่ชี้ทาง | ไฟล์นี้ + `tests/` คือความจริงกลาง · **เจอบทเรียนใหม่ให้เขียนลงที่นี่ทันที** อย่าปล่อยไว้ในแชท |
| 2 | **หนึ่งห้องหนึ่งงาน** — ปนกัน = AI งง + แพง · แยกห้อง = ความจำสะอาด ต่อกันได้ข้ามวัน | 1 session = 1 เรื่อง · **1 branch = 1 ของที่ปล่อยได้** (ดูหัวข้อ 🚦) · ห้ามไปแตะไฟล์ของ session อื่น |
| 3 | 🚫 **อย่าเชื่อ self-report ของ AI** — มันบอก "เทสต์ผ่านแล้ว" ได้ทั้งที่ไม่ได้รัน | **ต้องรันจริงแล้ววางผลที่ออกมา** · CI บน GitHub เป็นตัวตรวจอิสระอีกชั้น (ดูหัวข้อ 🍎) |
| 4 | 🔢 **ตัวเลขคือเกมของมัน** — hallucinate ตัวเลขง่ายมาก | **ห้ามพิมพ์ตัวเลขที่ไม่ได้มาจากผลรันจริง** — จำนวนเทสต์ · จำนวนข่าว · เวลาเป็นมิลลิวินาที ต้องก๊อปมาจาก output |

**สิ่งที่ต้องทำทุกครั้งก่อนบอกว่า "เสร็จ"**

1. รันชุดเทสต์จริง แล้ว **วางบรรทัดสรุปที่ได้** (เช่น `ผ่าน 39 · ตก 0`) ไม่ใช่เขียนเอาเอง
2. ถ้ายังไม่ได้รัน / รันไม่ได้ → **พูดตรงๆ ว่ายังไม่ได้ทดสอบ** อย่าเลี่ยงเป็นคำกำกวม
3. เรื่องที่ยืนยันจากที่นี่ไม่ได้ (Safari · AI ของจริง · Cloudflare · Google) → บอกว่า **ต้องให้เจ้าของเปิดดูเอง**

> ⚠️ **เตือนเจ้าของเมื่อไหร่** — เมื่อกำลังจะทำอะไรที่ชนข้อใดข้อหนึ่งข้างบน เช่น
> เอางานหลายเรื่องมาปนใน session เดียว · สั่งปล่อยของที่ยังไม่ได้รันเทสต์ ·
> ขอตัวเลขที่ต้องเดา (เช่น "ตอนนี้มีข่าวกี่ใบ" ทั้งที่ยิงเข้าเว็บจริงไม่ได้)

## 💬 วิธีคุยกับเจ้าของ — อ่านก่อนพิมพ์ตอบทุกครั้ง

เจ้าของบอกตรงๆ แล้วว่า **อ่านที่ตอบมาไม่เข้าใจ** ให้ทำตามนี้

### 1. ตอบคำถามที่ถามก่อน ในบรรทัดแรก

ถามว่า "ใช้ได้ไหม" → ตอบ **ได้ / ไม่ได้ / ยังไม่รู้** ก่อนอย่างอื่น
ห้ามเริ่มด้วยคำอธิบายว่าทำไม แล้วค่อยไปเฉลยตอนท้าย

| ❌ อย่าเขียนแบบนี้ | ✅ เขียนแบบนี้ |
|---|---|
| "Cloudflare Worker ออกเน็ตจาก IP ที่ใช้ร่วมกัน Google Trends จึงตอบ 429…" | "**ใช้ไม่ได้ครับ** เพราะ Google บล็อกเซิร์ฟเวอร์เรา" |

### 2. ใช้ภาษาคนธรรมดา ไม่ใช่ศัพท์ในโค้ด

ชื่อฟังก์ชัน ชื่อไฟล์ เลข error พวกนี้เป็นภาษาของโค้ด **ไม่ใช่ภาษาที่ใช้คุย**
ถ้าจำเป็นต้องใส่ ให้ใส่ในวงเล็บท้ายประโยคที่อธิบายเป็นภาษาคนแล้ว

| ❌ | ✅ |
|---|---|
| "`pruneStaleMerged()` มองว่าไม่ match แล้วจึงลบทิ้ง" | "ระบบเข้าใจว่าข่าวนั้นไม่เกี่ยวแล้ว เลยลบทิ้งเอง" |
| "`empty` flag ต้องเป็น false" | "ต้องไม่สรุปว่าไม่มีข้อมูล ทั้งที่ยังไม่รู้" |

### 3. สั้น

ตอบปกติ **ไม่เกิน 10 บรรทัด** · ตารางยาวๆ กับหัวข้อซ้อนหัวข้อเก็บไว้ใช้เฉพาะตอนสรุปงานใหญ่
รายละเอียดทางเทคนิคที่เจ้าของไม่ได้ถาม → เขียนลง commit message หรือ CLAUDE.md แทน

### 3b. เรื่องซับซ้อน — **ยกตัวอย่างให้เห็นภาพ** (เจ้าของสั่ง 27 ส.ค. 2026)

อธิบายกลไกด้วยคำอย่างเดียว เจ้าของอ่านแล้วยังนึกภาพไม่ออกว่า "แล้วบนจอจะเป็นยังไง"
**ให้เอาของจริงมาวางให้ดู** — รายการก่อน/หลัง · ตัวเลขที่จะเปลี่ยน · ข้อความที่จะขึ้นบนการ์ด

| ❌ อธิบายลอยๆ | ✅ ยกตัวอย่าง |
|---|---|
| "คลิปที่เพิ่งลงจะเอายอดสะสมมาเป็นวิวเพิ่ม" | วางลิสต์ 6 บรรทัดให้ดูเลยว่าอันไหนขึ้นบน อันไหนตกท้าย และป้ายเขียนว่าอะไร |

- ทำได้ตั้งแต่ **ตารางเทียบ 2 ช่อง** ไปจนถึง **ลิสต์ก่อน/หลัง** — ไม่ต้องหรู ขอให้เห็นของจริง
- ⚠️ **ตัวเลขในตัวอย่างต้องมาจากผลรันจริง** (กฎข้อ 4 ของหัวข้อ 🧭) ถ้าป้อนข้อมูลสมมติเข้าโค้ดจริง
  ให้บอกตรงๆ ว่า **ข้อมูลเข้าสมมติ · ผลที่ออกมาเป็นของจริงจากโค้ด** อย่าปล่อยให้เข้าใจว่าเป็นยอดจริงจากเว็บ
- ตัวอย่างมาก่อนคำอธิบายยาวๆ เสมอ — คนอ่านเข้าใจจากตัวอย่างแล้วมักไม่ต้องอ่านคำอธิบายอีก

### 4. บอกให้ชัดว่าต้องทำอะไรต่อ

จบด้วยสิ่งที่เจ้าของต้องทำ (ถ้ามี) เป็นข้อๆ สั้นๆ · ถ้าไม่ต้องทำอะไร ก็บอกว่าไม่ต้องทำอะไร

### 5. แยกให้ชัดว่า "ทดสอบแล้ว" กับ "ยังไม่ได้ทดสอบ"

เครื่องที่รัน session ยิงเน็ตออกไปข้างนอกไม่ได้หลายที่ (Google Trends, gstatic, docs ของ Google)
**อย่าพูดเหมือนของที่ยังไม่เคยลองว่ามันใช้ได้** ให้บอกตรงๆ ว่า "ยังไม่ได้ลองของจริง ต้องรบกวนเปิดดู"

## 📦 โครงสร้างโปรเจกต์

```
index.html      landing — การ์ดลิงก์ทุกแดชบอร์ด
trend/          PR Trend Dashboard — Google Alerts + Google Trends   ← งานฝั่ง PR
ir/             IR News Monitor — ข่าวนักลงทุนสัมพันธ์
issue/          Issue Dashboard — CP + หัวข้อที่จับตามอง
archives/       คลังข่าว — ค้นข่าวย้อนหลังจากไฟล์นิ่ง (ไม่มี API ของตัวเอง)
social/         Social Dashboard — สถิติช่องของเราเอง (YouTube/TikTok/Facebook) · 🔒 ปิดด้วย Cloudflare Access
admin/          หน้าจัดการ — 🚩 คำแนะนำตัดข่าว + ประกอบ query ของ keyword (ไม่มีลิงก์จาก landing)
sd.html         SD Trends — ความยั่งยืน
trends.html     Trends Explorer (เก่า) — ไม่มีลิงก์จาก landing แล้ว
functions/api/  Cloudflare Pages Functions (trend / ir / sd / flags)
functions/api/_lib/noise.js   🎯 ตัวกรอง "อะไรไม่ใช่ข่าว" ชุดเดียวที่ทุกแดชบอร์ด import ไปใช้
```

### 📊 หน้า `/social/` — สถิติช่องของเราเอง (คนละเรื่องกับแดชบอร์ดอื่น)

แดชบอร์ดอื่นทั้งหมดดู **สื่อภายนอก** (ข่าว เทรนด์) ส่วนอันนี้ดู **ช่องของเราเอง**
ต้องต่อ API ของแต่ละเจ้าและใช้สิทธิ์เจ้าของช่อง — ไม่ใช่ฟีดสาธารณะ

- **ไม่มีการ์ดบนหน้าหลักโดยตั้งใจ** (เจ้าของยืนยัน 20 ส.ค. 2026) ต้องรู้ URL ถึงเข้าได้
- **ปิดด้วย Cloudflare Access แล้ว** — `cp-dashboard-680.pages.dev/social` (ดูตารางหัวข้อ Access)
- ข้อมูลจำลองสำหรับดูหน้าตา: เติม `?mock=1` · ปิดช่องเพื่อทดสอบ: `?off=tiktok,facebook`
- **YouTube ต่อแล้ว 2 ชั้น** · TikTok กับ Facebook **ยังไม่ได้เชื่อม** (ขึ้นการ์ดบอกว่าต้องใส่ค่าอะไร)

| ชั้น | ได้อะไร | env |
|---|---|---|
| Data API (API key) | ยอด ณ ปัจจุบัน + คลิปล่าสุด · **ไม่มีประวัติรายวัน** | `YT_API_KEY` + `YT_CHANNEL_ID`/`YT_CHANNEL_HANDLE` |
| YouTube Analytics (OAuth) | รายวันย้อนหลัง · แชร์ · Retention · เวลาดู · Impressions/CTR | `GOOGLE_CLIENT_ID` `GOOGLE_CLIENT_SECRET` `YT_REFRESH_TOKEN` |

> 🚫 **ห้ามถาม YouTube Analytics ด้วย `channel==MINE` เป็นทางหลัก** — เจอบั๊กนี้มาแล้ว **2 รอบ**
> ถ้าบัญชี Google ที่กดอนุญาตไม่ได้เป็นเจ้าของช่อง `MINE` จะไปหยิบ "ช่องของบัญชีนั้น"
> ซึ่งมักว่างเปล่า แล้ว **ตอบ 200 พร้อมข้อมูลเปล่า ไม่มี error อะไรบอกเลย**
> ต้องถามด้วย `channel==<รหัสช่อง>` ก่อนเสมอ แล้วค่อยตกไปที่ MINE
> · รอบแรกเจอที่ `youtube.js` (ตัวเลขเป็นศูนย์ทั้งกระดาน) · รอบสองที่ `youtube-top.js`
>   (การ์ดคอนเทนต์ว่างเปล่าทั้งที่ตัวเลขช่องมาครบ) เพราะแก้ที่เดียวแล้วอีกที่ไม่ตาม
> · ตอนนี้ยกไปไว้ที่ `functions/social/_lib/store.js` (`channelQuery` + `resolveChannelId`) แล้ว
>   **แก้ที่เดียว ห้ามก๊อปไปวางในไฟล์ endpoint อีก**

> ⏰ **ข้อมูลของ YouTube ช้ากว่าปัจจุบัน 2-3 วันเสมอ** — เป็นปกติของต้นทาง เร่งไม่ได้
> · ทุกช่วงสำเร็จรูปจึงยึด `dataEnd()` = **วันล่าสุดที่ทุกช่องที่เปิดอยู่สรุปยอดแล้ว** ไม่ใช่วันนี้
>   (แบบเดียวกับ YouTube Studio กับ GA4) ไม่งั้น "7 วันล่าสุด" จะเหลือข้อมูลจริง 4 วัน
>   แล้วถูกเอาไปเทียบกับช่วงก่อนหน้าที่ครบ 7 → **ตัวเลข ▼ ดูแย่กว่าความจริงทุกครั้ง**
> · ยึด **ช่องที่ช้าที่สุด** ไม่ใช่เร็วที่สุด — หน้านี้มีไว้เทียบข้ามช่อง
> · ช่วง "กำหนดเอง" ไม่เลื่อนให้ แต่ขึ้นป้ายเตือนว่าท้ายช่วงยังไม่มีข้อมูล
> · **ถอด "วันนี้/เมื่อวาน" ออกแล้ว** — พอยึด anchor 2 คำนั้นโกหกทันที (กด "วันนี้" ได้วันที่ 3 วันก่อน)
> · เทสต์ `social.mjs` [62] คุมไว้

> 📐 **ป้ายเทียบ (delta) ใช้ % เสมอ ห้ามสลับหน่วยเอง** (เจ้าของแจ้ง 20 ส.ค. 2026)
> เคยมีกฎว่า "ฐานเดิมต่ำกว่า 1,000 ให้บอกเป็นจำนวนจริง" เจตนาดีแต่ผลคือ
> **การ์ดในแถวเดียวกันใช้คนละหน่วย** และหน่วยสลับไปมาเองตอนเปลี่ยนช่วงเวลา
> · จำนวนจริงกับฐานเดิมย้ายไปอยู่ใน tooltip แทน ไม่ได้หายไป
> · ข้อยกเว้นเดียว: **อัตราส่วน (ER · Retention · View rate) ใช้ `pt`** — ตัวมันเป็น % อยู่แล้ว

> 📊 **"อันดับคอนเทนต์" คิดจากยอดที่เกิดในช่วงที่เลือก ไม่ใช่วันที่ลง** (เจ้าของเลือกแบบ A · 19 ส.ค.)
> คลิปเก่าที่กลับมาดังต้องติดอันดับได้ · ใช้เกณฑ์นี้ **ทั้งการ์ดและตารางที่กางออกมา**
> เคยใช้คนละเกณฑ์กันอยู่ในหน้าเดียว ทำให้แถวย่อยรวมกันได้แค่ 4% ของยอดช่อง (เจ้าของถาม 20 ส.ค.)
> · ยังไม่ครบ 100% อยู่ดีเพราะต้นทางส่งอันดับมาได้จำกัด (50 ใบ) — **เขียนบอกไว้ใต้ตารางแล้ว ห้ามถอดออก**

> 🔤 **ชื่อตัวชี้วัดใช้ภาษาอังกฤษทับศัพท์** (เจ้าของสั่ง 19 ส.ค. 2026) — Likes / Comments / Shares
> · **View rate เป็นป้ายกลางๆ ห้ามเขียนว่า CTR ในคอลัมน์รวม** — YouTube เป็น CTR จริง (เห็นปกแล้วกด)
>   ส่วน Facebook ไม่มีการกด นับที่ "ดูเกิน 3 วิ" แทน · TikTok ไม่มีทั้งคู่ (คลิปเล่นเองในฟีด)
>   บทเรียนเดียวกับ "ดูจนจบ" ที่เคยเขียนผิดจนอ่านค่า YouTube ผิดความหมาย

> 🔒 **เซสชัน Access หมดอายุ = ต้องบอกให้ถูกเรื่อง** — Access จะตอบ "หน้าเข้าสู่ระบบ" เป็น HTML
> แทน JSON · เอาไป `r.json()` จะพังแล้วรายงานว่า "ต่อกับเซิร์ฟเวอร์ไม่ได้" ซึ่งพาไปไล่ผิดทาง
> · `apiGet()` ใน `social/data.js` เช็คชนิดของคำตอบก่อนแกะเสมอ → ขึ้นแถบแดงพร้อมปุ่มเข้าสู่ระบบใหม่
> · ปุ่มต้อง `location.reload()` **ห้ามยิง fetch ซ้ำ** — Access พาไปหน้าล็อกอินด้วย redirect ของทั้งหน้าเท่านั้น

### 🛠 หน้า `/admin/` — ทำไมเป็น path ไม่ใช่ subdomain

เจ้าของขอ `back.cp-dashboard-680.pages.dev` กับ `back.dev.cp-dashboard-680.pages.dev`
**ทั้งสองอันทำไม่ได้** — `*.pages.dev` ให้ subdomain ได้ชั้นเดียว และชื่อนั้นผูกกับ **ชื่อ branch**
(`dev.cp-dashboard-680.pages.dev` = branch ชื่อ `dev`) · ซ้อนสองชั้นอย่าง `back.dev.<project>`
ทั้งชื่อและใบรับรอง https ไม่รองรับเลย · ถ้าอยากได้โดเมนแยกจริงต้องใช้ **โดเมนของตัวเอง** (Custom domain)

ที่ใช้อยู่จึงเป็น `cp-dashboard-680.pages.dev/admin/` และ `dev.cp-dashboard-680.pages.dev/admin/`

- **✂️ ข่าวที่ระบบตัดทิ้ง** — เอา `alertVerify.dropped` + `swept.dropped` จาก response ของ API
  มาแสดง จัดกลุ่มตามเหตุผล (กลุ่มที่ตัดเยอะสุดขึ้นก่อน) แปลรหัสเป็นภาษาคนด้วยตาราง `WHY_TH`
  · **ไม่ได้ยิง API เพิ่ม ไม่แตะ KV** — ข้อมูลติดมากับ response ที่แดชบอร์ดใช้อยู่แล้ว
  · **โชว์แค่ข่าว 3 วันล่าสุด** (`DROP_DAYS` — เจ้าของสั่ง 14 ส.ค. 2026 "ให้มีอายุแค่ 3 วันจะได้ไม่รก")
    ⚠️ **กรองแค่การแสดงผล ไม่ได้แตะตรรกะการตัดเลย** ข่าวเก่ายังถูกตัดอยู่เหมือนเดิม
    · ต้องมี `at` (วันที่ข่าว) ติดมากับ record ที่ถูกตัด — เซ็ตทั้ง `verifyAlertItems` ของทั้ง 2 แดชบอร์ด
      และ `dropNoiseAfterArchive` ใน `_lib/noise.js` · **เพิ่มที่ push `dropped` ใหม่ตรงไหน ต้องใส่ `at` ด้วย**
    · ใบที่ **ไม่มีวันที่** ให้ **โชว์ไว้** ไม่ใช่ซ่อน — ซ่อนของที่ตัดสินไม่ได้ = หายเงียบโดยไม่มีใครรู้
    · หัวกล่องบอกจำนวนที่ซ่อนไว้เสมอ ("เก่ากว่านั้นอีก N") ไม่ให้เข้าใจผิดว่าระบบเลิกตัดของเก่าแล้ว
    · เทสต์ `dropage.mjs` คุมไว้
  · **↩ ข่าวที่เอากลับ** = สรุปว่ากฎไหนถูกกดคืนบ่อย ถ้าเกิน 3 ครั้งจะเตือนว่ากฎกว้างเกินไป
    ⚠️ **ตัวที่ตัดข่าวส่วนใหญ่เป็นกฎตายตัว ไม่ใช่ AI** — ตัดด้วยโดเมน/คำ แก้ได้เฉพาะข่าวใบนั้น
    ทางแก้จริงคือดูสถิติแล้วไปแก้กฎ · **ข้อยกเว้นคือชั้น AI ของคอลัมน์ CP** (13 ส.ค. 2026)
    ตรงนั้นการกด ↩/⚑ **สอนระบบจริง** รอบถัดไปตัดสินตามที่เคยกด (ดู `cpExamples` ท้ายไฟล์นี้)
  · **เพิ่มเหตุผลใหม่ใน `noiseReason()` เมื่อไหร่ ต้องเติม `WHY_TH` ใน `admin/app.js` ด้วย**
    ไม่งั้นจะขึ้นเป็นรหัสดิบ (`vendor` / `false-cp`) ให้เจ้าของอ่าน — เทสต์ `admin.mjs` คุมไว้
- **⚑ บนการ์ด = "ตัดข่าวใบนี้ทุกแดชบอร์ด"** (เจ้าของสั่ง 13 ส.ค. 2026)
  ของเดิม ⚑ ซ่อนเฉพาะแดชบอร์ดที่กด เพราะ flag เก็บแยกกอง (`flags:pr` / `flags:ir` / `flags:root`)
  ข่าวใบเดียวกันจึงยังโผล่ที่อื่น (เจอจริง: หน้า AQI ของ iqair)
  · ตอนนี้กด ⚑ จะ **POST ไป `/api/allow` ด้วย `mode:"block"`** เก็บลงรายการกลาง
    แล้ว `noiseReason()` ตัดให้ทุกแดชบอร์ด (เหตุผล `by-owner`)
  · กด **↩ เอากลับ** = ถอดออกจากรายการสั่งตัด · **อยู่สองฝั่งพร้อมกันไม่ได้** ฝั่งเซิร์ฟเวอร์บังคับให้
  · เก็บ allow กับ block ไว้ใน **blob เดียว** (`noise:allow`) → อ่าน KV ครั้งเดียวได้ทั้งคู่
    · blob รุ่นเก่าที่มีแต่ `items` ยังอ่านได้ ไม่ต้องย้ายข้อมูล · เทสต์ `blocksync.mjs` คุมไว้
  · ⚠️ **ไม่เห็นผลทันทีบนแดชบอร์ดอื่น** ต้องรอ cache ของ feed หมดอายุ (~1 ชม. หรือกด `?rebuild`)
- **✂️ ข่าวที่ระบบตัดทิ้ง + ปุ่ม ↩ เอากลับ** — เอา `alertVerify.dropped` + `swept.dropped`
  จาก response ของ API มาแสดง จัดกลุ่มตามเหตุผล แปลรหัสเป็นภาษาคนด้วยตาราง `WHY_TH`
  · **ไม่ได้ยิง API เพิ่มตอนแสดงผล** — ข้อมูลติดมากับ response ที่แดชบอร์ดใช้อยู่แล้ว
  · กด ↩ = POST ไป `/api/allow` เก็บลิงก์ลง KV (blob เดียว `noise:allow` เพดาน 500 รายการ)
    แล้ว `feeds.js` ทั้ง trend และ ir จะ **ข้ามทุกด่านตัด** ให้ข่าวใบนั้น
  · ⚠️ **`ALLOWED` เป็นตัวแปรระดับโมดูล ต้องตั้งค่าใหม่ทุกครั้งที่ build** — Workers ใช้โมดูล
    เดิมซ้ำข้าม request ถ้าไม่ตั้งใหม่จะค้างรายชื่อของ request ก่อนหน้า
  · ⚠️ **ไม่เห็นผลทันที** ต้องรอ cache ของ feed หมดอายุก่อน (~1 ชม. หรือกด `?rebuild`)
  · **เพิ่มเหตุผลใหม่ใน `noiseReason()` เมื่อไหร่ ต้องเติม `WHY_TH` ใน `admin/app.js` ด้วย**
    ไม่งั้นจะขึ้นเป็นรหัสดิบให้เจ้าของอ่าน — เทสต์ `back.mjs` กับ `allow.mjs` คุมไว้
- **ไม่มีการ์ดบน landing โดยตั้งใจ** — เป็นเครื่องมือของเจ้าของ ไม่ใช่แดชบอร์ดให้คนทั่วไปดู
- ⚠️ **ไม่มีระบบล็อกอิน** ใครรู้ลิงก์ก็เปิดได้ (มี `noindex` กัน Google เท่านั้น)
  ถ้าจะจำกัดสิทธิ์จริงต้องตั้ง **Cloudflare Access** หน้า path นี้

### 🔒 Cloudflare Access — ใครดูแล และล็อกอะไรได้จริงบ้าง

**ตั้งค่าในหน้า Cloudflare เท่านั้น ไม่ใช่งานเขียนโค้ด** — ไม่มีไฟล์ใน repo ที่เกี่ยวข้อง
session ไหนก็ทำให้ไม่ได้ (เครื่องที่รัน session เข้า Cloudflare ไม่ได้) **เจ้าของกดเอง**

| path | ล็อกได้แค่ไหน | ใครดูแล |
|---|---|---|
| `/social/*` | ✅ **ปิดสนิท** — API ของมันอยู่ใต้ path เดียวกัน (`/social/api/…`) นโยบายเดียวคลุมทั้งหน้าและ API | **session social** (เจ้าของแจ้งแล้ว 20 ส.ค. 2026) |
| `/admin/*` | ⚠️ **ได้แค่หน้าจอ** — API ที่มันใช้อยู่คนละ path (`/api/allow` · `/api/flags` · `/api/trend/feeds`) ซึ่งแดชบอร์ดสาธารณะใช้ร่วมกัน | session ที่ดูแล `/admin/` |

> 🚫 **ห้ามเอา `/api/*` เข้า Access** — แดชบอร์ดสาธารณะเรียกใช้อยู่ จะพังทั้งเว็บ
> 🚫 **กุญแจฝั่ง API ใช้แทนไม่ได้** — ปุ่ม ⚑/🗂 บนแดชบอร์ดสาธารณะยิงเข้า `/api/flags`
>    กุญแจจึงต้องฝังอยู่ในโค้ดหน้าเว็บที่ใครก็เปิดดูได้ = ไม่ใช่ความลับตั้งแต่แรก
>
> **ถ้าวันหนึ่งจะล็อกทั้งเว็บ** (`/*`) — ตอนนั้น `/api/` จะอยู่หลัง Access ด้วย
> พอ session หมดอายุมันจะตอบ **หน้าล็อกอินกลับมาแทน JSON** แดชบอร์ดจะขึ้นว่า
> "ดึงข้อมูลไม่สำเร็จ" แทนที่จะพาไปล็อกอิน โดยเฉพาะบนแอปที่ติดตั้งไว้
> · ต้องเพิ่มโค้ดดักว่า "คำตอบไม่ใช่ JSON = พาไปล็อกอิน" ให้ทุกแดชบอร์ดก่อน **ยังไม่ได้ทำ**
- โค้ดทำงานจริงอยู่ใน `flags.js` ไฟล์เดียวกับที่แดชบอร์ดใช้ — สั่งด้วย `ui:` ตอน `Flags.init()`
  `"fab"` ปุ่มลอยทั้ง 2 ปุ่ม (ของเดิม) · `"kw"` เหลือแต่ ➕ เพิ่ม keyword ← **แดชบอร์ดใช้อันนี้**
  · `"none"` ไม่มีปุ่มลอยเลย · `"admin"` กางในหน้า
- ⚠️ **กล่อง keyword ถูกผ่าครึ่งโดยตั้งใจ** — `openKw()` ดูจากโหมด admin แล้วเรนเดอร์คนละชุด

  | | แดชบอร์ด (ปุ่ม ➕) | หน้า admin |
  |---|---|---|
  | ช่องพิมพ์คำ + ชิพคำที่เก็บไว้ | ✅ | ชิพอย่างเดียว |
  | คำค้นที่ประกอบได้ · 📋 คัดลอก · ลิงก์ Google Alerts · ล้างคำทั้งหมด | ❌ | ✅ |

  เหตุผล: ตอนอ่านข่าวนึกคำได้ก็พิมพ์เก็บทันที ส่วนการเอาไปแปะใน Alert เป็นงานที่ทำทีเดียวจบ
  **เอาครึ่งไหนออกจากที่ไหนอีก ต้องแก้ทั้ง `openKw()` และเทสต์ `admin.mjs` คู่กัน**
- **ปุ่ม ⚑ กับ 🗂 บนการ์ดยังอยู่บนแดชบอร์ด** — เป็นทางเดียวที่ข่าวจะเข้ากอง "คำแนะนำตัดข่าว"
  ถอดออกเมื่อไหร่ = ฟีเจอร์ตายทั้งอัน
- หน้า admin ไม่มี `.panel` ให้ `flags.js` อ่านชื่อคอลัมน์เอง จึงมีตาราง `SCOPES` ใน `admin/app.js`
  **เปลี่ยนชื่อคอลัมน์ alert ในแดชบอร์ดไหน ต้องมาแก้ที่นี่ด้วย** ไม่งั้นชื่อบน admin จะค้างของเก่า
- flag ของแต่ละแดชบอร์ดเก็บคนละกอง: PR = `flags:pr` · IR = `flags:ir` · **Issue = `flags:root`**
  (`deriveScope()` ดูจาก path — `/issue/` ไม่เข้าเงื่อนไขไหนเลยจึงตกเป็น `root`)

> ⚠️ **ห้ามให้กล่องบนหน้า admin ชิงโฟกัสช่องพิมพ์เอง** — `refresh()` มีกฎว่า
> "ถ้ากำลังพิมพ์อยู่ในกล่องไหน อย่าวาดทับกล่องนั้น" กล่องที่กางค้างแล้ว auto-focus
> จะติดกฎนี้ตลอดเวลา ค่าที่ sync มาจาก KV เลยไม่ขึ้นสักที **ดูเหมือน sync พัง ทั้งที่ sync ถูก**

### 🗃️ หน้า `/archives/` — คลังข่าว (สร้าง 19 ส.ค. 2026)

ค้นข่าวย้อนหลังจากชีต Google · ออกแบบเผื่อ **15,000–20,000 แถว** ตั้งแต่วันแรก

**ข้อมูลไม่ได้ดึงสดตอนเปิดหน้า** — แปลงชีตเป็นไฟล์ JSON ไว้ก่อนด้วย
`node tools/build-archives.mjs <SHEET_ID>` แล้ว **commit ไฟล์ผลลัพธ์เข้า repo**
· ⚠️ **เครื่องที่รัน session ยิงเข้า Google ไม่ได้ (403)** ต้องโหลดชีตเป็น CSV มาก่อนแล้วใช้ `--csv <ไฟล์>`
· `--mock` = สร้างข้อมูลจำลอง 18,000 แถวไว้ทดสอบตอนยังไม่มี sheet id
· ⚠️ **ไม่ใช่ build step ของ Pages** เว็บยังเป็น static เหมือนเดิม · **ห้าม commit sheet id**

| | |
|---|---|
| แยกไฟล์ตามปี | `data/index.json` + `data/<ปี>.json` — เปิดหน้าโหลดแค่ **ปีล่าสุด** |
| ปีเก่าโหลดเมื่อไหร่ | ผู้ใช้เลือกวันที่ย้อนไปถึงปีนั้น (โหลดให้เอง) หรือกดปุ่ม "ค้นในปี …" ท้ายรายการ |
| โครงไฟล์ | `{ o:[สำนัก], c:[หมวด], r:[[พาดหัว, ลิงก์, วินาที, ลำดับสำนัก, [ลำดับหมวด]], …] }` |

**ช่องค้นหา: เว้นวรรค = "ต้องมีครบทุกคำ" (AND) แต่ไม่ต้องอยู่ติดกัน** (เจ้าของสั่ง 20 ส.ค. 2026)
`ปลาหมอ ปลากระป๋อง` = เอาข่าวที่มี **ทั้งสองคำ** จะอยู่ห่างกันแค่ไหนหรือสลับลำดับก็นับ

> ⚠️ **เคยทำเป็น "หรือ" แล้วผิด** (19 ส.ค.) — เอาคำใดคำหนึ่งก็ได้ ผลกว้างจนไม่ต่างกับไม่ได้กรอง
> ⚠️ **และไม่ใช่การเทียบเป็นสตริงเดียวที่มีช่องว่างตรงกลาง** — แบบนั้นคำต้องเรียงติดกันเป๊ะ
> `ฝุ่น เชียงใหม่` จะไม่เจอ "เชียงใหม่ … สู้ฝุ่น PM 2.5" ทั้งที่ควรเจอ

· อยากได้ **ทั้งวลีติดกันจริงๆ** ให้ใส่เครื่องหมายคำพูด `"PM 2.5"` (จำเป็น เพราะพาดหัวมีช่องว่าง
อยู่ข้างใน ถ้าไม่มีวิธีบอก คนพิมพ์ `PM 2.5` จะได้ข่าวที่มี "pm" กับ "2.5" อยู่คนละที่ปนมาด้วย
— วัดกับข้อมูลจริง: แยกคำ 76 ใบ · เป็นวลี 23 ใบ)
· ไฮไลต์หลายคำต้อง **รวมช่วงที่ทับกันก่อนวาด** ไม่งั้นได้ `<mark>` ซ้อน `<mark>`

> 🚫 **ห้ามใช้ search library ที่ตัดคำด้วยช่องว่าง** (Lunr · Fuse · MiniSearch · FlexSearch)
> **เจ้าของสั่งเป็นข้อห้ามข้อแรกของหน้านี้** — ภาษาไทยไม่มีช่องว่างระหว่างคำ
> ค้น "กุ้ง" ต้องเจอ **โรคกุ้ง** และ **ผลผลิตกุ้งทะเล** ด้วย
> · ใช้ `String.includes()` ไล่ทีละแถวตรงๆ ที่ 20,000 แถวเร็วพอโดยไม่ต้องทำ index
> · เทสต์ `archives.mjs` มีด่านจับไว้ (ลองเปลี่ยนเป็นเทียบทีละคำแล้ว → ตก 4 ข้อ)

⚠️ **กับดักที่เจอตอนเขียน — อย่าทำซ้ำ**

- **ยุบช่องว่างซ้ำตั้งแต่ตอนโหลดข้อมูล ไม่ใช่ตอนค้น** — ถ้าพาดหัวที่แสดง (`t`) กับตัวที่ใช้ค้น (`n`)
  ยาวไม่เท่ากัน ตำแหน่งที่หาเจอใน `n` จะเอาไปตัดชิ้นจาก `t` ไม่ได้ → **ไฮไลต์เพี้ยนทั้งพาดหัว**
  (พาดหัวที่แสดงจะกลายเป็นตัวอักษรเลื่อนตำแหน่ง) · ตอนนี้ `expand()` ยุบให้ตั้งแต่ต้น
  **เพิ่มการตัดอักขระใน `norm()` เมื่อไหร่ ต้องเลิกใช้วิธีนี้ทันที**
- **หางพาดหัวตัดตอนแสดงเท่านั้น** ("… - เทคโนโลยีชาวบ้าน - ข่าวสด") · **ตัวที่ใช้ค้นยังเป็นของเต็ม**
  ไม่งั้นค้นคำที่อยู่ในหางแล้วไม่เจอ ทั้งที่ในชีตมีจริง · `stripTail()` ตัดได้ไม่เกิน 3 ท่อน
  และตัดเฉพาะท่อนที่ **เป็นชื่อสำนักข่าวจริง** เท่านั้น
- **`หมวด` ในชีตเป็นหลายค่าคั่นจุลภาค ต้องแตกเป็น array** ไม่งั้นตัวกรองจะมีหมวดชื่อ
  "CPF, ปศุสัตว์" โผล่เป็นตัวเลือกเดียว
- **พิมพ์ค้น = `replaceState` · กดชิพ/เลือกวันที่ = `pushState`** ไม่งั้นกด back ถอยทีละตัวอักษร
- **ปุ่มเปิดตัวกรองอยู่ "ในช่องค้นหา" มุมขวา** (เจ้าของสั่ง 20 ส.ค. 2026 — วางเป็นปุ่มลอย
  ข้างนอกแล้วมองไม่เห็น) · ⚠️ ต้องเผื่อ `padding-right` ของ `#q` ให้พ้น **ทั้งปุ่มล้างคำค้น
  และปุ่มตัวกรอง** ไม่งั้นข้อความที่พิมพ์จะมุดใต้ปุ่ม · จอแคบเหลือแต่ไอคอน (ซ่อน `.flabel`)
  ไม่งั้นกินที่พิมพ์ไปเกือบครึ่งช่อง — เทสต์ `archives.mjs` [9c] วัดการชนกันไว้แล้ว
- **ตัวกรองเป็นการ์ดใบเดียวแบ่ง 3 ช่อง ไม่ใช่การ์ด 3 ใบ** (เจ้าของแจ้ง 20 ส.ค. 2026 —
  3 ใบสูงไม่เท่ากันและกินที่: ช่องวันที่เตี้ย ช่องสำนักข่าวสูง 210px)
  · จอกว้างล็อกความสูงทั้งแถวไว้ (`grid-template-rows`) ของยาวๆ เลื่อนอยู่ข้างในแทน
  · ⚠️ **`.fbox` ต้องมี `min-height:0`** ไม่งั้นลูกที่เป็น flex จะดันความสูงจนล้น
    แล้วโดน `overflow:hidden` ตัดทิ้ง = **รายชื่อสำนักข่าวท่อนล่างหายไปเลย เลื่อนหาไม่ได้**
    (เจอจริงตอนวัด: กล่องสูง 2321px ทั้งที่แถวสูง 172px) — เทสต์ `archives.mjs` [10] คุมไว้
- **กล่องตัวกรองพับไว้เป็นค่าตั้งต้น ทุกขนาดจอ** (เจ้าของสั่ง 20 ส.ค. 2026 — กล่อง 3 กล่อง
  ดันรายการข่าวตกจอ) · ต่างจาก `issue/trends.html` กับ `sd.html` ที่พับเฉพาะจอแคบ
  · จำสถานะใน `localStorage` (`archivesFiltersOpen`) ไม่ใช่ใน DOM อย่างเดียว
  · **หัวกล่องต้องสรุปว่ากรองอะไรไว้เสมอ** (`filterSummary()`) ไม่งั้นพับแล้วไม่รู้ว่าผลถูกกรองอยู่
  · **เปิด URL ที่มีตัวกรองติดมา ให้กางเองอัตโนมัติ** — เห็นผลถูกกรองแต่ไม่รู้ว่าด้วยอะไร คือกับดัก
  · ⚠️ `[hidden]` ต้องประกาศ `display:none !important` **ก่อน** กฎ `.filters { display:grid }`
    ไม่งั้น `display` ชนะแล้วพับไม่ลง
  · ⚠️ เทสต์ที่จะกดตัวกรองต้องกางก่อน (`openFilters()`) และเทสต์ที่วัด "ค่าตั้งต้น"
    ต้องใช้ context ใหม่ ไม่งั้นเจอสถานะที่เทสต์ก่อนหน้าจำไว้
- ชื่อสำนักข่าวยุบด้วย `archives/outlets.config.js` — **แก้ไฟล์นั้นได้เลย ไม่ต้องแตะโค้ด
  และไม่ต้องสร้างข้อมูลใหม่** · ค่าที่ไม่มีในตารางแสดงตามเดิม ไม่ได้ถูกซ่อน
  · ⚠️ **ค่าในชีตมีเครื่องหมายคำพูดติดมาด้วย** (`"ซีพี"` · `"cp" -tower`) เพราะเป็น query
    ของ Google Alerts ตรงๆ — **แก้ query เมื่อไหร่ ค่าที่นี่เปลี่ยนตาม ต้องมาเติมด้วย**
- **หางพาดหัวมี 2 ชั้น** ("… - เทคโนโลยีชาวบ้าน - ข่าวสด" = คอลัมน์ + สำนัก) ต้องตัดให้หมด
  · ตัดให้เองเมื่อท่อนนั้น **เป็นชื่อสำนัก** หรือ **ไปโผล่เป็นหางของข่าวตั้งแต่ 2 ใบขึ้นไป**
    (กฎที่สองนับจากข้อมูลจริง จึงตามข้อมูลใหม่ได้เองโดยไม่ต้องแก้ไฟล์)
  · หรือเป็น **ชื่อเว็บของข่าวใบนั้นเอง** (เทียบกับคอลัมน์สำนักข่าวของแถวนั้น หลังตัดอักขระพิเศษทิ้ง
    — "Pantip" กับ "pantip.com" ถือว่าอันเดียวกัน)
  · ท่อนที่โผล่ครั้งเดียวใส่มือใน `ARCHIVE_TAILS` ของ `outlets.config.js`
  · 🚫 **ห้ามตัดด้วย `split` แล้ว `join` กลับ** — ตัวคั่นในส่วนที่เก็บไว้จะถูกเขียนใหม่หมด
    ("A | B - ข่าวสด" กลายเป็น "A - B") พาดหัวเพี้ยนทั้งที่ไม่ได้ตั้งใจแก้ · ต้อง **เฉือนท้าย** เท่านั้น
    · เจอจริงตอนวัดกับข้อมูลจริง 1 ใบ — เทสต์ `archives.mjs` [4] คุมไว้แล้ว
  · 🚫 **ห้ามใส่คำที่เป็นเนื้อพาดหัวได้** — วัดกับข้อมูลจริงแล้ว 217 ใบ ตัดหาง 61 ใบ
    ไม่มีใบไหนถูกตัดผิด ("Shared Sensibilities: ผัสสะ - สัมผัส" เหลือครบ ตัดแต่ท้ายจริง)
- **โหมดมืดตาม `prefers-color-scheme` เหมือน `/trend/` `/ir/` `/issue/`**
  (`/social/` ตรึงสว่างอย่างเดียว — อันนั้นเป็นข้อยกเว้น ไม่ใช่มาตรฐาน)
  · ⚠️ `<mark>` ต้องกำหนดสีตัวอักษรเองทั้ง 2 โหมด ค่าเริ่มต้นของเบราว์เซอร์อ่านไม่ออกบนพื้นมืด

#### 🤖 ค้นด้วย AI — **โหมดเดียวของหน้านี้** (เจ้าของสั่ง 26 ส.ค. 2026)

**"อยากให้ search เป็นแบบ chat ai เช่น หาข่าวด้านดีของปลาหมอคางดำทั้งหมด"**
แล้วสั่งเพิ่มวันเดียวกัน: **"ทำ interface ให้รู้เลยว่าเป็น AI search ไม่ใช่ keyword และให้มีโหมดเดียวพอ"**

> 🔀 **มี 2 โหมดแล้ว แต่สลับด้วยมือ ไม่ใช่ระบบเดาเอง** (เจ้าของสั่งเพิ่ม 27 ส.ค. 2026:
> "เด่นกว่านี้ และ สลับเป็นหมวด ค้นด้วยคำ ได้ด้วย")
>
> | | 🤖 ค้นด้วย AI (ตั้งต้น) | 🔤 ค้นด้วยคำ |
> |---|---|---|
> | ยิง `/api/archives/ask` | ✅ | ❌ **ไม่ยิงเลย** — เร็ว ฟรี ตรวจสอบได้ |
> | ตีความประโยค/ช่วงเวลา/เงื่อนไข | ✅ | ❌ เอาที่พิมพ์ไปหาตรงๆ |
> | ต้องกด Enter/ปุ่มถึงได้ผล | ✅ | ✅ **เหมือนกัน** |
>
> · **ปุ่มสลับเป็นแบบ 2 ช่อง โชว์ทั้งสองตัวเลือกพร้อมกัน** (`#modeseg` · `.mseg`)
>   🚫 **ห้ามทำเป็นป้ายใบเดียวที่กดแล้วสลับ** — ทำมาแล้ว 2 รอบ เจ้าของบอกตรงๆ ว่า
>      **"คนจะไม่รู้ซิว่ากดได้"** ต่อให้ใส่ลูกศร ▾ + เงา + ทำเป็น `<button>` แล้วก็ตาม
>      มันยังอ่านเป็น "ป้ายบอกสถานะ" อยู่ดี · ช่องที่ยังไม่ได้เลือกคือสิ่งเดียวที่บอกได้ว่ากดตรงนี้ได้
>   📱 **จอแคบ ปุ่มลงมาอยู่แถวล่างคู่กับปุ่มค้น ไม่ใช่ย่อจนเหลือแต่ไอคอน**
>      วัดแล้ว: ยัดไว้ในช่อง = ที่พิมพ์เหลือ 156px (เกณฑ์ 180px) · ย้ายลงล่าง = 244px และยังมีคำกำกับครบ
>   ⚠️ สลับแล้วต้องเปลี่ยนให้ครบ: ช่องที่ระบายสี · placeholder · บรรทัด hint · คำบนปุ่ม (ถาม/ค้น)
>   เปลี่ยนไม่ครบ = ผู้ใช้ไม่แน่ใจว่าอยู่โหมดไหน
> · จำโหมดไว้ใน `localStorage` (`archivesMode`) และใส่ใน URL (`?mode=kw`) — **URL ชนะเสมอ**
> · เทสต์ `archivesai.mjs` [13] คุมไว้ (โดยเฉพาะข้อ "โหมดคำต้องไม่ยิง AI เลย")
>
> 🔴 **พิมพ์แล้วไม่ค้นสดอีกแล้ว — ต้องกด Enter หรือปุ่มถาม (ทั้ง 2 โหมด)**
> ของเดิมมี 2 ทางอยู่พร้อมกัน (พิมพ์แล้วกรองทันที + ปุ่มถาม AI) ซึ่งผู้ใช้แยกไม่ออกว่า
> ตอนไหนได้อะไร · **สิ่งที่เจ้าของสั่งเลิกคือ "ระบบเดาเองว่าที่พิมพ์เป็นคำถามหรือคำค้น"
> ไม่ใช่ "ห้ามมีโหมด"** — โหมดที่ผู้ใช้กดเลือกเองแล้วเห็นชัดว่าอยู่โหมดไหน ไม่ใช่ปัญหาเดียวกัน
> · ⚠️ **ห้ามเอาการค้นสดกลับมา** ตราบใดที่ยังมีปุ่มถาม — จะกลับไปเป็น 2 โหมดทันที
> · ⚠️ **ห้ามยิงถามระหว่างพิมพ์** — จะกลายเป็นถาม AI ทุกตัวอักษร
> · หน้าตาต้องบอกตั้งแต่แรกเห็นว่าเป็น AI: ป้าย **🤖 ค้นด้วย AI** ในบรรทัด hint ·
>   ไอคอน ✨ หน้าช่อง · placeholder เป็นคำถาม · ปุ่ม "ถาม" ใช้สีเน้น (ปุ่มตัวกรองไม่ใช้)
> · **ช่องพิมพ์เก็บคำถามของผู้ใช้ไว้ ห้ามเขียนทับด้วยคำค้นที่ AI แยกออกมา**
>   (ถาม "หาข่าวด้านดีของปลาหมอคางดำ" แล้วช่องกลายเป็น "ปลาหมอคางดำ" = ผู้ใช้งงว่าคำถามหายไปไหน)
>   คำค้นที่แยกได้ไปแสดงในแถบตีความแทน
> · เทสต์ `archivesai.mjs` [1] คุมทั้งชุด · `archives.mjs` มี helper `ask()` ที่กด Enter ให้

คำถามแบบนั้นมี 2 ส่วนที่ต้องทำคนละแบบ — **แยกให้ออกก่อนแตะโค้ด**

| ส่วนของคำถาม | ทำยังไง | ใครทำ |
|---|---|---|
| `ปลาหมอคางดำ` | เป็นตัวอักษรที่อยู่ในพาดหัว → `includes()` เหมือนเดิม | **เบราว์เซอร์** ทันที ฟรี |
| `ด้านดี` | ไม่ได้อยู่ในพาดหัว ต้องอ่านแล้วตีความ | **AI** ที่ `/api/archives/ask` |

- **`/archives/` มี Pages Function ของตัวเองแล้ว** (`functions/api/archives/ask.js`) — ก่อนหน้านี้ไม่มี
  · `GET ?q=<คำถาม>` → `{terms, from, to, judge}` · `POST {judge, titles[]}` → `{keep:[…]}`
- 💧 **ไม่เขียน KV เลย** — ผู้ใช้พิมพ์อะไรก็ได้ จำนวน key จึงไม่มีขอบเขต (กฎเดียวกับ `kwcheck`)
  ใช้ edge cache 24 ชม. · POST cache ตาม hash ของเนื้อคำขอ
- 🚫 **POST นี้ไม่ได้เขียน log** จึงไม่ชนกฎ "ห้ามเปิดให้ POST เข้ามาเขียน log" แต่**เปลืองโควตา AI ได้**
  → จำกัด `MAX_TITLES` 200 ใบ/คำขอ · `MAX_BODY` 64KB · อ่านทีละ 20 ใบ

> 🔴 **AI ใช้ไม่ได้ = เก็บทุกใบ ไม่ใช่ตัดทุกใบ** — ตัดทิ้งเงียบคือข่าวหายโดยไม่มีใครรู้
> ใช้ทั้ง 3 จุด: ไม่มี binding · ถามไม่สำเร็จ · ก้อนไหนตอบไม่ได้ก็เก็บทั้งก้อน
> · ฝั่งหน้าเว็บ: ระหว่างยังคัดไม่เสร็จให้ **แสดงทั้งหมดไปก่อน** (`judgeKeep = null`) หน้าว่างระหว่างรอ
>   อ่านแล้วเหมือน "ไม่มีข่าว" · เทสต์ `archivesai.mjs` [4] [5] คุมไว้
>
> ⚠️ **ต้องบอกเสมอว่าตีความคำถามเป็นอะไร** (แถบ `#askbar`: ถามอะไร · ค้นคำอะไร · คัดด้วยอะไร)
> ผลที่ถูกคัดโดยผู้ใช้ไม่รู้ว่าคัดด้วยอะไร = กับดักเดียวกับตัวกรองที่พับไว้แล้วไม่มีสรุป
> · `judge` กับ `ask` เข้า URL ด้วย ก๊อปลิงก์ส่งต่อแล้วต้องได้ผลเดิม
>
> 🚫 **ห้ามผูกกับปุ่ม Enter** — คนพิมพ์คำค้นธรรมดาแล้วเคาะ Enter ติดนิสัย จะกลายเป็นยิงถาม AI
> ทุกครั้งโดยไม่ตั้งใจ (ช้าลง + เปลืองโควตา) · เป็น **ปุ่มแยก** และห้ามเดาเองว่าที่พิมพ์มา
> เป็นคำถามหรือคำค้น — เดาผิดเมื่อไหร่ผู้ใช้จะงงว่าทำไมบางครั้งช้าและได้ผลไม่เหมือนกัน
>
> ⚠️ **จอแคบใส่ 3 ปุ่มในช่องค้นหาไม่ได้** — วัดแล้วเหลือที่พิมพ์ 148px (เกณฑ์ของเทสต์คือ 180px)
> ปุ่ม 🤖 จึงลงมาอยู่ **ใต้ช่อง** เมื่อจอ ≤ 560px
> · 🚫 **ห้ามแก้ด้วย `flex-wrap` ให้ปุ่มตกบรรทัด** — ปุ่มตัวกรอง (absolute ที่ไม่ได้กำหนด `top`)
>   จะย้ายตามไปด้วยแล้วหลุดออกนอกกรอบช่องค้นหา · ใช้ `padding-bottom` ของแถวแล้ววางปุ่มลงไปแทน
>   (เจอจริงตอนวัด — เทสต์ `archives.mjs` [9c] จับได้ทั้ง 2 เรื่อง)
>
> 🥇 **ต่อ Claude API ได้ — ฉลาดกว่ามาก แต่ต้องใส่กุญแจเอง** (26 ส.ค. 2026)
> `ANTHROPIC_API_KEY` มี = ใช้ Claude ก่อนเสมอ · ไม่มี = ตกไปใช้ Workers AI เหมือนเดิมทุกอย่าง
> · **ค่าตั้งต้น `claude-haiku-4-5`** (เจ้าของเลือกเอง 26 ส.ค. 2026: "ใช้ haiku น่าจะพอ")
>   เปลี่ยนรุ่นได้จาก Cloudflare ด้วย env `ANTHROPIC_MODEL` โดยไม่ต้องแก้โค้ด
> · ⚠️ **ชื่อรุ่นห้ามเติมวันที่ต่อท้าย** (`claude-haiku-4-5` ไม่ใช่ `claude-haiku-4-5-20251001`)
>   เติมแล้วกลายเป็นชื่อที่ไม่มีอยู่จริง ยิงไม่ผ่านทุกครั้ง — เทสต์ `archivesai.mjs` [8] คุมไว้
> · 🚫 **ห้ามส่ง `output_config.effort` ไปกับรุ่น haiku** — มันไม่รับ แล้วตอบ 400 ทันที
>   (เป็นของรุ่น opus เท่านั้น) เคยใส่ไว้ตอนตั้งต้นเป็น opus แล้วต้องถอดออกตอนเปลี่ยนเป็น haiku
> · 🔑 **ใส่เป็น Secret ใน Cloudflare ทั้ง Production และ Preview แล้ว Retry deployment**
>   **ห้าม commit ลง repo เด็ดขาด — repo เป็น public** (กฎเดิมของทั้งโปรเจกต์)
> · ⚠️ **เสียเงินตามการใช้** — คำถาม 1 คำถาม = ยิง 1 ครั้ง แล้วจำคำตอบ 24 ชม. ·
>   การคัดพาดหัวยิงเพิ่มตามจำนวนใบ (สูงสุด 200 ใบ = 10 ครั้ง)
> · ยิงตรงด้วย `fetch` ไม่ได้ใช้ SDK — โปรเจกต์นี้ไม่มีขั้นตอน build และ `functions/` ไม่มี npm เลย
>   (ท่าเดียวกับที่ `social-comment-extractor/worker/worker.js` ใช้อยู่แล้ว)
>
> 🐞 **รอบแรกใช้โมเดลเล็กตัวเดียวแล้วพัง** (เจ้าของเจอบน production 26 ส.ค. 2026)
> `llama-3.2-3b` **ตอบกลับมาจริงแต่ไม่เป็น JSON** → ขึ้นว่า "ตอบมาแต่แกะไม่ได้" ทุกคำถาม
> · ✅ แก้ 3 ชั้น: **ไล่โมเดลจากใหญ่ไปเล็ก** (`PLAN_MODELS` 70b → 8b → 3b · ชื่อที่ Cloudflare
>   ไม่รู้จักจะโยน error แล้วตกไปตัวถัดไปเอง) · **เขียนคำสั่งเป็นภาษาอังกฤษ เนื้อหาเป็นไทย**
>   (โมเดลเล็กทำตามรูปแบบได้ดีกว่ามาก) · **แกะ JSON ให้ทนขึ้น** (```json ครอบ · จุลภาคเกิน · ข้อความพ่วง)
> · การคัดพาดหัวใช้ `JUDGE_MODELS` เริ่มที่ตัวกลาง เพราะยิงเยอะกว่า (200 ใบ = 10 ครั้ง)
> · ข้อความบอกเหตุผลต้อง **ติดคำตอบดิบมาด้วย** ไม่งั้นไล่ปัญหาต่อไม่ได้
>
> 🐞 **ตัวกันพลาดเองพัง — ร้ายกว่าไม่มี** (เจ้าของเจอรอบสอง 26 ส.ค. 2026)
> `raw.replace is not a function` — คำตอบของ Workers AI **ไม่ได้เป็นสตริงเสมอไป**
> (บางโมเดลคืน object · บางตัวห่อใน `result` อีกชั้น) บรรทัดที่สร้างข้อความ error เองเลยพัง
> แล้ว **error หลุดออกไปทั้งฟังก์ชัน = โมเดลสำรองไม่มีวันได้ลอง**
> · ✅ `aiText()` แกะได้ทุกรูปแบบ (object ก็แปลงกลับเป็น JSON ให้) · และ **ทั้งรอบของ `runAI` อยู่ใน try**
>   ไม่ใช่แค่ตอนเรียก AI · เทสต์ `archivesai.mjs` [8] คุมไว้
> · 📏 บทเรียนที่ใช้ได้ต่อ: **โค้ดที่เขียนไว้กันพลาด ต้องกันตัวเองพลาดด้วย**
>
> 🐞 **คำถามเรื่องช่วงเวลาล้วนๆ เคยได้ 0 ข่าว** (เจ้าของเจอ 27 ส.ค. 2026: ถาม "ข่าวเมื่อวาน")
> AI ตีความถูกแล้ว — ตอบช่วงวันที่มาให้โดย**ไม่มีคำค้น** เพราะไม่มีคำไหนอยู่ในพาดหัวจริง
> แต่โค้ดโยนทิ้งเพราะ `terms` ว่าง แล้วตกไปค้นคำว่า "ข่าวเมื่อวาน" ในพาดหัว = ไม่มีทางเจอ
> · ✅ **"ไม่มีคำค้น" ≠ "ตีความไม่ออก"** — ถอยก็ต่อเมื่อ**ไม่ได้อะไรมาเลยสักอย่าง**
>   (ไม่มีทั้งคำ ทั้งวันที่ ทั้งเงื่อนไข) · แก้ทั้งฝั่ง `askPlan()` และฝั่งหน้าเว็บ (`runAsk`)
> · ✅ และ **ทางถอยตีความวันที่เองได้แล้ว** (`localDates()`) — เมื่อวาน · วันนี้ · เมื่อวานซืน ·
>   สัปดาห์นี้/ที่แล้ว · เดือนนี้/ที่แล้ว · ปีนี้/ที่แล้ว · 7 วัน · 30 วัน · **ไม่ต้องพึ่ง AI เลย**
> · ⚠️ **ต้องเฉือนคำนำออกจากสตริงตรงๆ ไม่ใช่แยกด้วยช่องว่าง** — "ข่าวเมื่อวาน" เป็นคำเดียว
> · 🚫 **ห้ามใส่คำสั้นกำกวมใน `LEAD_RE`** — "ขอ" ซ่อนอยู่ต้นคำว่า "ของ" แล้วเฉือนจนเหลือ "ง"
>   และเฉือนแล้วต้องเหลือ ≥ 4 ตัวอักษร (หรือว่างสนิท) ไม่งั้น "ข่าวสด" กลายเป็น "สด"
> · เทสต์ `archivesai.mjs` [12] + [8] คุมไว้
>
> ⏰ **ต้องบอกวันนี้ให้โมเดลรู้** ไม่งั้น "เดือนที่แล้ว" / "ปีนี้" แปลงเป็นวันที่ไม่ได้เลย
> · `todayTH()` / `monthRangeTH()` ใช้เวลาไทย — **Workers รันด้วย UTC** ใช้ตรงๆ ช่วงหัวค่ำจะได้วันของเมื่อวาน
>
> 🧠 **ไม่เจอเลย = ผ่อนเงื่อนไขให้เอง แล้วบอกว่าผ่อนอะไร** (`relaxIfEmpty()`)
> หน้านี้ใช้กฎ "ต้องมีครบทุกคำ" → คำเดียวที่ AI แต่งเกินมาก็ทำให้เหลือ 0 ทันที
> ลำดับการผ่อน: **ช่วงวันที่ก่อน** (AI เดาเดือนพลาดบ่อยสุด) → **แล้วค่อยตัดคำ**
> · ⚠️ **ตัดคำต้องลองทีละคำแล้วดูว่าคำไหนคือตัวที่ทำให้ไม่เจอ** ห้ามเดาว่า "คำสั้น = ไม่สำคัญ"
>   คำที่ผิดมักเป็นคำยาวที่ AI แต่งขึ้นมาเอง (คำมากสุด 6 การไล่ทุกแบบจึงถูกมาก)
> · 🚫 **ห้ามผ่อนเงียบๆ** — ไม่บอก ผู้ใช้จะนึกว่าผลที่เห็นตรงกับที่ถาม · เทสต์ [9] [9b] คุมไว้
>
> 🐞 **ทางถอยตอน AI ล่มก็เคยพังเงียบ** — ถาม "หาข่าว dna ของ ปลาหมอคางดำ" แล้วได้ **0 ข่าว**
> เพราะเอาทั้งประโยครวม "หาข่าว" กับ "ของ" ไปหาในพาดหัว (หน้านี้ใช้กฎ "ต้องมีครบทุกคำ")
> · ✅ `STOP_WORDS` ตัดคำถามทิ้งก่อนค้น · ตัดจนไม่เหลือ = คืนของเดิม ไม่ใช่คืนลิสต์ว่าง
>   (ค้นด้วยคำว่าง = ได้ทั้งคลัง) · เทสต์ `archivesai.mjs` [8] คุมไว้
> · ⚠️ **ยังแก้คำถามไทยที่เขียนติดกันไม่ได้** ("หาข่าวด้านดีของปลาหมอคางดำทั้งหมด" ไม่มีช่องว่างเลย
>   ตัดไม่ได้) ตอนนี้บอกผู้ใช้ตรงๆ ให้พิมพ์เฉพาะคำสำคัญ — **ทางแก้จริงคือทำให้ AI ตอบได้**
>
> ⚠️ **ยังไม่เคยลองกับ AI ของจริงจากที่นี่เลยสักครั้ง** — เครื่องที่รัน session ไม่มี binding `env.AI`
> เทสต์ปลอมคำตอบด้วย `page.route` ทั้งหมด ที่วัดคือ "หน้าเว็บทำตัวถูกไหมเมื่อได้คำตอบแบบนั้น"
> **ไม่ใช่ "AI ตีความแม่นแค่ไหน"** · โมเดลที่ใช้เป็นตัวเล็ก (`llama-3.2-3b`) ภาษาไทยอาจพลาด
> ต้องให้เจ้าของลองบน preview แล้วดูว่าแถบตีความตรงกับที่ถามไหม
>
> 🐞 **คำประสมยาวคำเดียว = ได้ 0 ข่าว** (เจ้าของเจอจริง 26 ส.ค. 2026 หลังต่อ Claude แล้ว)
> ถาม "หาข่าวเผาข้าวโพดทั้งหมด" → AI ตอบคำเดียวว่า `เผาข้าวโพด` ซึ่ง **ไม่มีในพาดหัวไหนเลย**
> (พาดหัวจริงเขียน "เผาไร่ข้าวโพด") · หน้านี้หาแบบตัวอักษรตรงกันเป๊ะ จึงเหลือ 0 ทันที
> · ✅ แก้ 3 ชั้น: **สั่งใน prompt ให้แยกคำประสมเป็นคำสั้นหลายคำ** + ใส่เคสนี้เป็นตัวอย่าง ·
>   **ยิงขอคำที่กว้างขึ้นอีกรอบเดียวเมื่อไม่เจอ** (`?broad=1` · cache แยก) ·
>   **ยังไม่เจอก็บอกตรงๆ ว่า "ไม่มีข่าวที่มีคำว่า X ในคลังเลย"**
> · ⚠️ **ตัวผ่อนเงื่อนไขเดิมช่วยไม่ได้เมื่อมีคำเดียว** — มันตัดคำได้ก็ต่อเมื่อมีตั้งแต่ 2 คำขึ้นไป
>   และฝั่งหน้าเว็บ **แยกคำไทยเองไม่ได้** (ไม่มีช่องว่างคั่น) จึงต้องให้ AI แยกให้เท่านั้น
> · 🚫 **ห้ามปล่อยให้เจอข้อความ "ลองลดตัวกรองลง"** ตอนที่ผู้ใช้ไม่ได้ตั้งตัวกรองอะไรไว้เลย
> · เทสต์ `archivesai.mjs` [9c] [9d] คุมไว้
>
> ✍️ **สะกดไม่ตรงเป๊ะก็ต้องเจอ** (เจ้าของแจ้ง 26 ส.ค. 2026: พิมพ์ "เอเลี่ยนสปีชี่" แล้วไม่เจอ
> ทั้งที่คลังมี "เอเลี่ยนสปีชีส์") · ต่างกันแค่วรรณยุกต์กับตัวการันต์ แต่การค้นแบบตรงเป๊ะมองว่าคนละคำ
> · แก้ 2 ทาง: **สั่งใน prompt ให้ AI แก้คำสะกดให้เป็นตัวที่ข่าวใช้จริง** ·
>   และฝั่งหน้าเว็บมี **`looseNorm()`** ตัดวรรณยุกต์ (่ ้ ๊ ๋) กับตัวการันต์ (์) ออกแล้วเทียบใหม่
> · 🚫 **ห้ามเอา `looseNorm` มาเป็นตัวค้นหลักเด็ดขาด** — มันทำให้ "กุ้ง" กับ "กุง" เป็นคำเดียวกัน
>   ซึ่งเป็นข้อห้ามข้อแรกของหน้านี้ · ใช้ได้เฉพาะตอนค้นแบบเป๊ะแล้วได้ 0 และต้องบอกผู้ใช้ทุกครั้ง
> · ⚠️ **โหมดผ่อนสะกดห้ามไฮไลต์** — ตัวที่ใช้เทียบสั้นกว่าพาดหัวจริง ตำแหน่งที่เจอเอาไปตัดชิ้น
>   จากพาดหัวไม่ได้ (พาดหัวจะเลื่อนตำแหน่งทั้งบรรทัด — กับดักเดียวกับที่ `expand()` เตือนไว้)
> · ลำดับการผ่อนทั้งหมด: **ช่วงวันที่ → ผ่อนสะกด → ตัดคำ → ขอคำที่กว้างขึ้น → บอกว่าไม่มีในคลัง**
> · เทสต์ `archivesai.mjs` [10] [10b] คุมไว้
>
> ⏳ **กดถามแล้วต้องขึ้นไอคอนหมุนทันที** (เจ้าของสั่ง 26 ส.ค. 2026)
> ของเดิมแถบตีความมีไอคอนหมุน แต่**รายการข้างล่างยังเป็นผลของคำถามก่อนหน้า** ผู้ใช้อ่านแล้ว
> เข้าใจว่านั่นคือคำตอบของคำถามใหม่ (กฎเดียวกับข้อ 5b: เปิดหน้ามาต้องขึ้นไอคอนหมุน ไม่ใช่ข่าวเก่า)
> · ต้องเรียก `renderList()` **ทันทีที่ตั้ง `judgeBusy = true`** ไม่ใช่รอคำตอบก่อน
> · ปุ่มถามก็เปลี่ยนเป็นไอคอนหมุนและกดซ้ำไม่ได้ · เทสต์ `archivesai.mjs` [11] คุมไว้
>
> 🎨 **ปุ่มตัวกรอง "ตอนกรองอยู่" ใช้ขอบสีเน้น ไม่ใช่พื้นทึบ** — ของเดิมพื้นทึบเหมือนปุ่มถามเป๊ะ
> พอกรองอยู่ 2 ปุ่มติดกันจะกลายเป็นก้อนสีน้ำเงินก้อนเดียว แยกไม่ออกว่ากดตรงไหน
>
> ⚠️ **อ่านให้แค่ 200 ใบแรกของผลค้น** — เกินนั้นขึ้นข้อความบอกให้ใส่คำให้แคบลง ห้ามเงียบ

เป็น static + Pages Functions ไม่มีขั้นตอน build

## ✅ ฟีเจอร์มาตรฐาน — ทุกแดชบอร์ดต้องมีครบ

**รวมถึงแดชบอร์ดใหม่ที่จะสร้างต่อจากนี้ด้วย** ทำไม่ครบถือว่ายังไม่เสร็จ

| # | ฟีเจอร์ | ทำยังไง |
|---|---|---|
| 1 | **favicon** | `<link rel="icon">` emoji ให้ตรงกับไอคอนการ์ดบน landing |
| 2 | **ติดตั้งเป็นแอปได้ (PWA)** | `manifest.webmanifest` + `sw.js` + `apple-touch-icon` |
| 2b | **ชวนติดตั้งเองตั้งแต่เปิดครั้งแรก** | `/installprompt.css` + `/installprompt.js` — **เฉพาะหน้าแรก** (ดูหัวข้อ 📲) |
| 3 | **เช็คเวอร์ชันใหม่อัตโนมัติ** | `<meta name="page-ver">` + สคริปต์เทียบเลข (ก๊อปจาก `sd.html`) |
| 4 | **ปุ่มกลับหน้าหลัก** | `<a href="/">🏠 หน้าหลัก</a>` ใน appbar |
| 5 | **รีเฟรชข้อมูลเงียบ** | `setInterval` ทุก 3 นาที (ถ้าหน้านั้นมีข้อมูลสด) |
| 5b | **เปิดหน้ามาต้องขึ้นไอคอนหมุน ไม่ใช่ข่าวเก่า** | `load()` วาด `กำลังดึงข้อมูล…` ทุกคอลัมน์เมื่อ **ไม่ใช่** auto-refresh |

> ⚠️ **ห้ามให้หน้าไหนเก็บสำเนาข่าวไว้ใน `localStorage` แล้วเปิดมาโชว์ของเก่าทันที** (เจ้าของสั่ง 14 ส.ค. 2026)
> `/ir/` เคยทำแบบนั้น (`ir_feeds_snapshot`) — เปิดหน้ามาเห็นข่าวของรอบที่แล้วโดย **ไม่มีอะไรบอกว่าเก่า**
> แล้วค่อยเปลี่ยนเป็นของจริงทีหลัง · ดูเผินๆ เหมือนเร็ว แต่ผู้ใช้อ่านข่าวเก่าอยู่โดยไม่รู้ตัว
> ตอนนี้ถอดออกแล้ว **ทั้ง 3 หน้าโหลดเหมือนกันหมด** — เทสต์ `irload.mjs` คุมไว้ (หน่วง API แล้ววัดว่า
> ระหว่างรอเห็นอะไร) · ถ้าจะเอา snapshot กลับมา ต้องทำครบทุกหน้าพร้อมกัน + มีป้ายบอกว่าเป็นของเก่า
| 6 | **ไอคอนหมุนตอนรอโหลด** | `<span class="spin"></span>` หน้าข้อความ ทุกจุดที่แปลว่า "ยังโหลดอยู่" |

### 📲 แถบชวนติดตั้งเป็นแอป — `/installprompt.js` ใช้ร่วมทุกหน้า

**เจ้าของสั่ง (21 ส.ค. 2026):** เปิดครั้งแรกทั้งมือถือและเดสก์ท็อป ให้เด้งถามเลยว่าจะติดตั้ง
shortcut ไหม — ของเดิมต้องไปหาเมนู "ติดตั้งแอป" ของเบราว์เซอร์เอง ซึ่งซ่อนลึกจนแทบไม่มีใครเจอ

| | Chrome / Edge (เดสก์ท็อป + Android) | **iOS ทุกเบราว์เซอร์** |
|---|---|---|
| สั่งติดตั้งด้วยโค้ดได้ไหม | ✅ `beforeinstallprompt` → เก็บไว้ → `.prompt()` ตอนผู้ใช้กด | ❌ **ไม่มี event นี้เลย** |
| แถบของเราแสดงอะไร | ปุ่ม **ติดตั้ง** + **ไว้ก่อน** | **บอกวิธี** (แชร์ ⬆︎ → เพิ่มไปยังหน้าจอโฮม) + **เข้าใจแล้ว** |

> 🚫 **ห้ามมีปุ่ม "ติดตั้ง" บน iOS** — กดแล้วไม่มีอะไรเกิดขึ้น ผู้ใช้จะนึกว่าเว็บพัง
> **ผู้ใช้ส่วนใหญ่ของเราอยู่บน iOS** ทางนี้จึงสำคัญไม่แพ้ทางแรก · เทสต์ `install.mjs` [2] คุมไว้
>
> 🔑 **`sw.js` ต้องมี fetch handler ไม่งั้น Chrome/Edge ไม่ยอมให้ติดตั้งเลย**
> (เจ้าของแจ้ง 25 ส.ค. 2026: "desktop ไม่ขึ้นอะไรเลย") เบราว์เซอร์จะไม่ยิง `beforeinstallprompt`
> ถ้า service worker ไม่มีตัวดัก fetch → **แถบไม่มีวันขึ้นบนเดสก์ท็อป/Android** ทั้งที่โค้ดหน้าเว็บถูกหมด
> · ตัวที่ใส่ไว้ **ว่างเปล่า ไม่เรียก `respondWith` สักครั้ง** จึงไม่ขัดกฎข้อ 0 ของ `sw.js`
>   (มีอยู่ให้เบราว์เซอร์นับว่ามี แต่ไม่แตะคำขอใดเลย = ทำหน้าเว็บพังไม่ได้ตามนิยาม)
> · เทสต์ `install.mjs` [5] คุมทั้ง "ต้องมี fetch" และ "ห้ามมี respondWith" คู่กัน
>
> ⚠️ **หน้าต่าง InPrivate / ไม่ระบุตัวตน เบราว์เซอร์ปิดการติดตั้งอยู่แล้ว** — แถบจะไม่ขึ้น
>    เป็นเรื่องปกติ ไม่ใช่บั๊ก · ทดสอบต้องใช้หน้าต่างปกติ
>
> 🚫 **เปิดจากในแอปแชต (ไลน์/เฟซบุ๊ก/ไอจี) ติดตั้งไม่ได้ทั้ง iOS และ Android**
> iOS เมนูแชร์เป็นของแอปนั้น ไม่มี "เพิ่มไปยังหน้าจอโฮม" · Android เป็น WebView ไม่ยิง event
> · **ห้ามบอกวิธีของ Safari ตอนที่ผู้ใช้อยู่ในไลน์** — ปุ่มนั้นไม่มีให้กด หาไม่เจอแล้วจะนึกว่าเว็บพัง
> · ขึ้นเป็น "เปิดในเบราว์เซอร์ก่อน" พร้อมวิธีของแอปนั้น (ตาราง `IN_APP` ใน `installprompt.js`)
> · ⚠️ **เลือกโหมดตอนครบเวลา ไม่ใช่ตั้งไว้ล่วงหน้า** — ถ้าเบราว์เซอร์ในแอปดันยิง event มา
>   แปลว่าติดตั้งได้จริง ต้องขึ้นปุ่มติดตั้ง ไม่ใช่ไล่ออกไปข้างนอก
>
> ⚠️ **iOS บอกตำแหน่งปุ่มแชร์ให้ตรงเครื่อง** — iPad อยู่**ด้านบน** ไม่ใช่ด้านล่างเหมือน iPhone
> และถ้าไม่ใช่ Safari (Chrome/Edge/Firefox บน iOS) **ห้ามเรียกว่า Safari** · เทสต์ [2c] คุมไว้
>
> ⚠️ **ต้อง `e.preventDefault()` ใน `beforeinstallprompt`** ไม่งั้นเบราว์เซอร์เด้งแถบของมันเอง
> แล้วจะมี 2 อันซ้อนกัน
>
> ⚠️ **หลบแถบ "มีเวอร์ชันใหม่" (`#updbar`)** — อันนั้นสำคัญกว่า ถ้ามันขึ้นอยู่ให้เงียบไว้ก่อน
> · `z-index` 9998 ต่ำกว่า `#updbar` (9999) **โดยตั้งใจ**
>
> ⏱ **แถบหายเอง ห้ามบังคับให้กด** (เจ้าของสั่ง 25 ส.ค. 2026: "ต้องกด? ไม่เอา bad user experience")
> ของเดิมมีปุ่ม `[เข้าใจแล้ว]` เป็น**ทางเดียว**ที่จะทำให้แถบหายไป — ทั้งที่มันแค่มาบอกข้อมูล ไม่ได้มาถาม
> · ปุ่มที่เหลือมีได้แค่ **"ติดตั้ง"** เท่านั้น (กดแล้วได้อะไรจริง) · ที่เหลือเป็น **✕ มุมขวาบน** เผื่อคนอยากปิดเดี๋ยวนี้
> · **`AUTO_HIDE_MS` = 9 วินาที เท่ากันทุกแบบ — เจ้าของกำหนดเอง ห้ามเปลี่ยนเองโดยไม่ถาม**
>   (สั่ง 6 วิ ก่อน แล้วสั่งยืดเป็น 9 วิ ในวันเดียวกัน)
> · เทสต์ `install.mjs` [2d] คุมไว้ (ถอดตัวจับเวลาออก → ตก 2 ข้อ)

> 🎯 **เกณฑ์เดียวคือ "ติดตั้งแล้วหรือยัง"** (เจ้าของสั่ง 25 ส.ค. 2026: "เช็ค ถ้าเช็คแล้วยังไม่มีก็โชว์เลย")
> **ยังไม่ได้ติดตั้ง = ขึ้นทุกครั้งที่เปิดหน้า** · ติดตั้งแล้ว = ไม่ขึ้นอีกเลย
> · ไม่ชวนเมื่อ `display-mode: standalone` / `navigator.standalone` (เปิดจากไอคอนบนหน้าจอ)
> · **ติดตั้งสำเร็จเท่านั้น** ที่นับว่า **เลิกชวนถาวร** (`installPromptDone`)
> · 🚫 **ปิดแถบ (✕) หรือปล่อยให้หายเอง ห้ามนับว่าไม่เอาถาวร** — รอบหน้าต้องขึ้นใหม่
> · กดติดตั้งแล้วไปยกเลิกในกล่องของเบราว์เซอร์ **ไม่นับว่าไม่เอาถาวร** เหมือนกัน
>
> ⚠️ **ของเดิมเงียบ 30 วันทันทีที่แถบขึ้นครั้งเดียว — ถอดออกแล้ว** เพราะกลายเป็นปัญหาจริง:
> เจ้าของทดสอบบน iPhone แล้วไม่ขึ้นสักที เพราะเครื่องจำค่าจากรอบทดสอบก่อนไว้
> ต้องไปกดล้างที่ `/selftest/` ถึงจะเห็น — **คนทั่วไปไม่มีทางรู้ว่าทำไมไม่ขึ้น**
> · ผลข้างเคียงที่ยอมรับ: คนที่ติดตั้งแล้วแต่ยังเปิดผ่าน Safari ปกติจะเห็นแถบอีก
>   (iOS ไม่มีสัญญาณบอกว่าติดตั้งแล้วตอนเปิดจากเบราว์เซอร์) — แลกกับการที่คนที่ยังไม่มีได้เห็นจริง
>
> ⚠️ **แถบนี้กำหนดสีของตัวเองครบทั้งโหมดสว่างและมืด ไม่ยืมตัวแปรของหน้าไหน**
> ยืมแล้วจะเพี้ยนทันทีที่หน้าไหนตั้งชื่อตัวแปรไม่เหมือนกัน (`/social/` ใช้ `--plane`/`--ink`)
>
> ⚠️ **ทดสอบ WebKit ที่นี่ไม่ได้** — ฝั่ง iOS ทดสอบได้แค่ "เลือกทางถูกไหมเมื่อเจอ UA ของ iPhone"
> **พฤติกรรมจริงบน Safari ต้องให้เจ้าของเปิดบนเครื่องยืนยันเอง**
>
> 📍 **อยู่ที่ `index.html` (หน้าแรก) หน้าเดียวเท่านั้น** (เจ้าของสั่ง 25 ส.ค. 2026:
> "พวก install เกะกะตอนดูข่าว ให้แสดงเฉพาะหน้าแรกพอ")
> · เดิมใส่ไว้ 8 หน้ารวมหน้าข่าวด้วย → **แถบลอยขึ้นมาบังตอนกำลังอ่านข่าว**
> · หน้าแรกเป็นหน้าเลือกเมนู ไม่มีอะไรให้อ่าน แถบจึงไม่เกะกะ
> · 🚫 **ห้ามใส่กลับที่หน้าข่าว** (`trend/` `ir/` `issue/*` `archives/` `sd.html`)
>   — เทสต์ `install.mjs` [4] ไล่เช็คทุกหน้า ใส่กลับเมื่อไหร่ตกทันที
> · ไม่ใส่ที่ `/admin/` · `/selftest/` · `/social/` เหมือนเดิม
>
> ⚠️ **ผลข้างเคียง: คนที่เข้าหน้าข่าวตรงๆ (bookmark/ลิงก์ที่แชร์กัน) จะไม่เห็นแถบเลย**
> แลกกับการไม่รบกวนตอนอ่านข่าว — เจ้าของเลือกทางนี้

### 🥇 คอลัมน์แรกต้องออกตัวก่อนเสมอ (เจ้าของสั่ง 25 ส.ค. 2026)

**"mobile ควรโหลด column แรกก่อนเป็นอันดับแรก"** — บนมือถือเห็นทีละคอลัมน์
คอลัมน์แรกคือของเดียวที่ผู้ใช้นั่งจ้องอยู่ ห้ามมีอะไรออกตัวก่อนมัน

วัดของเดิมด้วย Performance API บนจอ 390px (ไม่ใช่ประมาณเอา) เจอ 2 อย่างซ้อนกัน:

| | เดิม | ตอนนี้ |
|---|---|---|
| `/api/flags` (สถานะปุ่ม ⚑/🗂) | **44 ms** ← แซง | รอจนข่าวมาถึงก่อน (~440 ms) |
| `/api/trend/feeds` (ข่าวคอลัมน์แรก) | 99 ms | **~25 ms** |

- **ยิงตั้งแต่ใน `<head>`** — สคริปต์บรรทัดเดียวใน `index.html` เก็บไว้ที่ `window.__bootFeeds`
  แล้ว `app.js` มารับไปใช้ · ของเดิมต้องรอ parse `app.js` ทั้งไฟล์ + `Flags.init()` + `wire()` ให้จบก่อน
- **`Flags.init({ after: bootFeeds })`** — ค่าที่ได้จาก `/api/flags` มีไว้ระบายสถานะปุ่มเท่านั้น
  ไม่ได้วาดเนื้อหาอะไรเลย จึงต้องรอ · **`/admin/` ไม่ส่ง `after` มา** เพราะที่นั่น flag คือเนื้อหาของหน้า

> ⚠️ **URL ของ endpoint เขียนไว้ 2 ที่ (HTML กับ `FEEDS_EP` ใน `app.js`) — ต้องแก้คู่กัน**
> แต่ถึงลืม หน้าจะไม่พัง เพราะ `fetchFeeds()` **ยิงใหม่ให้เองเมื่อของที่ยิงล่วงหน้าล้มเหลว**
> · 🚫 **ห้ามเขียนเป็น `await bootFeeds` เฉยๆ** — เน็ตสะดุดวินาทีแรก หรือพิมพ์ URL ผิดที่เดียว
>   จะทำให้ทั้งหน้าขึ้น "ดึงข้อมูลไม่สำเร็จ" ทั้งที่ยังไม่เคยลองยิงด้วยตัวเอง
> · ⚠️ **`bootFeeds` ใช้ได้ครั้งเดียว** — กดปุ่ม 🔄 กับ auto-refresh ต้องยิงใหม่ (`takeBootFeeds()` เคลียร์ทิ้ง)
> · เทสต์ `firstcol.mjs` คุมทั้ง 5 ชั้น — ลองถอด `after:` ออก → ตก 3 ข้อ · ถอดทางถอย → ตก 3 ข้อ

### 🐢 โหลดทีละคอลัมน์ (lazy) — `/lazycol.js` ใช้ร่วมทุกแดชบอร์ด

**เจ้าของสั่ง (21 ส.ค. 2026):** บนมือถือแดชบอร์ดเป็น carousel **เห็นทีละคอลัมน์**
แต่ของเดิมยิงทุกต้นทางพร้อมกันตั้งแต่วินาทีแรก คอลัมน์ที่ผู้ใช้กำลังจ้องอยู่จึงต้อง
แย่งเน็ตกับคอลัมน์ที่ยังมองไม่เห็น (และรอคอลัมน์ที่ช้าที่สุดอย่าง YouTube ไปด้วย)

| | เดิม | ตอนนี้ |
|---|---|---|
| `/trend/` เปิดหน้าบนมือถือ | ยิง 4 คำขอ · วาด 7 คอลัมน์ | **ยิง 1 คำขอ · วาด 2 คอลัมน์** |
| เดสก์ท็อป | เหมือนเดิมทุกอย่าง | เหมือนเดิม (เห็นครบก็โหลดครบ) |

- ตัวจัดการอยู่ที่ **`/lazycol.js` ไฟล์เดียว** (`window.LazyCol`) — `trend/` `ir/` `issue/` โหลดร่วมกัน
- `LAZY_COLS` ใน `app.js` = ตารางคอลัมน์ที่มี endpoint ของตัวเอง → ชื่อคอลัมน์ : ฟังก์ชันดึงข้อมูล
  · ⚠️ **เพิ่มคอลัมน์ที่มี endpoint ของตัวเองเมื่อไหร่ ต้องใส่ทั้ง `LAZY_COLS` และ `SELF_LOADING`**
    ไม่ใส่ = ไม่มีใครโหลดให้ ค้างเป็นไอคอนหมุนถาวร
- **auto-refresh ทุก 3 นาที ดึงซ้ำเฉพาะคอลัมน์ที่ผู้ใช้เคยเปิดดูแล้ว** (`LazyCol.seen()`)
- `/ir/` ทุกคอลัมน์มาจากคำขอเดียว จึงประหยัดได้แค่ **แรงวาด** ไม่ได้ประหยัดเน็ต

> 🐞 **กับดักที่เจอตอนทำ — เขียน `state.data.sources.X = await ...` ตรงๆ ไม่ได้**
> JS หา object ปลายทาง (`state.data.sources`) **ก่อน** `await` ถ้าระหว่างรอ `load()`
> สร้าง `sources` ก้อนใหม่ ค่าที่ได้จะไปตกในก้อนเก่าที่ไม่มีใครอ่านแล้ว
> **อาการ: ยิงสำเร็จแล้วแต่คอลัมน์ค้างเป็นไอคอนหมุน และผลต่างกันทุกครั้งตามว่าใครเสร็จก่อน**
> · ต้องรับใส่ตัวแปรก่อนแล้วค่อยเขียนลง state (`reloadYTTrends` เขียนถูกอยู่แล้วจึงไม่เคยพัง)
> · และ `load()` ต้อง **เขียนทับ `sources` ทีละคีย์** ห้ามสร้างก้อนใหม่
> · เทสต์ `lazycol.mjs` [5] มีด่านจับระดับโค้ด
>
> 🐞 **`reveal` กับ `load()` ยิงซ้อนกันได้** — ตอนเปิดหน้า ตัว lazy สั่งดึงคอลัมน์ที่อยู่ในจอไปแล้ว
> ถ้า `load()` ไล่ดึงคอลัมน์ที่ `seen()` อีกรอบ จะกลายเป็นยิงคอลัมน์ละ **2 ครั้ง**
> (วัดเจอจริงบนเดสก์ท็อป) → มีธง `firstLoad` ข้ามรอบแรกไว้
>
> ⚠️ **ไอคอนหมุนของคอลัมน์ที่ยังไม่เปิด ยังตรงกับกฎข้อ 6 อยู่** เพราะวินาทีที่คอลัมน์
> โผล่เข้าจอ การโหลดเริ่มทันที · และมีตาข่ายกันเหนียวใน `lazycol.js` (`FALLBACK_MS` 8 วิ)
> เผื่อ IntersectionObserver ไม่ยิง จะไล่วัดตำแหน่งเองแล้วเปิดเฉพาะคอลัมน์ที่อยู่ในจอจริง
> · ไม่มี `IntersectionObserver` เลย (Safari เก่ามาก) = **เปิดทุกคอลัมน์ทันที** เหมือนเดิม
>   ยอมโหลดหนักดีกว่าหน้าว่าง — ⚠️ **ทดสอบ WebKit ที่นี่ไม่ได้ ต้องให้เจ้าของเปิดดูบน iOS เอง**

> ⚠️ ข้อ 6: **ไอคอนหมุนแปลว่า "กำลังมา" เท่านั้น** ถ้าโหลดเสร็จแล้วแต่ไม่มีข้อมูลจริงๆ
> **ต้องบอกว่าไม่มี ห้ามหมุนค้าง** — `items: []` ของ "ยังโหลดไม่เสร็จ" กับ "หมวดนี้ไม่มีเทรนด์"
> หน้าตาเหมือนกันเป๊ะ ต้องมีธง `loaded: true` ติดมากับ bucket ถึงจะแยกออก
> (เกิดจริง: เลือกหมวด 🩺 สุขภาพ ที่ไม่มีเทรนด์ แล้วขึ้น "กรุณารอซักครู่" ค้างตลอด ทั้งที่ป้ายบอก "0 คำ" แล้ว)
> · คุมด้วยเทสต์ `emptycat.mjs` — คอลัมน์ Google Trends / X / YouTube ทั้ง `trend/` และ `issue/`
>
> ⚠️ ข้อ 6: **หน้าใหม่/ข้อความรอใหม่ ต้องมีไอคอนหมุนด้วยเสมอ** — ข้อความเปล่าๆ อ่านแล้วเหมือนหน้าค้าง
> `/trend/` `/ir/` `/issue/` ใช้ `WAITING` ที่ประกาศไว้บนสุดของ `app.js` (แก้ที่เดียวได้ทุกจุด)
> · `sd.html` มีของตัวเองเพราะเป็นไฟล์เดี่ยว **และมี 2 โหมด** สีจึงต้องเป็น `currentColor` ห้ามฮาร์ดโค้ด
> · ห้ามใส่กับข้อความที่ไม่ใช่สถานะรอ (เช่น "ไม่พบรายการที่ตรงกับตัวกรอง") — หมุนค้างโดยไม่มีอะไรมา

> ⚠️ ข้อ 3: bump **`<meta name="page-ver">` ที่เดียว** (แดชบอร์ดใช้ `app.js?v=` แทน)
> **ห้ามเขียนเลขซ้ำไว้ในโค้ดอีกที่** — `var VER` / `APP_VER` อ่านจาก DOM เองแล้ว

### 🔁 ตัวเช็ค "มีเวอร์ชันใหม่" — เคยเด้งไม่หยุด 3 สาเหตุ แก้แล้วทั้งหมด (11 ส.ค. 2026)

เจ้าของแจ้งว่า "refresh version ใหม่บ่อยเกินไป เอาแค่วันละครั้งพอ" ไล่แล้วเจอ 3 ต้นเหตุซ้อนกัน:

| ต้นเหตุ | อาการ | แก้ยังไง |
|---|---|---|
| **เลขเวอร์ชันเขียนไว้ 2 ที่แล้วลืม bump คู่กัน** (`APP_VER` 102 vs `app.js?v=104` · issue 52 vs 53) | เจอ "มีเวอร์ชันใหม่" **ทุกครั้งที่เช็ค** เพราะรีโหลดมาก็ได้เลขเดิม วนไม่จบ | `APP_VER` / `var VER` **อ่านจาก DOM เอง** อ่านไม่ได้ = `Infinity` (เงียบไว้ ดีกว่าเดาผิด) |
| **sw ยิง `sw-updated` ตอน activate เสมอ รวมถึงติดตั้งครั้งแรก** | คนเปิดเว็บครั้งแรก / iOS ล้าง sw ทิ้งตอนไม่ได้เปิดเกิน 7 วัน → เจอแถบอัปเดตทั้งที่เพิ่งโหลดของล่าสุด | หน้าเว็บจำ `__hadCtrl = !!navigator.serviceWorker.controller` ตอนเริ่ม — ไม่มี controller = ติดตั้งครั้งแรก **ไม่ต้องบอกอะไร** |
| **เช็คถี่เกิน** — `RESUME_MIN_GAP` 60 วิ + `r.update()` ทุก visibilitychange | สลับกลับเข้าแอปทีไรเช็คทุกที | ทั้งคู่ยึดรอบ **24 ชม.** (`swChkAt` ใน localStorage) |

- **ทางลัดของเจ้าของ: แตะที่ป้ายเลขเวอร์ชัน `v105 · sw5`** = สั่งเช็คเดี๋ยวนี้ ไม่ต้องรอครบวัน
  (ใช้ตอนเพิ่ง release แล้วอยากรู้ว่าถึงเครื่องหรือยัง)
- เทสต์ `emptycat.mjs` คุมไว้: เวอร์ชันเท่ากันห้ามเด้ง · มีของใหม่ต้องเด้ง · สลับเข้าออกไม่ยิงซ้ำ

### 🔢 ป้ายเลขเวอร์ชัน — ใช้ตรวจว่าเครื่องได้ของใหม่หรือยัง

ทุกหน้ามี `<span id="vtag">` แสดงผลเป็น **`v34 · sw4`**

- `v` = เวอร์ชันของหน้า (อ่านจาก `<meta name="page-ver">` ถ้าไม่มีก็อ่าน `?v=` ของ `app.js`)
- `sw` = เวอร์ชัน service worker ที่ทำงานอยู่ในเครื่องจริง (หน้าถาม sw ตรงๆ)

> เอาไว้เลิกเดาว่า "แอปอัปเดตหรือยัง" — ให้ผู้ใช้อ่านเลขให้ฟังแล้วเทียบกับ
> ที่อยู่บน production ได้เลย เคยเสียเวลาไล่กันหลายรอบเพราะไม่มีตัวนี้

## 🧪 เทสต์อยู่ที่ `tests/` — รันได้จริง อย่าเดาว่าโค้ดถูก

ไฟล์เทสต์ที่ `CLAUDE.md` อ้างถึงทุกไฟล์อยู่ใน **`tests/`** แล้ว (ย้ายเข้า repo 14 ส.ค. 2026 —
ก่อนหน้านี้อยู่ในที่เก็บชั่วคราวของ session แล้วหายไปทุกครั้งที่ session จบ)

```bash
cd tests && npm install            # ครั้งเดียว เฉพาะเทสต์ที่เปิดเบราว์เซอร์
python3 -m http.server 8899 --directory .. &   # เทสต์เบราว์เซอร์ต้องมีเซิร์ฟเวอร์นี้
node noise.mjs · TZ=Asia/Bangkok node sheetorder.mjs
```

**แก้ตรรกะที่ไหน ให้รันเทสต์ที่คุมเรื่องนั้นก่อนบอกว่าเสร็จ** — ดูตารางว่าไฟล์ไหนคุมอะไรที่ `tests/README.md`
· **ตัวเว็บยังเป็น static ไม่มี build step เหมือนเดิม** `tests/package.json` ใช้กับเทสต์เท่านั้น

## 🍎 ทดสอบฝั่ง iOS/Safari — มี 2 ชั้น ใช้คนละงานกัน

**เจ้าของถาม 25 ส.ค. 2026:** "มีวิธีทำระบบ test ของ ios mobile เองไหม?" · "มีแต่ใช้ไม่สะดวก
ถ้าทำเองได้สะดวกกว่า" → ทำ 2 ชั้น ชั้นแรกอัตโนมัติ ชั้นสองไว้เปิดนานๆ ครั้ง

| | ชั้น 1 — WebKit ที่ GitHub | ชั้น 2 — `/selftest/` บนเครื่องจริง |
|---|---|---|
| รันเมื่อไหร่ | **เองทุกครั้งที่ push** | เจ้าของเปิดเอง |
| จับอะไรได้ | CSS · เลย์เอาต์ · JS ที่ Safari ไม่รองรับ | **ทุกอย่างที่ชั้น 1 ทำไม่ได้** |
| จับอะไรไม่ได้ | เมนูแชร์ · เพิ่มลงหน้าจอโฮม · `navigator.standalone` · safe-area จริง | — |

> 🚫 **ห้ามเคลมว่า "ทดสอบ Safari แล้ว" เพราะ CI ผ่าน** — WebKit ของ Playwright ไม่ใช่
> Safari บน iPhone · **เครื่องที่รัน session โหลด WebKit ไม่ได้ด้วยซ้ำ** (ลองแล้ว ต้นทางตอบ 403)

### `/selftest/` — หน้าตรวจตัวเองบนเครื่องจริง

เปิด `cp-dashboard-680.pages.dev/selftest/` บนเครื่องที่อยากตรวจ แล้วอ่านผล ✅/❌
· มีปุ่ม **📋 คัดลอกผลทั้งหมด** ให้ส่งกลับมาได้เลย ไม่ต้องพิมพ์เอง
· มีปุ่ม **🧹 ล้างความจำแถบชวนติดตั้ง** สำหรับทดสอบซ้ำ (ไม่งั้นติดกฎเงียบ 30 วัน)

ตรวจให้ 5 หมวด: เครื่องนี้คืออะไร · เงื่อนไขการติดตั้ง · แถบชวนติดตั้ง · หน้าตา · ข้อมูลยังมาไหม

> ⚠️ **ห้ามใส่ `/installprompt.js` ที่หน้านี้** — แถบจะเด้งทับผลตรวจและทำให้ค่าที่วัดเพี้ยน
> · **ไม่มีการ์ดบน landing โดยตั้งใจ** เป็นเครื่องมือ ไม่ใช่แดชบอร์ด
> · ⚠️ **หน้านี้เองก็ต้องมีเทสต์คุม** — ถ้ามันพังเงียบแล้วขึ้น ✅ ตลอด จะแย่กว่าไม่มีหน้านี้
>   `selftest.mjs` จึงจำลองของพัง (Access หมดอายุ · manifest 404) แล้ววัดว่ามันจับได้จริง
> · **ตาราง `IN_APP` ต้องตรงกับ `installprompt.js` เสมอ** ไม่งั้นหน้านี้จะรายงานคนละเรื่อง
>   กับที่ผู้ใช้เห็นจริง — เทสต์ไล่เทียบทุกชื่อให้แล้ว

## 🧪 เช็คลิสต์ก่อนบอกว่า "เสร็จ" — ทำทุกครั้งที่มีของใหม่

**ยังไม่เสร็จจนกว่าจะตอบครบทั้ง 4 ข้อ** เคยพลาดเพราะทำเสร็จบนเดสก์ท็อปแล้วคิดว่าจบ

| # | ต้องเช็ค | ถามตัวเองว่า |
|---|---|---|
| 1 | **ทุกแดชบอร์ด** | ของใหม่นี้ต้องมีที่ `trend/` `ir/` `issue/` `sd.html` และ landing ด้วยไหม? ถ้าเป็นฟีเจอร์พื้นฐาน = ต้องมีครบทุกที่ · ถ้าเป็นของเฉพาะทาง = **เขียนไว้ว่าทำไมถึงอยู่แค่หน้านั้น** |
| 2 | **แดชบอร์ดที่เพิ่งเพิ่มเข้ามา** | ถ้ารอบนี้สร้างแดชบอร์ดใหม่ มันได้ **ฟีเจอร์มาตรฐานทั้ง 5 ข้อ** ครบไหม? และมี **การ์ดบน landing** แล้วหรือยัง? แดชบอร์ดใหม่ต้องเท่าเทียมของเดิมตั้งแต่วันแรก ไม่ใช่ค่อยตามเก็บทีหลัง |
| 3 | **มือถือ** | จอแคบพังไหม? `/trend/` `/ir/` `/issue/` เป็น carousel ปัดซ้ายขวา ไม่ใช่กริด — ของที่กางออกมาในกริดจะดันจนหน้าพัง (เคยเกิดกับกล่องข่าวในคอลัมน์ X มาแล้ว) |
| 4 | **แอปที่ติดตั้งแล้ว** | เครื่องที่ติดตั้งเป็นแอปจะได้ของใหม่ไหม? bump `page-ver` + `?v=` ครบหรือยัง? |

> ⚠️ **คอลัมน์ที่มี `max-height` ของตัวเอง ต้องล้างค่าบนมือถือด้วย** (เจ้าของแจ้ง 14 ส.ค. 2026)
> `.panel[data-source="trends"]` ตั้ง `clamp(420px, 68vh, 760px)` ไว้สำหรับเดสก์ท็อป และกฎนี้อยู่
> **หลัง** กฎ `.panel { max-height: none }` ของมือถือในไฟล์ จึงชนะด้วยลำดับ — คอลัมน์ Google Trends
> เลยสูงแค่ 622px จากพื้นที่ 737px **เหลือช่องว่างใต้การ์ดเฉพาะคอลัมน์นั้น** ส่วนคอลัมน์อื่นเต็มจอปกติ
> (ของเดิมล้างให้แค่ `xtrends` กับ `yttrends` ลืม `trends`) · เทสต์ `mobile.mjs` วัดความสูงทุกคอลัมน์
> เทียบกับพื้นที่ในบอร์ดแล้ว — **เพิ่มคอลัมน์ใหม่ที่มี `max-height` เมื่อไหร่ ต้องมาล้างตรงนี้ด้วย**

### 🔁 ทำทุกครั้งหลังเขียนโค้ดเสร็จ ก่อนบอกผู้ใช้ว่าเสร็จ

**อย่ารีบบอกว่าเสร็จ** ให้ไล่ตรวจตัวเองก่อนเสมอ — ที่ผ่านมาบั๊กร้ายแรงที่สุด
(KV เขียน 50 ครั้ง/รอบ จนจะทำโควตาหมดทั้งโปรเจกต์) เจอตอนไล่ตรวจย้อนหลัง
ไม่ใช่ตอนเขียน

1. ตอบเช็คลิสต์ 4 ข้อข้างบนให้ครบ
2. **นับ KV read/write ต่อ 1 request** — เกิน 1-2 ครั้งถือว่าผิด ต้องแก้
3. รันตรวจ syntax + ทดสอบตรรกะด้วยข้อมูลจำลอง อย่าเดาว่าโค้ดถูก
   - **syntax ผ่าน ≠ ทำงานถูก** เคยใส่สคริปต์ไว้ใน `<head>` แล้วให้มันหา element
     ที่อยู่ท้าย `<body>` — ผ่าน syntax แต่ได้ `null` ทุกครั้ง ไม่ทำงานเลย
   - **ตรวจ harness ที่ใช้ทดสอบด้วย** Node รุ่นใหม่มี `navigator` เป็น global
     ที่เขียนทับตรงๆ ไม่ได้ ต้องใช้ `Object.defineProperty` ไม่งั้นผลทดสอบหลอก
4. ถามว่า "ถ้าของนี้พัง จะพังแค่ตรงนี้ หรือลามไปทั้งระบบ?" — ถ้าลาม ต้องมีตัวกัน
5. เขียนสิ่งที่เจอ/แก้ ลงข้อความที่ตอบผู้ใช้ **อย่าซ่อนความเสี่ยงที่รู้อยู่**

### ⏱ รอบสร้างข่าวใหม่ (`FRESH_MS`) = ตัวคุมโควตาเขียน KV — ห้ามลดโดยไม่คิดเลข

**เจอจาก `/selftest/` บน iPhone ของเจ้าของ (25 ส.ค. 2026):** ข่าวใช้เวลา **21 วินาที**
(รอบก่อนหน้า 7.6 วิ — แย่ลงเรื่อยๆ) ส่วน `/api/flags` ใช้ 52 ms → ปัญหาอยู่ที่ฝั่ง feeds อย่างเดียว

ไล่โค้ดแล้วเจอ 2 ชั้น ชั้นที่สองอันตรายกว่า:

1. **ถ้าไม่มีของใน cache ต้องสร้างใหม่ตรงนั้นเลย** — ดึงฟีด **26 แหล่ง** (แต่ละแหล่งรอได้ถึง
   `FETCH_TIMEOUT` 12 วิ) + อ่านคลังข่าวจาก KV + ให้ AI จัดหมวด + เขียนกลับ
   · ฟีดไหนอืดหรือตาย = รอจนครบ 12 วิ
2. 🚨 **`FRESH_MS` เดิม 3 นาที "พอดี" กับรอบ auto-refresh ของแดชบอร์ด (3 นาที)**
   → มีคำขอเข้ามาทีไรก็สร้างใหม่ทุกที · **สร้าง 1 ครั้ง = เขียน KV 2 ครั้ง**
   (คลังข่าวใน `saveArchives` + บันทึกระบบใน `finishLog` ที่ส่ง `built: true`)

| เปิดหน้าค้างไว้ | build/วัน | เขียน KV/วัน (ต่อ endpoint) |
|---|---|---|
| 8 ชม. ที่ 3 นาที | 160 | 320 |
| **24 ชม. ที่ 3 นาที** | 480 | **960** ← เกือบเต็มโควตา 1,000 ของทั้งโปรเจกต์ |
| 24 ชม. ที่ **15 นาที** | 96 | **192** |

**และมี 2 endpoint ที่ทำแบบนี้** (`trend/feeds` + `ir/feeds`) จึงคูณสอง

> ✅ แก้แล้ว: `FRESH_MS` **3 → 15 นาที** ทั้ง 2 ไฟล์ · ลดการเขียนลง 5 เท่า
> ข่าวเก่าสุดที่ผู้ใช้เห็นคือ 15 นาที ซึ่งยอมรับได้เพราะ**ฟีดต้นทางเองอัปเดตชั่วโมงละครั้ง**
>
> 🚫 **ห้ามลดกลับต่ำกว่า 10 นาที** — เทสต์ `syslog.mjs` [10] มีด่านจับ (ลองใส่ 3 นาทีแล้วตกจริง)
> · ด่านเดียวกันคุม "ห้ามเกิน 1 ชั่วโมง" ด้วย (ข่าวจะเก่าเกินไป) และ
>   "เขียน KV ที่เดียวต่อไฟล์" กันไม่ให้มีใครแอบเพิ่มจุดเขียน
>
> ⚠️ **ยังไม่ได้แก้เรื่อง 21 วินาที** — ตัวนี้แก้แค่โควตา KV
> ถ้าไม่มีของใน cache ผู้ใช้ยังต้องรอสร้างใหม่อยู่ดี
> ทางแก้ที่เสนอไว้: **เก็บสำเนาผลลัพธ์ไว้ใน KV แล้วเสิร์ฟทันทีเมื่อ cache หาย**
> (เจ้าของยังไม่สั่งทำ — 25 ส.ค. 2026)

### กฎเรื่อง KV — สำคัญมาก

`FLAGS_KV` ใช้ร่วมกันทั้งโปรเจกต์ (flag · archive ข่าว · related · หมวด X)
แผนฟรีเขียนได้ **1,000 ครั้ง/วัน** — ถ้า endpoint ไหนเขียนเกิน 1-2 ครั้งต่อ request
**โควตาจะหมดแล้วพังทั้งระบบ**

> 📋 **ตัวบันทึกระบบก็อยู่ใต้กฎนี้** (`functions/api/_lib/syslog.js` — 20 ส.ค. 2026)
> ตัวที่มีไว้ตรวจปัญหา ถ้าเขียนทุก request จะ**ทำโควตาหมดเอง** = กลายเป็นต้นเหตุเสียเอง
> · **ห้ามย้ายไปเรียกใน `onRequest` ของ `feeds.js`** เด็ดขาด (เทสต์ `syslog.mjs` มีด่านจับ)
> · `resetLog()` ต้องเรียกต้น `buildAndStore` ทุกครั้ง — ตัวกันเขียนซ้ำอยู่ระดับโมดูล
> · 🚫 **ห้ามเปิดให้ POST เข้ามาเขียน log** — ใครก็ยิงได้ โควตาหมดใน 1 นาที (รูเดียวกับ `/api/allow`)
> · 🚫 **ห้ามเก็บ log ใน edge cache แทน KV** — edge แยกตามศูนย์ข้อมูล เขียนที่หนึ่งอีกที่มองไม่เห็น
>   (ตัวกันเขียนซ้ำ `throttled()` ใช้ edge cache ได้ เพราะเก็บแค่ตัวนับ ไม่ใช่ตัว log)

### 📋 บันทึกระบบครอบทุกช่องแล้ว — แต่ "ไม่ได้บันทึกทุกอย่าง" โดยตั้งใจ

**เจ้าของทักไว้ (20 ส.ค. 2026):** สิ่งที่สั่งคือ **บันทึกกิจกรรมของระบบไว้ไล่ปัญหา**
ไม่ใช่กล่อง "ข่าวที่ระบบตัดทิ้ง" ซึ่งมีอยู่ก่อนแล้ว — ตอนแรกทำครอบแค่ 2 จุดจาก 20 จุด

ตอนนี้บันทึกที่ **`trend/feeds` · `ir/feeds` · `trend/trending` · `trend/xtrends` · `trend/yttrends` ·
`trend/kwcheck` · `trend/related` · `trend/trendnews` · `trend/archive` · `sd/news` ·
`api/allow` · `api/flags` · `issue/state` · `sd/state`**

| แบบ | เขียนเมื่อไหร่ | ใช้กับ |
|---|---|---|
| **ทุก build** (`built: true`) | ทุกครั้งที่ดึงข่าวรอบใหม่ | ✅ **เฉพาะ `trend/feeds` กับ `ir/feeds`** — มี cache key เดียว build ชั่วโมงละครั้ง |
| **เฉพาะตอนผิดปกติ** (ค่าปริยาย) | ต้นทางล่ม · ตกไปใช้ตัวสำรอง · ดึงได้ 0 รายการ · เขียนไม่สำเร็จ | ที่เหลือทั้งหมด |

> 🚫 **endpoint ที่ cache key แตกตามพารามิเตอร์ ห้ามส่ง `built: true`**
> `trending` แตกตาม geo/hours/**หมวด** · `kwcheck` แตกตาม **คำที่ผู้ใช้พิมพ์** (ไม่มีขอบเขต)
> ถ้าบันทึกทุก build จะเขียน KV เป็นสิบเท่าของข่าว — เทสต์ `syslog.mjs` [7] มีด่านจับ
>
> 🚫 **endpoint ที่เขียน KV อยู่แล้ว (`/api/allow` · `/api/flags` · `state`) ห้ามบันทึกตอนสำเร็จ**
> จะกลายเป็น **2 ครั้งต่อการกดปุ่ม 1 ครั้ง** และปุ่ม ⚑/🗂 อยู่บนแดชบอร์ดสาธารณะ ใครก็กดได้
> · บันทึกเฉพาะตอน `kv.put` โยน error — จังหวะนั้นไม่มีการเขียนสำเร็จอยู่แล้ว จึงไม่เพิ่มภาระ
>
> ⚠️ **`cache hit` ต้อง `return` ออกก่อนบรรทัด `startLog()` เสมอ** ไม่งั้นทุกคนที่เปิดหน้าเว็บ
> กินโควตาคนละครั้ง — เทสต์ `syslog.mjs` [7] วัดตำแหน่งบรรทัดไว้แล้ว
>
> ⚠️ **ตัวกันเขียนซ้ำ (`throttled()`) จำเป็น ไม่ใช่ของหรู** — ถ้าต้นทางล่มยาว **ทุก request
> จะเป็น build ที่ error** แล้วเขียน log คนละครั้ง = โควตาหมดใน 1 ชม.
> ตอนนี้ "เรื่องเดิม จาก endpoint เดิม" เขียนได้ครั้งเดียวต่อ 5 นาที
>
> ⚠️ **เพิ่ม `startLog("...")` ที่ไหน ต้องเติมชื่อไทยใน `SRC_TH` ของ `admin/app.js` ด้วย**
> ไม่งั้นเจ้าของเห็นเป็น path ของโค้ด (กฎเดียวกับ `WHY_TH`) — เทสต์ไล่เทียบทุกชื่อให้แล้ว

**ยังไม่ได้ทำ:** `/social/*` (6 endpoint) — ไฟล์นั้น **session social ดูแลอยู่และกำลังแก้**
แตะพร้อมกันจะชนกัน · ให้ session นั้นทำเอง โดย import จาก `functions/api/_lib/syslog.js` ไฟล์เดิม

#### 🔍 6 ข้อที่ทำให้ log "ใช้ไล่ปัญหาได้จริง" (เจ้าของรีวิว 21 ส.ค. 2026)

log ที่บอกแค่**จำนวน** ไม่ใช่ audit trail — เวลามีคนถาม "ทำไมข่าวชิ้นนี้ไม่ขึ้น" ต้องตอบได้

| # | ทำอะไร | กับดักที่ต้องระวัง |
|---|---|---|
| 1 | **กดดูรายชิ้นได้** — ป้าย "✂ ตัดออก 4" กดแล้วเห็นพาดหัว + ลิงก์ + เหตุผล | `L.drop()` กับ `L.item()` **ต้องเรียกคู่กันเสมอ** ไม่งั้นตัวเลขกับรายการไม่ตรง |
| 2 | **แสดงส่วนต่างจากรอบก่อน** `News 309 (-1)` | ต้องมีวงเล็บ — `309-1` อ่านเป็นเลขเดียวได้ · คำนวณในเบราว์เซอร์จากรอบก่อนของ**ช่องเดียวกัน** |
| 3 | **3 ระดับ: ปกติ / ผิดสังเกต / ล้มเหลว** | "ผิดสังเกต" = ต้นทางล่ม · ได้ 0 รายการ · ตัดทิ้งเกิน `DROP_ALERT` (40%) |
| 4 | **เวลาที่วัดได้จริง** | ⏱ `startLog()` ต้องอยู่ **บรรทัดแรกของ build** ไม่ใช่ตอนจะเขียน log · 0 ms แสดงเป็น `–` |
| 5 | **แยก 2 โซน** — บรรทัดบนตัวเลข pipeline (ตายตัว) · บรรทัดล่างเหตุผล (เฉพาะที่ > 0) | ลำดับใน `PIPE` ต้องคงที่ ไม่งั้นเทียบระหว่างแถวด้วยตาไม่ได้ |
| 6 | **ตัวกรอง** ช่อง · ช่วงวัน · เหตุผล · ค้นหา (ค้นถึงพาดหัวในรายละเอียดด้วย) | **ดึงมาทั้งก้อนครั้งเดียวแล้วกรองในเบราว์เซอร์** ถ้ากรองที่เซิร์ฟเวอร์ ส่วนต่างจะผิดเพราะรอบก่อนถูกกรองทิ้ง |

> 🐞 **บั๊ก "0 ms" มาจากตำแหน่งของ `startLog()`** — ของเดิมสร้างไว้ท้าย `buildAndStore`
> เลยวัดได้แต่เวลาของบล็อกเขียน log เอง · ย้ายขึ้นไปต้นฟังก์ชันแล้ว **ห้ามย้ายกลับ**
>
> 📏 **นโยบายเก็บ 2 ชั้น** — แถวสรุปเก็บ 300 แถว · **รายชิ้นเก็บแค่ 40 แถวล่าสุด และไม่เกิน 30 วัน**
> เกินนั้นถอดรายชิ้นออกแต่เก็บตัวเลขสรุปไว้ แล้วติดธง `trimmed`
> · ⚠️ **แถวที่ถูกถอดต้องบอกผู้ใช้ว่าทำไม** ("เก่าเกิน 30 วัน") ไม่ใช่กางแล้วเจอว่างเปล่า
> · เพดานรายชิ้น 15 ต่อแถว แต่ยังนับของที่เกินไว้ (`dropped`) เพื่อบอกว่า "แสดง 15 จาก 22"
> · เทสต์ `syslog.mjs` [8] [9] คุมทั้ง 6 ข้อและนโยบายเก็บไว้แล้ว

> ❌ อย่าเขียนแยก key ต่อรายการ (เคยเขียนไว้ 50 ครั้ง/รอบ)
> ✅ รวมเป็น blob เดียวแล้วเขียนครั้งเดียว — ดูตัวอย่างที่ `xtrends.js` (`xcatmap`)

### ⚠️ สถานะที่ผู้ใช้สร้าง ห้ามเก็บไว้ใน DOM อย่างเดียว

`renderAll()` ทำงานทุก 3 นาทีจาก auto-refresh และสร้าง `innerHTML` ใหม่ทั้งก้อน
อะไรที่ผู้ใช้กดเปิด/เลือกไว้แล้วเก็บอยู่ใน DOM เท่านั้น **จะหายทุกรอบ**

> เกิดขึ้นจริง: กางดูรายละเอียดเทรนด์ค้างไว้ → auto-refresh → ยุบหมด → ลิสต์สั้นลง
> → ค่า scroll ที่คืนกลับเกินความสูงใหม่ → **หน้าเด้งกลับขึ้นบนกลางที่กำลังอ่าน**
> แก้ด้วยการจำไว้ใน `state.trendOpen` แล้วกางคืนหลัง render (ดู `openTrend`)

เวลาเพิ่มอะไรที่ "กดแล้วเปลี่ยนสภาพ" ให้ถามตัวเองว่า **รอด renderAll() ไหม**

### 🏷 ชิพ CPF ในคอลัมน์ CP — จับแค่ 2 คำ ตรงทั้งคำ

เจ้าของสั่ง (13 ส.ค. 2026): **เอาแค่ `ซีพีเอฟ` กับ `cpf` เท่านั้น** — ตัด `cp foods` ·
`เจริญโภคภัณฑ์อาหาร` · `charoen pokphand foods` ออกจากลิสต์เดิม

> ⚠️ `cpf` **ต้องตรงทั้งคำ** ไม่งั้นไปจับ `CPFresh` (ทุเรียนแห่งชาติ CPFresh — คนละบริษัท)
> บทเรียนเดิมกับ SLAPP → slapped และ rcep → intercept · ใช้ `CPF_RE` เป็น regex ไม่ใช่ลิสต์คำ
> เพราะตัวจับหมวดตัวอื่นเทียบด้วย `includes` ซึ่งไม่มีขอบคำ
> · **อยู่ 3 ที่: `trend/app.js` · `issue/app.js` · `ir/app.js`** แก้ต้องแก้ให้ครบ — เทสต์ `cpfilter.mjs` คุมไว้

### 🗂 การจัดหมวดข่าว — PR ใช้แบบเดียวกับ IR แล้ว

ลำดับความน่าเชื่อ (เหมือนกันทั้ง `ir/` และ `trend/`):
**ผู้ใช้กดแก้เอง > ที่ AI เคยจัดไว้ > คำค้นแบบมั่นใจ > ให้ AI อ่านพาดหัว**

- ปุ่ม 🗂 บนการ์ดมาจาก `flags.js` — **ต้องส่ง `cats` ตอน `Flags.init()` ไม่งั้นปุ่มไม่ขึ้นเลย**
  (PR ไม่ได้ส่งอยู่นาน ปุ่มเลยหายไปทั้งหน้าโดยไม่มี error อะไรบอก)
- ผู้ใช้แก้หมวดเก็บใน `flags:pr` / `flags:ir` และถูกส่งกลับเป็นตัวอย่างสอน AI รอบถัดไป
- **คำกำกวมห้ามตัดสินคนเดียว** (`AMBIG_KW`) — "หมู" ชนชื่อนักสนุกเกอร์ · "เกษตร" ชนชื่อมหาวิทยาลัย
  ต้องมีคำบริบทที่ไม่กำกวมอย่างน้อย 1 คำถึงจะเชื่อ ไม่งั้นส่งให้ AI ตัดสิน
- คอลัมน์ X (`xtrends.js`) ใช้คนละชุดหมวด และมี `NEED_EVIDENCE` — ถ้า AI ตอบ pol/news/sport
  แต่ในแท็กไม่มีคำของหมวดนั้นเลย **ไม่เชื่อ** (โมเดลเล็กชอบเดาชื่อคนไทยเป็นการเมือง)

> ⚠️ **ตัวกรอง/ตัวจับคำรวมเป็นไลบรารีเดียวแล้ว** (`functions/api/_lib/noise.js` — 13 ส.ค. 2026)
> เหลือแต่ **โค้ดจัดหมวดข่าว** ที่ยังมี 2 ชุด (`ir/feeds.js` กับ `trend/feeds.js`)
> แก้ที่หนึ่งแล้วอีกที่ไม่ตาม — ถ้าจะแก้ตรรกะจัดหมวด ควรยกเข้าไลบรารีแบบเดียวกันก่อน

### 🔑 แก้ keyword ของ Google Alert — ต้องแก้ที่ `query` ใน config ด้วย

`feeds.js` แกะ keyword จาก `<title>` ของฟีด Alert เอง (`Google Alert - <query>`)
แต่ **Google ตัด title ให้สั้นเมื่อ query ยาว** keyword ท้ายๆ จะหายไปเงียบๆ ไม่มี error

ผลที่ตามมามี 2 ชั้น และชั้นที่สองอันตรายกว่า:
1. ข่าวจากคอลัมน์ News ที่ตรงกับคำท้ายๆ **ไม่ถูกดึงเข้าคอลัมน์ alert**
2. `pruneStaleMerged()` มองว่าข่าวที่เคยดึงเข้ามา "ไม่ match แล้ว" จึง **ลบทิ้ง**
   → คอลัมน์หดลงเรื่อยๆ โดยดูเหมือนไม่มีอะไรผิด

> ✅ ทางแก้: เขียน query ตัวเต็มไว้ที่ field `query` ของฟีดใน `trend-feeds.config.js`
> `feeds.js` จะเทียบว่าอันไหนได้คำมากกว่า (ฟีด vs config) แล้วยึดอันนั้น
> **แก้ alert ใน Google Alerts เมื่อไหร่ ต้องมาแก้ `query` ที่นี่ด้วยทุกครั้ง**

เวลาเพิ่มคำใหม่ ให้ระวัง 2 อย่าง:

- **คำอังกฤษสั้นๆ ชนกลางคำ** — `SLAPP` (คดีฟ้องปิดปาก) ไปจับ `slapped` ในพาดหัวอังกฤษ
  ซึ่งเจอบ่อยมาก (`slapped with a fine` / `slapped tariffs`) แก้แล้วด้วย `termPattern()`
  คำที่เป็นอักษรละตินล้วนต้องตรงทั้งคำ · คำไทยยังเทียบแบบ substring (ไม่มีช่องว่างคั่นคำ)
- **คำที่สั่งไม่เอา (`-shopee`)** อยู่ท้าย query ต่อจากคำสุดท้าย ถ้าไม่ถอดออกก่อนแยกด้วย `OR`
  คำสุดท้ายจะกลายเป็น `wastewater discharge -linkedin -jobdb …` ก้อนเดียวที่ไม่มีวัน match
  ตอนนี้ `parseAlertTerms()` ถอดให้แล้ว และ `parseAlertExcludes()` เอาไปกรองข่าวที่จะดึงเข้ามาด้วย

### ➕ `extraTerms` — คำที่ใส่ใน Google Alerts ไม่ได้แล้ว

ช่อง query ของ Google Alerts มีเพดานความยาว (~2,000 ตัวอักษร) ตอนนี้ 74 คำกินไปเกือบเต็ม
เติมอีกไม่ได้ (ตอนนี้มี 5 คำ: `สัตว์ต่างถิ่น` · `เอเลียนสปีชีส์` · `เอเลี่ยน สปีชีส์` · `alien species` ·
`invasive species`) · จึงมี field `extraTerms` ในฟีดของ `trend-feeds.config.js` สำหรับคำที่
**ไม่ได้อยู่ใน Google Alert แต่ใช้ดึงข่าวจากคอลัมน์ News เข้าคอลัมน์ alert**

| | `query` | `extraTerms` |
|---|---|---|
| ต้องตรงกับ Google Alerts | ✅ ตรงเป๊ะเสมอ | ❌ ไม่เกี่ยวกัน |
| ข่าวจาก Google Alert | ✅ | ❌ Google ไม่รู้จักคำพวกนี้ |
| ข่าวจากคอลัมน์ News (`mergeNewsIntoAlert`) | ✅ | ✅ |

> ⚠️ **ต้องเข้า `a2terms` ชุดเดียวกับที่ `pruneStaleMerged` ใช้** ไม่งั้นจะดึงเข้ามาแล้วลบทิ้งสลับกันทุกรอบ
> · ใส่ได้เฉพาะคำ **ไม่กำกวม** — "ต่างถิ่น" หรือ "รุกราน" เดี่ยวๆ จะดูดข่าวแรงงานต่างถิ่นกับข่าวสงครามเข้ามา
> เขียนเป็นคำประกอบเสมอ (`สัตว์ต่างถิ่น` · `ชนิดพันธุ์รุกราน`)
> · **แก้ `extraTerms` แล้วต้องแก้ `HARD_KW` ทั้ง `trend/app.js` และ `issue/app.js` ด้วย** (กฎหน้าต่าง 🔤 ข้างล่าง)
> และถ้าคำนั้นอยู่ในหัวข้อของชีต ต้องแก้ `TOPICS` ใน `archive.js` ให้ตรงกันอีกที่
> — เทสต์ `alien.mjs` คุมทั้ง 4 ที่ให้ตรงกันแล้ว

### 🔤 หน้าต่าง "ดู keyword" — ต้องอัปเดตทุกครั้งที่ keyword เปลี่ยน

**เจ้าของสั่งเป็นกฎถาวร (10 ส.ค. 2026):** แก้ keyword ที่ไหนเมื่อไหร่ ต้องทำให้หน้าต่าง
🔤 ดู keyword ของ **ทุกแดชบอร์ด** ตรงตามนั้นด้วยเสมอ ไม่ใช่แก้แต่ตัวกรองแล้วปล่อยหน้าจอค้างของเก่า

- ปุ่ม 🔤 อยู่ที่ **ทุกคอลัมน์ Alert ยกเว้นคอลัมน์ CP (`alert1`)** — `injectKwButtons()` ใน `flags.js`
  (เจ้าของสั่งไม่ต้องขึ้นที่ CP — query มีแค่ `"cp"` กับ `"ซีพี"` ไม่กี่คำ เปิดดูแล้วไม่ได้อะไร)
- `applyKeywords()` ใน `trend/ir/issue app.js` ไล่ทุกคอลัมน์ Alert ที่มีบนหน้า ไม่ใช่แค่คีย์ที่เขียนใน `HARD_KW`
- เลือกฝั่งที่ **ได้คำมากกว่า** ระหว่าง query สดจากฟีด กับ `HARD_KW` ที่ฝังไว้ในโค้ด
  (Google ตัด title ให้สั้นเมื่อ query ยาว ถ้ายึดของสดอย่างเดียวคำท้ายๆ จะหายเงียบๆ)
- เทสต์ที่คุมไว้: `kwpanel.mjs` — เปิดจริงทุกแดชบอร์ด กดปุ่มทุกคอลัมน์ แล้วนับคำ

> ⚠️ `ir/feeds.js` เคยเป็นโค้ดชุดเดียวกันแต่ยังไม่ได้แก้ — **ยกมาให้แล้ว (10 ส.ค.)**
> ตอนนี้ทั้ง trend และ ir ใช้ `termPattern()` เหมือนกัน (คำละตินต้องตรงทั้งคำ)
> ต้นเหตุที่ต้องยก: `rcep` ไปจับ `inte(rcep)t` — ข่าว F-16s intercept ของ Al Jazeera
> หลุดเข้าคอลัมน์การค้าของ IR **บทเรียนเดิมกับ SLAPP → slapped เป๊ะๆ แค่คนละคำ**

> (ของเดิม) ⚠️ `ir/feeds.js` มีโค้ดชุดเดียวกันแต่ **ยังไม่ได้แก้** — ตอนนี้ alert ของ IR ไม่มีคำ `-ไม่เอา`
> เลยยังไม่เจอปัญหา ถ้าวันไหนใส่ ต้องยกการแก้นี้ไปด้วย (ดูหนี้ทางเทคนิคเรื่องโค้ดซ้ำ)

### กฎเรื่อง cache — แก้โค้ดแล้วผู้ใช้ยังเห็นของเก่า

endpoint ที่เก็บผลลง KV + edge cache ต้องมี **เลขเวอร์ชันของข้อมูล** คั่นใน key
(`DATA_VER` ใน `yttrends.js` · `CACHE_VER` ใน `feeds.js`)

> ⚠️ **แก้โครงข้อมูลแล้วต้องบวกเลขนี้ทุกครั้ง** ไม่งั้นของเก่าจะถูกเสิร์ฟต่อจนกว่า cache จะหมดอายุ
> เกิดขึ้นจริง: แก้วิธีเรียง + เพิ่มธง `live` ของคอลัมน์ YouTube แล้ว deploy
> แต่ผู้ใช้ยังเห็นลำดับเก่าอยู่อีก 1 ชม. เพราะ KV ถือของเดิมไว้ **ดูเหมือนโค้ดไม่ทำงาน ทั้งที่ทำงานถูก**
> อาการสังเกต: ของใหม่ที่ควรเห็น (ป้าย/ฟิลด์ใหม่) ไม่ขึ้นเลยสักอัน

### YouTube API key — ใส่แล้ว ✅ (6 ส.ค. 2026)

`YT_API_KEY` อยู่ใน Cloudflare env แล้ว คอลัมน์ YouTube จึงได้ `source: youtube:api`
· `mode: official` = อันดับมาแรงทางการ · ยอดวิวจริง · เวลาอัปโหลดจริง · ธง live เชื่อได้

> ⚠️ **env ของ Pages แยก Production กับ Preview คนละชุด** ต้องใส่ทั้งสองที่
> ใส่แค่ Production แล้ว `dev` (Preview) จะไม่เห็น key เลย — เสียเวลาไล่กันมาแล้วรอบหนึ่ง
> และทุกครั้งที่แก้ env ต้อง **Retry deployment** ไม่งั้นค่าใหม่ไม่มีผล

### วิธีขอ YouTube API key (ฟรี) — เผื่อต้องสร้างใหม่

1. https://console.cloud.google.com/ → สร้าง project
2. APIs & Services → Library → เปิด **YouTube Data API v3**
3. Credentials → Create credentials → **API key**
4. Cloudflare → cp-dashboard → Settings → **Variables and Secrets** → เพิ่ม `YT_API_KEY`
   (ใส่เป็น **Secret** ไม่ใช่ plaintext)

> 🔑 **ห้ามส่ง key มาในแชทหรือ commit ลง repo** — repo เป็น public
> โค้ดอ่านจาก `env.YT_API_KEY` เอง ถ้าไม่มีก็ตกไปใช้ต้นทางสำรองตามเดิม ไม่พัง

### กฎเรื่อง `sw.js` — อันตรายกว่าไฟล์อื่น

`sw.js` ดักทุกการเปิดหน้าในโดเมน และมัน**ค้างอยู่ในเครื่องผู้ใช้** ถ้า push ตัวที่มีบั๊ก
ทั้งเว็บพังสำหรับคนที่ติดตั้งแอปไว้ และแก้ยากกว่าหน้าเว็บพังธรรมดา

0. **ห้ามให้ `sw.js` ดัก `fetch` แล้วเรียก `respondWith`** — ถ้ายิงเน็ตพลาดมันจะคืน
   `Response.error()` ซึ่งเบราว์เซอร์แปลว่า **ERR_FAILED = หน้าเปิดไม่ขึ้นเลย**
   เกิดขึ้นจริงกับ `/sd.html` (6 ส.ค. 2026) ตอนใช้ sw4
   ความสดของ HTML มาจาก `Cache-Control: no-cache` ใน `_headers` อยู่แล้ว
   **sw ไม่ต้องเข้ามายุ่ง — ยุ่งแล้วมีแต่ความเสี่ยง ไม่ได้อะไรเพิ่ม**
   ถ้าไม่มี `respondWith` เลย sw จะทำให้หน้าเว็บพังไม่ได้
1. แก้ `sw.js` ทีไร **ต้องทดสอบบน staging ก่อนเสมอ ห้ามข้าม**
2. ต้องบวก `SW_VERSION` ด้วย ไม่งั้นเบราว์เซอร์ไม่ติดตั้งตัวใหม่ (มันเทียบ byte)
3. ห้ามส่ง Request ที่ `mode=navigate` เข้า `fetch()` พร้อม init — สเปคจะลดระดับ
   mode เป็น `same-origin` เงียบๆ ให้ยิงด้วย `req.url` ตรงๆ แทน
4. ลงทะเบียนด้วย `{updateViaCache:"none"}` + เรียก `registration.update()` ทั้งตอน
   เปิดหน้าและตอน `visibilitychange` — ผู้ใช้จะได้ของใหม่เองโดยไม่ต้องทำอะไรเลย
   **ห้ามบอกผู้ใช้ให้ล้าง cache หรือถอนแอปติดตั้งใหม่** ถ้าต้องบอก แปลว่ากลไกยังผิด
5. **เช็คตอน "กลับเข้าแอป" ไม่ใช่ตั้งตัวจับเวลาถี่ๆ** — ตัวจับเวลากินทรัพยากรตอนที่
   ไม่มีใครดู ส่วนจังหวะที่ผู้ใช้กลับมาคือจังหวะเดียวที่การอัปเดตมีความหมาย
   ใช้ `visibilitychange` + กันกดสลับรัวๆ ด้วยระยะขั้นต่ำ 60 วินาที

> ⚠️ **หน้าที่ถูก resume ไม่ส่ง request อะไรเลย** — Android คืนภาพหน้าเดิมให้โดยไม่โหลดใหม่
> ไม่มีอะไรที่ deploy ไปจะไปถึงมันได้ จนกว่าจะมีการ navigate สักครั้ง
> วิธีปลุกที่เบาที่สุดคือ **กดเข้าแดชบอร์ดแล้วกด 🏠 กลับ** ไม่ใช่ถอนติดตั้ง

### ⚙️ กล่องตั้งค่ากลุ่มคำ (หน้าเทรนด์ของ `issue/trends.html` กับ `sd.html`)

เจ้าของสั่งให้เป็นแบบ **กดแล้วค่อยเปิด เฉพาะจอแคบ** (11 ส.ค. 2026) — บนมือถือกล่องนี้กินที่จนกราฟตกจอ
· ปิดไว้เป็นค่าตั้งต้น · หัวกล่องมีสรุปให้อ่านโดยไม่ต้องกาง (ชื่อกลุ่ม · จำนวนคำ · พื้นที่ · ช่วงเวลา)

> ⚠️ **เดสก์ท็อป (≥ 861px) ต้องกางไว้ตลอด ไม่มีปุ่มกด** — เจ้าของสั่งเพิ่มทีหลัง
> ทำด้วย CSS อย่างเดียว (`@media (min-width:861px)`) และ **ต้องทับ `[hidden]` ด้วย**
> ไม่งั้น UA stylesheet บังคับ `display:none` ชนะ · JS ยังจำสถานะไว้เหมือนเดิมสำหรับจอแคบ

- **จำสถานะเปิด/ปิดใน `localStorage`** (`issueTrendsOpen` / `sdTrendsOpen`) ไม่ใช่ใน DOM อย่างเดียว
- สรุปบนหัวกล่องอัปเดตใน `updateSubs()` — จุดเดียวที่ถูกเรียกทุกครั้งที่ค่าเปลี่ยน
- **สองไฟล์นี้เป็นโค้ดคนละชุดที่เนื้อหาเกือบเหมือนกัน แก้ที่หนึ่งต้องแก้อีกที่ด้วย**
  (`trends.html` ที่ root เป็นตัวเก่า ไม่มีลิงก์จาก landing แล้ว จึงไม่ได้ยกไปด้วย)
- เทสต์ `accordion.mjs` เปิดจริงทั้ง 2 หน้า ทั้งมือถือและเดสก์ท็อป

> ⚠️ **เทสต์ที่ไปกด `#geo` / `#period` ต้องกาง `#tbToggle` ก่อน** ไม่งั้นจะ timeout เพราะ element
> ถูกซ่อนอยู่ (เจอแล้วกับ `trendfix.mjs`)

### 😖 Google Trends ขึ้น "Oops! Something went wrong." — กันได้แค่ที่ต้นทาง

widget ของ Google อยู่ใน **iframe คนละโดเมน** → หน้าเราอ่านไม่ได้เลยว่ามันพัง
**จับไม่ได้ = ลองใหม่อัตโนมัติไม่ได้** ทำได้แค่ลดโอกาสโดนตั้งแต่แรก กับเปิดทางให้ผู้ใช้กดเอง

สาเหตุที่เจอมาแล้ว 2 แบบ — **อย่าเหมารวมเป็นอันเดียว**:

1. **คำที่ Google ยุบเป็นคำเดียวกัน** ถูกเอามา `+` กัน (`pm 2.5 + pm2.5`) → แก้แล้วที่ `uniqKeywords()`
2. **ยิงขอ widget ถี่เกินไป** — 1 widget = 1 iframe = 1 request · `sd.html` มี 3 อัน
   เปิดหน้าทีเดียวยิงรวด 3 ครั้งใน 2 วิ · เปลี่ยนช่วงเวลา/แท็บ 1 ครั้ง = ยิงใหม่ทั้งชุด

กันไว้ 3 ชั้นใน `renderWidgets()` (**โค้ดชุดเดียวกันทั้ง 2 ไฟล์ แก้ที่หนึ่งต้องแก้อีกที่**):

| ชั้น | ทำอะไร | ค่า |
|---|---|---|
| debounce | เปลี่ยนค่ารัวๆ ยุบเป็นครั้งเดียว | `WDEBOUNCE` 400ms |
| คิวเว้นระยะ | ไม่ยิง widget พร้อมกัน · `wNextAt` **ไม่รีเซ็ตตอน render ใหม่** | `WGAP` 2200ms |
| lazy | กล่องที่อยู่นอกจอยังไม่โหลด (`IntersectionObserver`) | `rootMargin` 300px |

- **กราฟหลัก (`w-ts`) โหลดเสมอ** แม้อยู่นอกจอ — ที่เหลือรอเลื่อนไปถึง
- `wVisible` จำว่ากล่องไหนอยู่ในจอ **จำเป็น** เพราะ `observe()` ตัวที่ observe อยู่แล้วเป็น no-op
  ไม่ยิง callback ซ้ำ → ตอน render รอบสองต้องสั่ง `queueWidget()` เองจาก `wVisible`
- ทุกกล่องมี **ปุ่ม 🔄 โหลดใหม่รายกล่อง** + ลิงก์ ↗ เปิดใน Google Trends (ทางหนีเมื่อกดแล้วยังไม่ผ่าน)
- เทสต์ `wlazy.mjs` — ปลอม `window.trends` แล้วนับว่าถูกสั่งวาดกี่ครั้ง เมื่อไหร่ (ยิงของจริงจาก sandbox ไม่ได้)

**โหมดสว่าง/มืด — เจ้าของบอกว่าไม่ต้องทำตัวสลับ** มีแค่ `index.html` กับ `sd.html`
ที่มีปุ่มสลับ ส่วนหน้าอื่น **ตรึงเป็นโหมดเดียว อย่าไปเพิ่มตัวสลับเอง**

> 🔴 **แก้ของที่เคยเขียนผิดไว้ตรงนี้ (17 ส.ค. 2026):** เดิมเขียนว่า `/trend/` `/ir/` `/issue/`
> "เป็นโหมดมืดล้วน" — **ไม่จริง ทั้ง 3 หน้าเป็นโหมดสว่างมาตั้งแต่แรก**
> (`--bg:#f4f6fb` · `--surface:#ffffff` · `--text:#1f2937`) · ความเข้าใจผิดนี้เกือบทำให้
> ทำ `/social/` เป็นธีมมืดคนละแบบกับพี่น้อง **ยืนยันจากไฟล์ `styles.css` เสมอ อย่าเชื่อบรรทัดนี้อย่างเดียว**

### 🎨 ชุดสีกลางของแดชบอร์ด — 5 ตัวนี้ต้องตรงกันทุกหน้า

`/trend/` `/ir/` `/issue/` `/social/` ใช้ค่าเดียวกันหมด **แก้ที่ไหนต้องแก้ให้ครบ**
ไม่งั้นสลับหน้าแล้วสีกระโดด

| ตัวแปร | ค่า | |
|---|---|---|
| `--bg` (social ใช้ชื่อ `--plane`) | `#f4f6fb` | พื้นหลังหน้า |
| `--surface` | `#ffffff` | พื้นการ์ด/กล่อง |
| `--border` | `#e4e8f0` | เส้นขอบ |
| `--text` (social ใช้ชื่อ `--ink`) | `#1f2937` | ตัวอักษรหลัก |
| `--muted` | `#6b7280` | ตัวอักษรจาง |

> ⚠️ **สีประจำช่องต้องเข้มพอสำหรับพื้นสว่าง** — ตอนทำ `/social/` เคยใช้สีแบรนด์ตรงๆ
> (`#25f4ee` ฟ้าอ่อนของ TikTok) แล้ว **เส้นกราฟจางจนมองไม่เห็นบนพื้นขาว**
> ใช้ `#dc2626` / `#0d9488` / `#2563eb` แทน · เทสต์ `social.mjs` [20] คุมทั้งความคมชัด
> ตามเกณฑ์ WCAG และความตรงกันของชุดสีกับหน้าอื่น

## 🖥️ วัดเลย์เอาต์ได้จริง อย่าเดาจาก CSS

เครื่องที่รัน session มี **Chromium + Playwright ติดตั้งไว้แล้ว** ใช้เรนเดอร์หน้าจริงแล้ววัดได้เลย
ไม่ต้องเดาว่า CSS จะออกมาเป็นยังไง

```bash
python3 -m http.server 8899 &          # เสิร์ฟไฟล์ static (API จะ error แต่เลย์เอาต์วัดได้)
# playwright อยู่ใน node_modules ของ scratchpad
# ต้องระบุ executablePath เอง: /opt/pw-browsers/chromium  (+ args ["--no-sandbox"])
```

สิ่งที่ควรวัดทุกครั้งที่แตะเลย์เอาต์มือถือ — เทียบกับ `innerWidth` / `innerHeight`:
`document.scrollingElement.scrollWidth` · ความกว้าง+ตำแหน่งซ้ายของลูก `body` ทุกตัว · `.board` bottom

> ⚠️ **มีแต่ Chromium เท่านั้น — WebKit ดาวน์โหลดไม่ได้** (`playwright install webkit` ล้มเพราะเน็ตขาออกถูกบล็อก)
> แปลว่าวัดได้จริงเฉพาะ **Chrome / Edge** (engine เดียวกัน) · **Safari กับทุกเบราว์เซอร์บน iOS
> เป็น WebKit ทดสอบที่นี่ไม่ได้ ต้องให้เจ้าของเปิดดูเอง** อย่าเคลมว่าเทสต์ Safari แล้ว
>
> สิ่งที่ยังพอทำได้เพื่อลดความเสี่ยงฝั่ง Safari: จำลองเบราว์เซอร์ที่ไม่รู้จักหน่วยใหม่
> ด้วย `page.addStyleTag` ทับค่าให้เป็นตัวสำรอง แล้ววัดว่าเลย์เอาต์ยังใช้ได้ (ดู `NO_DVH` ในชุดวัด)

> ⚠️ **กับดักที่เจอมาแล้ว: `margin: 0 auto` + `display: flex` ของ parent**
> พอเปลี่ยน `body` เป็น flex column ลูกที่มี `margin: 0 auto` (จากกฎเดสก์ท็อป) จะ **ไม่ยืดเต็มจอ**
> เพราะ auto margin บนแกนขวางทำให้ `stretch` ไม่ทำงาน ความกว้างกลายเป็นเท่าเนื้อหา/`max-width`
> ผลที่วัดได้จริง: แถบค้นหาเหลือ 231px · `.board` พองไป 1680px · **เบราว์เซอร์มือถือขยาย viewport
> ตามจนทั้งหน้าซูมออก (390 → 1560)** = หน้าตาพังทั้งหน้า
> แก้ด้วยการบังคับ `width: 100%; max-width: none; margin-left: 0; margin-right: 0` ให้ลูกทุกตัว

## 📱 แพลตฟอร์มที่ต้องคำนึงถึงทุกครั้ง

**ทุกอย่างที่เป็นฝั่งแอป (PWA / service worker / การอัปเดต / หน้าตาบนมือถือ)
ต้องคิดครบทุกช่องในตารางนี้เสมอ — ไม่ใช่เทสบนเดสก์ท็อปแล้วจบ**

|  | Chrome | Edge | Safari |
|---|:---:|:---:|:---:|
| **เดสก์ท็อป** | ✅ | ✅ | ✅ |
| **Android** | ✅ | ✅ | — |
| **iOS (iPhone/iPad)** | ✅ | ✅ | ✅ ← **ผู้ใช้ส่วนใหญ่อยู่ตรงนี้** |

Edge เป็น Chromium ตัวเดียวกับ Chrome — อะไรที่ผ่าน Chrome ก็ผ่าน Edge
ตัวที่ต้องระวังคือ **Safari/iOS** เพราะพฤติกรรมต่างจากตัวอื่น

### ข้อควรระวังของ Safari / iOS

- service worker ใช้ได้ตั้งแต่ **iOS 11.3** — ฟีเจอร์อัปเดตอัตโนมัติทำงานปกติ
- **iOS อาจล้าง service worker ทิ้ง** ถ้าไม่เปิดแอปเกิน ~7 วัน → พอเปิดใหม่
  มันลงทะเบียนเองอัตโนมัติ ไม่ต้องให้ผู้ใช้ทำอะไร
- **Safari โหมดส่วนตัวไม่มี service worker** → หน้าเว็บทำงานปกติทุกอย่าง
  แค่ไม่มีระบบอัปเดตอัตโนมัติ
  ⚠️ อย่าวางโค้ดที่ต้องทำงานเสมอไว้ใน `if("serviceWorker" in navigator){...}`
  (เคยพลาดกับป้ายเวอร์ชันมาแล้ว — Safari โหมดส่วนตัวไม่เห็นป้ายเลย)
- iOS ต้องมี `apple-touch-icon` + `apple-mobile-web-app-capable` ถึงจะติดตั้งสวย
- ⚠️ **Safari กับไอคอนบนหน้าจอโฮมอาจไม่แชร์ service worker กัน** (iOS แยก storage
  ของ home-screen app ออกจากตัวเบราว์เซอร์) ต่างจาก Android ที่ Chrome กับแอป
  ที่ติดตั้งใช้ registration เดียวกัน → บน iOS การ "เปิดใน Safari ให้อัปเดต"
  อาจไม่ช่วยแอปบนหน้าจอโฮม ต้องเปิดจากไอคอนเอง

### วิธีดันของใหม่เข้าเครื่องที่ค้าง (เรียงจากเบาไปหนัก)

| แพลตฟอร์ม | ทำอะไร |
|---|---|
| **Android** | เปิดเว็บใน Chrome ธรรมดา 1 ครั้ง — แอปที่ติดตั้งใช้ registration เดียวกัน จะได้ตามไปด้วย |
| **iOS** | เปิดจากไอคอนบนหน้าจอโฮม (iOS ปิดแอปจริงบ่อย เปิดใหม่มักเป็นการโหลดสด) |
| **ทั้งคู่ ถ้ายังค้าง** | ถอน/ลบไอคอน แล้วติดตั้งใหม่ 1 ครั้ง |

> ใช้ **ป้ายเลขเวอร์ชัน** ตรวจว่าสำเร็จหรือยัง ไม่ต้องเดา

## 🗂️ งานแต่ละแดชบอร์ดควรอยู่ที่ไหน

- **เทรนด์ทั่วไป** (# ไหนดัง, คนค้นอะไร ไม่ผูกกับ CP) → `trend/` (PR)
- **CP ถูกพูดถึงยังไง** (mentions, ประเด็นที่จับตา) → `issue/`
- ข่าวนักลงทุน → `ir/` · ความยั่งยืน → `sd.html`

## 🔍 สิ่งที่เคยทดสอบแล้ว — อย่าเสียเวลาลองซ้ำ

ยิงจาก Cloudflare Worker (5 ส.ค. 2026):

| แหล่ง | ผล |
|---|---|
| YouTube RSS | ✅ ใช้ได้ ฟรี ไม่ต้องมี key |
| getdaytrends.com/thailand/ | ✅ ได้เทรนด์ X ไทย (71KB, สะอาด) — **ตัวหลักที่เลือก** |
| trends24.in/thailand/ | ✅ ได้เหมือนกัน (305KB, ขยะเยอะกว่า) — ตัวสำรอง |
| TikTok Creative Center | ❌ `40101 no permission` ทั้งยิงเปล่าและใส่ header ครบ — ทางฟรีปิด |
| Facebook (www/mbasic/embed) | ❌ login wall / 400 / ได้แต่เปลือก — ต้องใช้ Meta API เท่านั้น |
| Nitter (net/poast) | ❌ 520 / bot check |
| Pantip (tag/search API) | ❌ 404 / 401 access denied |
| X syndication | ⚠️ ได้ข้อมูล แต่เป็นทวีตเก่าในโปรไฟล์ ไม่ใช่เทรนด์ |
| YouTube หน้า Trending | ❌ ปิดถาวร ก.ค. 2025 · YouTube Charts เหลือแค่เพลง/หนัง/เกม ใช้กับ PR ไม่ได้ |
| YouTube Data API | ⚠️ `chart=mostPopular` ยังมี แต่ต้องมี key + โควตา และตั้งแต่ ก.ค. 2025 เหลือ 3 หมวดเหมือนกัน |
| Invidious / Piped | ✅ คลิปมาแรงรายประเทศ เป็น JSON สะอาด ฟรี ไม่ต้องมี key — **ตัวหลักของคอลัมน์ YouTube** |

> ⚠️ **Invidious / Piped เป็น instance อาสาสมัคร ล่มบ่อยและมัก "ล่มแบบค้าง" ไม่ใช่ตอบ error**
> จึงต้อง **ยิงทุก instance พร้อมกันแล้วเอาอันที่ตอบก่อน** (`Promise.any`) ห้ามไล่ทีละตัว
> ไล่ทีละตัว 5 แหล่ง × timeout 7 วิ = ผู้ใช้รอ 35 วินาทีก่อนเห็นอะไร
> และ **คอลัมน์ที่พึ่งแหล่งแบบนี้ต้องโหลดแยก ไม่รวมใน `Promise.all` ของบอร์ด**
> ไม่งั้นต้นทางอืดทีเดียวค้างทั้งแดชบอร์ด (ดู `reloadYTTrends` ใน `trend/app.js`)

### 📰 โหมด ข่าว / ทั่วไป ของคอลัมน์ X กับ YouTube

ปุ่มสลับอยู่แถวเดียวกับ dropdown ประเทศ · **ข่าว = ตัด เกม เพลง บันเทิง กีฬา** (หนังกับคลิปตลกอยู่ในบันเทิง)

ค่าตั้งต้นต่างกันโดยตั้งใจ เพราะสองแหล่งดึงข้อมูลได้ไม่เหมือนกัน:

| | ตั้งต้น | ได้ข่าวมาจากไหน |
|---|---|---|
| **YouTube** | 📰 ข่าว | ขอชาร์ตหมวด **News & Politics** (`videoCategoryId=25`) จาก API โดยตรง = ชาร์ตข่าวจริงเต็ม 50 คลิป |
| **X** | 🌐 ทั่วไป | getdaytrends ให้ท็อป 50 มาชุดเดียว **ขอชาร์ตข่าวแยกไม่ได้** ต้องกรองจากหมวดที่จัดไว้ ซึ่งเหลือไม่กี่รายการ |

> ⚠️ `catFiltered` ใน response ของ `yttrends` บอกว่า **server กรองหมวดให้แล้วหรือยัง**
> ถ้า `false` (ไม่มี key / ประเทศนั้นไม่มีชาร์ตข่าว / ตกไปใช้ Invidious) หน้าเว็บต้องกรองด้วยคำเอง
> **ห้ามกรองซ้ำเมื่อ `catFiltered` เป็น true** — ข่าวจริงหลายคลิปไม่มีคำว่า "ข่าว" ในชื่อ จะหายไปเฉยๆ

### 📈 "มาแรง" ของคอลัมน์ YouTube = **วิว/ชม.** ไม่ใช่ "วิวที่เพิ่มในช่วงที่เลือก" (27 ส.ค. 2026)

**เจ้าของถามพร้อมภาพ:** "ทำไมหมวดข่าวถึงมี view เยอะกว่าทั่วไป? ทั่วไปน่าจะกว้างกว่าและเยอะกว่า"
· ป้ายท้ายคอลัมน์ตอนนั้น: ข่าว **46 จาก 50** · ทั่วไป **20 จาก 50**

ต้นเหตุ: `d{w}` ("วิวเพิ่มใน N ชม.") มี **2 ทางที่คนละหน่วยกัน แต่หน้าเว็บเขียนเหมือนกันเป๊ะ**

| ทาง | ได้อะไร | เจอกับคลิปแบบไหน |
|---|---|---|
| มีภาพยอดวิวเก่าให้ลบ | วิวที่เพิ่มจริง | คลิปที่เคยอยู่ในชาร์ตรอบก่อน |
| **คลิปเพิ่งลงในช่วงนั้น** | **ยกยอดสะสมทั้งก้อนมาเป็น "วิวเพิ่ม"** | หมวดข่าว — ลงวันนี้แทบทุกใบ |
| ไม่เข้าทั้งสองข้อ | `null` → **ถูกดันไปท้ายลิสต์** | หมวดทั่วไป — MV/หนัง/เกมอายุหลายวัน |

ผลคือหมวดข่าวได้ตัวเลขเต็มก้อนทุกใบ ส่วน MV 12 ล้านวิวตกท้ายลิสต์เพราะ "ไม่รู้"
**เลขที่เห็นจึงไม่ได้แปลว่าข่าวมาแรงกว่า** — เป็นคนละหน่วยกันเฉยๆ

✅ แก้แล้ว: `rateFor()` ใน `yttrends.js` ส่ง **`r{w}` (วิว/ชม.) · `rh{w}` (วัดจากกี่ชั่วโมง) · `rb{w}`**
(`"m"` = วัดจากสถิติที่เก็บไว้ · `"u"` = เฉลี่ยตั้งแต่คลิปลง) แล้ว `renderYTTrends()` เรียงด้วย `r{w}`

> 🚫 **ห้ามกลับไปเรียงด้วย `d{w}`** และ 🚫 **ห้ามคำนวณ `d{w} / winH` เองที่หน้าเว็บ**
> คลิปที่ลงมา 5 ชม. จะถูกหารด้วย 24 = ต่ำกว่าจริง 5 เท่า · ตัวหารที่ถูกคือ `rh{w}` ที่ server ส่งมา
> · `d{w}` ยังอยู่และยังเคร่งครัดเหมือนเดิม **ห้ามผ่อนเกณฑ์ของมันให้หลวมลง** — มันคือตัวเลข "จริงแท้"
>   ส่วน `r{w}` คือตัวเลข "ใช้เทียบกันได้" คนละหน้าที่กัน
>
> ⚠️ **ต้องเขียนกำกับบนการ์ดเสมอว่าวัดจากช่วงไหน** (`.ywhy`) — ตัวเลขที่วัดจาก 2 ชม. กับ 24 ชม.
> เชื่อได้ไม่เท่ากัน · และต้องแยกให้ออกว่า "วัดจริง" กับ "เฉลี่ยตั้งแต่ลง" — **บทเรียนโดยตรงจาก `d{w}`
> ที่เขียนข้อความเดียวกันให้ทั้งสองแบบจนอ่านผิดมาตลอด**
>
> ⚠️ **ภาพที่ใหม่กว่า 45 นาที ห้ามเอามาหาร** (`RATE_MIN_GAP_MS`) ช่วงสั้นเกินไป ตัวเลขจะเหวี่ยง
> · ต้นทางไม่บอกเวลาที่คลิปลง → คืน **`null`** ไม่ใช่ 0 (0 แปลว่า "ไม่มีคนดู" ซึ่งคนละเรื่อง)
>
> 🐞 **`histHours` เคยไม่ถูกแมปใน `fetchYTTrends()`** หน้าเว็บจึงอ่านได้ 0 เสมอ แล้วขึ้นป้าย
> "เพิ่งเริ่มเก็บสถิติ (0 ชม.)" ตลอดกาลทั้งที่เก็บมาหลายชั่วโมงแล้ว — แก้พร้อมกันรอบนี้
>
> ⚠️ **หมวดทั่วไปเก็บสถิติช้ากว่าหมวดข่าว** เพราะ server ดึงเฉพาะโหมดที่มีคนเปิดดู
> และค่าตั้งต้นคือ 📰 ข่าว · เป็นเหตุผลที่ "20 จาก 50" ต่ำกว่า "46 จาก 50" อีกชั้นหนึ่ง
>
> เทสต์ `ytrate.mjs` คุมทั้งหมด (รวมข้อพิสูจน์อาการเดิมไว้ด้วย เพื่อกันไม่ให้ใครแก้กลับ)
> · **ยังไม่เคยเห็นผลกับข้อมูลจริง** — เครื่องที่รัน session ยิงเข้า YouTube และเข้าเว็บเราเองไม่ได้
>   เทสต์ป้อนข้อมูลสมมติเข้าโค้ดจริง วัดว่า "โค้ดเดินทางไหน" ไม่ใช่ "ตัวเลขบนเว็บถูกไหม"

### 🔢 เลขหมวดของ Google Trends — เคยพิมพ์เองแล้วผิดทั้งชุด (แก้ 12 ส.ค. 2026)

`topics` ที่ Google ติดมากับแต่ละเทรนด์ใช้ 2 ที่: **ตัวกรองหมวดใน dropdown** กับ **ตัวเลือกว่าจะไฮไลต์แถวไหน**
(`FOOD_CAT` ใน `trend/app.js` + `issue/app.js` · `VALID_CATS` ใน `functions/api/trend/trending.js`)

ของเดิมมีคนพิมพ์ตารางเอง **แล้วข้ามหมวด Climate** เลข 4-11 เลยเลื่อนไปหมด
เลข 5 ที่เขียนว่า "อาหาร" จริงๆ คือ **"บันเทิง"** → **ดาราติดเทรนด์ถูกไฮไลต์เป็นเรื่องอาหารทุกวัน**
(เจอจาก "พัชราภา ไชยเชื้อ" ขึ้นไฮไลต์ทั้งที่ไม่ใช่ทั้งอาหารและเครือ CP) · **ผิดแบบไม่มี error อะไรเลย**

แก้รอบแรกด้วยการเลื่อนเลขให้ตรงลิสต์ทางการของ Google (เรียงตามตัวอักษรอังกฤษ 1-19) **แล้วยังผิดอยู่**
เลข 6 ที่ควรเป็น Food กลับดัน "ปาแลร์โม่ พบ ยูเวนตุส" กับ "หวยลาว" ขึ้นมาแทน

> 🐞 **ป้าย "เครือ CP" เคยดูจากคำค้นที่เกี่ยวข้องด้วย — ผิด** (เจ้าของส่งภาพมา 27 ส.ค. 2026)
> เทรนด์ "พิมพิชยา ก๊กรัมย์" (นักวอลเลย์บอล) ติดป้าย **เครือ CP** เพราะคำค้นที่เกี่ยวข้อง
> มีคำว่า **ทรูไอดี** (แอปที่คนใช้ดูถ่ายทอดสด) → คนดูบอลผ่านแอปของทรู **ไม่ได้แปลว่าเป็นข่าวของเครือ**
> · ✅ `pinScore()` ตัดสินป้าย CP จาก **ชื่อเทรนด์อย่างเดียว** แล้ว (ส่วนอาหารยังใช้กองคำรวมได้
>   เพราะมีด่านคำบริบทคุมอยู่) · แก้ทั้ง `trend/app.js` และ `issue/app.js` · เทสต์ `pintest.mjs` [6] คุมไว้
> · 📏 เป็นกับดักเดียวกับ "ดูสนุกเกอร์สด" ที่เคยถูกไฮไลต์เป็นอาหาร — **คำสั้นในกองคำรวมชนง่ายมาก**
>
> 🚫 **สรุป: เลขที่ index 10 ของ payload เชื่อไม่ได้ ห้ามเอามาตัดสินว่าเป็นอาหาร**
> ไม่มีชุดเลขไหนอธิบายได้ทั้ง ดารา(5) · บอล(6) · หวย(6) พร้อมกัน
> **ยืนยันของจริงจากที่นี่ไม่ได้ด้วย** — เครื่องที่รัน session ยิงเข้า Google และเข้าเว็บเราเองไม่ได้ (403)
> ตอนนี้ `pinScore()` ตัดสินจาก **คำในชื่อเทรนด์อย่างเดียว** (`PIN_FOOD_RE` + `PIN_FOOD_BRAND_RE`)
> ตรวจสอบได้ · อธิบายได้ · เขียนเทสต์ได้ — เทสต์ `pintest.mjs` คุมทั้ง 3 เคสที่เคยผิดไว้แล้ว

- แบรนด์อาหารที่ในชื่อไม่มีคำว่าอาหาร (starbucks · mixue · บอนชอน) อยู่ใน `PIN_FOOD_BRAND_RE`
  เพิ่มได้เรื่อยๆ แต่ **ห้ามใส่ชื่อที่ชนชื่อเล่นคนดัง** — "เบียร์" / "สุกี้" ใส่แล้วจะดันข่าวคนขึ้นมาแทน
- ⚠️ **คำอาหารที่กำกวมต้องมีบริบทหนุน** (เจ้าของแจ้ง 14 ส.ค. 2026: "ดูสนุกเกอร์สด" ถูกไฮไลต์เป็นอาหาร)
  ลิสต์คำแยกเป็น 3 ชุด — `PIN_FOOD_STRONG_RE` (เจอคำเดียวเชื่อได้) · `PIN_FOOD_AMBIG_RE`
  (หมู/ไก่/ไข่/ปลา/ข้าว/ทะเล… ไปโผล่ในชื่อคน-สถานที่ได้) · `PIN_FOOD_CTX_RE` (คำยืนยันว่าเป็นของกิน)
  · **คำกำกวมนับเป็นอาหารก็ต่อเมื่อมีคำบริบทด้วย** — วิธีเดียวกับ `AMBIG_KW` ของการจัดหมวดข่าว
  · 🚫 **ห้ามใส่คำสั้นที่ซ่อนอยู่ในคำอื่นลงใน `PIN_FOOD_CTX_RE`** ภาษาไทยไม่มีช่องว่างคั่นคำ:
    `ทอด` ซ่อนใน **ถ่ายทอด**สด · `นึ่ง` ซ่อนใน **หนึ่ง** · `ย่าง` ซ่อนใน **อย่าง** · `ตลาด` ไปชนตลาดหุ้น
    (ทั้ง 4 คำนี้เคยใส่แล้วพัง ถอดออกแล้ว) — เทสต์ `pintest.mjs` [5c] คุมไว้
- ⚠️ **dropdown เลือกหมวดยังกรองด้วยเลขชุดนี้อยู่** (`cat=` → `VALID_CATS` ใน `trending.js`)
  ยังไม่ได้ยืนยันว่ากรองถูก ถ้าเจ้าของลองแล้วผลไม่ตรงหมวด ให้ถอด dropdown ทิ้ง
  เพราะไม่มีทางรู้เลขที่ถูกต้องได้จาก sandbox
- ⚠️ **เลขหมวดใน dropdown เขียนซ้ำอยู่ใน `index.html` ทั้ง trend และ issue** — ตอนเลื่อนเลขตาราง
  (12 ส.ค.) แก้แต่ `app.js` ลืมแก้ HTML ผลคือ "เกม" ในหน้าเว็บส่งเลข 6 ที่โค้ดแปลว่า "อาหาร"
  → เลือกหมวดเกมแล้ว **ไฮไลต์ทุกแถวโดยไม่มีป้ายเหตุผล** (เจ้าของเจอ 13 ส.ค. 2026)
  แก้แล้ว + เทสต์ `pintest.mjs` [5b] เทียบ option ใน HTML กับ `TREND_CATS` ทุกตัว —
  **แก้ตารางหมวดเมื่อไหร่ ต้องแก้ dropdown ใน HTML ทั้ง 2 หน้าด้วย**
- **ไฮไลต์ต้องบอกได้เสมอว่าเพราะอะไร** — ติดป้าย `เครือ CP` / `🍔 อาหาร` ไว้ที่แถวที่ถูกดันขึ้น
  ไฮไลต์ลอยๆ ที่ไม่มีเหตุผลติดมา คือสิ่งที่ทำให้เจ้าของต้องมาถามว่า "ทำไมอันนี้ถึงเด่น"
  · เปิดหมวดอาหารอยู่แล้วทุกแถวเป็นอาหาร จึงไม่ติดป้ายให้รก (ดู `_pinWhy`)

### 🔎 คอลัมน์ "เช็ค Trend" — ใช้ตัดสินว่าคำไหนควรเอาเข้า Alert

`/api/trend/kwcheck?q=...&geo=TH&time=today+12-m` · อยู่ที่ `trend/` ที่เดียว (เป็นงานฝั่ง PR)

ข้อมูล 2 ชั้น ชั้นล่างไม่มีก็ไม่พัง:

| ชั้น | ได้อะไร | ดึงจากไหน |
|---|---|---|
| **Google Trends** | กราฟความสนใจ + คำที่เกี่ยวข้อง | **embed ในเบราว์เซอร์ผู้ใช้** |
| **Google Ads (Keyword Planner)** | ยอดค้นหาจริงต่อเดือน | Worker · ⏳ รอ developer token |

> 🚫 **เคยลองยิง Google Trends จาก Worker แล้วใช้ไม่ได้จริง — อย่าทำซ้ำ**
> `/trends/api/explore` + `/widgetdata/*` ตอบ **429 ทุกครั้ง** เพราะ Cloudflare Worker
> ออกเน็ตจาก IP ที่ใช้ร่วมกับคนทั้งโลก · ลองซ้ำ/หน่วงเวลาไม่ช่วย (ทดสอบจากการใช้จริง
> พิมพ์กี่คำก็ไม่ผ่านสักคำ) · `fetchKeywordCheck()` ยังอยู่ในโค้ดแต่ไม่มีใครเรียกจากหน้าเว็บแล้ว
>
> ✅ ทางที่ใช้ได้คือ **embed ทางการของ Google** (`ssl.gstatic.com/trends_nrtr/.../embed_loader.js`)
> เพราะทำงานในเบราว์เซอร์ของผู้ใช้ = ใช้โควตาของเครื่องผู้ใช้ ไม่ใช่ของ Cloudflare
> แลกกับการที่หน้าตาเป็น UI ของ Google (พื้นขาว) และจัดสไตล์เองไม่ได้
>
> ⚠️ `/api/trend/trending` ที่ใช้ในคอลัมน์ Google Trends **ไม่เจอปัญหานี้** เพราะเป็นคนละ endpoint
> (`batchexecute` RPC `i0OFE`) — อย่าเหมารวมว่า Trends ทั้งหมดยิงจาก Worker ไม่ได้

**ยืนยันด้วยการยิงจริงแล้ว (7 ส.ค. 2026):**
`POST googleads.googleapis.com/v21/customers/{id}:generateKeywordHistoricalMetrics` → `401 UNAUTHENTICATED`
= endpoint มีจริง เรียกผ่าน **REST ได้ ไม่ต้องใช้ gRPC/SDK** → Worker ยิงตรงได้
· `v20` ใช้ได้ · **`v19` ลงไปตอบ 404 = ปลดระวางแล้ว**

env ที่ต้องใส่ (Cloudflare **Secret** เท่านั้น — repo เป็น public):
`GADS_DEVELOPER_TOKEN` `GADS_CUSTOMER_ID` `GADS_CLIENT_ID` `GADS_CLIENT_SECRET` `GADS_REFRESH_TOKEN`
(+ `GADS_LOGIN_CUSTOMER_ID` ถ้าบัญชีอยู่ใต้ Manager) — ขาดตัวไหนก็ได้ ระบบจะบอกเองว่าขาดอะไร

> ⚠️ **ตัวเลขที่ได้อาจเป็นช่วง ไม่ใช่เลขเป๊ะ** — Keyword Planner ให้เลขละเอียดเฉพาะบัญชี
> ที่มียอดใช้จ่ายโฆษณาจริง บัญชีที่ไม่ยิงแอดจะได้เป็นช่วงกว้าง (เช่น 1K–10K) ทั้งบนเว็บและผ่าน API
>
> ⚠️ `geoTargetConstants` / `languageConstants` เป็น **criteria ID ตัวเลข** ไม่ใช่ชื่อประเทศ/ภาษา
> ในโค้ดใส่ไทย = `2764` · ภาษาไทย = `1044` ไว้ **แต่ยังไม่ได้ยืนยัน** (docs ของ Google บล็อกจาก sandbox)

### 💧 ทำไม kwcheck ไม่เขียน KV เลย

ผู้ใช้พิมพ์คำอะไรก็ได้ = **จำนวน key ไม่มีขอบเขต** ถ้าเขียนลง KV ทุกคำที่ถูกค้น
โควตา 1,000 ครั้ง/วันที่ใช้ร่วมกันทั้งโปรเจกต์จะหมดจากคอลัมน์เดียว — ใช้ edge cache 6 ชม. พอ

### ⚠️ คอลัมน์ที่โหลดเอง ห้ามโดน error ของ load() เหวี่ยงใส่

`load()` เคยเขียน skeleton/error ทับ **ทุก** คอลัมน์ ทำให้ `yttrends` (โหลดแยก) กับ `kwcheck`
(รอผู้ใช้พิมพ์) ขึ้นว่า "ดึงข้อมูลไม่สำเร็จ" ทั้งที่ตัวเองไม่ได้พังและยังกดใช้ได้
ตอนนี้มี `SELF_LOADING` กันไว้แล้ว — **เพิ่มคอลัมน์ที่โหลดเองเมื่อไหร่ ต้องใส่ชื่อลงในนั้นด้วย**
และคอลัมน์แบบนั้นต้อง **วาดครั้งแรกเอง** ไม่งั้นถ้า `load()` ล้ม `renderAll()` จะไม่ถูกเรียก แล้วจะว่างเปล่า

### ทำไมคอลัมน์ X กับ YouTube อยู่แค่ `trend/`

เป็นของ **เฉพาะทาง** ไม่ใช่ฟีเจอร์พื้นฐานที่ทุกแดชบอร์ดต้องมี — ตามตารางข้างบน
เทรนด์ทั่วไปที่ไม่ผูกกับ CP เป็นงานฝั่ง PR จึงอยู่ที่ `trend/` ที่เดียว
`issue/` ดูว่า CP ถูกพูดถึงยังไง · `ir/` ข่าวนักลงทุน · `sd.html` ความยั่งยืน — คนละโจทย์กัน

## 🔧 n8n — เครื่องมือที่มีให้ใช้ ให้พิจารณาเป็นทางเลือกด้วยเสมอ

**เจ้าของแจ้งไว้ (14 ส.ค. 2026):** มี **n8n** ใช้ได้ ออกแบบอะไรใหม่ให้ชั่งดูด้วยว่า
ทำใน n8n ดีกว่าเขียนโค้ดเองไหม **ไม่ใช่เขียนเองอย่างเดียวโดยไม่พิจารณา**

### แก้ของที่เราติดอยู่ได้จริง 3 ข้อ

| ปัญหาที่มีอยู่ | n8n ช่วยยังไง |
|---|---|
| **Pages ตั้ง cron ไม่ได้** → คลังข่าวโตเฉพาะตอนมีคนเปิดเว็บ วันไหนไม่มีใครเปิด **ข่าวหายถาวร** (ตอนนี้ยืมตัวตั้งเวลาของ Google Sheet มาปลุกให้) | n8n ตั้งเวลาเองได้ ยิงมาปลุก `/api/...` ตามรอบ — **เลิกพึ่งชีต** |
| **OAuth / refresh token** ของ YouTube · Facebook · TikTok ต้องเขียนเองและต่ออายุเอง | n8n มีตัวจัดการ credential ต่ออายุ token ให้เอง |
| **โควตา API** ถ้ายิงทุกครั้งที่มีคนเปิดหน้าจะเปลือง | ดึงตามรอบเก็บไว้ที่เดียว หน้าเว็บมาอ่านของที่เตรียมไว้ |

### ❓ ยังไม่รู้ — ต้องถามก่อนออกแบบพึ่งมัน

- n8n รันอยู่ที่ไหน (เครื่องตัวเอง / n8n Cloud / เซิร์ฟเวอร์บริษัท)
- **ยิงออกอินเทอร์เน็ตได้ไหม** ถ้าอยู่หลังไฟร์วอลล์จะได้ทางเดียว (มันยิงเข้าเรา เราเรียกมันไม่ได้)
- ใครดูแล ถ้า workflow พังใครแก้ · พังแล้วรู้ได้ยังไง

> ⚠️ **ถ้าให้ n8n ยิงเข้ามาเขียนข้อมูล ต้องมีกุญแจเสมอ** ไม่งั้นใครก็ยิงข้อมูลปลอมเข้าแดชบอร์ดได้
> · เก็บกุญแจเป็น **Secret** ทั้งฝั่ง Cloudflare และฝั่ง n8n **ห้าม commit** (repo เป็น public)
>
> ⚠️ **ยังไม่เคยทดสอบกับ n8n จริงเลยสักครั้ง** — เครื่องที่รัน session ยิงเน็ตออกไม่ได้
> ทุกอย่างข้างบนเป็นการประเมินจากความสามารถของ n8n ที่รู้จัก **ต้องลองจริงก่อนเชื่อ**

## ⚠️ หนี้ทางเทคนิคที่รู้อยู่

- `/api/sd/img?u=<url>` เป็น **image proxy แบบเปิด** — ใครใส่ URL อะไรก็ได้
  (มีตัวกัน: ต้องเป็น http/https, ต้องได้ `content-type: image/*`, จำกัด 3MB, timeout 8 วิ)
  ถ้าจะปิดช่อง = จำกัดเฉพาะโดเมนข่าวที่อยู่ในลิสต์
- การ์ด `issue/` บน landing ยังติดป้าย 🚧 Under Construction ทั้งที่ใช้งานได้แล้ว
- **แถบแท็บของ `/issue/` เขียนซ้ำอยู่ใน 3 ไฟล์** (`index.html` · `trends.html` · `three.html`)
  แก้ที่หนึ่งแล้วอีกที่ไม่ตาม — เคยตกหล่นมาแล้ว: หน้า 3 ชื่อ "➕ หน้า 3" ใน 2 ไฟล์แรก
  แต่ชื่อ "📊 กระแสข่าว" ในไฟล์ของตัวเอง (แก้แล้ว 20 ส.ค. 2026)
  · **เพิ่ม/เปลี่ยนแท็บเมื่อไหร่ ต้องแก้ให้ครบทุกไฟล์ + bump เลขเวอร์ชันของไฟล์ที่แตะ**
  · ⚠️ `issue/trends.html` **ไม่มีตัวบอกเวอร์ชันเลย** (ไม่มีทั้ง `page-ver` และ `app.js?v=`)
    ป้ายมุมขวาบนจึงขึ้นเป็น `v?` — ยังไม่ได้แก้ ถ้าจะแก้ต้องเช็คกับเทสต์ `emptycat.mjs` ด้วย
- `trends.html` ยัง deploy อยู่แต่ไม่มีลิงก์จาก landing (มีช่วงเวลา 1เดือน/3เดือน/12เดือน/5ปี
  ที่ `trend/` ปัจจุบันไม่มี — ปัจจุบันมีแค่ `now 1-d` / `now 7-d`)
- `COORDINATION.md` ตกยุค — ยังอ้างไฟล์ที่ root ทั้งที่ย้ายเป็นโฟลเดอร์แล้ว
- branch `claude/google-trends-dashboard-lt5k09` เป็น history คนละสาย (ต้นแบบ ก.ค.) ทิ้งได้
- 🔴 **แก้ 2 บรรทัดที่เคยเขียนผิดไว้ตรงนี้ (26 ส.ค. 2026)** — เดิมเขียนว่า "ไม่มี staging แยกจริง
  `dev` คือ production" และ "`main` ค้างที่ `6614efa` ไม่ได้ deploy ที่ไหน" **ทั้งคู่ไม่จริงแล้ว**
  ตอนนี้ `main` = production (`cp-dashboard-680.pages.dev`) · `dev` = staging · ทุก branch มี preview ของตัวเอง
- **`claude/webkit-probe` เป็น branch ทดลองชั่วคราว ห้าม merge — ต้องลบทิ้ง**
  (เครื่องที่รัน session ลบ branch บนต้นทางไม่ได้ ต้องให้เจ้าของกดลบที่ GitHub)
- **WebKit ที่ CI ยังตก 2 ชุด** (`archives.mjs` · `social.mjs`) — จากเดิมตก 10 ชุด
  ยังตั้ง `continue-on-error` ไว้ **ถอดออกไม่ได้จนกว่าจะเหลือ 0** ไม่งั้น push ไม่ผ่านทุกครั้ง
  · ⚠️ ยังไม่รู้ว่าเป็นข้อจำกัดของเครื่องมือทดสอบ หรือ **Safari พังจริง** ต้องไล่ดูก่อน
- คลังข่าว `pr:archive` เก็บ **90 วัน** (สูงสุด 3,000/คอลัมน์ ~3 MB) — เพดานค่าเดียวของ KV คือ 25 MB
  ถ้าจะเก็บถึง 1 ปี (~40 MB) **ต้องย้ายไป D1** ไม่ใช่แค่เพิ่มเลขวัน
  ดึงออกเป็นไฟล์ได้ที่ `/api/trend/archive?src=all&days=90` (CSV/JSON)
- คอลัมน์ YouTube พึ่ง instance สาธารณะของคนอื่น (Invidious/Piped) ที่ **หายไปเลยได้**
  มีตัวกันแล้ว (ยิงหลายตัวพร้อมกัน → แกะหน้า YouTube เอง → เสิร์ฟของเก่าใน KV)
  แต่ถ้าวันหนึ่งพังทั้งหมด ทางแก้ถาวรคือขอ YouTube Data API key (ฟรี มีโควตา 10k/วัน)

## 🔎 ข่าวในคอลัมน์ Alert มาจาก 2 ทาง — แก้คนละที่กัน

ป้ายบอกที่มาอยู่ **เฉพาะในรายการตัดข่าวบนหน้า `/admin/`** — เจ้าของสั่งเอาออกจากการ์ดบนแดชบอร์ด
(บนแดชบอร์ดแค่อ่านข่าว ไม่ได้แก้อะไร ป้ายเลยเป็นแค่ตัวรบกวน) · **ดูป้ายก่อนแก้เสมอ**

| ป้าย | มาจากไหน | แก้ที่ไหน |
|---|---|---|
| 🔔 มาจาก Google Alert | ฟีด Alert ของ Google ตรงๆ | **Google Alerts** → แก้ query (ใส่ `-คำ` / `-site:`) |
| 📰 มาจากคอลัมน์ข่าว | ข่าวจากคอลัมน์ News ที่ตรง keyword แล้ว `mergeNewsIntoAlert()` ดึงเข้ามา | **ในโค้ด** (ดูตารางล่าง) |

ที่มาเก็บไว้ในฟิลด์ `fromNews` ของ item (ทั้ง `trend/feeds.js` และ `ir/feeds.js` เซ็ตให้แล้ว)
· ตอนกด ⚑ จะถูกเก็บลง record เป็น `from: "news" | "alert"` → หน้า admin อ่านค่านี้

**ของที่ดึงมาจากข่าว ใช้คำอะไรตัดสิน:**

| คอลัมน์ | ใช้อะไร match | เอาออกยังไง |
|---|---|---|
| PR/Issue `alert1` (CP) | `CP_BRANDS` ใน `functions/api/trend/feeds.js` | แก้ลิสต์ในโค้ด |
| PR/Issue `alert2` (จับตามอง) | คำจาก query ของ Alert + คำ `-ไม่เอา` | แก้ query ใน Google Alerts |
| IR `alert1` / `alert2` | `CP_BRANDS` / `ALERT2_KEEP` ใน `functions/api/ir/feeds.js` | แก้ลิสต์ในโค้ด |

> ⚠️ **`-site:` ใช้กับของที่ดึงมาจากข่าวไม่ได้เลย** — เราดึงเข้ามาเอง ไม่ได้ผ่าน Google
> และตัวกรอง `-คำ` เทียบกับพาดหัว/สรุปเท่านั้น `site:` จึงไม่มีวัน match
> ปุ่ม "＋ ตัดเว็บ" บนหน้า admin จึง **ซ่อนไปเลย** ในแถวที่มาจากข่าว ไม่ให้กดแล้วนึกว่าแก้ได้
>
> ⚠️ **`-คำ` กรองของที่ดึงมาจากข่าวได้เฉพาะ `alert2` ของ PR/Issue** (ต้องรอ release keyword ชุดใหม่)
> ส่วน `alert1` กับฝั่ง IR ยังไม่ได้ส่ง excludes เข้าไป — ถ้าจะให้ครบต้องแก้เพิ่ม

### 🚫 ชื่อที่มีคำของเครือ CP แต่ไม่ใช่ข่าวของเครือ

อยู่ใน `CP_FALSE` (คำตรงตัว) กับ `CP_FALSE_RX` (แพตเทิร์น) ของ `feeds.js` ทั้ง trend และ ir
· ฝั่งหน้าเว็บมีคู่แฝดคือ `PIN_FALSE_RE` ใน `trend/app.js` + `issue/app.js` — **แก้ที่ไหนต้องแก้ให้ครบทั้ง 4 ที่**

| ชื่อ | คืออะไร |
|---|---|
| บีแอลซีพี · BLCP | โรงไฟฟ้า BLCP Power |
| ซีพีเอ็น · CPN | Central Pattana |
| บีซีพีจี · บีซีพี | กลุ่มบางจาก |
| **ทรูดิจิทัล พาร์ค · True Digital Park** | **สถานที่จัดงาน/ที่ตั้งออฟฟิศ** ข่าวที่พูดถึงมันไม่ใช่ข่าวของเครือ |
| **ทรูธโซเชียล · Truth Social · Trump Media** | **แอปของทรัมป์** — คำว่า "ทรู" อยู่ต้นคำพอดี ข่าวขาดทุนของ Trump Media เลยหลุดเข้าคอลัมน์ CP |
| **ซีพีพี · Central precocious puberty** | **ศัพท์การแพทย์** (ภาวะเป็นหนุ่มสาวก่อนวัย) — บทความ BBC หลุดเข้าคอลัมน์ CP · ⚠️ เขียนเป็น `ซีพีพี(?!ซี)` เพราะ **ซีพีพีซี (CPPC) เป็นบริษัทในเครือจริง** ตัดเพลินจะพังชื่อจริง |

> 🐞 **ชื่อเครือที่เขียนแบบมีจุดเคยจับไม่ได้เลย** (เจ้าของส่งภาพจาก `/admin/` มา 27 ส.ค. 2026)
> ข่าว "เจาะเบื้องหลังความร่วมมือ **C.P. Group** x Arise Venture x True x Amazon" ถูกตัดด้วยเหตุผล
> "ไม่มีชื่อเครือ CP" ทั้งที่ชื่ออยู่ในพาดหัวเต็มๆ — ลิสต์เก็บ `cp group` แต่ข่าวเขียน `c.p. group`
> · ✅ `cpEvidence()` **ตัดจุดในชื่อย่อออกก่อนเทียบ** (`c.p.` → `cp` · `ซี.พี.` → `ซีพี`)
> · ⚠️ **ต้องตัดจุดก่อน แล้วค่อยตัดชื่อลวง** ไม่งั้น "ซี.พี.เอ็น" จะรอดด่านชื่อลวงไปได้
> · ⚠️ ตัวจับต้องรับ `\p{M}` (สระ/วรรณยุกต์) ด้วย — "ซี" ลงท้ายด้วยสระ ี ซึ่ง Unicode ไม่นับเป็น "ตัวอักษร"
>   เขียนแค่ `\p{L}` แล้ว "ซี.พี." จะไม่ถูกตัดจุดเลย (เจอตอนวัดจริง)
>
> 🚂 **"CP" เดี่ยวๆ + บริบทของเครือ = เชื่อได้เลย ไม่ต้องรอ AI** (`CP_STRONG_CTX`)
> ข่าว "รฟท.นัดถก **CP** พรุ่งนี้ ชี้ขาด 'ไฮสปีดเทรน 3 สนามบิน' หยุดเดินรถแอร์พอร์ตลิงก์"
> เคยถูกส่งให้ AI ตัดสินแล้ว **AI ตอบผิด** จนข่าวจริงถูกตัดทิ้ง
> · คำบริบท: ไฮสปีด · แอร์พอร์ต(เรล)ลิงก์ · รฟท · 3 สนามบิน · เครือ · กลุ่มซีพี
> · ⚠️ **ใช้ได้ก็ต่อเมื่อเจอ "CP" อยู่แล้วเท่านั้น** — คำพวกนี้ลอยๆ ไม่นับ ไม่งั้นข่าวสนามบินไหลเข้าหมด
> · เทสต์ `weakcp.mjs` [8] คุมไว้ทั้งฝั่งจับและฝั่ง "ห้ามจับเกิน"
>
> 🌫️ **หน้ารายงานค่าฝุ่นของเทศบาล/อบต. ไม่ใช่ข่าว** (`DUSTPAGE_RE` · เหตุผล `dustpage`)
> เจอจริง: "รายงานสถานการณ์ PM 2.5 - เทศบาล เมือง ปู่เจ้าสมิงพราย" หลุดเข้าคอลัมน์จับตามอง
> · ตระกูลเดียวกับ `DATAPAGE_HOSTS` (iqair/aqicn) แต่เว็บเทศบาลมีเป็นร้อยแห่ง จึงดักที่ **รูปพาดหัว**
> · ⚠️ **ต้องครบ 2 อย่าง**: ขึ้นต้นว่า "รายงานสถานการณ์ ฝุ่น/PM" **และ** มีชื่อหน่วยงานท้องถิ่น
>   เอาอย่างใดอย่างหนึ่งจะไปโดนข่าวจริง ("รายงานสถานการณ์ PM 2.5 เชียงใหม่วิกฤต")
>
> ⚠️ **ทรูดิจิทัล พาร์ค จับเฉพาะที่มี พาร์ค/ปาร์ค/park ต่อท้าย** — "ทรูดิจิทัล กรุ๊ป" เป็นบริษัทของทรูจริง ห้ามตัด
> · สะกดได้หลายแบบ (เว้นวรรค · ทัล/ตอล · พ/ป) จึงเขียนเป็นแพตเทิร์นใน `CP_FALSE_RX` แทนการไล่พิมพ์ทุกแบบ
>
> ⚠️ **กฎชื่อลวงต้องอยู่ใน `noiseReason()` ด้วย ไม่ใช่อยู่แต่ใน `verifyAlertItems()`** (13 ส.ค. 2026)
> `verifyAlertItems` ทำงาน **ก่อน** ดึงของเก่าจาก KV กลับมา ของที่เก็บไว้ตั้งแต่ยังไม่มีกฎจึงไหลกลับ
> เข้าคอลัมน์โดยไม่ผ่านด่านแล้วอยู่ยาว (เจอจริง: ข่าวทรัมป์/ทรูธโซเชียล ยังอยู่หลัง release แล้ว)
> · วางไว้ **ท้ายสุด** ของ `noiseReason` ให้กฎที่เจาะจงกว่าตอบก่อน ไม่งั้นหน้าสินค้าโฮมโปรที่มีรหัส
>   รุ่น `-CP` จะรายงานว่าตัดเพราะ "ชื่อลวง" แทนที่จะเป็น "ร้านค้า" — ตัดเหมือนกันแต่เจ้าของอ่านแล้วงง
>
> 🐞 **ด่าน "ไปอ่านเนื้อข่าวจริง" ของคอลัมน์ที่ไม่ใช่ CP ไม่เคยทำงานเลย** (แก้ 20 ส.ค. 2026)
> `verifyAlertItems` ส่ง `extra` (ลิสต์ชื่อเครือ CP) เข้า `bodyHasKeep` ทุกคอลัมน์
> คอลัมน์อื่นค่านี้เป็น `[]` → `bodyHasKeep` คืน `false` ทันที **โดยไม่เคยเปิดหน้าข่าวเลย**
> ผลคือ **ทุกใบที่คำ match ไม่อยู่ในพาดหัว ถูกตัดทิ้งหมด** ด้วยเหตุผล "ไม่อยู่ในพาดหัว/เนื้อ"
> ซึ่งเป็นข้อความที่โกหก — ไม่เคยอ่านเนื้อสักครั้ง (เจ้าของเจอ: ตัดข่าว 9 ใบที่เกี่ยวตรงๆ
> เช่น "วอนรัฐเร่งแก้ 'โรคกุ้ง' …" ที่คำอยู่ทั้งในพาดหัวและเนื้อข่าว)
> · แก้ 2 ชั้น: คอลัมน์ที่ไม่มีลิสต์ประจำคอลัมน์ให้ใช้ **คำที่ Google ไฮไลต์ไว้ในใบนั้น**
>   (`verdict[i].terms`) · และ `bodyHasKeep` เจอลิสต์ว่างต้องคืน **`null` (ตัดสินไม่ได้)** ไม่ใช่ `false`
> · **พาดหัวจากฟีดถูกตัดสั้นบ่อย** (ลงท้าย "…") คำจึงหลุดออกไปอยู่ในส่วนที่ถูกตัด
>   ด่านอ่านเนื้อข่าวมีไว้รับเคสนี้โดยเฉพาะ พอมันไม่ทำงาน ข่าวพาดหัวยาวเลยหายเป็นแถบ
> · เทสต์ `keepterms.mjs` คุมไว้ (ลองสลับกลับเป็นของเดิมแล้ว → ตก 4 ข้อ)

> ⚠️ **`bodyHasKeep()` ต้อง `dropFalseCP(body)` ก่อนหาเสมอ** — บทความเรื่องทรัมป์เอ่ยถึง
> "ทรูธโซเชียล" ทั้งบทความ ถ้าไม่ตัดออกก่อน `body.includes("ทรู")` จะจริงตลอด แล้วด่านอ่านเนื้อข่าว
> จะปล่อยข่าวคนละเรื่องเข้าคอลัมน์ CP ทุกใบ
>
> ⚠️ **ตัดชื่อลวงออกก่อนเช็คเสมอ** (`dropFalseCP`) ไม่ใช่เช็คแล้วค่อยตัด — ข่าวที่มีทั้งชื่อจริงและชื่อลวง
> (เช่น "ศุภชัย เจียรวนนท์ พูดที่ทรูดิจิทัล พาร์ค") ต้องยังเข้าคอลัมน์ CP อยู่
>
> ⚠️ ตอนเพิ่มชื่อลวง **ให้ไล่เช็คด้วยว่าคำที่เหลือยังจับข่าวจริงได้ไหม** — รอบนี้เจอว่า `PIN_FALSE_RE`
> ไม่มี "เจียรวนนท์" ข่าวที่มีแต่ชื่อผู้บริหารเลยหลุด (ก่อนหน้านี้ติดบนได้เพราะบังเอิญมีคำว่า "ทรู"
> ในชื่อสถานที่) เติมให้แล้ว

### 🧹 ของที่ไม่ใช่ข่าว — ตัดทิ้งอัตโนมัติใน `noiseReason()`

> 🎯 **ตัดที่เดียว = ตัดทุกแดชบอร์ด** (เจ้าของสั่งเป็นกฎถาวร 13 ส.ค. 2026)
> ตัวกรองทั้งชุดอยู่ที่ **`functions/api/_lib/noise.js` ไฟล์เดียว** · `trend/feeds.js` กับ `ir/feeds.js`
> แค่ `import` ไปใช้ **ห้ามก๊อปลิสต์/ฟังก์ชันไปวางในแดชบอร์ดอีก** เด็ดขาด
> · **แดชบอร์ดใหม่ที่จะสร้างต่อจากนี้ ต้อง import จากไฟล์นี้ตั้งแต่วันแรก**
> · เทสต์ `prwire.mjs` มีด่านคอยจับ: ถ้าเจอ `const SHOP_HOSTS =` หรือ `function noiseReason(`
>   โผล่ในไฟล์แดชบอร์ดเมื่อไหร่ เทสต์ตกทันที

**ที่ย้ายเข้าไลบรารีแล้ว:** ลิสต์เว็บ/คำทั้งหมด · `noiseReason` · `dropNoiseAfterArchive` ·
`realCP` `hasFalseCP` `dropFalseCP` `CP_BRANDS` `CP_FALSE*` · `hostOf` `outletOf` `normLink` ·
`termPattern` `buildMatchers` `anyTermIn` `highlightedTerms` `hlWrap` `stripMarks` ·
`WEAK_TERMS` `ROUNDUP_RE` · รายชื่อ "↩ เอากลับ" (`setAllowed` / `isAllowed`)

**ที่ยังแยกกันโดยตั้งใจ:** `ALERT2_KEEP` (คำเฉพาะคอลัมน์ของ IR) · `AMBIG_KW` · การจัดหมวดข่าว
— พวกนี้เป็นเรื่องของแต่ละแดชบอร์ด ไม่ใช่กฎกลาง

> ⚠️ `setAllowed()` **ต้องเรียกใหม่ทุกครั้งที่ build** — ตัวแปรอยู่ระดับโมดูล และ Workers
> ใช้โมดูลเดิมซ้ำข้าม request ถ้าไม่ตั้งใหม่จะค้างรายชื่อของ request ก่อนหน้า

**จับที่โดเมนเป็นหลัก แม่นกว่าจับคำมาก** คำเอาไว้กันเว็บที่ยังไม่อยู่ในลิสต์

> ⚠️ **กฎเว็บแจกข่าว (newswit/thaipr) ใช้กับคอลัมน์ CP เท่านั้น** — เงื่อนไข "ชื่อเครือต้องอยู่ในพาดหัว"
> เอาไปใช้กับ `alert2` ไม่ได้ เพราะคอลัมน์นั้นตามอุตสาหกรรม ไม่ได้ตามเครือ
> (เจอจริง 13 ส.ค.: TFG แจ้งผลประกอบการ · กรมประมงยืนยันมาตรฐานเชื้อดื้อยา — โดนตัดทั้งคู่)
> `noiseReason()` จึงรับ `src` เข้าไปด้วย · เทสต์ `prwire.mjs` คุมไว้
>
> ⚠️ **สรุปที่ติดมากับฟีดบางใบไม่ใช่สรุปของข่าวใบนั้น** — เป็นบล็อก "ข่าวที่เกี่ยวข้อง" หรือเศษ
> หน้ารวมข่าวที่พ่วงมา (เจอจริง 13 ส.ค. 2026: การ์ด "สี่มุมเมืองออนไลน์" มีสรุปเป็นข่าว
> "ซีพี แอ็กซ์ตร้า โชว์ผลงานครึ่งปีแรก" ซึ่งคนละใบกัน) · `looksLikeListing()` ใน `_lib/parser.js`
> ตัดทิ้งตั้งแต่ตอนอ่านฟีด — สัญญาณคือ **ขึ้นต้นด้วยเศษวันที่/เวลา** หรือ **มีวันที่ไทย ≥ 2 ชุด**
> · ตัดสรุปทิ้งดีกว่าเอามาแสดงให้สับสน และกันไม่ให้ไปเจอชื่อเครือในนั้นแล้วดึงข่าวผิดเข้าคอลัมน์
> · เทสต์ `listing.mjs` คุมทั้งฝั่งจับและฝั่ง "ห้ามตัดสรุปจริงทิ้ง"
>
> 🐞 **`looksLikeListing()` จับได้ไม่หมด — บล็อกที่ไม่มีวันที่หลุดทุกใบ** (เจ้าของแจ้ง 21 ส.ค. 2026 พร้อมภาพ)
> การ์ด 2 ใบในคอลัมน์ "หัวข้อที่จับตามอง" คนละข่าวกันสนิท (ข่าว ฮ.บินไล่ยิง · ข่าวปิดทางรถไฟนราธิวาส)
> แต่มีสรุปเหมือนกันเป๊ะว่า "ทช. สำรวจปลาหมอคางดำ 4 จังหวัด…" — เป็นบล็อกข่าวแนะนำของ Workpoint
> ที่ **ไม่มีสัญญาณวันที่เลย** `looksLikeListing` จึงมองไม่เห็น
> · เสียหาย 2 ชั้น: อ่านแล้วสับสน **+ ดูดข่าวคนละเรื่องเข้าคอลัมน์** เพราะ `mergeNewsIntoAlert`
>   ของ `alert2` เทียบคำที่ **พาดหัว + สรุป** (คำว่า "ปลาหมอคางดำ" อยู่ในสรุปปลอม)
> · ✅ แก้ด้วย `dropSharedSnippets()` ใน `_lib/noise.js` — **สรุปก้อนเดียวกันไปโผล่ที่ข่าวคนละลิงก์
>   ตั้งแต่ 2 ใบขึ้นไป = ไม่ใช่สรุปของข่าวใบไหนเลย** ตัดสรุปทิ้ง (ไม่ได้ลบข่าว) ติดธง `sharedSnip`
> · ⚠️ **ต้องนับด้วย "จำนวนลิงก์" ไม่ใช่ "จำนวนใบ"** — ข่าวใบเดียวโผล่ได้หลายคอลัมน์เป็นเรื่องปกติ
>   นับใบจะตัดสรุปของข่าวปกติทิ้งฟรีทุกใบ · และสรุปสั้นกว่า 30 ตัวอักษรไม่เข้าเกณฑ์ (ซ้ำกันได้โดยบังเอิญ)
> · ⚠️ **ต้องเรียก 2 รอบ** — ก่อน `mergeNewsIntoAlert` (กันดูดผิด) และอีกครั้งก่อน `pruneStaleMerged`
>   (กวาดของเก่าที่ค้างในคลัง KV ตั้งแต่ยังไม่มีกฎนี้ ไม่งั้นรอ 90 วัน) · **บวก `CACHE_VER` ด้วยทุกครั้ง**
> · เทสต์ `sharedsnip.mjs` คุมทั้ง 4 ชั้น รวมข้อพิสูจน์ว่า `looksLikeListing` จับเคสนี้ไม่ได้

> ⚠️ **คอลัมน์ CP: ห้ามตัดสินจาก `it.snippet`** (เจ้าของสั่ง 13 ส.ค. 2026)
> สรุปที่ติดมากับฟีดเป็น **"ข่าวที่เกี่ยวข้อง"** ไม่ใช่เนื้อข่าวใบนั้น ชื่อเครือที่โผล่ตรงนั้นจึงไม่ได้
> แปลว่าข่าวใบนี้เป็นข่าวของเครือ (เคสจริง: "7 ยักษ์ผูกเหลาฟาม์าห์ เขย่าธุรกิจร้านยา" ·
> "จูหรงจี้ อดีตนายกฯ จีน ถึงแก่อนิจกรรม") · **ลำดับที่ถูกคือ พาดหัว → เนื้อข่าวจริง**
> ชื่อเครืออยู่ในพาดหัว = ผ่าน · ไม่อยู่ = ไปอ่านเนื้อข่าวจริง (`bodyHasKeep`) ·
> **อ่านไม่ได้ = ให้ AI อ่านพาดหัวตัดสิน** (`aiHeadlineIsCP` — โมเดลเดียวกับที่จัดหมวดข่าว)
> AI ตอบไม่ได้/ตอบไม่ครบ/ไม่มี binding ค่อยปล่อยผ่าน · ตัดพลาดมีตาข่าย: กด ↩ เอากลับที่ `/admin/` ได้
>
> 🔍 **ชื่อเครือต้อง "ยืนเป็นคำของตัวเอง" ไม่ใช่ไปเจอกลางคำอื่น** (เจ้าของสั่ง 14 ส.ค. 2026)
> ภาษาไทยไม่มีช่องว่างคั่นคำ `hay.includes("ซีพี")` จึงไปเจอใน **เอ็ม-ซีพี-ไอ** (สารเคมี MCP ของ halal.co.th)
> แล้ว **ผ่านตั้งแต่ชั้น 1 ไม่มีวันไปถึงชั้น AI** — เจอจริงในคอลัมน์ CP ที่มีของไม่เกี่ยวเยอะมาก
> · แก้ด้วย `cpEvidence()` ใน `_lib/noise.js` → `"strong"` (ผ่านเลย) · `"weak"` (ส่งให้ AI) · `""` (ไม่เจอ)
> · **weak มี 2 กรณี** — จงใจตั้งให้แคบ
>   1. ชื่อเครือ**ถูกขนาบด้วยตัวอักษรไทยทั้งสองข้าง** (= อยู่กลางคำอื่นแน่ๆ)
>      "เครือซีพี" / "ซีพีเอฟ" มีขอบด้านหนึ่งเป็นช่องว่าง/ขอบข้อความ = strong ไม่โดนหางเลข
>   2. **เจอแค่ `CP` เดี่ยวๆ** (`CP_WEAK_BRANDS` — เพิ่ม 20 ส.ค. 2026)
>
> 🐞 **`CP` เดี่ยวๆ ไม่เคยอยู่ในลิสต์เลย ข่าวอังกฤษล้วนจึงหายเงียบ** (แก้ 20 ส.ค. 2026)
> เจอจริง: **"เปิดแผน รฟฟท.บริหาร 'แอร์พอร์ตเรลลิงก์' รับมือเลิกสัญญาไฮสปีด CP"** ของกรุงเทพธุรกิจ
> ถูกตัดด้วยเหตุผล "ไม่มีชื่อเครือ CP ในพาดหัว" **ทั้งที่คำว่า CP อยู่ในพาดหัว**
> — ลิสต์มี `ซีพี` `cpf` `cp all` แต่ไม่มี `cp` เดี่ยว ข่าวจึงตกไปด่านอ่านเนื้อแล้วโดนตัด
> · 🚫 **ห้ามใส่ `cp` ลง `CP_BRANDS` ตรงๆ** — CP เป็นตัวย่อของอย่างอื่นเยอะ (cerebral palsy ·
>   ภาควิชาวิศวะคอมฯ จุฬาฯ · รหัสรุ่นสินค้า) ปล่อยผ่านฟรี = ของไม่เกี่ยวไหลเข้าคอลัมน์ทันที
> · ✅ อยู่ใน `CP_WEAK_BRANDS` แยกต่างหาก → คืน **weak** เสมอ = ส่งให้ AI อ่านพาดหัวตัดสิน
> · **`cpEvidence` เลิกใช้ `indexOf` แล้ว เปลี่ยนไปใช้ `termPattern()`** (คำละตินต้องตรงทั้งคำ)
>   ของเดิม `cpf` ไปเจอกลาง `cpfresh` · `cp all` เจอใน `cp allocation` — บทเรียนเดิมกับ SLAPP → slapped
>   · `cpfresh` เป็นแบรนด์จริงของเครือ จึงใส่เป็นคำเต็มไว้ในลิสต์แทน
> · เทสต์ `weakcp.mjs` [7] คุมไว้ทั้ง 12 เคส (รวม CPU · CP3 · CPI ที่ต้องไม่นับ)
> · **weak ไม่ได้แปลว่าตัดทิ้ง** แค่ไม่ให้ผ่านฟรี ต้องให้ AI อ่านพาดหัวตัดสิน (เหตุผล `ai-weak-cp`)
> · ⚠️ **จำคำตอบ AI ไว้ 7 วันต่อข่าว 1 ใบ** (`cachedHeadlineIsCP` — edge cache **ไม่ใช่ KV**)
>   ไม่งั้นทุก build จะถามซ้ำทั้งคอลัมน์ = เรียก AI หลายพันครั้ง/วัน · เพดาน `AI_CP_MAX` 12 ใบ/build
> · **ยังทำเฉพาะคอลัมน์ CP (`alert1`)** — เจ้าของสั่งเริ่มแค่นี้ก่อน คอลัมน์อื่นยังใช้กฎตายตัวล้วน
> · เทสต์ `weakcp.mjs` คุมไว้ (รวมทั้งการนับว่าถาม AI กี่ครั้ง)

> 🧠 **ชั้น AI เรียนรู้จากที่เจ้าของกดเอง** (เจ้าของสั่ง 13 ส.ค. 2026: "ตัดอันไหนก็ให้ไปเรียนรู้")
> `cpExamples()` ใน `_lib/noise.js` แปลงคำตัดสินเป็นตัวอย่างสอน AI ทุกครั้งที่ถาม (few-shot)
> — วิธีเดียวกับที่ใช้สอนการจัดหมวดข่าวอยู่แล้ว · **ไม่ใช่การเทรนโมเดล** ผลมาทันทีและถอนได้ทันที
> · กด **↩ เอากลับ** = ตัวอย่าง `y` · กด **⚑ สั่งตัด** = ตัวอย่าง `n`
> · **ไม่มี KV read เพิ่มเลย** — อ่านจาก blob `noise:allow` ที่ `feeds.js` อ่านอยู่แล้วทุก build
> · ⚠️ **↩ ที่กดด้วยเหตุผลอื่นไม่นับ** (เอากลับหน้าประกาศงาน/หน้าขายของ ไม่ได้แปลว่ามันเป็นข่าวของเครือ)
>   นับเฉพาะใบที่ถูกตัดด้วยเหตุผลของคอลัมน์ CP — ดูชุด `CP_DROP_WHY`
>   **เพิ่มเหตุผลใหม่ที่แปลว่า "ไม่ใช่ข่าวเครือ" เมื่อไหร่ ต้องเติมใน `CP_DROP_WHY` ด้วย**
> · ⚠️ **พาดหัวที่มีชื่อเครืออยู่แล้วไม่เอามาเป็นตัวอย่าง** — ใบแบบนั้นผ่านตั้งแต่ด่านแรก
>   ไม่มีวันมาถึงชั้น AI ใส่ไปก็ไม่ตรงกับของจริงที่ AI ต้องตัดสิน
> · ⚠️ **จำกัด 8 ตัวอย่างและคละ y/n เสมอ** โมเดลเล็ก ถ้าเห็นแต่ `n` จะตอบ `n` รัว — เทสต์ `cplearn.mjs` คุมไว้
> · **ของที่ดึงมาจากคอลัมน์ข่าวก็ต้องผ่านด่านนี้ด้วย** — ทางลัด `it.fromNews` ถูกปิดสำหรับ `alert1`
> · **ตอน merge/prune ข่าวจากคอลัมน์ News เข้า `alert1` ก็ยึดพาดหัวเท่านั้น** (13 ส.ค. 2026 รอบสาม)
>   ของเดิม match ที่ title+snippet → ข่าวตลาดหุ้นของเดลินิวส์ถูกดูดเข้าเพราะสรุปเป็น "รายการข่าวอื่น"
>   ที่มีพาดหัวข่าว 'ซีพี' ใบอื่นพ่วงมา (สรุปแบบนั้นหลุด `looksLikeListing` เพราะเขียนวันที่แบบ
>   ชื่อเดือนเต็ม "13 สิงหาคม 2569" — แก้แล้ว: `TH_MONTH` รู้จักทั้งชื่อย่อและชื่อเต็ม)
>   เกณฑ์ merge ต้องตรงกับ prune เป๊ะเสมอ ไม่งั้นดึงเข้า-ลบทิ้งสลับกันทุกรอบ — เทสต์ `stockwrap.mjs` คุมทั้ง 3 ชั้น

| ประเภท | ตัวอย่างที่เจอจริง | ลิสต์ |
|---|---|---|
| ประกาศงาน | jobsdb · jooble · jobbkk · **monster.co.th** (ประกาศงานภาษาอังกฤษ) | `JOB_HOSTS` · `JOB_RE` |
| อสังหา/ให้เช่า | dotproperty · ddproperty · livinginsider | `PROP_HOSTS` · `PROP_RE` |
| หน้าขายสินค้า/บริการ | epower (เครื่องกรอง/เซนเซอร์วัดฝุ่น) | `VENDOR_RE` |
| โฆษณาแฝงในข่าว | ครีม/เซรั่ม "หาซื้อได้ที่เซเว่น" (ข่าวสด) | `AD_PRODUCT_RE` **และ** `AD_PITCH_RE` |
| เว็บแจกข่าว PR | newswit · thaipr · prnewswire · **ryt9** — **ตัดเฉพาะใบที่ชื่อเครือ CP ไม่อยู่ในพาดหัว** (เคยตัดทั้งเว็บ แล้วข่าวจริงของ ซีพี แอ็กซ์ตร้า/Makro หายไปด้วย) | `PR_HOSTS` + `cpEvidence(title)` |
| หน้าข้อมูลค่าฝุ่น/อากาศรายเมือง | iqair (หน้าเมือง "Bieber" แคลิฟอร์เนีย หลุดเข้าคอลัมน์ PM2.5) · aqicn · accuweather | `DATAPAGE_HOSTS` |
| **หน้างานอีเวนต์/นิทรรศการ** | นิทรรศการศิลปะบน `greener.bangkok.go.th/en/**event**/…` — เข้ามาเพราะ **เมนูบนสุดของเว็บมีคำว่า PM 2.5 อยู่ทุกหน้า** | `EVENT_PATH_RE` |
| **หน้ารวมบทความของเว็บ** | `สู้ฝุ่น PM 2.5 **Archives** - ข่าวท้องถิ่น` (หน้า tag/หมวดของ WordPress ไม่ใช่ข่าว) · เจ้าของสั่งตัดทั้งหมด 14 ส.ค. 2026 | `ARCHIVE_RE` |
| หน้าดูหนัง/ซีรีส์ | netflix — หน้าโปรโมตเรื่อง ไม่ใช่ข่าว · ⚠️ **ห้ามใส่ trueid** (เป็นของทรูในเครือ CP) | `STREAM_HOSTS` |
| หน้าสินค้าสัตว์เลี้ยง/วัสดุก่อสร้าง | vif.pet ("CP" คือชื่อรุ่นแผ่นรองซับ) · โฮมโปร (รหัสสี `-CP`) | `SHOP_HOSTS` · `CP_FALSE_RX` |
| **ร้านค้าออนไลน์ของเครือเอง** | AllOnline · shopat24 · makroclick · 7eleven.co.th — หน้าพวกนี้มีชื่อเครือเต็มไปหมด **ด่านชื่อเครือกรองไม่ออก ต้องตัดที่โดเมน** | `SHOP_HOSTS` + `SHOP_RE` (`฿\s*\d`) |
| ร้านค้าออนไลน์ · รายงานประจำวัน · แกลเลอรีรูป · ข่าว PR ราชการ | (ของเดิม) | `SHOP_*` · `DAILY_RE` · `GALLERY_RE` · `PR_RE` |

> ⚠️ **`verifyAlertItems()` ทำงาน "ก่อน" `mergeArchives()`** — ของเก่าที่เก็บไว้ใน KV ตอนยังไม่มีตัวกรอง
> จะไหลกลับเข้ามาโดยไม่ผ่านด่าน จึงต้องมี `dropNoiseAfterArchive()` กวาดอีกรอบหลัง archive
> ไม่งั้นต้องรอ 10 วันกว่าของเก่าจะหมดอายุไปเอง **เพิ่มตัวกรองใหม่ทีไร ให้เช็คว่ากวาดของเก่าด้วยไหม**
>
> ⚠️ **ด่าน CP (พาดหัว→เนื้อ→AI) ก็เจอกับดักเดียวกันนี้ — แก้แล้วด้วย "ด่านตรวจรอบสอง"** (14 ส.ค. 2026)
> เจอจริง: khaosod/dailynews/naewna ที่ 'ซีพี' อยู่แค่ในบล็อกข่าวแนะนำ ค้างในคอลัมน์เพราะเข้ามา
> ตั้งแต่ก่อนมีด่าน · วิธีแก้: ใบที่ผ่านด่านแบบชี้ขาดติดธง `vfy` (เวอร์ชัน `VFY_VER`) ลง item
> → หลัง `mergeArchives` ใบที่ไม่มีธง (ของเก่า/ยังไม่เคยตัดสิน) ถูกส่งเข้า `verifyAlertItems` ซ้ำ
> แล้ว **ตัดออกจาก `archiveOut` ด้วย ไม่ใช่แค่หน้าจอ** — ไม่วนกลับมาให้ตัดใหม่ทุกรอบ
> · เพดานยิงอ่านเนื้อ `BODY_FETCH_MAX` 12 ใบ/build (กันชนโควตา subrequest 50) เกินเพดานเก็บไว้
>   โดยไม่ติดธง รอบถัดไปตรวจต่อจนหมด backlog เอง · **บวก `VFY_VER` = สั่งตรวจของเก่าใหม่ทั้งคลัง**
> · `ir/feeds.js` แยกการเขียนคลังเป็น `saveArchives()` แล้ว (เหมือน trend — เขียน KV ครั้งเดียวเท่าเดิม)
> · คอลัมน์รวมข่าวสั้นหลายบริษัท ("โลกธุรกิจ - มาร์เก็ตนิวส์" ของแนวหน้า) เพิ่มใน `ROUNDUP_RE` แล้ว
> · เทสต์ `secondpass.mjs` คุมไว้ · ⚠️ ข่าวที่เนื้อ "เอ่ยถึงเครือจริง" (เช่นได้รางวัลจาก ซีพี ออลล์)
>   ด่านเนื้อข่าวจะเก็บไว้ — ถ้าเจ้าของไม่อยากเห็น ต้องกด ⚑ เอง (แล้วระบบจะเรียนรู้ต่อจากที่กด)
>
> ⚠️ **`ARCHIVE_RE` กับ `EVENT_PATH_RE` ดูคนละที่กัน — สลับกันแล้วตัดข่าวจริงทันที**
> · `ARCHIVE_RE` ดู **พาดหัว** (ลิงก์ `/archives/12345` เป็น permalink ข่าวจริงของ WordPress)
> · `EVENT_PATH_RE` ดู **ลิงก์** (คำว่า "นิทรรศการ" โผล่ในพาดหัวข่าวจริงบ่อย เช่นข่าวเครือออกบูท)
> · เทสต์ `noise.mjs` คุมทั้งฝั่งจับและฝั่ง "ห้ามตัดข่าวจริง" ของทั้งสองกฎ

> ⚠️ **กฎ `ARCHIVE_RE` ดูจากพาดหัวเท่านั้น ห้ามเอาไปจับที่ลิงก์** — เว็บ WordPress จำนวนมาก
> ใช้ `/archives/12345` เป็น permalink ของ **ข่าวจริง** ถ้าจับที่ลิงก์จะตัดข่าวหายไปทั้งเว็บ
> · `\b` ใช้กับพาดหัวไทยได้ เพราะอักษรไทยไม่ใช่ `\w` จึงนับเป็นขอบคำอยู่แล้ว
> · เทสต์ `noise.mjs` คุมทั้งฝั่งจับและฝั่ง "ห้ามตัดข่าวจริงที่ลิงก์มี /archives/"

> ⚠️ **คำที่เสี่ยงตัดข่าวจริง อย่าใส่** — เช่น "เครื่องฟอกอากาศ" ที่โผล่ในข่าว PM2.5 จริงบ่อย
> (ใส่แต่ "เครื่องกรองน้ำ/เครื่องกรองอากาศ" ที่เป็นภาษาแคตตาล็อกมากกว่า)
> เพิ่มคำใหม่ทุกครั้ง **ต้องรันเทสต์ `noise.mjs` ที่มีพาดหัวข่าวจริงคุมไว้ด้วย**

## 📊 เก็บข่าวลง Google Sheet — 4 หัวข้อ (CPF · ปลาหมอคางดำ · PM2.5 · Alien species)

**ชีตเป็นฝ่ายมาดึงเอง** ไม่ใช่เรายิงเข้าไป — `sheet/news-to-sheet.gs` วางไว้ใน Apps Script ของชีต
ตั้งเวลาให้วิ่งทุก 1 ชม. แล้วมาขอที่ `/api/trend/archive?topics=...&format=json`

ทำไมไม่ให้ Cloudflare ยิงเข้าชีตเอง:
1. **Cloudflare Pages ตั้ง cron ไม่ได้** (Workers ทำได้ Pages ไม่ได้) → ต้องรอให้มีคนเปิดแดชบอร์ด = ไม่แน่นอน
2. ต้องเก็บ URL ของ Web App ไว้เป็น secret · ใครได้ URL ไปเขียนลงชีตเจ้าของได้เลย

| | |
|---|---|
| หัวข้อที่รู้จัก | `cpf` · `blackchin` · `pm25` · `alien` (ตาราง `TOPICS` ใน `archive.js`) |
| `alien` จับแค่คำรวม | เจ้าของสั่ง (10 ส.ค.) ให้เอาแค่ alien species ไทย+อังกฤษ กับ "สัตว์ต่างถิ่น" · **ไม่ไล่ชื่อชนิดพันธุ์ทีละตัว** (เคยใส่ปลาซัคเกอร์/หอยเชอรี่/ผักตบชวา แล้วถอดออก — ละเอียดเกินที่ต้องการ) |
| คอลัมน์ในชีต | สำนักข่าว · พาดหัว · link · วันที่ · หมวด |
| ลำดับแถว | **ข่าวใหม่แทรกไว้บนสุดเสมอ** (`insertRowsBefore(2, n)`) |
| ข่าวย้อนหลัง | Bing ให้ย้อนได้แค่ 2-5 วัน (เป็นแค่หน้าผลค้นหาปัจจุบัน ไม่มีพารามิเตอร์วันที่) — ความลึกที่มีมาจาก**คลังของเราเอง** 90 วัน · ถ้าอยากได้เก่ากว่านั้นต้องหาแหล่งข่าวที่มีคลังย้อนหลัง (ยังไม่ได้ทำ) |
| กันข่าวซ้ำ | เทียบ `link` ที่ตัด `?utm_...` และ `/` ท้ายออกแล้ว |

> ⚠️ **คอลัมน์วันที่ในชีตปนกันทั้ง Date จริงกับข้อความ** — เทียบด้วย `cellTime_()` เสมอ
> เอา `String(Date)` มาเรียงแบบตัวอักษรไม่ได้: `"Wed Aug 12"` > `"Thu Aug 13"` (W > T)
> **เกิดจริง: `checkStatus()` รอบแรกรายงานว่า "เรียงถูกแล้ว" ทั้งที่ของใหม่ตกอยู่ก้นชีต**
> · `sortNewestFirst()` ก็เลี่ยง `Range.sort()` แล้วเรียงเองใน JS ด้วยเหตุผลเดียวกัน
> · ตอนแสดงผล **ห้ามบวก 7 ชม.เอง** — ข้อความในชีตเป็นเวลาไทยอยู่แล้ว · เทสต์ต้องรัน `TZ=Asia/Bangkok`
>
> 🔎 **ชีตค้าง — อย่าเดา ใช้ `checkStatus()`** (เพิ่ม 13 ส.ค. 2026) เลือกฟังก์ชันในกล่องข้าง ▶ Run
> แล้วอ่าน Execution log · บอกครบว่า ชีตมีถึงวันไหน · แถวบนสุด/ล่างสุดเป็นวันไหน · คลังมีถึงวันไหน ·
> มีของใหม่ที่ชีตยังไม่มีกี่ใบ → **แยกออกทันทีว่าติดฝั่งชีต (เรียง/เขียน) หรือฝั่งคลังข่าว**
> · เคยเดาผิดมาแล้ว 2 รอบ (โทษการเรียงชีต แล้วโทษว่าไม่มีคนเปิดแดชบอร์ด) ทั้งที่ยืนยันจาก sandbox ไม่ได้
> · **ตัวตั้งเวลาวิ่งอยู่หรือไม่ ดูที่ Apps Script → Executions** ไม่ใช่เดาจากวันที่ในชีต
>
> 🚨 **คลังข่าวโตเฉพาะตอนมีคนเปิดแดชบอร์ด** — Cloudflare Pages ตั้ง cron ไม่ได้ คลัง `pr:archive`
> ถูกเขียนตอนมีคนขอ `/api/trend/feeds` เท่านั้น **วันไหนไม่มีใครเปิดหน้าเว็บ ข่าววันนั้นหายถาวร**
> (สงสัยว่าเป็นต้นเหตุที่ชีตมีข่าววันที่ 12 ส.ค. อยู่ใบเดียว) · แก้แล้วโดยให้ `syncNews()` ของชีต
> **แตะ `/api/trend/feeds` ก่อนทุกครั้ง** (`warmArchive_`) = ตัวตั้งเวลาของชีตทำหน้าที่ cron ให้ทั้งระบบ
> · เขียน KV เพิ่มไม่เกิน 24 ครั้ง/วัน · เทสต์ `sheetorder.mjs` เช็คลำดับการยิงไว้แล้ว
>
> ⚠️ **ข่าวใหม่ต้องแทรกไว้บนสุด ไม่ใช่ต่อท้าย** (แก้ 13 ส.ค. 2026) — ของเดิมต่อท้ายด้วย
> `getLastRow()+1` แล้วไม่เคยเรียงชีตให้ พอเจ้าของกด Sort เรียงวันที่ใหม่→เก่าครั้งหนึ่ง
> ข่าวที่เข้ามาหลังจากนั้นไปกองอยู่ **ก้นชีต** เปิดดูข้างบนจึงเห็นวันที่ค้างวันเดิม
> **ดูเหมือนตัวตั้งเวลาหยุดวิ่ง ทั้งที่วิ่งอยู่** · มี `sortNewestFirst()` ไว้ซ่อมชีตเก่าครั้งเดียว
> · เทสต์ `sheetorder.mjs` ปลอม SpreadsheetApp แล้วรัน `syncNews` จริง
>
> ⚠️ **ลิงก์ของ Bing เป็นตัวเปลี่ยนทางที่เปลี่ยนทุกชั่วโมง** (`apiclick.aspx?...&tid=<เปลี่ยนทุกรอบ>&url=<ลิงก์จริง>`)
> คลังข่าว dedupe ด้วยลิงก์ จึงมองข่าวใบเดียวเป็นคนละใบทุกรอบ — **เจอจริง: ข่าวเดียวซ้ำ 27 แถวในชีต**
> แก้แล้วที่ `unwrapRedirect()` ใน `_lib/parser.js` (แกะทุก source ไม่ใช่แค่ alert) + `mergeArchives`
> แกะก่อน dedupe เพื่อยุบของเก่าที่ค้างใน KV ด้วย · ฝั่งชีตกันซ้ำ 2 ทาง (ลิงก์ · สำนักข่าว+พาดหัว)
> และกันซ้ำ **ภายในรอบเดียวกัน** ด้วย — ของเดิมอ่าน `seen` มาก่อนเขียน 27 ใบในรอบเดียวจึงผ่านหมด
> · ล้างของเก่าที่ค้างในชีตด้วยฟังก์ชัน `cleanupDupes()`
>
> ⚠️ **`syncNews` เขียนแต่แถวใหม่ ไม่เคยกลับไปแก้ของเดิม** — แก้ฝั่งเซิร์ฟเวอร์แล้วแถวที่อยู่ในชีต
> ยังเป็นของเก่าอยู่เสมอ ต้องมีฟังก์ชันซ่อมแยกทุกครั้ง (`fixClippedTitles()` ซ่อมพาดหัวที่ถูกตัด
> จับคู่ด้วย **สำนักข่าว + ต้นพาดหัว** ไม่ใช่ลิงก์ เพราะแถวเก่าเก็บลิงก์ Bing ที่เปลี่ยนทุกชั่วโมง)
>
> ⚠️ **บางฟีดตัดพาดหัวโดยไม่ใส่ "…"** (จบห้วนๆ กลางประโยค) — ดูแค่จุดไข่ปลาจึงไม่พอ
> `looksClipped()` เลยนับพาดหัวที่ยาว ≥ 80 ตัวอักษรและไม่จบด้วยเครื่องหมายจบประโยคว่า "น่าสงสัย"
> **เดาเกินไปไม่เสียหาย เพราะด่านตอนรับค่ากลับเข้ม** (ต้องยาวกว่าเดิม + ขึ้นต้นเหมือนกัน)
> · แต่ **ห้ามเอากฎเดานี้ไปคัดฝั่ง "ตัวเต็มที่จะเอามาทับ"** — พาดหัวเต็มที่ยาวจะถูกมองว่ายังถูกตัด
> แล้วโดนคัดทิ้งจนไม่เหลืออะไรให้ซ่อมเลย (เจอตอนเขียนเทสต์) ฝั่งนั้นต้องเช็คจุดไข่ปลาแบบเข้มเท่านั้น
> · มีธง `it.tfix` ติดไว้กับข่าวที่เช็คแล้ว ไม่งั้นข่าวที่ "พาดหัวถูกอยู่แล้ว" จะกินคิว 20 ใบทุกรอบ
>
> ⚠️ **Bing ตัดพาดหัวให้สั้น** ลงท้ายด้วย "…" ตัวเต็มไม่ได้อยู่ในฟีดเลย · `fillClippedTitles()`
> ไปอ่าน `og:title` จากหน้าข่าวจริง **ยิงไม่เกิน 8 ใบต่อ request และเฉพาะตอนทำงานเบื้องหลัง**
> ผลเก็บลง cache 7 วัน + ลงคลังข่าว จึงจ่ายครั้งเดียวต่อข่าว 1 ใบ · มีกฎกันเขียนทับผิด:
> ตัวใหม่ต้องยาวกว่า ไม่ถูกตัด และขึ้นต้นเหมือนเดิม ไม่งั้นจะได้ชื่อเว็บมาแทนพาดหัว
>
> ⚠️ **ลำดับต้องเป็น `mergeArchives` → `fillClippedTitles` → `saveArchives` เท่านั้น**
> เคยวางตัวเติมไว้ก่อน merge ผลคือแตะได้แต่ข่าวที่เพิ่งดึงมา **พาดหัวเก่าที่ค้างใน KV
> ไม่มีวันถูกเติมเลย** ผู้ใช้จึงยังเห็นพาดหัวขาดหลัง release · `mergeArchives` จึง
> **ไม่เขียน KV เอง** แต่คืนค่าให้ `saveArchives` เขียนทีเดียวหลังเติมเสร็จ
> (ยังเขียน KV ครั้งเดียวต่อ request เหมือนเดิม — เทสต์ `dupes.mjs` คุมทั้งลำดับและจำนวนครั้ง)
>
> ⚠️ **ข่าวใบเดียวเข้าได้หลายหัวข้อ** (เช่น CPF ปล่อยปลาหมอคางดำ) — ใส่ทุกหัวข้อคั่น `, `
> ไม่เลือกอันใดอันหนึ่ง เพราะจะทำให้อีกหัวข้อหายจากชีต · ตัวกรองในชีตใช้ "มีคำว่า..." ได้ตามปกติ
>
> ⚠️ **คลังข่าวเก็บ 90 วัน** ถ้าตัวตั้งเวลาในชีตหยุดวิ่งเกิน 90 วัน ช่วงที่ขาดจะหายถาวร
> ดึงย้อนหลังรอบละ 7 วัน (`DAYS`) เผื่อไว้เกินความถี่ที่ตั้ง — รอบไหนไม่วิ่งยังตามเก็บได้
>
> ⚠️ **ไม่ใส่ `topics=` = ได้ทุกข่าวเหมือนเดิม** ของเดิมที่ใช้ `=IMPORTDATA()` อยู่จึงไม่พัง
> (คอลัมน์ `หมวด` ต่อท้ายเป็นคอลัมน์ใหม่ ไม่ได้ไปสลับของเดิม)
