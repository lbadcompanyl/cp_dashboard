# CLAUDE.md

บันทึกสำหรับ session ถัดไป — อ่านก่อนเริ่มงานทุกครั้ง

## 🚦 Environment / Branch — สำคัญที่สุด อ่านก่อนแตะอะไร

Cloudflare Pages project = **`cp-dashboard-680`**

Git เชื่อมกับ `lbadcompanyl/cp_dashboard` · Automatic deployments: **Enabled**
→ **ทุก push ขึ้น production branch = deploy ขึ้นเว็บจริงทันที ไม่มีขั้นยืนยัน**

### เช็ค production branch ก่อนเสมอ — ห้ามเดา

```
Cloudflare → cp-dashboard → Settings → Build → Branch control → Production branch
```

ค่าที่ตั้งอยู่ตอนนี้คือ branch ไหน **branch นั้นคือ production** ไม่ว่ามันจะชื่ออะไร

> 📌 **ประวัติที่ต้องรู้:** เคยตั้งเป็น `claude/dashboard-google-alerts-news-0mw9bg`
> (branch งาน!) อยู่พักหนึ่ง ทำให้ push งานระหว่างทางขึ้นเว็บจริงทุกครั้งโดยไม่มีใครรู้
> เป็นต้นเหตุที่ debug endpoint หลุดขึ้น production 2 รอบ

### เป้าหมายที่ตกลงกันไว้

| branch | บทบาท | URL | ใครสั่งได้ |
|---|---|---|---|
| `claude/*` | branch งาน | preview URL อัตโนมัติ | Claude push เองได้ |
| `dev` | **staging** | `dev.cp-dashboard-680.pages.dev` | Claude push เองได้ |
| `main` | **production** | `cp-dashboard-680.pages.dev` | ❌ **เจ้าของสั่งเท่านั้น** |

ไหลของงาน: `claude/*` → `dev` (ดูบน staging) → **อนุมัติ** → `main` (ผู้ใช้เห็น)

> ⚠️ **อย่าสับสนกับ `trend-dashboard.pages.dev`** — อันนั้นคือโปรเจกต์ **ต้นทาง**
> ที่โค้ดหน้า 3 คอลัมน์ถูกยกมา (ดู `TREND-HANDOFF.md`) **ไม่ใช่แดชบอร์ดนี้**
> เคยเข้าใจผิดมาแล้ว ทำให้ยิง URL ผิดโปรเจกต์ทั้งหมด

### ✅ keyword ชุดใหม่ 74 คำ — ปล่อยขึ้น production แล้ว (จันทร์ 10 ส.ค. 2026)

กันไว้ตั้งแต่ 7 ส.ค. ตามที่เจ้าของสั่ง แล้วปล่อยพร้อมของอื่นทั้งหมดวันจันทร์
มีอะไรอยู่ในนั้น: `trend-feeds.config.js` (query ตัวเต็ม) · `functions/api/trend/feeds.js`
(`CACHE_VER` 50 · ตัดคำ `-ไม่เอา` · คำละตินต้องตรงทั้งคำ) · `HARD_KW` ใน `trend/app.js` + `issue/app.js`

> ⚠️ **ยังไม่ได้ยืนยันว่า query ใน Google Alerts ของจริงตรงกับใน config** — เครื่องที่รัน session
> ยิงเข้าเว็บไม่ได้ ต้องให้เจ้าของเปิดเทียบเอง **ถ้าไม่ตรง คอลัมน์จะหดลงเรื่อยๆ เงียบๆ**
> (ดูหัวข้อ 🔑 แก้ keyword ของ Google Alert)

### 📦 วิธีกันของบางอย่างไม่ให้ขึ้น production — ถ้าต้องทำอีก

**ห้ามใช้วิธี "merge แล้วค่อย revert"** — เคยทำ 2 รอบแล้วติดกับดัก: พอ main มี commit ที่ revert
ทับไว้ ครั้งต่อไป git จะมองว่า "main แก้ dev ไม่ได้แก้" แล้ว **เก็บ revert ไว้เงียบๆ**
ของใหม่เลยไม่ขึ้นทั้งที่ merge ผ่าน

✅ วิธีที่ถูก: **reset `dev` ให้เท่า `main` แล้ววางของที่กันไว้เป็น commit บนสุด**
งานระหว่างสัปดาห์แทรกไว้ "ใต้" commit นั้นเสมอ (`reset` แล้ว `cherry-pick` ของที่กันไว้กลับขึ้นบน)
· ให้ commit ที่กันไว้ใช้ **เลขเวอร์ชันสูงๆ** (เช่น 100 / 50) จะได้ไม่ต้องไล่แก้เลขทุกครั้งที่ cherry-pick

### กฎเหล็ก

1. **ห้าม push ขึ้น production โดยไม่ได้รับคำสั่งชัดเจน** — เคยพลาดมาแล้ว 2 ครั้ง
   การ "บอกทีหลัง" หรือ "หมายเหตุไว้ท้ายข้อความ" ไม่นับว่าขออนุญาต
2. **โค้ดทดลอง / debug endpoint ห้ามขึ้น production** — ลบทิ้งก่อน merge ทุกครั้ง
3. งานพัฒนาอยู่บน branch งาน (`claude/...`) → ทดสอบบน staging → ขึ้น production เมื่อได้รับอนุมัติ
4. ถ้าไม่แน่ใจว่า branch ไหนคือ environment ไหน — **ถาม อย่าเดา**

## 💬 วิธีคุยกับเจ้าของ — อ่านก่อนพิมพ์ตอบทุกครั้ง

เจ้าของบอกตรงๆ แล้วว่า **อ่านที่ตอบมาไม่เข้าใจ** ให้ทำตามนี้

### 1. ตอบคำถามที่ถามก่อน ในบรรทัดแรก

ถามว่า "ใช้ได้ไหม" → ตอบ **ได้ / ไม่ได้ / ยังไม่รู้** ก่อนอย่างอื่น
ห้ามเริ่มด้วยคำอธิบายว่าทำไม แล้วค่อยไปเฉลยตอนท้าย

| ❌ อย่าเขียนแบบนี้ | ✅ เขียนแบบนี้ |
|---|---|
| "Cloudflare Worker ออกเน็ตจาก IP ที่ใช้ร่วมกัน Google Trends จึงตอบ 429…" | "**ใช้ไม่ได้ครับ** เพราะ Google บล็อกเซิร์ฟเวอร์เรา" |

### 2. ใช้ภาษาคนธรรมดา ไม่ใช่ศัพท์ในโค้ด

ชื่อฟังก์ชัน ชื่อไฟล์ เลข error พวกนี้เป็นภาษาของโค้ด **ไม่ใช่ภาษาที่ใช้คุย**
ถ้าจำเป็นต้องใส่ ให้ใส่ในวงเล็บท้ายประโยคที่อธิบายเป็นภาษาคนแล้ว

| ❌ | ✅ |
|---|---|
| "`pruneStaleMerged()` มองว่าไม่ match แล้วจึงลบทิ้ง" | "ระบบเข้าใจว่าข่าวนั้นไม่เกี่ยวแล้ว เลยลบทิ้งเอง" |
| "`empty` flag ต้องเป็น false" | "ต้องไม่สรุปว่าไม่มีข้อมูล ทั้งที่ยังไม่รู้" |

### 3. สั้น

ตอบปกติ **ไม่เกิน 10 บรรทัด** · ตารางยาวๆ กับหัวข้อซ้อนหัวข้อเก็บไว้ใช้เฉพาะตอนสรุปงานใหญ่
รายละเอียดทางเทคนิคที่เจ้าของไม่ได้ถาม → เขียนลง commit message หรือ CLAUDE.md แทน

### 4. บอกให้ชัดว่าต้องทำอะไรต่อ

จบด้วยสิ่งที่เจ้าของต้องทำ (ถ้ามี) เป็นข้อๆ สั้นๆ · ถ้าไม่ต้องทำอะไร ก็บอกว่าไม่ต้องทำอะไร

### 5. แยกให้ชัดว่า "ทดสอบแล้ว" กับ "ยังไม่ได้ทดสอบ"

เครื่องที่รัน session ยิงเน็ตออกไปข้างนอกไม่ได้หลายที่ (Google Trends, gstatic, docs ของ Google)
**อย่าพูดเหมือนของที่ยังไม่เคยลองว่ามันใช้ได้** ให้บอกตรงๆ ว่า "ยังไม่ได้ลองของจริง ต้องรบกวนเปิดดู"

## 📦 โครงสร้างโปรเจกต์

```
index.html      landing — การ์ดลิงก์ทุกแดชบอร์ด
trend/          PR Trend Dashboard — Google Alerts + Google Trends   ← งานฝั่ง PR
ir/             IR News Monitor — ข่าวนักลงทุนสัมพันธ์
issue/          Issue Dashboard — CP + หัวข้อที่จับตามอง
admin/          หน้าจัดการ — 🚩 คำแนะนำตัดข่าว + ประกอบ query ของ keyword (ไม่มีลิงก์จาก landing)
sd.html         SD Trends — ความยั่งยืน
trends.html     Trends Explorer (เก่า) — ไม่มีลิงก์จาก landing แล้ว
functions/api/  Cloudflare Pages Functions (trend / ir / sd / flags)
```

### 🛠 หน้า `/admin/` — ทำไมเป็น path ไม่ใช่ subdomain

เจ้าของขอ `back.cp-dashboard-680.pages.dev` กับ `back.dev.cp-dashboard-680.pages.dev`
**ทั้งสองอันทำไม่ได้** — `*.pages.dev` ให้ subdomain ได้ชั้นเดียว และชื่อนั้นผูกกับ **ชื่อ branch**
(`dev.cp-dashboard-680.pages.dev` = branch ชื่อ `dev`) · ซ้อนสองชั้นอย่าง `back.dev.<project>`
ทั้งชื่อและใบรับรอง https ไม่รองรับเลย · ถ้าอยากได้โดเมนแยกจริงต้องใช้ **โดเมนของตัวเอง** (Custom domain)

ที่ใช้อยู่จึงเป็น `cp-dashboard-680.pages.dev/admin/` และ `dev.cp-dashboard-680.pages.dev/admin/`

- **✂️ ข่าวที่ระบบตัดทิ้ง** — เอา `alertVerify.dropped` + `swept.dropped` จาก response ของ API
  มาแสดง จัดกลุ่มตามเหตุผล (กลุ่มที่ตัดเยอะสุดขึ้นก่อน) แปลรหัสเป็นภาษาคนด้วยตาราง `WHY_TH`
  · **ไม่ได้ยิง API เพิ่ม ไม่แตะ KV** — ข้อมูลติดมากับ response ที่แดชบอร์ดใช้อยู่แล้ว
  · **↩ ข่าวที่เอากลับ** = สรุปว่ากฎไหนถูกกดคืนบ่อย ถ้าเกิน 3 ครั้งจะเตือนว่ากฎกว้างเกินไป
    ⚠️ **ตัวที่ตัดข่าวเป็นกฎตายตัว ไม่ใช่ AI** (AI ในโปรเจกต์นี้ใช้ "จัดหมวด" อย่างเดียว)
    กดเอากลับจึงไม่ได้สอนอะไรระบบ — แก้ได้เฉพาะข่าวใบนั้น · ทางแก้จริงคือดูสถิติแล้วไปแก้กฎ
  · **เพิ่มเหตุผลใหม่ใน `noiseReason()` เมื่อไหร่ ต้องเติม `WHY_TH` ใน `admin/app.js` ด้วย**
    ไม่งั้นจะขึ้นเป็นรหัสดิบ (`vendor` / `false-cp`) ให้เจ้าของอ่าน — เทสต์ `dropview.mjs` คุมไว้
- **✂️ ข่าวที่ระบบตัดทิ้ง + ปุ่ม ↩ เอากลับ** — เอา `alertVerify.dropped` + `swept.dropped`
  จาก response ของ API มาแสดง จัดกลุ่มตามเหตุผล แปลรหัสเป็นภาษาคนด้วยตาราง `WHY_TH`
  · **ไม่ได้ยิง API เพิ่มตอนแสดงผล** — ข้อมูลติดมากับ response ที่แดชบอร์ดใช้อยู่แล้ว
  · กด ↩ = POST ไป `/api/allow` เก็บลิงก์ลง KV (blob เดียว `noise:allow` เพดาน 500 รายการ)
    แล้ว `feeds.js` ทั้ง trend และ ir จะ **ข้ามทุกด่านตัด** ให้ข่าวใบนั้น
  · ⚠️ **`ALLOWED` เป็นตัวแปรระดับโมดูล ต้องตั้งค่าใหม่ทุกครั้งที่ build** — Workers ใช้โมดูล
    เดิมซ้ำข้าม request ถ้าไม่ตั้งใหม่จะค้างรายชื่อของ request ก่อนหน้า
  · ⚠️ **ไม่เห็นผลทันที** ต้องรอ cache ของ feed หมดอายุก่อน (~1 ชม. หรือกด `?rebuild`)
  · **เพิ่มเหตุผลใหม่ใน `noiseReason()` เมื่อไหร่ ต้องเติม `WHY_TH` ใน `admin/app.js` ด้วย**
    ไม่งั้นจะขึ้นเป็นรหัสดิบให้เจ้าของอ่าน — เทสต์ `back.mjs` กับ `allow.mjs` คุมไว้
- **ไม่มีการ์ดบน landing โดยตั้งใจ** — เป็นเครื่องมือของเจ้าของ ไม่ใช่แดชบอร์ดให้คนทั่วไปดู
- ⚠️ **ไม่มีระบบล็อกอิน** ใครรู้ลิงก์ก็เปิดได้ (มี `noindex` กัน Google เท่านั้น)
  ถ้าจะจำกัดสิทธิ์จริงต้องตั้ง **Cloudflare Access** หน้า path นี้
- โค้ดทำงานจริงอยู่ใน `flags.js` ไฟล์เดียวกับที่แดชบอร์ดใช้ — สั่งด้วย `ui:` ตอน `Flags.init()`
  `"fab"` ปุ่มลอยทั้ง 2 ปุ่ม (ของเดิม) · `"kw"` เหลือแต่ ➕ เพิ่ม keyword ← **แดชบอร์ดใช้อันนี้**
  · `"none"` ไม่มีปุ่มลอยเลย · `"admin"` กางในหน้า
- ⚠️ **กล่อง keyword ถูกผ่าครึ่งโดยตั้งใจ** — `openKw()` ดูจากโหมด admin แล้วเรนเดอร์คนละชุด

  | | แดชบอร์ด (ปุ่ม ➕) | หน้า admin |
  |---|---|---|
  | ช่องพิมพ์คำ + ชิพคำที่เก็บไว้ | ✅ | ชิพอย่างเดียว |
  | คำค้นที่ประกอบได้ · 📋 คัดลอก · ลิงก์ Google Alerts · ล้างคำทั้งหมด | ❌ | ✅ |

  เหตุผล: ตอนอ่านข่าวนึกคำได้ก็พิมพ์เก็บทันที ส่วนการเอาไปแปะใน Alert เป็นงานที่ทำทีเดียวจบ
  **เอาครึ่งไหนออกจากที่ไหนอีก ต้องแก้ทั้ง `openKw()` และเทสต์ `admin.mjs` คู่กัน**
- **ปุ่ม ⚑ กับ 🗂 บนการ์ดยังอยู่บนแดชบอร์ด** — เป็นทางเดียวที่ข่าวจะเข้ากอง "คำแนะนำตัดข่าว"
  ถอดออกเมื่อไหร่ = ฟีเจอร์ตายทั้งอัน
- หน้า admin ไม่มี `.panel` ให้ `flags.js` อ่านชื่อคอลัมน์เอง จึงมีตาราง `SCOPES` ใน `admin/app.js`
  **เปลี่ยนชื่อคอลัมน์ alert ในแดชบอร์ดไหน ต้องมาแก้ที่นี่ด้วย** ไม่งั้นชื่อบน admin จะค้างของเก่า
- flag ของแต่ละแดชบอร์ดเก็บคนละกอง: PR = `flags:pr` · IR = `flags:ir` · **Issue = `flags:root`**
  (`deriveScope()` ดูจาก path — `/issue/` ไม่เข้าเงื่อนไขไหนเลยจึงตกเป็น `root`)

> ⚠️ **ห้ามให้กล่องบนหน้า admin ชิงโฟกัสช่องพิมพ์เอง** — `refresh()` มีกฎว่า
> "ถ้ากำลังพิมพ์อยู่ในกล่องไหน อย่าวาดทับกล่องนั้น" กล่องที่กางค้างแล้ว auto-focus
> จะติดกฎนี้ตลอดเวลา ค่าที่ sync มาจาก KV เลยไม่ขึ้นสักที **ดูเหมือน sync พัง ทั้งที่ sync ถูก**

เป็น static + Pages Functions ไม่มีขั้นตอน build

## ✅ ฟีเจอร์มาตรฐาน — ทุกแดชบอร์ดต้องมีครบ

**รวมถึงแดชบอร์ดใหม่ที่จะสร้างต่อจากนี้ด้วย** ทำไม่ครบถือว่ายังไม่เสร็จ

| # | ฟีเจอร์ | ทำยังไง |
|---|---|---|
| 1 | **favicon** | `<link rel="icon">` emoji ให้ตรงกับไอคอนการ์ดบน landing |
| 2 | **ติดตั้งเป็นแอปได้ (PWA)** | `manifest.webmanifest` + `sw.js` + `apple-touch-icon` |
| 3 | **เช็คเวอร์ชันใหม่อัตโนมัติ** | `<meta name="page-ver">` + สคริปต์เทียบเลข (ก๊อปจาก `sd.html`) |
| 4 | **ปุ่มกลับหน้าหลัก** | `<a href="/">🏠 หน้าหลัก</a>` ใน appbar |
| 5 | **รีเฟรชข้อมูลเงียบ** | `setInterval` ทุก 3 นาที (ถ้าหน้านั้นมีข้อมูลสด) |
| 6 | **ไอคอนหมุนตอนรอโหลด** | `<span class="spin"></span>` หน้าข้อความ ทุกจุดที่แปลว่า "ยังโหลดอยู่" |

> ⚠️ ข้อ 6: **หน้าใหม่/ข้อความรอใหม่ ต้องมีไอคอนหมุนด้วยเสมอ** — ข้อความเปล่าๆ อ่านแล้วเหมือนหน้าค้าง
> `/trend/` `/ir/` `/issue/` ใช้ `WAITING` ที่ประกาศไว้บนสุดของ `app.js` (แก้ที่เดียวได้ทุกจุด)
> · `sd.html` มีของตัวเองเพราะเป็นไฟล์เดี่ยว **และมี 2 โหมด** สีจึงต้องเป็น `currentColor` ห้ามฮาร์ดโค้ด
> · ห้ามใส่กับข้อความที่ไม่ใช่สถานะรอ (เช่น "ไม่พบรายการที่ตรงกับตัวกรอง") — หมุนค้างโดยไม่มีอะไรมา

> ⚠️ ข้อ 3: ต้อง bump **ทั้ง `<meta name="page-ver">` และ `var VER`** คู่กันทุกครั้งที่แก้ไฟล์
> ไม่งั้นเครื่องที่ติดตั้งเป็นแอปจะค้างหน้าเก่า (เคยเกิดกับการ์ด Issue มาแล้ว)

### 🔢 ป้ายเลขเวอร์ชัน — ใช้ตรวจว่าเครื่องได้ของใหม่หรือยัง

ทุกหน้ามี `<span id="vtag">` แสดงผลเป็น **`v34 · sw4`**

- `v` = เวอร์ชันของหน้า (อ่านจาก `<meta name="page-ver">` ถ้าไม่มีก็อ่าน `?v=` ของ `app.js`)
- `sw` = เวอร์ชัน service worker ที่ทำงานอยู่ในเครื่องจริง (หน้าถาม sw ตรงๆ)

> เอาไว้เลิกเดาว่า "แอปอัปเดตหรือยัง" — ให้ผู้ใช้อ่านเลขให้ฟังแล้วเทียบกับ
> ที่อยู่บน production ได้เลย เคยเสียเวลาไล่กันหลายรอบเพราะไม่มีตัวนี้

## 🧪 เช็คลิสต์ก่อนบอกว่า "เสร็จ" — ทำทุกครั้งที่มีของใหม่

**ยังไม่เสร็จจนกว่าจะตอบครบทั้ง 4 ข้อ** เคยพลาดเพราะทำเสร็จบนเดสก์ท็อปแล้วคิดว่าจบ

| # | ต้องเช็ค | ถามตัวเองว่า |
|---|---|---|
| 1 | **ทุกแดชบอร์ด** | ของใหม่นี้ต้องมีที่ `trend/` `ir/` `issue/` `sd.html` และ landing ด้วยไหม? ถ้าเป็นฟีเจอร์พื้นฐาน = ต้องมีครบทุกที่ · ถ้าเป็นของเฉพาะทาง = **เขียนไว้ว่าทำไมถึงอยู่แค่หน้านั้น** |
| 2 | **แดชบอร์ดที่เพิ่งเพิ่มเข้ามา** | ถ้ารอบนี้สร้างแดชบอร์ดใหม่ มันได้ **ฟีเจอร์มาตรฐานทั้ง 5 ข้อ** ครบไหม? และมี **การ์ดบน landing** แล้วหรือยัง? แดชบอร์ดใหม่ต้องเท่าเทียมของเดิมตั้งแต่วันแรก ไม่ใช่ค่อยตามเก็บทีหลัง |
| 3 | **มือถือ** | จอแคบพังไหม? `/trend/` `/ir/` `/issue/` เป็น carousel ปัดซ้ายขวา ไม่ใช่กริด — ของที่กางออกมาในกริดจะดันจนหน้าพัง (เคยเกิดกับกล่องข่าวในคอลัมน์ X มาแล้ว) |
| 4 | **แอปที่ติดตั้งแล้ว** | เครื่องที่ติดตั้งเป็นแอปจะได้ของใหม่ไหม? bump `page-ver` + `?v=` ครบหรือยัง? |

### 🔁 ทำทุกครั้งหลังเขียนโค้ดเสร็จ ก่อนบอกผู้ใช้ว่าเสร็จ

**อย่ารีบบอกว่าเสร็จ** ให้ไล่ตรวจตัวเองก่อนเสมอ — ที่ผ่านมาบั๊กร้ายแรงที่สุด
(KV เขียน 50 ครั้ง/รอบ จนจะทำโควตาหมดทั้งโปรเจกต์) เจอตอนไล่ตรวจย้อนหลัง
ไม่ใช่ตอนเขียน

1. ตอบเช็คลิสต์ 4 ข้อข้างบนให้ครบ
2. **นับ KV read/write ต่อ 1 request** — เกิน 1-2 ครั้งถือว่าผิด ต้องแก้
3. รันตรวจ syntax + ทดสอบตรรกะด้วยข้อมูลจำลอง อย่าเดาว่าโค้ดถูก
   - **syntax ผ่าน ≠ ทำงานถูก** เคยใส่สคริปต์ไว้ใน `<head>` แล้วให้มันหา element
     ที่อยู่ท้าย `<body>` — ผ่าน syntax แต่ได้ `null` ทุกครั้ง ไม่ทำงานเลย
   - **ตรวจ harness ที่ใช้ทดสอบด้วย** Node รุ่นใหม่มี `navigator` เป็น global
     ที่เขียนทับตรงๆ ไม่ได้ ต้องใช้ `Object.defineProperty` ไม่งั้นผลทดสอบหลอก
4. ถามว่า "ถ้าของนี้พัง จะพังแค่ตรงนี้ หรือลามไปทั้งระบบ?" — ถ้าลาม ต้องมีตัวกัน
5. เขียนสิ่งที่เจอ/แก้ ลงข้อความที่ตอบผู้ใช้ **อย่าซ่อนความเสี่ยงที่รู้อยู่**

### กฎเรื่อง KV — สำคัญมาก

`FLAGS_KV` ใช้ร่วมกันทั้งโปรเจกต์ (flag · archive ข่าว · related · หมวด X)
แผนฟรีเขียนได้ **1,000 ครั้ง/วัน** — ถ้า endpoint ไหนเขียนเกิน 1-2 ครั้งต่อ request
**โควตาจะหมดแล้วพังทั้งระบบ**

> ❌ อย่าเขียนแยก key ต่อรายการ (เคยเขียนไว้ 50 ครั้ง/รอบ)
> ✅ รวมเป็น blob เดียวแล้วเขียนครั้งเดียว — ดูตัวอย่างที่ `xtrends.js` (`xcatmap`)

### ⚠️ สถานะที่ผู้ใช้สร้าง ห้ามเก็บไว้ใน DOM อย่างเดียว

`renderAll()` ทำงานทุก 3 นาทีจาก auto-refresh และสร้าง `innerHTML` ใหม่ทั้งก้อน
อะไรที่ผู้ใช้กดเปิด/เลือกไว้แล้วเก็บอยู่ใน DOM เท่านั้น **จะหายทุกรอบ**

> เกิดขึ้นจริง: กางดูรายละเอียดเทรนด์ค้างไว้ → auto-refresh → ยุบหมด → ลิสต์สั้นลง
> → ค่า scroll ที่คืนกลับเกินความสูงใหม่ → **หน้าเด้งกลับขึ้นบนกลางที่กำลังอ่าน**
> แก้ด้วยการจำไว้ใน `state.trendOpen` แล้วกางคืนหลัง render (ดู `openTrend`)

เวลาเพิ่มอะไรที่ "กดแล้วเปลี่ยนสภาพ" ให้ถามตัวเองว่า **รอด renderAll() ไหม**

### 🗂 การจัดหมวดข่าว — PR ใช้แบบเดียวกับ IR แล้ว

ลำดับความน่าเชื่อ (เหมือนกันทั้ง `ir/` และ `trend/`):
**ผู้ใช้กดแก้เอง > ที่ AI เคยจัดไว้ > คำค้นแบบมั่นใจ > ให้ AI อ่านพาดหัว**

- ปุ่ม 🗂 บนการ์ดมาจาก `flags.js` — **ต้องส่ง `cats` ตอน `Flags.init()` ไม่งั้นปุ่มไม่ขึ้นเลย**
  (PR ไม่ได้ส่งอยู่นาน ปุ่มเลยหายไปทั้งหน้าโดยไม่มี error อะไรบอก)
- ผู้ใช้แก้หมวดเก็บใน `flags:pr` / `flags:ir` และถูกส่งกลับเป็นตัวอย่างสอน AI รอบถัดไป
- **คำกำกวมห้ามตัดสินคนเดียว** (`AMBIG_KW`) — "หมู" ชนชื่อนักสนุกเกอร์ · "เกษตร" ชนชื่อมหาวิทยาลัย
  ต้องมีคำบริบทที่ไม่กำกวมอย่างน้อย 1 คำถึงจะเชื่อ ไม่งั้นส่งให้ AI ตัดสิน
- คอลัมน์ X (`xtrends.js`) ใช้คนละชุดหมวด และมี `NEED_EVIDENCE` — ถ้า AI ตอบ pol/news/sport
  แต่ในแท็กไม่มีคำของหมวดนั้นเลย **ไม่เชื่อ** (โมเดลเล็กชอบเดาชื่อคนไทยเป็นการเมือง)

> ⚠️ ตอนนี้โค้ดจัดหมวดมี 2 ชุด (`ir/feeds.js` กับ `trend/feeds.js`) เนื้อหาเกือบเหมือนกัน
> แก้ที่หนึ่งแล้วอีกที่ไม่ตาม — ถ้าจะแก้ตรรกะควรรวมเป็นไลบรารีเดียวก่อน

### 🔑 แก้ keyword ของ Google Alert — ต้องแก้ที่ `query` ใน config ด้วย

`feeds.js` แกะ keyword จาก `<title>` ของฟีด Alert เอง (`Google Alert - <query>`)
แต่ **Google ตัด title ให้สั้นเมื่อ query ยาว** keyword ท้ายๆ จะหายไปเงียบๆ ไม่มี error

ผลที่ตามมามี 2 ชั้น และชั้นที่สองอันตรายกว่า:
1. ข่าวจากคอลัมน์ News ที่ตรงกับคำท้ายๆ **ไม่ถูกดึงเข้าคอลัมน์ alert**
2. `pruneStaleMerged()` มองว่าข่าวที่เคยดึงเข้ามา "ไม่ match แล้ว" จึง **ลบทิ้ง**
   → คอลัมน์หดลงเรื่อยๆ โดยดูเหมือนไม่มีอะไรผิด

> ✅ ทางแก้: เขียน query ตัวเต็มไว้ที่ field `query` ของฟีดใน `trend-feeds.config.js`
> `feeds.js` จะเทียบว่าอันไหนได้คำมากกว่า (ฟีด vs config) แล้วยึดอันนั้น
> **แก้ alert ใน Google Alerts เมื่อไหร่ ต้องมาแก้ `query` ที่นี่ด้วยทุกครั้ง**

เวลาเพิ่มคำใหม่ ให้ระวัง 2 อย่าง:

- **คำอังกฤษสั้นๆ ชนกลางคำ** — `SLAPP` (คดีฟ้องปิดปาก) ไปจับ `slapped` ในพาดหัวอังกฤษ
  ซึ่งเจอบ่อยมาก (`slapped with a fine` / `slapped tariffs`) แก้แล้วด้วย `termPattern()`
  คำที่เป็นอักษรละตินล้วนต้องตรงทั้งคำ · คำไทยยังเทียบแบบ substring (ไม่มีช่องว่างคั่นคำ)
- **คำที่สั่งไม่เอา (`-shopee`)** อยู่ท้าย query ต่อจากคำสุดท้าย ถ้าไม่ถอดออกก่อนแยกด้วย `OR`
  คำสุดท้ายจะกลายเป็น `wastewater discharge -linkedin -jobdb …` ก้อนเดียวที่ไม่มีวัน match
  ตอนนี้ `parseAlertTerms()` ถอดให้แล้ว และ `parseAlertExcludes()` เอาไปกรองข่าวที่จะดึงเข้ามาด้วย

### ➕ `extraTerms` — คำที่ใส่ใน Google Alerts ไม่ได้แล้ว

ช่อง query ของ Google Alerts มีเพดานความยาว (~2,000 ตัวอักษร) ตอนนี้ 74 คำกินไปเกือบเต็ม
เติมอีกไม่ได้ (ตอนนี้มี 5 คำ: `สัตว์ต่างถิ่น` · `เอเลียนสปีชีส์` · `เอเลี่ยน สปีชีส์` · `alien species` ·
`invasive species`) · จึงมี field `extraTerms` ในฟีดของ `trend-feeds.config.js` สำหรับคำที่
**ไม่ได้อยู่ใน Google Alert แต่ใช้ดึงข่าวจากคอลัมน์ News เข้าคอลัมน์ alert**

| | `query` | `extraTerms` |
|---|---|---|
| ต้องตรงกับ Google Alerts | ✅ ตรงเป๊ะเสมอ | ❌ ไม่เกี่ยวกัน |
| ข่าวจาก Google Alert | ✅ | ❌ Google ไม่รู้จักคำพวกนี้ |
| ข่าวจากคอลัมน์ News (`mergeNewsIntoAlert`) | ✅ | ✅ |

> ⚠️ **ต้องเข้า `a2terms` ชุดเดียวกับที่ `pruneStaleMerged` ใช้** ไม่งั้นจะดึงเข้ามาแล้วลบทิ้งสลับกันทุกรอบ
> · ใส่ได้เฉพาะคำ **ไม่กำกวม** — "ต่างถิ่น" หรือ "รุกราน" เดี่ยวๆ จะดูดข่าวแรงงานต่างถิ่นกับข่าวสงครามเข้ามา
> เขียนเป็นคำประกอบเสมอ (`สัตว์ต่างถิ่น` · `ชนิดพันธุ์รุกราน`)
> · **แก้ `extraTerms` แล้วต้องแก้ `HARD_KW` ทั้ง `trend/app.js` และ `issue/app.js` ด้วย** (กฎหน้าต่าง 🔤 ข้างล่าง)
> และถ้าคำนั้นอยู่ในหัวข้อของชีต ต้องแก้ `TOPICS` ใน `archive.js` ให้ตรงกันอีกที่
> — เทสต์ `alien.mjs` คุมทั้ง 4 ที่ให้ตรงกันแล้ว

### 🔤 หน้าต่าง "ดู keyword" — ต้องอัปเดตทุกครั้งที่ keyword เปลี่ยน

**เจ้าของสั่งเป็นกฎถาวร (10 ส.ค. 2026):** แก้ keyword ที่ไหนเมื่อไหร่ ต้องทำให้หน้าต่าง
🔤 ดู keyword ของ **ทุกแดชบอร์ด** ตรงตามนั้นด้วยเสมอ ไม่ใช่แก้แต่ตัวกรองแล้วปล่อยหน้าจอค้างของเก่า

- ปุ่ม 🔤 อยู่ที่ **ทุกคอลัมน์ Alert ยกเว้นคอลัมน์ CP (`alert1`)** — `injectKwButtons()` ใน `flags.js`
  (เจ้าของสั่งไม่ต้องขึ้นที่ CP — query มีแค่ `"cp"` กับ `"ซีพี"` ไม่กี่คำ เปิดดูแล้วไม่ได้อะไร)
- `applyKeywords()` ใน `trend/ir/issue app.js` ไล่ทุกคอลัมน์ Alert ที่มีบนหน้า ไม่ใช่แค่คีย์ที่เขียนใน `HARD_KW`
- เลือกฝั่งที่ **ได้คำมากกว่า** ระหว่าง query สดจากฟีด กับ `HARD_KW` ที่ฝังไว้ในโค้ด
  (Google ตัด title ให้สั้นเมื่อ query ยาว ถ้ายึดของสดอย่างเดียวคำท้ายๆ จะหายเงียบๆ)
- เทสต์ที่คุมไว้: `kwpanel.mjs` — เปิดจริงทุกแดชบอร์ด กดปุ่มทุกคอลัมน์ แล้วนับคำ

> ⚠️ `ir/feeds.js` เคยเป็นโค้ดชุดเดียวกันแต่ยังไม่ได้แก้ — **ยกมาให้แล้ว (10 ส.ค.)**
> ตอนนี้ทั้ง trend และ ir ใช้ `termPattern()` เหมือนกัน (คำละตินต้องตรงทั้งคำ)
> ต้นเหตุที่ต้องยก: `rcep` ไปจับ `inte(rcep)t` — ข่าว F-16s intercept ของ Al Jazeera
> หลุดเข้าคอลัมน์การค้าของ IR **บทเรียนเดิมกับ SLAPP → slapped เป๊ะๆ แค่คนละคำ**

> (ของเดิม) ⚠️ `ir/feeds.js` มีโค้ดชุดเดียวกันแต่ **ยังไม่ได้แก้** — ตอนนี้ alert ของ IR ไม่มีคำ `-ไม่เอา`
> เลยยังไม่เจอปัญหา ถ้าวันไหนใส่ ต้องยกการแก้นี้ไปด้วย (ดูหนี้ทางเทคนิคเรื่องโค้ดซ้ำ)

### กฎเรื่อง cache — แก้โค้ดแล้วผู้ใช้ยังเห็นของเก่า

endpoint ที่เก็บผลลง KV + edge cache ต้องมี **เลขเวอร์ชันของข้อมูล** คั่นใน key
(`DATA_VER` ใน `yttrends.js` · `CACHE_VER` ใน `feeds.js`)

> ⚠️ **แก้โครงข้อมูลแล้วต้องบวกเลขนี้ทุกครั้ง** ไม่งั้นของเก่าจะถูกเสิร์ฟต่อจนกว่า cache จะหมดอายุ
> เกิดขึ้นจริง: แก้วิธีเรียง + เพิ่มธง `live` ของคอลัมน์ YouTube แล้ว deploy
> แต่ผู้ใช้ยังเห็นลำดับเก่าอยู่อีก 1 ชม. เพราะ KV ถือของเดิมไว้ **ดูเหมือนโค้ดไม่ทำงาน ทั้งที่ทำงานถูก**
> อาการสังเกต: ของใหม่ที่ควรเห็น (ป้าย/ฟิลด์ใหม่) ไม่ขึ้นเลยสักอัน

### YouTube API key — ใส่แล้ว ✅ (6 ส.ค. 2026)

`YT_API_KEY` อยู่ใน Cloudflare env แล้ว คอลัมน์ YouTube จึงได้ `source: youtube:api`
· `mode: official` = อันดับมาแรงทางการ · ยอดวิวจริง · เวลาอัปโหลดจริง · ธง live เชื่อได้

> ⚠️ **env ของ Pages แยก Production กับ Preview คนละชุด** ต้องใส่ทั้งสองที่
> ใส่แค่ Production แล้ว `dev` (Preview) จะไม่เห็น key เลย — เสียเวลาไล่กันมาแล้วรอบหนึ่ง
> และทุกครั้งที่แก้ env ต้อง **Retry deployment** ไม่งั้นค่าใหม่ไม่มีผล

### วิธีขอ YouTube API key (ฟรี) — เผื่อต้องสร้างใหม่

1. https://console.cloud.google.com/ → สร้าง project
2. APIs & Services → Library → เปิด **YouTube Data API v3**
3. Credentials → Create credentials → **API key**
4. Cloudflare → cp-dashboard → Settings → **Variables and Secrets** → เพิ่ม `YT_API_KEY`
   (ใส่เป็น **Secret** ไม่ใช่ plaintext)

> 🔑 **ห้ามส่ง key มาในแชทหรือ commit ลง repo** — repo เป็น public
> โค้ดอ่านจาก `env.YT_API_KEY` เอง ถ้าไม่มีก็ตกไปใช้ต้นทางสำรองตามเดิม ไม่พัง

### กฎเรื่อง `sw.js` — อันตรายกว่าไฟล์อื่น

`sw.js` ดักทุกการเปิดหน้าในโดเมน และมัน**ค้างอยู่ในเครื่องผู้ใช้** ถ้า push ตัวที่มีบั๊ก
ทั้งเว็บพังสำหรับคนที่ติดตั้งแอปไว้ และแก้ยากกว่าหน้าเว็บพังธรรมดา

0. **ห้ามให้ `sw.js` ดัก `fetch` แล้วเรียก `respondWith`** — ถ้ายิงเน็ตพลาดมันจะคืน
   `Response.error()` ซึ่งเบราว์เซอร์แปลว่า **ERR_FAILED = หน้าเปิดไม่ขึ้นเลย**
   เกิดขึ้นจริงกับ `/sd.html` (6 ส.ค. 2026) ตอนใช้ sw4
   ความสดของ HTML มาจาก `Cache-Control: no-cache` ใน `_headers` อยู่แล้ว
   **sw ไม่ต้องเข้ามายุ่ง — ยุ่งแล้วมีแต่ความเสี่ยง ไม่ได้อะไรเพิ่ม**
   ถ้าไม่มี `respondWith` เลย sw จะทำให้หน้าเว็บพังไม่ได้
1. แก้ `sw.js` ทีไร **ต้องทดสอบบน staging ก่อนเสมอ ห้ามข้าม**
2. ต้องบวก `SW_VERSION` ด้วย ไม่งั้นเบราว์เซอร์ไม่ติดตั้งตัวใหม่ (มันเทียบ byte)
3. ห้ามส่ง Request ที่ `mode=navigate` เข้า `fetch()` พร้อม init — สเปคจะลดระดับ
   mode เป็น `same-origin` เงียบๆ ให้ยิงด้วย `req.url` ตรงๆ แทน
4. ลงทะเบียนด้วย `{updateViaCache:"none"}` + เรียก `registration.update()` ทั้งตอน
   เปิดหน้าและตอน `visibilitychange` — ผู้ใช้จะได้ของใหม่เองโดยไม่ต้องทำอะไรเลย
   **ห้ามบอกผู้ใช้ให้ล้าง cache หรือถอนแอปติดตั้งใหม่** ถ้าต้องบอก แปลว่ากลไกยังผิด
5. **เช็คตอน "กลับเข้าแอป" ไม่ใช่ตั้งตัวจับเวลาถี่ๆ** — ตัวจับเวลากินทรัพยากรตอนที่
   ไม่มีใครดู ส่วนจังหวะที่ผู้ใช้กลับมาคือจังหวะเดียวที่การอัปเดตมีความหมาย
   ใช้ `visibilitychange` + กันกดสลับรัวๆ ด้วยระยะขั้นต่ำ 60 วินาที

> ⚠️ **หน้าที่ถูก resume ไม่ส่ง request อะไรเลย** — Android คืนภาพหน้าเดิมให้โดยไม่โหลดใหม่
> ไม่มีอะไรที่ deploy ไปจะไปถึงมันได้ จนกว่าจะมีการ navigate สักครั้ง
> วิธีปลุกที่เบาที่สุดคือ **กดเข้าแดชบอร์ดแล้วกด 🏠 กลับ** ไม่ใช่ถอนติดตั้ง

### ⚙️ กล่องตั้งค่ากลุ่มคำ (หน้าเทรนด์ของ `issue/trends.html` กับ `sd.html`)

เจ้าของสั่งให้เป็นแบบ **กดแล้วค่อยเปิด เฉพาะจอแคบ** (11 ส.ค. 2026) — บนมือถือกล่องนี้กินที่จนกราฟตกจอ
· ปิดไว้เป็นค่าตั้งต้น · หัวกล่องมีสรุปให้อ่านโดยไม่ต้องกาง (ชื่อกลุ่ม · จำนวนคำ · พื้นที่ · ช่วงเวลา)

> ⚠️ **เดสก์ท็อป (≥ 861px) ต้องกางไว้ตลอด ไม่มีปุ่มกด** — เจ้าของสั่งเพิ่มทีหลัง
> ทำด้วย CSS อย่างเดียว (`@media (min-width:861px)`) และ **ต้องทับ `[hidden]` ด้วย**
> ไม่งั้น UA stylesheet บังคับ `display:none` ชนะ · JS ยังจำสถานะไว้เหมือนเดิมสำหรับจอแคบ

- **จำสถานะเปิด/ปิดใน `localStorage`** (`issueTrendsOpen` / `sdTrendsOpen`) ไม่ใช่ใน DOM อย่างเดียว
- สรุปบนหัวกล่องอัปเดตใน `updateSubs()` — จุดเดียวที่ถูกเรียกทุกครั้งที่ค่าเปลี่ยน
- **สองไฟล์นี้เป็นโค้ดคนละชุดที่เนื้อหาเกือบเหมือนกัน แก้ที่หนึ่งต้องแก้อีกที่ด้วย**
  (`trends.html` ที่ root เป็นตัวเก่า ไม่มีลิงก์จาก landing แล้ว จึงไม่ได้ยกไปด้วย)
- เทสต์ `accordion.mjs` เปิดจริงทั้ง 2 หน้า ทั้งมือถือและเดสก์ท็อป

> ⚠️ **เทสต์ที่ไปกด `#geo` / `#period` ต้องกาง `#tbToggle` ก่อน** ไม่งั้นจะ timeout เพราะ element
> ถูกซ่อนอยู่ (เจอแล้วกับ `trendfix.mjs`)

**โหมดสว่าง/มืด — เจ้าของบอกว่าไม่ต้องทำ** มีแค่ `index.html` กับ `sd.html`
ส่วน `/trend/` `/ir/` `/issue/` เป็นโหมดมืดล้วน **อย่าไปเพิ่มเอง**

## 🖥️ วัดเลย์เอาต์ได้จริง อย่าเดาจาก CSS

เครื่องที่รัน session มี **Chromium + Playwright ติดตั้งไว้แล้ว** ใช้เรนเดอร์หน้าจริงแล้ววัดได้เลย
ไม่ต้องเดาว่า CSS จะออกมาเป็นยังไง

```bash
python3 -m http.server 8899 &          # เสิร์ฟไฟล์ static (API จะ error แต่เลย์เอาต์วัดได้)
# playwright อยู่ใน node_modules ของ scratchpad
# ต้องระบุ executablePath เอง: /opt/pw-browsers/chromium  (+ args ["--no-sandbox"])
```

สิ่งที่ควรวัดทุกครั้งที่แตะเลย์เอาต์มือถือ — เทียบกับ `innerWidth` / `innerHeight`:
`document.scrollingElement.scrollWidth` · ความกว้าง+ตำแหน่งซ้ายของลูก `body` ทุกตัว · `.board` bottom

> ⚠️ **มีแต่ Chromium เท่านั้น — WebKit ดาวน์โหลดไม่ได้** (`playwright install webkit` ล้มเพราะเน็ตขาออกถูกบล็อก)
> แปลว่าวัดได้จริงเฉพาะ **Chrome / Edge** (engine เดียวกัน) · **Safari กับทุกเบราว์เซอร์บน iOS
> เป็น WebKit ทดสอบที่นี่ไม่ได้ ต้องให้เจ้าของเปิดดูเอง** อย่าเคลมว่าเทสต์ Safari แล้ว
>
> สิ่งที่ยังพอทำได้เพื่อลดความเสี่ยงฝั่ง Safari: จำลองเบราว์เซอร์ที่ไม่รู้จักหน่วยใหม่
> ด้วย `page.addStyleTag` ทับค่าให้เป็นตัวสำรอง แล้ววัดว่าเลย์เอาต์ยังใช้ได้ (ดู `NO_DVH` ในชุดวัด)

> ⚠️ **กับดักที่เจอมาแล้ว: `margin: 0 auto` + `display: flex` ของ parent**
> พอเปลี่ยน `body` เป็น flex column ลูกที่มี `margin: 0 auto` (จากกฎเดสก์ท็อป) จะ **ไม่ยืดเต็มจอ**
> เพราะ auto margin บนแกนขวางทำให้ `stretch` ไม่ทำงาน ความกว้างกลายเป็นเท่าเนื้อหา/`max-width`
> ผลที่วัดได้จริง: แถบค้นหาเหลือ 231px · `.board` พองไป 1680px · **เบราว์เซอร์มือถือขยาย viewport
> ตามจนทั้งหน้าซูมออก (390 → 1560)** = หน้าตาพังทั้งหน้า
> แก้ด้วยการบังคับ `width: 100%; max-width: none; margin-left: 0; margin-right: 0` ให้ลูกทุกตัว

## 📱 แพลตฟอร์มที่ต้องคำนึงถึงทุกครั้ง

**ทุกอย่างที่เป็นฝั่งแอป (PWA / service worker / การอัปเดต / หน้าตาบนมือถือ)
ต้องคิดครบทุกช่องในตารางนี้เสมอ — ไม่ใช่เทสบนเดสก์ท็อปแล้วจบ**

|  | Chrome | Edge | Safari |
|---|:---:|:---:|:---:|
| **เดสก์ท็อป** | ✅ | ✅ | ✅ |
| **Android** | ✅ | ✅ | — |
| **iOS (iPhone/iPad)** | ✅ | ✅ | ✅ ← **ผู้ใช้ส่วนใหญ่อยู่ตรงนี้** |

Edge เป็น Chromium ตัวเดียวกับ Chrome — อะไรที่ผ่าน Chrome ก็ผ่าน Edge
ตัวที่ต้องระวังคือ **Safari/iOS** เพราะพฤติกรรมต่างจากตัวอื่น

### ข้อควรระวังของ Safari / iOS

- service worker ใช้ได้ตั้งแต่ **iOS 11.3** — ฟีเจอร์อัปเดตอัตโนมัติทำงานปกติ
- **iOS อาจล้าง service worker ทิ้ง** ถ้าไม่เปิดแอปเกิน ~7 วัน → พอเปิดใหม่
  มันลงทะเบียนเองอัตโนมัติ ไม่ต้องให้ผู้ใช้ทำอะไร
- **Safari โหมดส่วนตัวไม่มี service worker** → หน้าเว็บทำงานปกติทุกอย่าง
  แค่ไม่มีระบบอัปเดตอัตโนมัติ
  ⚠️ อย่าวางโค้ดที่ต้องทำงานเสมอไว้ใน `if("serviceWorker" in navigator){...}`
  (เคยพลาดกับป้ายเวอร์ชันมาแล้ว — Safari โหมดส่วนตัวไม่เห็นป้ายเลย)
- iOS ต้องมี `apple-touch-icon` + `apple-mobile-web-app-capable` ถึงจะติดตั้งสวย
- ⚠️ **Safari กับไอคอนบนหน้าจอโฮมอาจไม่แชร์ service worker กัน** (iOS แยก storage
  ของ home-screen app ออกจากตัวเบราว์เซอร์) ต่างจาก Android ที่ Chrome กับแอป
  ที่ติดตั้งใช้ registration เดียวกัน → บน iOS การ "เปิดใน Safari ให้อัปเดต"
  อาจไม่ช่วยแอปบนหน้าจอโฮม ต้องเปิดจากไอคอนเอง

### วิธีดันของใหม่เข้าเครื่องที่ค้าง (เรียงจากเบาไปหนัก)

| แพลตฟอร์ม | ทำอะไร |
|---|---|
| **Android** | เปิดเว็บใน Chrome ธรรมดา 1 ครั้ง — แอปที่ติดตั้งใช้ registration เดียวกัน จะได้ตามไปด้วย |
| **iOS** | เปิดจากไอคอนบนหน้าจอโฮม (iOS ปิดแอปจริงบ่อย เปิดใหม่มักเป็นการโหลดสด) |
| **ทั้งคู่ ถ้ายังค้าง** | ถอน/ลบไอคอน แล้วติดตั้งใหม่ 1 ครั้ง |

> ใช้ **ป้ายเลขเวอร์ชัน** ตรวจว่าสำเร็จหรือยัง ไม่ต้องเดา

## 🗂️ งานแต่ละแดชบอร์ดควรอยู่ที่ไหน

- **เทรนด์ทั่วไป** (# ไหนดัง, คนค้นอะไร ไม่ผูกกับ CP) → `trend/` (PR)
- **CP ถูกพูดถึงยังไง** (mentions, ประเด็นที่จับตา) → `issue/`
- ข่าวนักลงทุน → `ir/` · ความยั่งยืน → `sd.html`

## 🔍 สิ่งที่เคยทดสอบแล้ว — อย่าเสียเวลาลองซ้ำ

ยิงจาก Cloudflare Worker (5 ส.ค. 2026):

| แหล่ง | ผล |
|---|---|
| YouTube RSS | ✅ ใช้ได้ ฟรี ไม่ต้องมี key |
| getdaytrends.com/thailand/ | ✅ ได้เทรนด์ X ไทย (71KB, สะอาด) — **ตัวหลักที่เลือก** |
| trends24.in/thailand/ | ✅ ได้เหมือนกัน (305KB, ขยะเยอะกว่า) — ตัวสำรอง |
| TikTok Creative Center | ❌ `40101 no permission` ทั้งยิงเปล่าและใส่ header ครบ — ทางฟรีปิด |
| Facebook (www/mbasic/embed) | ❌ login wall / 400 / ได้แต่เปลือก — ต้องใช้ Meta API เท่านั้น |
| Nitter (net/poast) | ❌ 520 / bot check |
| Pantip (tag/search API) | ❌ 404 / 401 access denied |
| X syndication | ⚠️ ได้ข้อมูล แต่เป็นทวีตเก่าในโปรไฟล์ ไม่ใช่เทรนด์ |
| YouTube หน้า Trending | ❌ ปิดถาวร ก.ค. 2025 · YouTube Charts เหลือแค่เพลง/หนัง/เกม ใช้กับ PR ไม่ได้ |
| YouTube Data API | ⚠️ `chart=mostPopular` ยังมี แต่ต้องมี key + โควตา และตั้งแต่ ก.ค. 2025 เหลือ 3 หมวดเหมือนกัน |
| Invidious / Piped | ✅ คลิปมาแรงรายประเทศ เป็น JSON สะอาด ฟรี ไม่ต้องมี key — **ตัวหลักของคอลัมน์ YouTube** |

> ⚠️ **Invidious / Piped เป็น instance อาสาสมัคร ล่มบ่อยและมัก "ล่มแบบค้าง" ไม่ใช่ตอบ error**
> จึงต้อง **ยิงทุก instance พร้อมกันแล้วเอาอันที่ตอบก่อน** (`Promise.any`) ห้ามไล่ทีละตัว
> ไล่ทีละตัว 5 แหล่ง × timeout 7 วิ = ผู้ใช้รอ 35 วินาทีก่อนเห็นอะไร
> และ **คอลัมน์ที่พึ่งแหล่งแบบนี้ต้องโหลดแยก ไม่รวมใน `Promise.all` ของบอร์ด**
> ไม่งั้นต้นทางอืดทีเดียวค้างทั้งแดชบอร์ด (ดู `reloadYTTrends` ใน `trend/app.js`)

### 📰 โหมด ข่าว / ทั่วไป ของคอลัมน์ X กับ YouTube

ปุ่มสลับอยู่แถวเดียวกับ dropdown ประเทศ · **ข่าว = ตัด เกม เพลง บันเทิง กีฬา** (หนังกับคลิปตลกอยู่ในบันเทิง)

ค่าตั้งต้นต่างกันโดยตั้งใจ เพราะสองแหล่งดึงข้อมูลได้ไม่เหมือนกัน:

| | ตั้งต้น | ได้ข่าวมาจากไหน |
|---|---|---|
| **YouTube** | 📰 ข่าว | ขอชาร์ตหมวด **News & Politics** (`videoCategoryId=25`) จาก API โดยตรง = ชาร์ตข่าวจริงเต็ม 50 คลิป |
| **X** | 🌐 ทั่วไป | getdaytrends ให้ท็อป 50 มาชุดเดียว **ขอชาร์ตข่าวแยกไม่ได้** ต้องกรองจากหมวดที่จัดไว้ ซึ่งเหลือไม่กี่รายการ |

> ⚠️ `catFiltered` ใน response ของ `yttrends` บอกว่า **server กรองหมวดให้แล้วหรือยัง**
> ถ้า `false` (ไม่มี key / ประเทศนั้นไม่มีชาร์ตข่าว / ตกไปใช้ Invidious) หน้าเว็บต้องกรองด้วยคำเอง
> **ห้ามกรองซ้ำเมื่อ `catFiltered` เป็น true** — ข่าวจริงหลายคลิปไม่มีคำว่า "ข่าว" ในชื่อ จะหายไปเฉยๆ

### 🔎 คอลัมน์ "เช็ค Trend" — ใช้ตัดสินว่าคำไหนควรเอาเข้า Alert

`/api/trend/kwcheck?q=...&geo=TH&time=today+12-m` · อยู่ที่ `trend/` ที่เดียว (เป็นงานฝั่ง PR)

ข้อมูล 2 ชั้น ชั้นล่างไม่มีก็ไม่พัง:

| ชั้น | ได้อะไร | ดึงจากไหน |
|---|---|---|
| **Google Trends** | กราฟความสนใจ + คำที่เกี่ยวข้อง | **embed ในเบราว์เซอร์ผู้ใช้** |
| **Google Ads (Keyword Planner)** | ยอดค้นหาจริงต่อเดือน | Worker · ⏳ รอ developer token |

> 🚫 **เคยลองยิง Google Trends จาก Worker แล้วใช้ไม่ได้จริง — อย่าทำซ้ำ**
> `/trends/api/explore` + `/widgetdata/*` ตอบ **429 ทุกครั้ง** เพราะ Cloudflare Worker
> ออกเน็ตจาก IP ที่ใช้ร่วมกับคนทั้งโลก · ลองซ้ำ/หน่วงเวลาไม่ช่วย (ทดสอบจากการใช้จริง
> พิมพ์กี่คำก็ไม่ผ่านสักคำ) · `fetchKeywordCheck()` ยังอยู่ในโค้ดแต่ไม่มีใครเรียกจากหน้าเว็บแล้ว
>
> ✅ ทางที่ใช้ได้คือ **embed ทางการของ Google** (`ssl.gstatic.com/trends_nrtr/.../embed_loader.js`)
> เพราะทำงานในเบราว์เซอร์ของผู้ใช้ = ใช้โควตาของเครื่องผู้ใช้ ไม่ใช่ของ Cloudflare
> แลกกับการที่หน้าตาเป็น UI ของ Google (พื้นขาว) และจัดสไตล์เองไม่ได้
>
> ⚠️ `/api/trend/trending` ที่ใช้ในคอลัมน์ Google Trends **ไม่เจอปัญหานี้** เพราะเป็นคนละ endpoint
> (`batchexecute` RPC `i0OFE`) — อย่าเหมารวมว่า Trends ทั้งหมดยิงจาก Worker ไม่ได้

**ยืนยันด้วยการยิงจริงแล้ว (7 ส.ค. 2026):**
`POST googleads.googleapis.com/v21/customers/{id}:generateKeywordHistoricalMetrics` → `401 UNAUTHENTICATED`
= endpoint มีจริง เรียกผ่าน **REST ได้ ไม่ต้องใช้ gRPC/SDK** → Worker ยิงตรงได้
· `v20` ใช้ได้ · **`v19` ลงไปตอบ 404 = ปลดระวางแล้ว**

env ที่ต้องใส่ (Cloudflare **Secret** เท่านั้น — repo เป็น public):
`GADS_DEVELOPER_TOKEN` `GADS_CUSTOMER_ID` `GADS_CLIENT_ID` `GADS_CLIENT_SECRET` `GADS_REFRESH_TOKEN`
(+ `GADS_LOGIN_CUSTOMER_ID` ถ้าบัญชีอยู่ใต้ Manager) — ขาดตัวไหนก็ได้ ระบบจะบอกเองว่าขาดอะไร

> ⚠️ **ตัวเลขที่ได้อาจเป็นช่วง ไม่ใช่เลขเป๊ะ** — Keyword Planner ให้เลขละเอียดเฉพาะบัญชี
> ที่มียอดใช้จ่ายโฆษณาจริง บัญชีที่ไม่ยิงแอดจะได้เป็นช่วงกว้าง (เช่น 1K–10K) ทั้งบนเว็บและผ่าน API
>
> ⚠️ `geoTargetConstants` / `languageConstants` เป็น **criteria ID ตัวเลข** ไม่ใช่ชื่อประเทศ/ภาษา
> ในโค้ดใส่ไทย = `2764` · ภาษาไทย = `1044` ไว้ **แต่ยังไม่ได้ยืนยัน** (docs ของ Google บล็อกจาก sandbox)

### 💧 ทำไม kwcheck ไม่เขียน KV เลย

ผู้ใช้พิมพ์คำอะไรก็ได้ = **จำนวน key ไม่มีขอบเขต** ถ้าเขียนลง KV ทุกคำที่ถูกค้น
โควตา 1,000 ครั้ง/วันที่ใช้ร่วมกันทั้งโปรเจกต์จะหมดจากคอลัมน์เดียว — ใช้ edge cache 6 ชม. พอ

### ⚠️ คอลัมน์ที่โหลดเอง ห้ามโดน error ของ load() เหวี่ยงใส่

`load()` เคยเขียน skeleton/error ทับ **ทุก** คอลัมน์ ทำให้ `yttrends` (โหลดแยก) กับ `kwcheck`
(รอผู้ใช้พิมพ์) ขึ้นว่า "ดึงข้อมูลไม่สำเร็จ" ทั้งที่ตัวเองไม่ได้พังและยังกดใช้ได้
ตอนนี้มี `SELF_LOADING` กันไว้แล้ว — **เพิ่มคอลัมน์ที่โหลดเองเมื่อไหร่ ต้องใส่ชื่อลงในนั้นด้วย**
และคอลัมน์แบบนั้นต้อง **วาดครั้งแรกเอง** ไม่งั้นถ้า `load()` ล้ม `renderAll()` จะไม่ถูกเรียก แล้วจะว่างเปล่า

### ทำไมคอลัมน์ X กับ YouTube อยู่แค่ `trend/`

เป็นของ **เฉพาะทาง** ไม่ใช่ฟีเจอร์พื้นฐานที่ทุกแดชบอร์ดต้องมี — ตามตารางข้างบน
เทรนด์ทั่วไปที่ไม่ผูกกับ CP เป็นงานฝั่ง PR จึงอยู่ที่ `trend/` ที่เดียว
`issue/` ดูว่า CP ถูกพูดถึงยังไง · `ir/` ข่าวนักลงทุน · `sd.html` ความยั่งยืน — คนละโจทย์กัน

## ⚠️ หนี้ทางเทคนิคที่รู้อยู่

- `/api/sd/img?u=<url>` เป็น **image proxy แบบเปิด** — ใครใส่ URL อะไรก็ได้
  (มีตัวกัน: ต้องเป็น http/https, ต้องได้ `content-type: image/*`, จำกัด 3MB, timeout 8 วิ)
  ถ้าจะปิดช่อง = จำกัดเฉพาะโดเมนข่าวที่อยู่ในลิสต์
- การ์ด `issue/` บน landing ยังติดป้าย 🚧 Under Construction ทั้งที่ใช้งานได้แล้ว
- `trends.html` ยัง deploy อยู่แต่ไม่มีลิงก์จาก landing (มีช่วงเวลา 1เดือน/3เดือน/12เดือน/5ปี
  ที่ `trend/` ปัจจุบันไม่มี — ปัจจุบันมีแค่ `now 1-d` / `now 7-d`)
- `COORDINATION.md` ตกยุค — ยังอ้างไฟล์ที่ root ทั้งที่ย้ายเป็นโฟลเดอร์แล้ว
- branch `claude/google-trends-dashboard-lt5k09` เป็น history คนละสาย (ต้นแบบ ก.ค.) ทิ้งได้
- **ไม่มี staging แยกจริง** — `dev` คือ production ถ้าอยากได้ที่ทดสอบที่ปลอดภัย
  ต้องเปิด preview deployment ของ Pages หรือตั้งโปรเจกต์ staging แยก
- `main` ค้างที่ `6614efa` ไม่ได้ deploy ที่ไหน ตกรุ่นไป 23 commit — จะ sync ให้ตรงหรือลบก็ได้
- คลังข่าว `pr:archive` เก็บ **90 วัน** (สูงสุด 3,000/คอลัมน์ ~3 MB) — เพดานค่าเดียวของ KV คือ 25 MB
  ถ้าจะเก็บถึง 1 ปี (~40 MB) **ต้องย้ายไป D1** ไม่ใช่แค่เพิ่มเลขวัน
  ดึงออกเป็นไฟล์ได้ที่ `/api/trend/archive?src=all&days=90` (CSV/JSON)
- คอลัมน์ YouTube พึ่ง instance สาธารณะของคนอื่น (Invidious/Piped) ที่ **หายไปเลยได้**
  มีตัวกันแล้ว (ยิงหลายตัวพร้อมกัน → แกะหน้า YouTube เอง → เสิร์ฟของเก่าใน KV)
  แต่ถ้าวันหนึ่งพังทั้งหมด ทางแก้ถาวรคือขอ YouTube Data API key (ฟรี มีโควตา 10k/วัน)

## 🔎 ข่าวในคอลัมน์ Alert มาจาก 2 ทาง — แก้คนละที่กัน

ป้ายบอกที่มาอยู่ **เฉพาะในรายการตัดข่าวบนหน้า `/admin/`** — เจ้าของสั่งเอาออกจากการ์ดบนแดชบอร์ด
(บนแดชบอร์ดแค่อ่านข่าว ไม่ได้แก้อะไร ป้ายเลยเป็นแค่ตัวรบกวน) · **ดูป้ายก่อนแก้เสมอ**

| ป้าย | มาจากไหน | แก้ที่ไหน |
|---|---|---|
| 🔔 มาจาก Google Alert | ฟีด Alert ของ Google ตรงๆ | **Google Alerts** → แก้ query (ใส่ `-คำ` / `-site:`) |
| 📰 มาจากคอลัมน์ข่าว | ข่าวจากคอลัมน์ News ที่ตรง keyword แล้ว `mergeNewsIntoAlert()` ดึงเข้ามา | **ในโค้ด** (ดูตารางล่าง) |

ที่มาเก็บไว้ในฟิลด์ `fromNews` ของ item (ทั้ง `trend/feeds.js` และ `ir/feeds.js` เซ็ตให้แล้ว)
· ตอนกด ⚑ จะถูกเก็บลง record เป็น `from: "news" | "alert"` → หน้า admin อ่านค่านี้

**ของที่ดึงมาจากข่าว ใช้คำอะไรตัดสิน:**

| คอลัมน์ | ใช้อะไร match | เอาออกยังไง |
|---|---|---|
| PR/Issue `alert1` (CP) | `CP_BRANDS` ใน `functions/api/trend/feeds.js` | แก้ลิสต์ในโค้ด |
| PR/Issue `alert2` (จับตามอง) | คำจาก query ของ Alert + คำ `-ไม่เอา` | แก้ query ใน Google Alerts |
| IR `alert1` / `alert2` | `CP_BRANDS` / `ALERT2_KEEP` ใน `functions/api/ir/feeds.js` | แก้ลิสต์ในโค้ด |

> ⚠️ **`-site:` ใช้กับของที่ดึงมาจากข่าวไม่ได้เลย** — เราดึงเข้ามาเอง ไม่ได้ผ่าน Google
> และตัวกรอง `-คำ` เทียบกับพาดหัว/สรุปเท่านั้น `site:` จึงไม่มีวัน match
> ปุ่ม "＋ ตัดเว็บ" บนหน้า admin จึง **ซ่อนไปเลย** ในแถวที่มาจากข่าว ไม่ให้กดแล้วนึกว่าแก้ได้
>
> ⚠️ **`-คำ` กรองของที่ดึงมาจากข่าวได้เฉพาะ `alert2` ของ PR/Issue** (ต้องรอ release keyword ชุดใหม่)
> ส่วน `alert1` กับฝั่ง IR ยังไม่ได้ส่ง excludes เข้าไป — ถ้าจะให้ครบต้องแก้เพิ่ม

### 🚫 ชื่อที่มีคำของเครือ CP แต่ไม่ใช่ข่าวของเครือ

อยู่ใน `CP_FALSE` (คำตรงตัว) กับ `CP_FALSE_RX` (แพตเทิร์น) ของ `feeds.js` ทั้ง trend และ ir
· ฝั่งหน้าเว็บมีคู่แฝดคือ `PIN_FALSE_RE` ใน `trend/app.js` + `issue/app.js` — **แก้ที่ไหนต้องแก้ให้ครบทั้ง 4 ที่**

| ชื่อ | คืออะไร |
|---|---|
| บีแอลซีพี · BLCP | โรงไฟฟ้า BLCP Power |
| ซีพีเอ็น · CPN | Central Pattana |
| บีซีพีจี · บีซีพี | กลุ่มบางจาก |
| **ทรูดิจิทัล พาร์ค · True Digital Park** | **สถานที่จัดงาน/ที่ตั้งออฟฟิศ** ข่าวที่พูดถึงมันไม่ใช่ข่าวของเครือ |
| **ทรูธโซเชียล · Truth Social · Trump Media** | **แอปของทรัมป์** — คำว่า "ทรู" อยู่ต้นคำพอดี ข่าวขาดทุนของ Trump Media เลยหลุดเข้าคอลัมน์ CP |

> ⚠️ **ทรูดิจิทัล พาร์ค จับเฉพาะที่มี พาร์ค/ปาร์ค/park ต่อท้าย** — "ทรูดิจิทัล กรุ๊ป" เป็นบริษัทของทรูจริง ห้ามตัด
> · สะกดได้หลายแบบ (เว้นวรรค · ทัล/ตอล · พ/ป) จึงเขียนเป็นแพตเทิร์นใน `CP_FALSE_RX` แทนการไล่พิมพ์ทุกแบบ
>
> ⚠️ **ตัดชื่อลวงออกก่อนเช็คเสมอ** (`dropFalseCP`) ไม่ใช่เช็คแล้วค่อยตัด — ข่าวที่มีทั้งชื่อจริงและชื่อลวง
> (เช่น "ศุภชัย เจียรวนนท์ พูดที่ทรูดิจิทัล พาร์ค") ต้องยังเข้าคอลัมน์ CP อยู่
>
> ⚠️ ตอนเพิ่มชื่อลวง **ให้ไล่เช็คด้วยว่าคำที่เหลือยังจับข่าวจริงได้ไหม** — รอบนี้เจอว่า `PIN_FALSE_RE`
> ไม่มี "เจียรวนนท์" ข่าวที่มีแต่ชื่อผู้บริหารเลยหลุด (ก่อนหน้านี้ติดบนได้เพราะบังเอิญมีคำว่า "ทรู"
> ในชื่อสถานที่) เติมให้แล้ว

### 🧹 ของที่ไม่ใช่ข่าว — ตัดทิ้งอัตโนมัติใน `noiseReason()`

อยู่ใน `feeds.js` ทั้ง trend และ ir · **จับที่โดเมนเป็นหลัก แม่นกว่าจับคำมาก** คำเอาไว้กันเว็บที่ยังไม่อยู่ในลิสต์

| ประเภท | ตัวอย่างที่เจอจริง | ลิสต์ |
|---|---|---|
| ประกาศงาน | jobsdb · jooble · jobbkk · **monster.co.th** (ประกาศงานภาษาอังกฤษ) | `JOB_HOSTS` · `JOB_RE` |
| อสังหา/ให้เช่า | dotproperty · ddproperty · livinginsider | `PROP_HOSTS` · `PROP_RE` |
| หน้าขายสินค้า/บริการ | epower (เครื่องกรอง/เซนเซอร์วัดฝุ่น) | `VENDOR_RE` |
| โฆษณาแฝงในข่าว | ครีม/เซรั่ม "หาซื้อได้ที่เซเว่น" (ข่าวสด) | `AD_PRODUCT_RE` **และ** `AD_PITCH_RE` |
| เว็บแจกข่าว PR | newswit · thaipr · prnewswire — **ตัดเฉพาะใบที่ชื่อเครือ CP ไม่อยู่ในพาดหัว** (เคยตัดทั้งเว็บ แล้วข่าวจริงของ ซีพี แอ็กซ์ตร้า/Makro หายไปด้วย) | `PR_HOSTS` + `realCP(title)` |
| ร้านค้าออนไลน์ · รายงานประจำวัน · แกลเลอรีรูป · ข่าว PR ราชการ | (ของเดิม) | `SHOP_*` · `DAILY_RE` · `GALLERY_RE` · `PR_RE` |

> ⚠️ **`verifyAlertItems()` ทำงาน "ก่อน" `mergeArchives()`** — ของเก่าที่เก็บไว้ใน KV ตอนยังไม่มีตัวกรอง
> จะไหลกลับเข้ามาโดยไม่ผ่านด่าน จึงต้องมี `dropNoiseAfterArchive()` กวาดอีกรอบหลัง archive
> ไม่งั้นต้องรอ 10 วันกว่าของเก่าจะหมดอายุไปเอง **เพิ่มตัวกรองใหม่ทีไร ให้เช็คว่ากวาดของเก่าด้วยไหม**
>
> ⚠️ **คำที่เสี่ยงตัดข่าวจริง อย่าใส่** — เช่น "เครื่องฟอกอากาศ" ที่โผล่ในข่าว PM2.5 จริงบ่อย
> (ใส่แต่ "เครื่องกรองน้ำ/เครื่องกรองอากาศ" ที่เป็นภาษาแคตตาล็อกมากกว่า)
> เพิ่มคำใหม่ทุกครั้ง **ต้องรันเทสต์ `noise.mjs` ที่มีพาดหัวข่าวจริงคุมไว้ด้วย**

## 📊 เก็บข่าวลง Google Sheet — 4 หัวข้อ (CPF · ปลาหมอคางดำ · PM2.5 · Alien species)

**ชีตเป็นฝ่ายมาดึงเอง** ไม่ใช่เรายิงเข้าไป — `sheet/news-to-sheet.gs` วางไว้ใน Apps Script ของชีต
ตั้งเวลาให้วิ่งทุก 1 ชม. แล้วมาขอที่ `/api/trend/archive?topics=...&format=json`

ทำไมไม่ให้ Cloudflare ยิงเข้าชีตเอง:
1. **Cloudflare Pages ตั้ง cron ไม่ได้** (Workers ทำได้ Pages ไม่ได้) → ต้องรอให้มีคนเปิดแดชบอร์ด = ไม่แน่นอน
2. ต้องเก็บ URL ของ Web App ไว้เป็น secret · ใครได้ URL ไปเขียนลงชีตเจ้าของได้เลย

| | |
|---|---|
| หัวข้อที่รู้จัก | `cpf` · `blackchin` · `pm25` · `alien` (ตาราง `TOPICS` ใน `archive.js`) |
| `alien` จับแค่คำรวม | เจ้าของสั่ง (10 ส.ค.) ให้เอาแค่ alien species ไทย+อังกฤษ กับ "สัตว์ต่างถิ่น" · **ไม่ไล่ชื่อชนิดพันธุ์ทีละตัว** (เคยใส่ปลาซัคเกอร์/หอยเชอรี่/ผักตบชวา แล้วถอดออก — ละเอียดเกินที่ต้องการ) |
| คอลัมน์ในชีต | สำนักข่าว · พาดหัว · link · วันที่ · หมวด |
| ข่าวย้อนหลัง | Bing ให้ย้อนได้แค่ 2-5 วัน (เป็นแค่หน้าผลค้นหาปัจจุบัน ไม่มีพารามิเตอร์วันที่) — ความลึกที่มีมาจาก**คลังของเราเอง** 90 วัน · ถ้าอยากได้เก่ากว่านั้นต้องหาแหล่งข่าวที่มีคลังย้อนหลัง (ยังไม่ได้ทำ) |
| กันข่าวซ้ำ | เทียบ `link` ที่ตัด `?utm_...` และ `/` ท้ายออกแล้ว |

> ⚠️ **ลิงก์ของ Bing เป็นตัวเปลี่ยนทางที่เปลี่ยนทุกชั่วโมง** (`apiclick.aspx?...&tid=<เปลี่ยนทุกรอบ>&url=<ลิงก์จริง>`)
> คลังข่าว dedupe ด้วยลิงก์ จึงมองข่าวใบเดียวเป็นคนละใบทุกรอบ — **เจอจริง: ข่าวเดียวซ้ำ 27 แถวในชีต**
> แก้แล้วที่ `unwrapRedirect()` ใน `_lib/parser.js` (แกะทุก source ไม่ใช่แค่ alert) + `mergeArchives`
> แกะก่อน dedupe เพื่อยุบของเก่าที่ค้างใน KV ด้วย · ฝั่งชีตกันซ้ำ 2 ทาง (ลิงก์ · สำนักข่าว+พาดหัว)
> และกันซ้ำ **ภายในรอบเดียวกัน** ด้วย — ของเดิมอ่าน `seen` มาก่อนเขียน 27 ใบในรอบเดียวจึงผ่านหมด
> · ล้างของเก่าที่ค้างในชีตด้วยฟังก์ชัน `cleanupDupes()`
>
> ⚠️ **`syncNews` เขียนแต่แถวใหม่ ไม่เคยกลับไปแก้ของเดิม** — แก้ฝั่งเซิร์ฟเวอร์แล้วแถวที่อยู่ในชีต
> ยังเป็นของเก่าอยู่เสมอ ต้องมีฟังก์ชันซ่อมแยกทุกครั้ง (`fixClippedTitles()` ซ่อมพาดหัวที่ถูกตัด
> จับคู่ด้วย **สำนักข่าว + ต้นพาดหัว** ไม่ใช่ลิงก์ เพราะแถวเก่าเก็บลิงก์ Bing ที่เปลี่ยนทุกชั่วโมง)
>
> ⚠️ **บางฟีดตัดพาดหัวโดยไม่ใส่ "…"** (จบห้วนๆ กลางประโยค) — ดูแค่จุดไข่ปลาจึงไม่พอ
> `looksClipped()` เลยนับพาดหัวที่ยาว ≥ 80 ตัวอักษรและไม่จบด้วยเครื่องหมายจบประโยคว่า "น่าสงสัย"
> **เดาเกินไปไม่เสียหาย เพราะด่านตอนรับค่ากลับเข้ม** (ต้องยาวกว่าเดิม + ขึ้นต้นเหมือนกัน)
> · แต่ **ห้ามเอากฎเดานี้ไปคัดฝั่ง "ตัวเต็มที่จะเอามาทับ"** — พาดหัวเต็มที่ยาวจะถูกมองว่ายังถูกตัด
> แล้วโดนคัดทิ้งจนไม่เหลืออะไรให้ซ่อมเลย (เจอตอนเขียนเทสต์) ฝั่งนั้นต้องเช็คจุดไข่ปลาแบบเข้มเท่านั้น
> · มีธง `it.tfix` ติดไว้กับข่าวที่เช็คแล้ว ไม่งั้นข่าวที่ "พาดหัวถูกอยู่แล้ว" จะกินคิว 20 ใบทุกรอบ
>
> ⚠️ **Bing ตัดพาดหัวให้สั้น** ลงท้ายด้วย "…" ตัวเต็มไม่ได้อยู่ในฟีดเลย · `fillClippedTitles()`
> ไปอ่าน `og:title` จากหน้าข่าวจริง **ยิงไม่เกิน 8 ใบต่อ request และเฉพาะตอนทำงานเบื้องหลัง**
> ผลเก็บลง cache 7 วัน + ลงคลังข่าว จึงจ่ายครั้งเดียวต่อข่าว 1 ใบ · มีกฎกันเขียนทับผิด:
> ตัวใหม่ต้องยาวกว่า ไม่ถูกตัด และขึ้นต้นเหมือนเดิม ไม่งั้นจะได้ชื่อเว็บมาแทนพาดหัว
>
> ⚠️ **ลำดับต้องเป็น `mergeArchives` → `fillClippedTitles` → `saveArchives` เท่านั้น**
> เคยวางตัวเติมไว้ก่อน merge ผลคือแตะได้แต่ข่าวที่เพิ่งดึงมา **พาดหัวเก่าที่ค้างใน KV
> ไม่มีวันถูกเติมเลย** ผู้ใช้จึงยังเห็นพาดหัวขาดหลัง release · `mergeArchives` จึง
> **ไม่เขียน KV เอง** แต่คืนค่าให้ `saveArchives` เขียนทีเดียวหลังเติมเสร็จ
> (ยังเขียน KV ครั้งเดียวต่อ request เหมือนเดิม — เทสต์ `dupes.mjs` คุมทั้งลำดับและจำนวนครั้ง)
>
> ⚠️ **ข่าวใบเดียวเข้าได้หลายหัวข้อ** (เช่น CPF ปล่อยปลาหมอคางดำ) — ใส่ทุกหัวข้อคั่น `, `
> ไม่เลือกอันใดอันหนึ่ง เพราะจะทำให้อีกหัวข้อหายจากชีต · ตัวกรองในชีตใช้ "มีคำว่า..." ได้ตามปกติ
>
> ⚠️ **คลังข่าวเก็บ 90 วัน** ถ้าตัวตั้งเวลาในชีตหยุดวิ่งเกิน 90 วัน ช่วงที่ขาดจะหายถาวร
> ดึงย้อนหลังรอบละ 7 วัน (`DAYS`) เผื่อไว้เกินความถี่ที่ตั้ง — รอบไหนไม่วิ่งยังตามเก็บได้
>
> ⚠️ **ไม่ใส่ `topics=` = ได้ทุกข่าวเหมือนเดิม** ของเดิมที่ใช้ `=IMPORTDATA()` อยู่จึงไม่พัง
> (คอลัมน์ `หมวด` ต่อท้ายเป็นคอลัมน์ใหม่ ไม่ได้ไปสลับของเดิม)
