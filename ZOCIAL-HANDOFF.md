# Zocial Eye → คอลัมน์ใหม่บน `/issue/` — ไฟล์ส่งต่อ

> เขียนไว้ให้ **session ใหม่** ที่จะทำฝั่ง pipeline อ่านก่อนเริ่ม
> เจ้าของเคาะข้อตกลงทั้งหมดในไฟล์นี้แล้ว (2 ก.ย. 2026)
>
> 📌 **อ่าน `CLAUDE.md` ก่อนเสมอ** — ไฟล์นี้เสริม ไม่ได้แทน

## 1. ตกลงกันแล้วว่าอะไร

| เรื่อง | ที่ตกลง |
|---|---|
| ปลายทาง | **คอลัมน์ใหม่บน `/issue/`** (ไม่ใช่ `/trend/` — spec ฉบับแรกเขียนผิด) |
| endpoint | `/issue/api/listen` — **อยู่ใต้ path เดียวกับหน้า** จึงได้ Cloudflare Access ครอบให้เอง |
| เทคโนโลยี | **static HTML + JS ธรรมดา** เหมือนทุกหน้าในโปรเจกต์ · **ไม่ใช้ React/Vite/Tailwind** |
| ลบข้อมูลเก่า | ทำ **ตอนมีคน upload** (วันละครั้ง) · **ไม่ใช้ cron** |
| ความถี่ | ผู้ใช้ export จาก Zocial เองแล้ว upload **วันละครั้ง** |

### ทำไมไม่ไป `/trend/`

1. `/trend/` = เทรนด์ทั่วไปที่ **ไม่ผูกกับ CP** — คนละคำถามกับ "คนพูดถึง CP ยังไง"
2. `/trend/` **ไม่มี Cloudflare Access** ส่วน `/issue/` ล็อกอยู่แล้ว → ย้ายมา = แก้เรื่องสิทธิ์ให้ฟรี

### ทำไมไม่ใช้ React

เว็บนี้เป็น **static ล้วน ไม่มีขั้นตอน build เลย** (`package.json` ที่มีใช้กับเทสต์เท่านั้น)
ใส่ React = ต้องเปลี่ยนวิธี deploy ทั้งโปรเจกต์ **กระทบทุก session**

## 2. แบ่งงาน — เจอกันที่เส้น API

| ห้อง | ทำอะไร | ไฟล์ที่แตะได้ |
|---|---|---|
| **session แดชบอร์ดข่าว** (มีอยู่แล้ว) | คอลัมน์บน `/issue/` + ข้อมูลจำลอง | `issue/index.html` · `issue/app.js` · `issue/styles.css` |
| **session ใหม่ (Zocial)** | อัปโหลด · D1 · clean · noise · sentiment · aggregate · admin | `functions/issue/api/**` · `issue/upload/**` · `issue/zadmin/**` · schema |

> 🚫 **ห้ามแตะข้ามกัน** — `/issue/` มี **3 ห้อง** ดูแลอยู่แล้ว
> · `issue/app.js` + `issue/index.html` = session แดชบอร์ดข่าว
> · `issue/sentiment.html` + `issue/sentiment-eval.html` = session social-comment-extractor
> · ของใหม่ทั้งหมด = session Zocial
>
> ⚠️ **`issue/index.html` เป็นข้อยกเว้น** — คอลัมน์ใหม่ต้องเพิ่มที่นี่ **ให้ session แดชบอร์ดข่าวเป็นคนเพิ่ม**
> ไม่ใช่ session Zocial แก้เอง (ไม่งั้นชนกันตอน merge)

### API contract — ตกลงไว้ก่อน ต่างคนต่างทำได้เลย

```
GET /issue/api/listen?date=2026-09-02&campaign=<id>&limit=30
→ 200
{
  "date": "2026-09-02",
  "campaign": "<id>",
  "updatedAt": "2026-09-02T10:51:34.296Z",
  "hasData": true,
  "cards": [
    {
      "id": "…",
      "source": "facebook",          // facebook|x|instagram|tiktok|youtube|forum|news
      "account": "ชื่อเพจ",           // null ถ้าเป็นบุคคลธรรมดา (ดู §4)
      "accountType": "page",         // page|person|unknown
      "snippet": "ข้อความ ≤200 ตัวอักษร",
      "url": "https://…",
      "postedAt": "2026-09-02T09:12:00+07:00",
      "engagement": 1234,
      "comments": 87,                // ⚠️ นับเฉพาะที่อยู่ในไฟล์ ดู §5
      "sent": { "neg": 54, "neu": 26, "pos": 7 },   // sentiment ของ "คอมเมนต์ใต้โพสต์"
      "postSent": "neg"              // sentiment ของตัวโพสต์เอง
    }
  ]
}
```

**กติกาของ response — ห้ามผิด**

- ❌ **ไม่มีข้อมูลของวันนั้น ห้ามคืน `cards: []` เฉยๆ** ต้องส่ง `hasData: false` มาด้วย
  ไม่งั้นหน้าเว็บแยกไม่ออกระหว่าง **"ยังโหลดไม่เสร็จ"** กับ **"วันนั้นลืม upload"** แล้วจะหมุนค้าง
  (บทเรียนเดิมของโปรเจกต์ — ดูหัวข้อ "ไอคอนหมุนแปลว่ากำลังมาเท่านั้น" ใน `CLAUDE.md`)
- ❌ **ไม่รู้ ห้ามกลืนเป็นค่าใดค่าหนึ่ง** — คำนวณ sentiment ไม่ได้ ให้ส่ง `null` ห้ามเติม `"neu"`
  (บทเรียนแพงที่สุดของโปรเจกต์: 79% ของแถวเป็น "กลาง" เพราะโค้ดเติมให้เงียบๆ แล้วเกือบสรุปผิดทั้งหมด)
- เวลาเป็น **ISO 8601 พร้อม offset** ไม่ใช่ string ไทย

## 3. กฎของ `/issue/` ที่ต้องรู้ก่อนแตะ

**คอลัมน์ 1 อัน = HTML 16 บรรทัด** (วัดจากของจริง) หน้าตาแบบนี้

```html
<section class="panel" data-source="listen">
  <div class="phead">
    <span class="ptitle"><span class="dot alert"></span>📣 คนพูดถึงเรา</span>
    <span class="pcount" data-count>—</span>
  </div>
  <div class="filters"> … dropdown … </div>
  <div class="list" data-list></div>
</section>
```

| ⚠️ ข้อควรระวัง | ทำไม |
|---|---|
| **ต้องลงทะเบียนใน `LAZY_COLS` และ `SELF_LOADING`** | คอลัมน์ที่มี endpoint ของตัวเองแต่ไม่ได้ลงทะเบียน = **ไม่มีใครโหลดให้ ค้างเป็นไอคอนหมุนถาวร** |
| **มือถือเป็น carousel ปัดซ้ายขวา ไม่ใช่กริด** | ของที่กางออกมาในกริดจะดันจนหน้าพัง — เคยเกิดแล้ว |
| **คอลัมน์ที่ตั้ง `max-height` เอง ต้องล้างค่าบนมือถือ** | ไม่งั้นเหลือช่องว่างใต้การ์ดเฉพาะคอลัมน์นั้น |
| **แถบแท็บของ `/issue/` เขียนซ้ำอยู่ 3 ไฟล์** | `index.html` · `trends.html` · `three.html` — แก้ต้องแก้ให้ครบ |
| **bump `app.js?v=` ทุกครั้ง** | เครื่องที่ติดตั้งเป็นแอปจะไม่เห็นของใหม่ (ตอนนี้ `v=68`) |
| **สถานะที่ผู้ใช้กดไว้ ห้ามเก็บใน DOM อย่างเดียว** | `renderAll()` วาดใหม่ทุก 3 นาที ของที่กางไว้จะหายหมด |

## 4. 🔐 Privacy — แก้ที่ spec เดิมขัดกันเอง (เจ้าของเคาะแล้ว)

**ปัญหาของ spec เดิม:** เก็บ `account_hash` แทนชื่อจริงของบุคคล **แต่ยังเก็บ `direct_url`**
→ กด URL เข้าไปก็เห็นชื่อ **การ hash จึงไม่ได้ปกป้องอะไรเลย** เป็นการหลอกตัวเอง

**✅ ทางที่เลือก — ตรงไปตรงมา**

| | |
|---|---|
| เก็บอะไร | **ชื่อจริง + URL** ทั้งเพจและบุคคล · **ไม่ต้อง hash** |
| ป้องกันด้วยอะไร | **Cloudflare Access** (จำกัดอีเมลองค์กร) + **retention** (ลบตามกำหนด) |
| README ต้องเขียนว่าอะไร | **ตามที่ทำจริง** — 🚫 **ห้ามเขียนว่า "ไม่เก็บชื่อจริง"** |

> ⚠️ **ข้อความใน README คือสิ่งที่ฝ่ายกฎหมายใช้ตัดสิน** — เขียนไม่ตรงกับที่ทำจริง
> อันตรายกว่าไม่เขียนเลย · ให้ฝ่ายกฎหมาย CPF review ก่อนขึ้น production

## 5. 🗑 Retention — แก้ที่ spec เดิมขัดกันเอง (เจ้าของเคาะแล้ว)

**ปัญหาของ spec เดิม:** `daily_news` เก็บ **ถาวร** ทั้งข้อความ 200 ตัวอักษรและ URL
ขัดกับ §6 ที่ตั้งใจลบ raw ใน 180 วัน — ลบ raw แต่สำเนายังอยู่ถาวร = ไม่ได้ลบจริง

**✅ ทางที่เลือก — แยกตามว่าเป็นใคร**

| ประเภท | เก็บนานแค่ไหน | เหตุผล |
|---|---|---|
| **เพจ / สื่อ / องค์กร** (นิติบุคคล) | **ถาวร** | เป็นนิติบุคคล + จำเป็นต่องาน monitoring ย้อนหลัง |
| **บุคคลธรรมดา** | **ลบตาม retention เหมือน raw** (180 วัน) | เป็นข้อมูลส่วนบุคคล |
| `daily_aggregate` (ตัวเลขล้วน) | **ถาวร** | ไม่มีข้อมูลระบุตัวบุคคล |

⚠️ แปลว่า `daily_news` ต้องมี field `account_type` และ **ตัวลบต้องดูจาก field นี้**
· เทสต์ต้องมีข้อ: "ลบของบุคคล ไม่แตะของเพจ · ไม่แตะ `daily_aggregate`"

## 6. ✅ blocker ที่แก้แล้ว vs ที่ยังค้าง

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | spec เขียน stack ผิด (Vite+React+Tailwind) | ✅ **แก้แล้ว** — static ธรรมดา |
| 2 | Pages ตั้ง cron ไม่ได้ → retention ไม่ทำงาน | ✅ **แก้แล้ว** — ลบตอน upload |
| 3 | เอา `/api/*` เข้า Access = เว็บพังทั้งหมด | ✅ **แก้แล้ว** — ย้ายไป `/issue/api/` |
| 4 | hash ชื่อ แต่เก็บ URL = หลอกตัวเอง | ✅ **แก้แล้ว** — §4 |
| 5 | `daily_news` ถาวร ขัดกับ retention | ✅ **แก้แล้ว** — §5 |
| 6 | `comment_count` ไม่ใช่ยอดจริง | ⚠️ **ต้องเขียนกำกับบนการ์ด** — §7 |
| 7 | alert badge เพี้ยนเมื่อลืม upload | ⚠️ **ต้องแก้ตอนคำนวณ** — §7 |
| 8 | timezone ของ `Post time` | ❓ **ยังไม่รู้** — §7 |
| 9 | D1 ยังไม่เคยเปิดใช้ในโปรเจกต์นี้ | ❓ **ต้องยืนยันก่อน** — §7 |
| 10 | sentiment ซ้อนกับ `social-comment-extractor` | 🔴 **ยังค้าง** — §8 |

## 7. ⚠️ 4 เรื่องที่ยังต้องตัดสินใจ/ระวัง

### 7.1 `comment_count` ไม่ใช่ยอดคอมเมนต์จริง

นับได้แค่คอมเมนต์ **ที่อยู่ในไฟล์ export** ไม่ใช่ยอดจริงบนโพสต์
→ **ตัวเลขจะเปลี่ยนทุกครั้งที่ upload ไฟล์ใหม่**

🚫 **ห้ามแสดงเป็น "87 คอมเมนต์" เฉยๆ** — คนอ่านจะเข้าใจว่าเป็นยอดจริง
✅ เขียนกำกับ เช่น **"87 คอมเมนต์ในไฟล์"** หรือมี tooltip อธิบาย

### 7.2 alert badge เพี้ยนเมื่อลืม upload

เกณฑ์คือ "negative วันนี้ > 2.5 × ค่าเฉลี่ย 7 วัน"
ถ้าเสาร์-อาทิตย์ไม่มีใคร upload → ค่าเฉลี่ยถูกหารด้วย 7 ทั้งที่มีข้อมูลจริง 5 วัน → **เฉลี่ยต่ำเกินจริง → เตือนมั่ว**

✅ **นับเฉพาะวันที่มีข้อมูลจริงเข้าค่าเฉลี่ย** และถ้ามีข้อมูลน้อยกว่า 4 วัน **อย่าเพิ่งเตือน** ให้ขึ้นว่า "ข้อมูลไม่พอ"

### 7.3 timezone ของ `Post time` — ✅ **ตอบแล้ว: เวลาไทย** (เจ้าของยืนยัน 3 ก.ย. 2026)

ไฟล์ส่งเวลามาเป็น **เวลาไทย (UTC+7)** เขียนเป็นข้อความตรงๆ `2026-06-15 00:00:00`
(ไม่ใช่ตัวเลข serial ของ Excel และไม่มี offset ติดมา)

> ⚠️ **ยังไม่ได้ปักตายในโค้ด โดยตั้งใจ** — หน้า `/issue/upload/` มีช่องให้เลือกและโชว์ผลทันที
> ว่าแถวแรกจะถูกบันทึกเป็นวันไหน + ปุ่มเปิดโพสต์จริงไปเทียบ
> วันหลัง Zocial เปลี่ยนรูปแบบ ก็สลับได้เองโดยไม่ต้องแก้โค้ด · ค่าตั้งต้น = เวลาไทย
> · **Workers รันด้วย UTC** — ทุกที่ที่คิดวันที่ต้องผ่าน `thaiDateKey()` เท่านั้น ห้ามใช้ `new Date()` ตรงๆ

### 7.4 D1 ยังไม่เคยถูกใช้ในโปรเจกต์นี้เลย

ตรวจแล้ว: `functions/` ทั้งหมดใช้ **KV อย่างเดียว** (`FLAGS_KV`) ไม่มี D1 สักที่

👉 ก่อนเริ่ม phase 1 ต้องยืนยัน: เปิด D1 ได้ไหม · โควตาพอไหม (~1,000 แถว/วัน) · ใครสร้าง binding

> 💡 ถ้า D1 ติดปัญหา ทางสำรองคือ **KV blob ต่อวัน** (`listen:<campaign>:<date>`)
> แต่ **ต้องระวังโควตา KV**: ทั้งโปรเจกต์เขียนได้ **1,000 ครั้ง/วัน** ใช้ร่วมกันทุกแดชบอร์ด
> upload วันละครั้ง = เขียนไม่กี่ครั้ง จึงพอไหว **แต่ห้ามเขียนแยก key ต่อ row เด็ดขาด**

## 7.5 📄 สิ่งที่รู้จากไฟล์ export ของจริง (3 ก.ย. 2026)

เจ้าของส่งไฟล์จริงมา 1 ไฟล์ · 12,004 แถว · 8.5 MB · ช่วง 15 มิ.ย. – 15 ก.ค. 2026
**ยิงเข้าหน้า `/issue/upload/` แล้ววัดผลจริง ไม่ได้อ่านสเปกเอา**

### หัวตาราง 27 คอลัมน์ (ชื่อจริง)

```
Account · Message · Direct URL · Post URL · Comment URL · Reply comment URL · Source ·
Post time · Engagement · Main keyword · Sub keyword · Follower count · Sentiment · Category ·
Track post · Track account · Note · _id · Image labels · Image URL ·
Account label audience size · Account label categories · Account label type ·
Account label TSA · Logo recognition · Scene recognition · Named entity recognition
```

### 🔴 3 เรื่องที่ทำให้ข้อมูลผิดแบบเงียบๆ ถ้าไม่รู้

| # | เรื่อง | ตัวเลขจากไฟล์จริง |
|---|---|---|
| 1 | **ลิงก์ใช้กันแถวซ้ำไม่ได้** — คอมเมนต์หลายใบใช้ลิงก์เดียวกัน | 12,004 แถว มีลิงก์ไม่ซ้ำแค่ **6,349** · ต้องใช้ **`_id`** (ไม่ซ้ำครบ 12,004) |
| 2 | **โพสต์ + คอมเมนต์ + คำตอบกลับ ปนกันในชีตเดียว** | โพสต์ **3,548** · คอมเมนต์ **7,062** · ตอบกลับ **1,394** → นับทุกแถวเป็นโพสต์ = เกินจริง **3.4 เท่า** |
| 3 | **ไฟล์มี 9 ชีต** — `all` (ครบ) + แยกรายช่อง 8 ชีต | อ่านผิดชีต = ได้ข้อมูลไม่ครบโดยไม่มีอะไรบอก |

· แยกชนิดแถวด้วย `Reply comment URL` → `Comment URL` → ไม่มีทั้งคู่คือโพสต์
  **แม่น 100% ในไฟล์จริง** (แถวคอมเมนต์มี `Direct URL` ตรงกับ `Comment URL` ทุกใบ)
· `Post URL` = ตัวจับกลุ่มว่าคอมเมนต์ใบไหนอยู่ใต้โพสต์ไหน → ใช้ทำ `sent:{neg,neu,pos}` ของการ์ดได้จริง
· หน้า preview จึงโชว์ **ชนิดของแถว + ช่องทาง** ไว้ให้เห็นทันทีว่าอ่านชีตผิดไหม

### ❓ `Account label type` ไม่ได้บอกว่าเพจหรือคน — **ต้องให้เจ้าของเคาะ**

ค่าที่มีจริงมีแค่ 3 แบบ: **Public Figure 8,308 · (ว่าง) 3,273 · Brand 423**

| ค่า | ตอนนี้นับเป็น | ผลต่อ retention |
|---|---|---|
| `Brand` | เพจ | เก็บถาวร |
| `Public Figure` | **บอกไม่ได้** | ลบตามกำหนด ← 🔴 รอเจ้าของเคาะ |
| ว่าง | บอกไม่ได้ (ดูจากช่องทางแทน) | ลบตามกำหนด |

> `Public Figure` เป็นคนจริง (= ข้อมูลส่วนบุคคล) แต่บนเฟซบุ๊กมักเป็น "เพจ"
> **ห้ามเดา** — ตอนนี้เลือกทางที่ปลอดภัยกว่าคือถือว่าต้องลบ · ผลคือ **11,287 จาก 11,989 ใบ
> ถูกจัดเป็น "ลบตามกำหนด"** ถ้าเจ้าของอยากเก็บ KOL ไว้ย้อนดู ต้องเปลี่ยนตรงนี้

### เรื่องเล็กที่เจอด้วย

- `Source` ใช้ชื่อเดียวกับที่เราตั้งไว้อยู่แล้ว + มีค่า **`other`** เพิ่มมา (12 แถว)
- `Category` เป็น `-` ทุกแถว — ใช้ไม่ได้
- `Sentiment` ของ Zocial: **Negative 7,237 · Neutral 4,348 · Positive 419** (ลบ 60%)
  🚫 ยังไม่เอาขึ้นการ์ด — ดู §8
- 15 แถวไม่มีข้อความเลย (โพสต์ที่มีแต่รูป) → ถูกตัดพร้อมบอกเหตุผล
- ชื่อไฟล์: `ZE_all_message_on_<campaign><ช่วงวันที่>_<เลขรัน>.xlsx`
  🚫 **campaign id อยู่ในชื่อไฟล์ ห้าม commit** — โค้ดอ่านตอนรันเท่านั้น
- อ่าน + วาดผลไฟล์ 8.5 MB เสร็จใน **6.5 วินาที** ในเบราว์เซอร์ (ไม่ต้องพึ่งเซิร์ฟเวอร์)

## 8. 🔴 blocker ที่ยังค้าง — sentiment

**มีของอยู่แล้วในโปรเจกต์** (session `social-comment-extractor` ทำไว้):

```
social-comment-extractor/RUBRIC-CP.md   เกณฑ์ตัดสิน sentiment ต่อ CP (เจ้าของเคาะแล้ว 24 ส.ค. 2026)
social-comment-extractor/BASELINE.md    ผลวัดความแม่นทุกรอบ + pattern ที่พลาด
social-comment-extractor/FEEDBACK.md    ระบบเรียนรู้จากที่เจ้าของแก้ป้าย
```

**คำถามที่ยังไม่มีคำตอบ** — เจ้าของกำลังถาม session นั้นอยู่

> `RUBRIC-CP` ตอบคำถามว่า **"คนนี้มองแบรนด์ CP ยังไง"**
> แต่ news feed ต้องการ **"โทนของคอมเมนต์ใต้โพสต์นี้โดยรวม"**
> — อาจเป็นคนละคำถามกัน

| ถ้าคำตอบคือ | ทำยังไง |
|---|---|
| **ใช้ร่วมกันได้** | `RUBRIC-CP.md` เป็นความจริงกลางอันเดียว · 🚫 **ห้ามเขียน lexicon ชุดใหม่แข่ง** |
| **คนละคำถาม** | แยก lexicon ได้ แต่ **ต้องเขียนไว้ให้ชัดว่าต่างกันตรงไหน** ไม่งั้นเว็บเดียวมี 2 มาตรฐานที่ตอบไม่เหมือนกัน |

🚧 **เริ่ม phase 1–2 (upload / D1 / clean / noise) ได้เลยโดยไม่ต้องรอ** — sentiment เป็น phase 2 ท่อนท้าย
ระหว่างรอ ให้เก็บ `sentiment_raw` จาก Zocial ไว้ก่อน แล้วปล่อย `sentiment_final = null`

> ⚠️ **`sentiment` ของ Zocial ผิดบ่อยและผิดแรง** (ข่าว CSR ถูกตีเป็นลบ · โพสต์โจมตีบริษัทถูกตีเป็นบวก)
> 🚫 **ห้ามเอา `sentiment_raw` ขึ้นแสดงบนการ์ดโดยไม่บอกว่าเป็นค่าดิบจาก Zocial**

## 9. ฟีเจอร์มาตรฐานที่ต้องมี

หน้า upload กับหน้า admin ของ Zocial ก็ต้องมีครบเหมือนหน้าอื่น:

1. favicon emoji · 2. ปุ่ม 🏠 กลับหน้าหลัก · 3. `<meta name="page-ver">` + ตัวเช็คเวอร์ชัน
4. **ไอคอนหมุนตอนรอโหลด** ทุกจุดที่แปลว่า "ยังโหลดอยู่"
5. **เปิดหน้ามาต้องขึ้นไอคอนหมุน ไม่ใช่ของเก่า**

🚫 **ห้ามใส่ `/installprompt.js`** — แถบชวนติดตั้งอยู่ที่หน้าแรกหน้าเดียวเท่านั้น
🚫 **ไม่ต้องมีการ์ดบน landing** — เป็นเครื่องมือภายใน เหมือน `/admin/` และ `/social/`

## 10. เทสต์ที่ต้องมี (ก่อนบอกว่าเสร็จ)

เทสต์อยู่ที่ `tests/` รันด้วย `node <ไฟล์>.mjs` · ดูตารางว่าไฟล์ไหนคุมอะไรที่ `tests/README.md`

| ต้องคุมอะไร | หมายเหตุ |
|---|---|
| **`node tests/imports.mjs` ต้องผ่าน 100%** | 🔥 **ด่านบังคับก่อน push ทุกครั้ง** — Cloudflare รวมทุกไฟล์ใน `functions/` เป็นก้อนเดียว **ไฟล์เดียวประกอบไม่ขึ้น = ทั้งเว็บสร้างไม่ได้** เคยพังมาแล้ว 3 วัน · `node --check` **ไม่พอ** |
| upload ไฟล์ซ้ำ ต้องไม่เกิดแถวซ้ำ | |
| ไฟล์ครอบหลายวัน → แตกเป็น aggregate รายวันถูก | เช่น export วันจันทร์ครอบ ศ-ส-อา |
| retention ลบเฉพาะที่ควรลบ | **ลบของบุคคล ไม่แตะของเพจ · ไม่แตะ `daily_aggregate`** |
| noise filter + sentiment | ใช้ fixture จากไฟล์ export จริง |
| **นับ KV/D1 write ต่อ 1 request** | เกิน 1–2 ครั้งถือว่าผิด |

> 🚫 **อย่าเชื่อ self-report ของ AI** — ต้อง **รันจริงแล้ววางบรรทัดสรุปที่ออกมา** (เช่น `ผ่าน 39 · ตก 0`)
> ห้ามเขียนตัวเลขที่ไม่ได้มาจาก output จริง
> 🧪 **เทสต์ต้องพิสูจน์ว่าจับของพังได้จริง** — ลองทำให้พังแล้วดูว่าตก ไม่งั้นเทสต์ที่ผ่านตลอดไม่มีค่า

## 11. ข้อห้ามที่ผิดแล้วเจ็บ

- 🚫 **ห้าม push ขึ้น `main` โดยไม่ได้รับคำสั่งชัดเจน** — `main` = เว็บจริง deploy ทันที
- 🚫 **ห้าม commit secret / campaign id / sheet id** — repo เป็น **public** · secret ใส่ที่ Cloudflare เท่านั้น
  (และต้องใส่ **ทั้ง Production และ Preview** แล้ว **Retry deployment** ไม่งั้นค่าใหม่ไม่มีผล)
- 🚫 **ห้าม bot/script login เข้า Zocial หรือ automate การกด export** — ผิด policy ผู้ให้บริการ
- 🚫 **ห้ามเอา `/api/*` (ของเดิม) เข้า Cloudflare Access** — แดชบอร์ดสาธารณะเรียกอยู่ จะพังทั้งเว็บ
- 🚫 **ห้าม log ตัวข้อความ** — log เก็บแค่ metadata (จำนวนแถว · วันที่ · campaign)
- 🚫 **ห้ามใช้ `git revert` / `git branch -f` / `cherry-pick` ปล่อยงาน** — ดูวิธีปล่อยที่ `CLAUDE.md`
- 🚫 **ห้าม hardcode ตัวเลขบนแดชบอร์ด** — ทุกตัวต้องมาจากการคำนวณจริง

## 12. เริ่มยังไง

1. อ่าน `CLAUDE.md` ทั้งไฟล์ (ยาวแต่จำเป็น) + ไฟล์นี้
2. ~~ตอบ §7.3 (timezone)~~ ✅ ตอบแล้ว = เวลาไทย · **§7.4 (D1) ยังค้าง** รอเจ้าของเปิด
3. สร้าง branch ของตัวเอง `claude/zocial-ingest-xxxx` — **1 branch = 1 ของที่ปล่อยได้**
4. ~~เริ่ม phase 1 (schema + upload + validate + preview)~~ ✅ **ทำแล้ว** (3 ก.ย. 2026)
   · `issue/upload/zocial-lib.js` ฟังก์ชันล้วน · `issue/upload/` หน้าอ่าน+ตรวจไฟล์ ·
     `tools/zocial/schema.sql` โครง D1 (**ยังไม่ได้รัน**) · เทสต์ `tests/zocial.mjs` + `tests/zocialui.mjs`
   · เหลือแค่ **ท่อนบันทึกลง D1** ซึ่งรอ §7.4
5. คอลัมน์บน `/issue/` **ไม่ต้องทำ** — session แดชบอร์ดข่าวทำให้ด้วยข้อมูลจำลองไปก่อน
   พอ API จริงพร้อม แค่บอกมา จะสลับไปใช้ของจริงโดยไม่ต้องแก้อะไร

> ✍️ **เจอบทเรียนใหม่ ให้เขียนกลับลงไฟล์นี้หรือ `CLAUDE.md` ทันที** อย่าปล่อยไว้ในแชท
> — แชทหายเมื่อ session จบ ไฟล์ไม่หาย
